<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>ARIMA</title>
    <url>/2025/12/15/ARIMA/</url>
    <content><![CDATA[<p>这么火的ARIMA，来看看( ´•̥̥̥ω•̥̥̥` )<br><span id="more"></span><br>    说在前面：本文讲到的都属于时间序列相关模型，本质任务就是使用过去的数据预测未来的数据。<br>篇幅有点长，可以分部分选择阅读</p>
<h1 id="时间序列模型"><a href="#时间序列模型" class="headerlink" title="时间序列模型"></a>时间序列模型</h1><h2 id="简单概念"><a href="#简单概念" class="headerlink" title="简单概念"></a>简单概念</h2><p>从专业定义上来讲：时间序列是按时间顺序排列的数据集合。这些数据可以在任何时间间隔（例如，每秒、每分钟、每小时、每天、每月、每年等）观察和收集。<br>时间序列可以简单理解为一串与时间相关的数字，比如每月的销售额，每年的GDP，每天的气温等。</p>
<h2 id="时间序列分类"><a href="#时间序列分类" class="headerlink" title="时间序列分类"></a>时间序列分类</h2><p>时间序列分为两类：  </p>
<ul>
<li>时期时间序列：结果表示一段时间总和，比如国家今年GDP，就是这一年的总和。</li>
<li>时点时间序列：结果表示目标某一个时刻达到的值，比如今天的温度，这个月你的体重。  </li>
</ul>
<p>简单的区分方法：时期时间序列的累加是有意义的，比如2024年和2025年GDP累加，就表示这两年的GDP之和；而时点时间序列的累加是没有意义的，比如今天的气温+明天的气温，或者你这个月的体重+上个月的体重，这些值没有意义。  </p>
<h2 id="单变量和多变量"><a href="#单变量和多变量" class="headerlink" title="单变量和多变量"></a>单变量和多变量</h2><p>单变量时间序列只涉及一个变量随时间的变化。比如上面的每月的销售额变化，只看了销售额这一个变量。<br>既然有单变量，就会有多变量。多变量时间序列描述一个事件多个指标随时间的变化，而且由于是同一个事件，各个指标之间也会有关系。<br>比如出售商品，涉及到销售额，广告费，产品数量等指标，这些指标之间会产生联系。  </p>
<p>从预测结果的角度，单变量的预测只需要考虑该变量前面时间的数据；而多变量中，每一个变量的预测都需要考虑前面时间每个变量的数据，并不是简单的多个单变量组合起来。  </p>
<h2 id="时间序列与机器学习"><a href="#时间序列与机器学习" class="headerlink" title="时间序列与机器学习"></a>时间序列与机器学习</h2><p>从前面可以了解到，时间序列就是通过过去数据来预测未来数据，容易联想到机器学习。机器学习也是从已知的数据训练出模型去预测未知的数据。这样看来，是不是用机器学习就可以解除时间序列的预测问题呢？  </p>
<p>答案是否定的。  </p>
<p>机器学习和时间序列模型的内在不同，</p>
<ul>
<li>对于机器学习，其训练数据直接往往没有联系，只是单纯组合并训练出模型结果。</li>
<li>对于时间序列模型，其数据是严格依赖时间的，因此无法忽略数据之间的联系，其数据之间存在相互影响，需要额外考虑。  </li>
</ul>
<p>这里我将机器学习和时间序列模型放在同等地位理解，下面的内容也和机器学习过程有很多相似。</p>
<h2 id="时间序列模型建模过程"><a href="#时间序列模型建模过程" class="headerlink" title="时间序列模型建模过程"></a>时间序列模型建模过程</h2><p>时间序列模型也是预测未知数据的模型，各步骤类似机器学习。</p>
<h3 id="划分数据集"><a href="#划分数据集" class="headerlink" title="划分数据集"></a>划分数据集</h3><p>前面说过，时间序列相关模型，本质任务就是使用过去的数据预测未来的数据。所以训练集一定是用历史数据，而测试集要预测的时间一定在训练集之后。  </p>
<h3 id="训练方式"><a href="#训练方式" class="headerlink" title="训练方式"></a>训练方式</h3><h4 id="单步预测"><a href="#单步预测" class="headerlink" title="单步预测"></a>单步预测</h4><p>仅预测需要的某一个时间点的值，比如通过1月-8月的销售量，预测9月的。<br>这里，我们只关心这一个点的预测结果，而不考虑后面的结果。</p>
<h4 id="多步预测"><a href="#多步预测" class="headerlink" title="多步预测"></a>多步预测</h4><p>预测需要的某一段时间的值，比如通过1月-8月的销售量，预测9月-12月的。<br>这里，我们不只关心一个点的预测结果。很明显，离现在越远的未来越难预测，因此应该要尽量压缩训练集与测试集之间的时间差。<br>一种预测方法是按照时间进行，将先预测到的值作为后面预测的参考。比如使用6-8月预测出9月的值，然后使用7-8月的真实值+9月的预测值预测出10月的，以此类推。<br>当然，对于这样的预测结果，缺陷也是很明显的，因为误差会逐步累计。如果第一步预测有问题，那么后面误差会越来越大。</p>
<h4 id="逐点预测与分布预测"><a href="#逐点预测与分布预测" class="headerlink" title="逐点预测与分布预测"></a>逐点预测与分布预测</h4><p>逐点预测是指在需要的时间点，预测一个最可能的值。这是我们最常见的预测形式，例如在天气预测中，我们可能会预测明天的最高气温为28度。<br>分布预测，顾名思义，即预测需要的点，在该点各个结果可能出现的分别情况。例如在天气预测中，我们可能会预测明天的最高气温为27-29度的可能性为70%，25-26以及30-31的可能性为10%等。</p>
<h2 id="时序任务分类"><a href="#时序任务分类" class="headerlink" title="时序任务分类"></a>时序任务分类</h2><h3 id="Forecasting"><a href="#Forecasting" class="headerlink" title="Forecasting"></a>Forecasting</h3><p>即前面常举例的预测任务，这里的数据一般需要满足：</p>
<ul>
<li>数据样本由时间顺序排列</li>
<li>各个样本之间的时间间隔保持一致</li>
<li>数据样本之间存在关联</li>
<li><p>过去的样本数据影响未来的预测值</p>
<h3 id="Time-Supervision"><a href="#Time-Supervision" class="headerlink" title="Time Supervision"></a>Time Supervision</h3><p>类似机器学习，<br>时序有监督学习包括时间序列回归和时间序列分类两种有监督学习方法（Time Series Regression/ Time Series Classification），指的是在<strong>多变量</strong>时间序列上，基于时间或时间相关的数据，完成回归或分类任务，这类任务对样本与样本之间的时间顺序、相关等都没有要求、反而更关注特征之间的关系，因此这类任务要求：</p>
</li>
<li><p>数据中必须存在除时间之外的其他特征，且特征越多越好</p>
</li>
<li>可以完成“去时序化”处理，将时序数据彻底变为一般机器学习数据</li>
<li>这一类任务是学习特征值、预测标签值，并不关心样本。</li>
</ul>
<p>时间序列模型同机器学习一样有很多应用，也有很多实例算法，比如下面即将讲到的：</p>
<h1 id="AR模型"><a href="#AR模型" class="headerlink" title="AR模型"></a>AR模型</h1><h2 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h2><p>AR模型，即自回归模型（Autoregressive Model），是统计学中处理时间序列的常用方法，广泛应用于经济学、信息学、自然现象预测及计算机视觉等领域。  </p>
<h2 id="公式："><a href="#公式：" class="headerlink" title="公式："></a>公式：</h2><script type="math/tex; mode=display">X_t = \phi_ 0 + \phi_ 1 X_{t-1} + \phi_ 2 X_{t-2} + \dots + \phi_ p X_{t-p} + \varepsilon_t</script><p>其中， $X<em>t$ 为t时刻的预测值， $\phi</em> 0$ 为常数项， $X<em>t$ 到 $X</em>{t-p}$ 为历史数据，阶数 $p$ 表示考虑前p个的历史值，$\phi<em> 1$ 到 $\phi</em> p$ 表示权重。</p>
<h2 id="重要理解"><a href="#重要理解" class="headerlink" title="重要理解"></a>重要理解</h2><p>AR模型当前的结果一定依赖于过去的积累，因此AR模型相信：一个时间点上的标签值一定是依赖于之前的时间点上的标签值而存在的。</p>
<h1 id="MA模型"><a href="#MA模型" class="headerlink" title="MA模型"></a>MA模型</h1><h2 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h2><p>MA模型，即移动平均模型，是时间序列分析中的一种模型，它描述的是当前时间点的数据与过去<strong>噪声</strong>的关系。<br>严格定义上来讲：其模型的定义是基于白噪声序列的假设。白噪声是一种特殊的时间序列模型，每个时间点的数据都是独立且服从相同分布的，且具有常数的均值和方差。</p>
<h2 id="公式"><a href="#公式" class="headerlink" title="公式"></a>公式</h2><script type="math/tex; mode=display">X_t = \mu + \varepsilon_t - \theta_ 1 \varepsilon_{t-1} - \theta_ 2 \varepsilon_{t-2} - \dots - \theta_q \varepsilon_{t-q}</script><p>其中，</p>
<ul>
<li>$X_t$ 为t时刻的预测值（此处实际上是误差的预测），</li>
<li>$\varepsilon_t$ 为当前误差，</li>
<li>$\mu$ 为常数项, 是时间序列的均值或期望值</li>
<li>$\varepsilon<em>{t-1}$ 到 $\varepsilon</em>{t-q}$ 为历史的误差，</li>
<li>q表示考虑前q个，</li>
<li>$\theta<em> 1$ 到 $\theta</em> q$ 表示权重。<h2 id="重要理解-1"><a href="#重要理解-1" class="headerlink" title="重要理解"></a>重要理解</h2>从定义上来看：MA模型的基本思想和基本假设与AR模型大不相同。MA模型的基本思想是：大部分时候时间序列应当是相对稳定的。在稳定的基础上，每个时间点上的标签值受过去一段时间内、不可预料的各种偶然事件影响而波动。<br>也就是说，AR模型是考虑了数据从历史以来的稳定性，而MA模型考虑这种稳定性上的突发情况。因此，二者相辅相成，组成ARMA模型。</li>
</ul>
<h1 id="ARMA-p-q-模型（自回归移动平均模型）"><a href="#ARMA-p-q-模型（自回归移动平均模型）" class="headerlink" title="ARMA(p,q) 模型（自回归移动平均模型）"></a>ARMA(p,q) 模型（自回归移动平均模型）</h1><script type="math/tex; mode=display">X_t = \phi_0 + \phi_1 X_{t-1} + \dots + \phi_p X_{t-p} + \varepsilon_t - \theta_1 \varepsilon_{t-1} - \dots - \theta_q \varepsilon_{t-q}</script><h1 id="ARIMA模型"><a href="#ARIMA模型" class="headerlink" title="ARIMA模型"></a>ARIMA模型</h1><h2 id="组成"><a href="#组成" class="headerlink" title="组成"></a>组成</h2><p>ARIMA模型全称为自回归差分移动平均模型（Autoregressive Integrated Moving Average Model）。ARIMA模型主要由三部分构成，分别为自回归模型（AR）、差分过程（I）和移动平均模型（MA）。</p>
<ul>
<li>使用差分将时间序列变平稳</li>
<li>使用AR考虑过去历史值的影响</li>
<li>使用MA考虑过去误差，突发事件的影响  </li>
</ul>
<p>在ARIMA模型（$ARIMA(p,d,q)$）中，参数选择的核心逻辑是 <strong>“先定d（差分阶数）→ 再选p（AR阶数）和q（MA阶数）”</strong>，其中：</p>
<ul>
<li>$d$ 依赖 <strong>ADF平稳性检验</strong>（确保序列平稳）；</li>
<li>$p$ 和 $q$ 结合 <strong>ACF/PACF图形特征</strong>（直观判断）和 <strong>AIC/BIC准则</strong>（客观择优）；</li>
<li>最终通过 <strong>残差检验</strong> 验证模型合理性。</li>
</ul>
<h2 id="差分"><a href="#差分" class="headerlink" title="差分"></a>差分</h2><p>1阶差分：$\nabla X<em>t = X_t - X</em>{t-1}$<br>d阶差分：$\nabla^d X_t = \nabla(\nabla^{d-1} X_t)$  </p>
<p>一个序列要平稳，关键是「统计特性不随时间变化」，核心需要满足：</p>
<ol>
<li>均值恒定：不管在哪个时间点（比如 t=10 或 t=100），序列的平均水平不变；</li>
<li>方差恒定：序列的波动幅度（离散程度）不随时间变大或变小；  </li>
</ol>
<p>于是，对于各种非平稳的序列，比如线性增长或者二次增长等，就可以通过一次或多次差分使序列变平稳，序列平稳也是AR和MA模型使用的前提。</p>
<h2 id="模型核心公式"><a href="#模型核心公式" class="headerlink" title="模型核心公式"></a>模型核心公式</h2><script type="math/tex; mode=display">\nabla^d X_t = \phi_0 + \phi_1 \nabla^d X_{t-1} + \dots + \phi_p \nabla^d X_{t-p} + \varepsilon_t - \theta_1 \varepsilon_{t-1} - \dots - \theta_q \varepsilon_{t-q}</script><p>这个公式基本上是将AR模型和MA模型的公式组合在一起，其中有重复的常数项就合并了，各项的定义同前面的公式。</p>
<h2 id="ACF与PACF"><a href="#ACF与PACF" class="headerlink" title="ACF与PACF"></a>ACF与PACF</h2><h3 id="概念铺垫"><a href="#概念铺垫" class="headerlink" title="概念铺垫"></a>概念铺垫</h3><ol>
<li><strong>时间序列平稳性</strong>：以下所有公式均基于「宽平稳序列」（均值恒定、方差恒定、自协方差仅与时间间隔有关）——非平稳序列需先差分（如ARIMA的d步），再对差分后的平稳序列分析ACF/PACF；</li>
<li><strong>滞后阶数</strong>：用 $k$ 表示「时间间隔」（ $k=0,1,2,…$ ），$k=0$表示序列与自身的相关（恒为1），$k=1$表示 $y<em>t$ 与 $y</em>{t-1}$ 的相关，$k=2$ 表示 $y<em>t$ 与 $y</em>{t-2}$ 的相关，以此类推。</li>
</ol>
<h3 id="ACF"><a href="#ACF" class="headerlink" title="ACF"></a>ACF</h3><h4 id="1-核心定义"><a href="#1-核心定义" class="headerlink" title="1. 核心定义"></a>1. 核心定义</h4><p>ACF（自相关函数，Auto-Correlation Function）是描述时间序列中「相隔$k$期的两个观测值 $y<em>t$ 和 $y</em>{t-k}$ 之间的线性相关程度」，反映的是「总相关关系」（包含直接相关和通过中间变量的间接相关）。</p>
<h4 id="2-数学公式"><a href="#2-数学公式" class="headerlink" title="2. 数学公式"></a>2. 数学公式</h4><h5 id="（1）总体自相关函数（理论值）"><a href="#（1）总体自相关函数（理论值）" class="headerlink" title="（1）总体自相关函数（理论值）"></a>（1）总体自相关函数（理论值）</h5><p>设平稳序列 $y_t$ 的均值为 $\mu = E(y_t)$，方差为 $\sigma^2 = Var(y_t)$，则滞后 $k$ 阶的ACF记为 $\rho_k$：</p>
<script type="math/tex; mode=display">
\rho_k = \frac{Cov(y_t, y_{t-k})}{\sqrt{Var(y_t)} \cdot \sqrt{Var(y_{t-k})}} = \frac{\gamma_k}{\gamma_0}</script><p>其中：</p>
<ul>
<li>$\gamma<em>k = Cov(y_t, y</em>{t-k}) = E[(y<em>t - \mu)(y</em>{t-k} - \mu)]$：滞后$k$阶的自协方差（衡量$y<em>t$与$y</em>{t-k}$的线性关联强度）；</li>
<li>$\gamma_0 = Cov(y_t, y_t) = Var(y_t) = \sigma^2$：滞后0阶的自协方差（即序列本身的方差）。</li>
</ul>
<h5 id="（2）样本自相关函数（实际计算值）"><a href="#（2）样本自相关函数（实际计算值）" class="headerlink" title="（2）样本自相关函数（实际计算值）"></a>（2）样本自相关函数（实际计算值）</h5><p>建模时需用样本数据估计ACF，记为$\hat{\rho}_k$：</p>
<script type="math/tex; mode=display">
\hat{\rho}_k = \frac{\sum_{t=k+1}^T (y_t - \bar{y})(y_{t-k} - \bar{y})}{\sum_{t=1}^T (y_t - \bar{y})^2}</script><p>其中：</p>
<ul>
<li>$T$：样本量；</li>
<li>$\bar{y} = \frac{1}{T}\sum_{t=1}^T y_t$：样本均值；</li>
<li>分子：样本自协方差（滞后$k$阶）；</li>
<li>分母：样本方差（滞后0阶自协方差）。</li>
</ul>
<h4 id="3-关键性质"><a href="#3-关键性质" class="headerlink" title="3. 关键性质"></a>3. 关键性质</h4><ul>
<li>对称性：$\rho<em>k = \rho</em>{-k}$（$y<em>t$与$y</em>{t-k}$的相关和$y_{t-k}$与$y_t$的相关相同）；</li>
<li>取值范围：$-1 \leq \rho_k \leq 1$（与普通相关系数一致，越接近±1，相关越强）；</li>
<li>$\rho_0 = 1$（序列与自身完全相关）。</li>
</ul>
<h3 id="PACF"><a href="#PACF" class="headerlink" title="PACF"></a>PACF</h3><h4 id="1-核心定义-1"><a href="#1-核心定义-1" class="headerlink" title="1. 核心定义"></a>1. 核心定义</h4><p>PACF（偏自相关函数，Partial Auto-Correlation Function）是描述时间序列中「相隔 $k$ 期的两个观测值 $y<em>t$ 和 $y</em>{t-k}$，在剔除了中间 $k-1$ 个观测值（$y<em>{t-1}, y</em>{t-2}, …, y_{t-k+1}$）的干扰后，剩余的直接线性相关程度」——简单说，PACF“剥离了间接相关”，只保留「直接相关关系」。</p>
<h4 id="2-数学公式-1"><a href="#2-数学公式-1" class="headerlink" title="2. 数学公式"></a>2. 数学公式</h4><h5 id="（1）直观定义（条件相关视角）"><a href="#（1）直观定义（条件相关视角）" class="headerlink" title="（1）直观定义（条件相关视角）"></a>（1）直观定义（条件相关视角）</h5><p>滞后$k$阶的PACF记为 $\phi_{kk}$ ，本质是以下线性回归模型中的系数 $\beta_k$ ：</p>
<script type="math/tex; mode=display">
y_t = \beta_1 y_{t-1} + \beta_2 y_{t-2} + ... + \beta_k y_{t-k} + \epsilon_t</script><p>即 $\phi<em>{kk} = \beta_k$——表示在控制了 $y</em>{t-1}$ 到 $y<em>{t-k+1}$ 的影响后， $y</em>{t-k}$ 对 $y_t$ 的直接影响系数。</p>
<h5 id="（2）严格公式（Yule-Walker方程推导）"><a href="#（2）严格公式（Yule-Walker方程推导）" class="headerlink" title="（2）严格公式（Yule-Walker方程推导）"></a>（2）严格公式（Yule-Walker方程推导）</h5><script type="math/tex; mode=display">
\phi_{kk} = \frac{\rho_k - \sum_{i=1}^{k-1} \phi_{k-1,i} \rho_{k-i}}{1 - \sum_{i=1}^{k-1} \phi_{k-1,i} \rho_i}</script><p>其中：</p>
<ul>
<li>$\phi_{k-1,i}$：滞后$k-1$阶PACF模型中的系数；</li>
<li>$\rho_k$：滞后$k$阶的ACF值。</li>
</ul>
<h5 id="（3）样本偏自相关函数（实际计算值）"><a href="#（3）样本偏自相关函数（实际计算值）" class="headerlink" title="（3）样本偏自相关函数（实际计算值）"></a>（3）样本偏自相关函数（实际计算值）</h5><p>建模时用样本数据估计，记为 $\hat{\phi}_{kk}$ ，通常通过「递推法」计算（避免直接解高阶方程组），核心递推公式：</p>
<script type="math/tex; mode=display">
\hat{\phi}_{11} = \hat{\rho}_1</script><script type="math/tex; mode=display">
\hat{\phi}_{kk} = \hat{\rho}_k - \sum_{i=1}^{k-1} \hat{\phi}_{k-1,i} \hat{\rho}_{k-i} \quad (k \geq 2)</script><h4 id="3-关键性质-1"><a href="#3-关键性质-1" class="headerlink" title="3. 关键性质"></a>3. 关键性质</h4><ul>
<li>取值范围：$-1 \leq \phi_{kk} \leq 1$；</li>
<li>$\phi_{11} = \rho_1$（滞后1阶时，无中间变量，ACF=PACF）；</li>
<li>高阶滞后时，PACF会快速衰减或截尾（核心用于AR模型定阶）。</li>
</ul>
<h3 id="ACF与PACF的区别"><a href="#ACF与PACF的区别" class="headerlink" title="ACF与PACF的区别"></a>ACF与PACF的区别</h3><p>简单理解</p>
<ul>
<li>假设序列是 $y<em>t$ （今天的气温）、$y</em>{t-1}$（昨天的气温）、$y_{t-2}$（前天的气温）；</li>
<li>ACF($k=2$)： $\rho_2$ 衡量今天和前天的「总相关」——包含“前天→昨天→今天”的间接相关（前天影响昨天，昨天再影响今天）和“前天→今天”的直接相关；</li>
<li>PACF($k=2$)： $\phi_{22}$ 衡量今天和前天的「直接相关」——剔除了昨天的干扰，只看前天是否直接影响今天（比如前天的冷空气是否直接持续到今天，不通过昨天传递）。</li>
</ul>
<h2 id="ADF检验"><a href="#ADF检验" class="headerlink" title="ADF检验"></a>ADF检验</h2><p>$d$ 是使非平稳序列变为平稳序列所需的 <strong>最小差分次数</strong>，核心工具是 <strong>ADF检验</strong>（Augmented Dickey-Fuller Test）。</p>
<h3 id="核心原理"><a href="#核心原理" class="headerlink" title="核心原理"></a>核心原理</h3><p>ADF检验的目的是 <strong>检验序列是否存在单位根</strong>：</p>
<ul>
<li>原假设 $H_0$：序列存在单位根 → 非平稳；</li>
<li>备择假设 $H_1$：序列不存在单位根 → 平稳；</li>
<li>判定规则：计算ADF统计量，与临界值（1%/5%/10%）对比：<ul>
<li>若 <strong>ADF统计量 &lt; 临界值</strong> → 拒绝 $H_0$ → 序列平稳；</li>
<li>若 <strong>ADF统计量 ≥ 临界值</strong> → 接受 $H_0$ → 序列非平稳，需差分。</li>
</ul>
</li>
</ul>
<h3 id="步骤流程"><a href="#步骤流程" class="headerlink" title="步骤流程"></a>步骤流程</h3><ol>
<li>对原始序列 $y_t$ 做ADF检验：<ul>
<li>若平稳（ADF统计量 &lt; 5%临界值）→ $d=0$；</li>
<li>若不平稳 → 做1阶差分（ $d^1<em>t = y_t - y</em>{t-1}$ ），对 $d^1$ 做ADF检验；</li>
</ul>
</li>
<li>对1阶差分序列 $d^1$ 做ADF检验：<ul>
<li>若平稳 → $d=1$；</li>
<li>若不平稳 → 做2阶差分（$d^2<em>t = d^1_t - d^1</em>{t-1}$），对 $d^2_t$ 做ADF检验；</li>
</ul>
</li>
<li>对2阶差分序列 $d^2_t$ 做ADF检验：<ul>
<li>若平稳 → $d=2$；</li>
<li>若仍不平稳 → 优先检查序列是否有季节性（需用SARIMA模型），而非继续增加 $d$（$d≥3$ 时模型易过拟合，现实中极少用）。</li>
</ul>
</li>
</ol>
<h3 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h3><ul>
<li>$d$ 通常取0、1、2，超过2时模型解释性差，且易出现过拟合；</li>
<li>若原始序列本身平稳（如白噪声），则 $d=0$（此时ARIMA退化为ARMA模型）；</li>
<li>差分过度会导致序列信息丢失（如2阶差分可能把平稳序列变成“过度差分”，方差变大），因此需用ADF检验严格验证，而非盲目差分。</li>
</ul>
<h2 id="确定-p-和-q：AR阶数和MA阶数"><a href="#确定-p-和-q：AR阶数和MA阶数" class="headerlink" title="确定 p 和 q：AR阶数和MA阶数"></a>确定 p 和 q：AR阶数和MA阶数</h2><p>确定 $d$ 后，基于 <strong>差分后的平稳序列</strong>（记为 $y_{diff}^d$），通过“ACF/PACF图形特征”初步判断 $p$ 和 $q$，再用“AIC/BIC准则”客观择优。</p>
<h3 id="ACF-PACF图形直观判断（快速定范围）"><a href="#ACF-PACF图形直观判断（快速定范围）" class="headerlink" title="ACF/PACF图形直观判断（快速定范围）"></a>ACF/PACF图形直观判断（快速定范围）</h3><p>ACF（自相关函数）反映“总相关”，PACF（偏自相关函数）反映“直接相关”，两者的 <strong>截尾/拖尾特征</strong> 是选择 $p$ 和 $q$ 的核心依据（仅适用于平稳序列）：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>模型类型</th>
<th>ACF（自相关函数）</th>
<th>PACF（偏自相关函数）</th>
<th>核心逻辑</th>
</tr>
</thead>
<tbody>
<tr>
<td>AR(p)</td>
<td>拖尾（缓慢衰减至0）</td>
<td>截尾（$k=p$后落入置信区间）</td>
<td>PACF“截尾”对应AR的阶数p：比如PACF在k=2后显著为0，说明p=2</td>
</tr>
<tr>
<td>MA(q)</td>
<td>截尾（$k=q$后落入置信区间）</td>
<td>拖尾（缓慢衰减至0）</td>
<td>ACF“截尾”对应MA的阶数q：比如ACF在k=3后显著为0，说明q=3</td>
</tr>
<tr>
<td>ARMA(p,q)</td>
<td>拖尾</td>
<td>拖尾</td>
<td>无法仅通过图形确定，需结合AIC/BIC遍历搜索</td>
</tr>
</tbody>
</table>
</div>
<p>其实实际问题，通常采用逐个检验的傻瓜方法来确定p和q，通常都是较小的数字。</p>
<h2 id="AIC和BIC"><a href="#AIC和BIC" class="headerlink" title="AIC和BIC"></a>AIC和BIC</h2><p>在 ARIMA 模型参数选择中，AIC（赤池信息准则）和 BIC（贝叶斯信息准则）是客观判断最优模型的核心工具—— 本质是通过 “平衡模型拟合效果” 和 “模型复杂度”，避免欠拟合或过拟合，最终选出 “性价比最高” 的模型。  </p>
<p>在ARIMA模型参数选择中，<strong>AIC（赤池信息准则）和BIC（贝叶斯信息准则）是客观判断最优模型的核心工具</strong>——本质是通过“平衡模型拟合效果”和“模型复杂度”，避免欠拟合或过拟合，最终选出“性价比最高”的模型。以下从「核心作用、公式含义、判断规则、关键差异、实操步骤」全维度讲解，结合ARIMA场景落地：</p>
<h3 id="核心作用：解决“主观判断的不足”"><a href="#核心作用：解决“主观判断的不足”" class="headerlink" title="核心作用：解决“主观判断的不足”"></a>核心作用：解决“主观判断的不足”</h3><p>之前用ACF/PACF图判断p、q时，“拖尾/截尾”的界定有主观性（比如有人认为k=2截尾，有人认为k=3）；而AIC/BIC通过<strong>量化指标</strong>，从“所有候选模型”中选出最优，核心目标是：</p>
<blockquote>
<p>在“拟合得好”和“参数少（简单）”之间找最优解——既不因为参数太少导致欠拟合，也不因为参数太多导致过拟合。</p>
</blockquote>
<h3 id="公式与核心含义"><a href="#公式与核心含义" class="headerlink" title="公式与核心含义"></a>公式与核心含义</h3><p>AIC和BIC的公式都围绕“似然值（拟合度）”和“参数个数（复杂度）”展开，先记住核心公式和每个符号的意义：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>准则</th>
<th>公式（简化版）</th>
<th>各部分含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>AIC</td>
<td>$AIC = 2k - 2\ln(L)$</td>
<td>- $k$：模型的参数个数（ARIMA中$k = p + q + 1$，+1是常数项；比如ARIMA(2,1,1)的$k=2+1+1=4$）<br>- $\ln(L)$：模型的对数似然值（$L$是似然值，越大表示模型对数据的拟合效果越好；$\ln(L)$越大，$-2\ln(L)$越小）<br>- $2k$：复杂度惩罚项（参数越多，惩罚越重）</td>
</tr>
<tr>
<td>BIC</td>
<td>$BIC = k\ln(n) - 2\ln(L)$</td>
<td>- $k$：同AIC（参数个数）<br>- $n$：样本量（数据行数）<br>- $\ln(n)$：样本量越大，对参数的惩罚越重<br>- $-2\ln(L)$：同AIC（拟合度项）</td>
</tr>
</tbody>
</table>
</div>
<h4 id="公式的核心逻辑："><a href="#公式的核心逻辑：" class="headerlink" title="公式的核心逻辑："></a>公式的核心逻辑：</h4><ul>
<li><strong>拟合度越好</strong>（$\ln(L)$越大）→ $-2\ln(L)$越小 → AIC/BIC越小（越优）；</li>
<li><strong>参数越多</strong>（$k$越大）→ 惩罚项（$2k$或$k\ln(n)$）越大 → AIC/BIC越大（越差）；</li>
<li>AIC/BIC的最终值，是“拟合度”和“复杂度”的综合得分——<strong>得分越低，模型越优</strong>。</li>
</ul>
<h3 id="AIC-vs-BIC："><a href="#AIC-vs-BIC：" class="headerlink" title="AIC vs BIC："></a>AIC vs BIC：</h3><h4 id="对比"><a href="#对比" class="headerlink" title="对比"></a>对比</h4><p>两者的核心区别是<strong>对“模型复杂度（参数个数）”的惩罚力度不同</strong>，直接决定了适用场景：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>特征</th>
<th>AIC（赤池信息准则）</th>
<th>BIC（贝叶斯信息准则）</th>
</tr>
</thead>
<tbody>
<tr>
<td>惩罚力度</td>
<td>轻（惩罚项是$2k$，与样本量无关）</td>
<td>重（惩罚项是$k\ln(n)$，样本量越大，惩罚越重）</td>
</tr>
<tr>
<td>核心倾向</td>
<td>优先保证拟合效果，允许稍多参数（易选复杂模型）</td>
<td>优先保证模型简单，严控参数个数（易选简单模型）</td>
</tr>
<tr>
<td>适用场景</td>
<td>1. 样本量小（n<100）<br>2. 希望模型拟合精度高<br>3. 允许少量过拟合</td>
<td>1. 样本量大（n≥100）<br>2. 追求模型解释性（参数少）<br>3. 避免过拟合</td>
</tr>
<tr>
<td>极端情况</td>
<td>样本量再大，也可能选参数多的模型</td>
<td>样本量足够大时，一定会选参数最少的“有效模型”</td>
</tr>
</tbody>
</table>
</div>
<h4 id="实操选择技巧："><a href="#实操选择技巧：" class="headerlink" title="实操选择技巧："></a>实操选择技巧：</h4><ul>
<li>若AIC和BIC指向同一个模型 → 直接选这个模型（最优解）；</li>
<li>若AIC和BIC指向不同模型：<ul>
<li>样本量小（比如n&lt;50）→ 选AIC最优（避免欠拟合）；</li>
<li>样本量大（比如n≥100）→ 选BIC最优（避免过拟合，模型更简洁）；</li>
<li>折中方案：选两个模型中“参数更少、AIC/BIC接近最小值”的（比如AIC最小是162（p=1,q=1），次小是163（p=1,q=0），选p=1,q=0）。</li>
</ul>
</li>
</ul>
<h3 id="常见误区"><a href="#常见误区" class="headerlink" title="常见误区"></a>常见误区</h3><ol>
<li><strong>误区1</strong>：AIC/BIC越小越好 → 错！如果参数搜索范围过大（比如p=0~10），可能选到过拟合模型（AIC小但泛化能力差），需结合ACF/PACF限定搜索范围（通常0~3/0~4）；</li>
<li><strong>误区2</strong>：只看AIC/BIC，忽略残差检验 → 错！AIC/BIC是“择优”，但不能保证模型“有效”（比如序列有季节性，ARIMA本身不适用，AIC/BIC再小也没用）；</li>
<li><strong>误区3</strong>：不同模型的AIC/BIC可直接对比 → 错！只有“同一数据集、同一d值”的ARIMA模型，AIC/BIC才可比（比如d=1的模型和d=2的模型，AIC不可比）；</li>
<li><strong>误区4</strong>：BIC一定比AIC好 → 错！样本量小时，BIC的强惩罚可能导致欠拟合（比如明明p=2更好，BIC却选p=1），需结合场景。</li>
</ol>
<h3 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h3><p>AIC和BIC的核心判断逻辑是：<strong>在限定的p/q范围内，选准则值最小的模型</strong>，其中：</p>
<ul>
<li>AIC：“拟合优先”，适合小样本；</li>
<li>BIC：“简洁优先”，适合大样本；</li>
<li>最终必须用残差检验验证，避免“最优但无效”的模型。</li>
</ul>
<p>这两个准则的价值是“把主观判断变成客观量化”，是ARIMA参数选择中最核心的“兜底工具”——结合ACF/PACF的直观判断+AIC/BIC的客观择优，才能选出最优的p、q、d。</p>
<h2 id="ARIMA模型小结"><a href="#ARIMA模型小结" class="headerlink" title="ARIMA模型小结"></a>ARIMA模型小结</h2><div class="table-container">
<table>
<thead>
<tr>
<th>步骤</th>
<th>核心工具</th>
<th>目标</th>
<th>输出</th>
</tr>
</thead>
<tbody>
<tr>
<td>1</td>
<td>ADF检验</td>
<td>使序列平稳</td>
<td>确定d</td>
</tr>
<tr>
<td>2</td>
<td>ACF/PACF图</td>
<td>初步判断p和q的范围</td>
<td>p_range, q_range</td>
</tr>
<tr>
<td>3</td>
<td>AIC/BIC准则</td>
<td>客观选择最优p和q</td>
<td>确定p, q</td>
</tr>
<tr>
<td>4</td>
<td>残差检验</td>
<td>验证模型合理性</td>
<td>确认参数或调整</td>
</tr>
</tbody>
</table>
</div>
<h1 id="详细代码"><a href="#详细代码" class="headerlink" title="详细代码"></a>详细代码</h1><p>下面代码很多部分使用python <code>statsmodels</code> 库，先去下载一下。<br>使用股票价格预测举例</p>
<h2 id="差分-1"><a href="#差分-1" class="headerlink" title="差分"></a>差分</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"></span><br><span class="line">PATH=<span class="string">&#x27;ChinaBank.csv&#x27;</span></span><br><span class="line"></span><br><span class="line">ChinaBank = pd.read_csv(PATH,index_col = <span class="string">&#x27;Date&#x27;</span>,parse_dates=[<span class="string">&#x27;Date&#x27;</span>])</span><br><span class="line"></span><br><span class="line">ChinaBank.head()</span><br><span class="line">ChinaBank.index = pd.to_datetime(ChinaBank.index)</span><br><span class="line">sub = ChinaBank.loc[<span class="string">&#x27;2014-01&#x27;</span>:<span class="string">&#x27;2014-06&#x27;</span>,<span class="string">&#x27;Close&#x27;</span>]</span><br><span class="line"></span><br><span class="line">sub.head()</span><br><span class="line">train = sub.loc[<span class="string">&#x27;2014-01&#x27;</span>:<span class="string">&#x27;2014-03&#x27;</span>]</span><br><span class="line">test = sub.loc[<span class="string">&#x27;2014-04&#x27;</span>:<span class="string">&#x27;2014-06&#x27;</span>]</span><br><span class="line"><span class="comment">#查看训练集的时间序列与数据(只包含训练集)</span></span><br><span class="line">plt.figure(figsize=(<span class="number">12</span>,<span class="number">6</span>))</span><br><span class="line">plt.plot(train)</span><br><span class="line">plt.xticks(rotation=<span class="number">45</span>) <span class="comment">#旋转45度</span></span><br><span class="line">plt.show()</span><br><span class="line"><span class="comment">#.diff(1)做一个时间间隔</span></span><br><span class="line">ChinaBank[<span class="string">&#x27;diff_1&#x27;</span>] = ChinaBank[<span class="string">&#x27;Close&#x27;</span>].diff(<span class="number">1</span>) <span class="comment">#1阶差分</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#对一阶差分数据在划分时间间隔</span></span><br><span class="line">ChinaBank[<span class="string">&#x27;diff_2&#x27;</span>] = ChinaBank[<span class="string">&#x27;diff_1&#x27;</span>].diff(<span class="number">1</span>) <span class="comment">#2阶差分</span></span><br><span class="line"></span><br><span class="line">fig = plt.figure(figsize=(<span class="number">12</span>,<span class="number">10</span>))</span><br><span class="line"><span class="comment">#原数据</span></span><br><span class="line">ax1 = fig.add_subplot(<span class="number">311</span>)</span><br><span class="line">ax1.plot(ChinaBank[<span class="string">&#x27;Close&#x27;</span>])</span><br><span class="line"><span class="comment">#1阶差分</span></span><br><span class="line">ax2 = fig.add_subplot(<span class="number">312</span>)</span><br><span class="line">ax2.plot(ChinaBank[<span class="string">&#x27;diff_1&#x27;</span>])</span><br><span class="line"><span class="comment">#2阶差分</span></span><br><span class="line">ax3 = fig.add_subplot(<span class="number">313</span>)</span><br><span class="line">ax3.plot(ChinaBank[<span class="string">&#x27;diff_2&#x27;</span>])</span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure>
<p>这个代码可以直观看到差分可以将时间序列变平稳。</p>
<h2 id="ADF"><a href="#ADF" class="headerlink" title="ADF"></a>ADF</h2><p>使用ADF检验确定d<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">import</span> statsmodels.api <span class="keyword">as</span> sm</span><br><span class="line"><span class="keyword">from</span> statsmodels.tsa.seasonal <span class="keyword">import</span> seasonal_decompose</span><br><span class="line"><span class="keyword">from</span> statsmodels.tsa.stattools <span class="keyword">import</span> adfuller <span class="keyword">as</span> ADF</span><br><span class="line"></span><br><span class="line"><span class="comment"># 计算原始序列、一阶差分序列、二阶差分序列的单位根检验结果</span></span><br><span class="line">ChinaBank[<span class="string">&#x27;diff_1&#x27;</span>] = ChinaBank[<span class="string">&#x27;diff_1&#x27;</span>].fillna(<span class="number">0</span>)</span><br><span class="line">ChinaBank[<span class="string">&#x27;diff_2&#x27;</span>] = ChinaBank[<span class="string">&#x27;diff_2&#x27;</span>].fillna(<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">timeseries_adf = ADF(ChinaBank[<span class="string">&#x27;Close&#x27;</span>].tolist())</span><br><span class="line">timeseries_diff1_adf = ADF(ChinaBank[<span class="string">&#x27;diff_1&#x27;</span>].tolist())</span><br><span class="line">timeseries_diff2_adf = ADF(ChinaBank[<span class="string">&#x27;diff_2&#x27;</span>].tolist())</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 打印单位根检验结果</span></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&#x27;timeseries_adf : &#x27;</span>, timeseries_adf)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&#x27;timeseries_diff1_adf : &#x27;</span>, timeseries_diff1_adf)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&#x27;timeseries_diff2_adf : &#x27;</span>, timeseries_diff2_adf)</span><br></pre></td></tr></table></figure><br>下面是运行结果<br><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">timeseries_adf :  (0.527919808483182, 0.9856974415734416, 9, 335, &#123;&#x27;1%&#x27;: -3.4500219858626227, &#x27;5%&#x27;: -2.870206553997666, &#x27;10%&#x27;: -2.571387268879483&#125;, -734.0738716811488)</span><br><span class="line">timeseries_diff1_adf :  (-6.177185544979001, 6.58710923976123e-08, 8, 336, &#123;&#x27;1%&#x27;: -3.449962981927952, &#x27;5%&#x27;: -2.870180642420163, &#x27;10%&#x27;: -2.5713734527352607&#125;, -735.8436797171294)</span><br><span class="line">timeseries_diff2_adf :  (-9.202545123160352, 1.9841232339615405e-15, 13, 331, &#123;&#x27;1%&#x27;: -3.4502615951739393, &#x27;5%&#x27;: -2.8703117734117742, &#x27;10%&#x27;: -2.5714433728242714&#125;, -717.2833732193085)</span><br></pre></td></tr></table></figure><br>只需要看第二个数（P值），如果小于一个较小值（如0.05），表示接受该差分结果。具体说明看上面，其中证明过程类似高中学的相关性检验。</p>
<h2 id="p和q初步确定"><a href="#p和q初步确定" class="headerlink" title="p和q初步确定"></a>p和q初步确定</h2><p>前面代码不变，加上下面这些<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">fig = plt.figure(figsize=(<span class="number">12</span>,<span class="number">7</span>))</span><br><span class="line"></span><br><span class="line">ax1 = fig.add_subplot(<span class="number">211</span>)</span><br><span class="line">fig = sm.graphics.tsa.plot_acf(train, lags=<span class="number">20</span>,ax=ax1)</span><br><span class="line">ax1.xaxis.set_ticks_position(<span class="string">&#x27;bottom&#x27;</span>) <span class="comment"># 设置坐标轴上的数字显示的位置，top:显示在顶部  bottom:显示在底部</span></span><br><span class="line"><span class="comment">#fig.tight_layout()</span></span><br><span class="line"></span><br><span class="line">ax2 = fig.add_subplot(<span class="number">212</span>)</span><br><span class="line">fig = sm.graphics.tsa.plot_pacf(train, lags=<span class="number">20</span>, ax=ax2)</span><br><span class="line">ax2.xaxis.set_ticks_position(<span class="string">&#x27;bottom&#x27;</span>)</span><br><span class="line"><span class="comment">#fig.tight_layout()</span></span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure><br>其中，是使用<code>sm.graphics.tsa.plot_acf</code>帮你直接确定的拖尾，不需要自己找，详细原理就是前面ACF和PACF的公式。</p>
<h2 id="p和q的具体确定"><a href="#p和q的具体确定" class="headerlink" title="p和q的具体确定"></a>p和q的具体确定</h2><p>通过上面查找拖尾大致确定p和q范围后，就可以进一步通过遍历确定具体值。<br>遍历中检查的原理就是上面提到的AIC，BIC准则<br>下面以BIC为例<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> itertools</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> seaborn <span class="keyword">as</span> sns</span><br><span class="line"><span class="comment">#确定pq的取值范围</span></span><br><span class="line">p_min = <span class="number">0</span></span><br><span class="line">d_min = <span class="number">0</span></span><br><span class="line">q_min = <span class="number">0</span></span><br><span class="line">p_max = <span class="number">5</span></span><br><span class="line">d_max = <span class="number">0</span></span><br><span class="line">q_max = <span class="number">5</span></span><br><span class="line"><span class="comment">#Initialize a DataFrame to store the results,，以BIC准则</span></span><br><span class="line">results_bic = pd.DataFrame(index=[<span class="string">&#x27;AR&#123;&#125;&#x27;</span>.<span class="built_in">format</span>(i) <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(p_min,p_max+<span class="number">1</span>)],</span><br><span class="line">                           columns=[<span class="string">&#x27;MA&#123;&#125;&#x27;</span>.<span class="built_in">format</span>(i) <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(q_min,q_max+<span class="number">1</span>)])</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> p,d,q <span class="keyword">in</span> itertools.product(<span class="built_in">range</span>(p_min,p_max+<span class="number">1</span>),</span><br><span class="line">                               <span class="built_in">range</span>(d_min,d_max+<span class="number">1</span>),</span><br><span class="line">                               <span class="built_in">range</span>(q_min,q_max+<span class="number">1</span>)):</span><br><span class="line">    <span class="keyword">if</span> p==<span class="number">0</span> <span class="keyword">and</span> d==<span class="number">0</span> <span class="keyword">and</span> q==<span class="number">0</span>:</span><br><span class="line">        results_bic.loc[<span class="string">&#x27;AR&#123;&#125;&#x27;</span>.<span class="built_in">format</span>(p), <span class="string">&#x27;MA&#123;&#125;&#x27;</span>.<span class="built_in">format</span>(q)] = np.nan</span><br><span class="line">        <span class="keyword">continue</span></span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        model = sm.tsa.ARIMA(train, order=(p, d, q),</span><br><span class="line">                               <span class="comment">#enforce_stationarity=False,</span></span><br><span class="line">                               <span class="comment">#enforce_invertibility=False,</span></span><br><span class="line">                              )</span><br><span class="line">        results = model.fit()</span><br><span class="line">        results_bic.loc[<span class="string">&#x27;AR&#123;&#125;&#x27;</span>.<span class="built_in">format</span>(p), <span class="string">&#x27;MA&#123;&#125;&#x27;</span>.<span class="built_in">format</span>(q)] = results.bic</span><br><span class="line">    <span class="keyword">except</span>:</span><br><span class="line">        <span class="keyword">continue</span></span><br><span class="line"></span><br><span class="line">results_bic = results_bic[results_bic.columns].astype(<span class="built_in">float</span>)</span><br><span class="line"><span class="comment">#绘制热力图</span></span><br><span class="line">fig, ax = plt.subplots(figsize=(<span class="number">10</span>, <span class="number">8</span>))</span><br><span class="line">ax = sns.heatmap(results_bic,</span><br><span class="line">                 mask=results_bic.isnull(),</span><br><span class="line">                 ax=ax,</span><br><span class="line">                 annot=<span class="literal">True</span>,</span><br><span class="line">                 fmt=<span class="string">&#x27;.2f&#x27;</span>,</span><br><span class="line">                 cmap=<span class="string">&quot;Purples&quot;</span></span><br><span class="line">                 )</span><br><span class="line"></span><br><span class="line">ax.set_title(<span class="string">&#x27;BIC&#x27;</span>)</span><br><span class="line">plt.show()</span><br><span class="line"><span class="built_in">print</span>(results_bic.stack().idxmin())</span><br></pre></td></tr></table></figure><br>过程就是遍历并逐个训练，计算得分，得到最优值。这样的手动遍历可以看出不同的参数组合的不同影响。<br>当然，也有更加简单的方法<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">train_results = sm.tsa.arma_order_select_ic(train, ic=[<span class="string">&#x27;aic&#x27;</span>, <span class="string">&#x27;bic&#x27;</span>], trend=<span class="string">&#x27;n&#x27;</span>, max_ar=<span class="number">8</span>, max_ma=<span class="number">8</span>)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&#x27;AIC&#x27;</span>, train_results.aic_min_order)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&#x27;BIC&#x27;</span>, train_results.bic_min_order)</span><br></pre></td></tr></table></figure><br>直接使用statsmodels 内置的 ARMA 阶数自动选择函数，直接得到最优的p和q</p>
<h2 id="残差检验"><a href="#残差检验" class="headerlink" title="残差检验"></a>残差检验</h2><p>绘制残差的自相关函数（ACF）图，<br>这是一步关键的检验，如果这里出了大问题，那前面的参数就需要进行调整<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#根据以上求得</span></span><br><span class="line">p = <span class="number">1</span></span><br><span class="line">d = <span class="number">0</span></span><br><span class="line">q = <span class="number">0</span></span><br><span class="line">model = sm.tsa.ARIMA(train, order=(p,d,q))</span><br><span class="line">results = model.fit()</span><br><span class="line">resid = results.resid <span class="comment">#获取残差</span></span><br><span class="line"><span class="comment">#绘制</span></span><br><span class="line"><span class="comment">#查看测试集的时间序列与数据(只包含测试集)</span></span><br><span class="line">fig, ax = plt.subplots(figsize=(<span class="number">12</span>, <span class="number">5</span>))</span><br><span class="line"></span><br><span class="line">ax = sm.graphics.tsa.plot_acf(resid, lags=<span class="number">40</span>,ax=ax)</span><br><span class="line"></span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure><br>如果观察到除了第一个以外的点都在指定区域内，就算通过检验。</p>
<h2 id="模型预测"><a href="#模型预测" class="headerlink" title="模型预测"></a>模型预测</h2><p>经历九九八十一难，终于构建成了模型，现在使用这个模型进行预测<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">predict_sunspots = results.predict(dynamic=<span class="literal">False</span>)</span><br><span class="line">fig, ax = plt.subplots(figsize=(<span class="number">12</span>, <span class="number">6</span>))</span><br><span class="line">ax = sub.plot(ax=ax)</span><br><span class="line"><span class="comment">#预测数据</span></span><br><span class="line">predict_sunspots.plot(ax=ax)</span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure><br>同时绘制出原始数据和预测数据</p>
<h1 id="拓展—SARIMA"><a href="#拓展—SARIMA" class="headerlink" title="拓展—SARIMA"></a>拓展—SARIMA</h1><p>SARIMA(p,d,q)(P,D,Q)s 模型（季节性自回归积分移动平均模型）<br>该模型就是将ARIMA的差分增加滞后性，比如每周的数据，可以进行滞后7的差分，年度每月数据进行滞后12的差分等。其他方面和ARIMA相似。</p>
<h2 id="滞后差分定义"><a href="#滞后差分定义" class="headerlink" title="滞后差分定义"></a>滞后差分定义</h2><p>1阶季节性差分（周期s）：$\nabla<em>s X_t = X_t - X</em>{t-s}$<br>D阶季节性差分：$\nabla_s^D X_t = \nabla_s(\nabla_s^{D-1} X_t)$</p>
<h2 id="核心公式"><a href="#核心公式" class="headerlink" title="核心公式"></a>核心公式</h2><p>$\nabla^d \nabla<em>s^D X_t = \phi_0 + \sum</em>{i=1}^p \phi<em>i \nabla^d \nabla_s^D X</em>{t-i} + \sum<em>{j=1}^P \Phi_j \nabla^d \nabla_s^D X</em>{t-js} + \varepsilon<em>t - \sum</em>{k=1}^q \theta<em>k \varepsilon</em>{t-k} - \sum<em>{l=1}^Q \Theta_l \varepsilon</em>{t-ls}$</p>
]]></content>
      <categories>
        <category>数学建模</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>ARIMA</tag>
        <tag>时序模型</tag>
        <tag>AR模型</tag>
        <tag>MA模型</tag>
        <tag>机器学习</tag>
      </tags>
  </entry>
  <entry>
    <title>2023美赛D题论文阅读</title>
    <url>/2025/12/14/2023%E7%BE%8E%E8%B5%9BD%E9%A2%98%E8%AE%BA%E6%96%87%E9%98%85%E8%AF%BB/</url>
    <content><![CDATA[<p>联合国17个可持续发展目标的优先级排序与发展预测<br><span id="more"></span></p>
<h1 id="问题综述"><a href="#问题综述" class="headerlink" title="问题综述"></a>问题综述</h1><h2 id="核心需求"><a href="#核心需求" class="headerlink" title="核心需求"></a>核心需求</h2><p>SDGs并非孤立存在，存在协同关系（相互促进）和权衡关系（资源竞争），论文需解决5大核心问题：</p>
<ul>
<li>构建SDGs间的关系网络（量化目标间关联）；</li>
<li>制定SDGs的优先级排序标准；</li>
<li>基于优先级预测未来10年SDGs发展；</li>
<li>量化COVID-19等突发事件的影响；</li>
<li>拓展模型在企业/组织中的应用。</li>
</ul>
<h2 id="整体建模框架"><a href="#整体建模框架" class="headerlink" title="整体建模框架"></a>整体建模框架</h2><ol>
<li>数据预处理 → 关系图模型（揭示SDGs关联）</li>
<li>关系图模型 → 优先级模型（多指标排序）</li>
<li>优先级模型 → 时间香槟塔模型（未来10年预测）</li>
<li>时间香槟塔模型 → 突发事件分析（COVID-19影响）</li>
<li>时间香槟塔模型 → 敏感性分析（验证鲁棒性）</li>
</ol>
<p>核心逻辑：先量化SDGs的“关联强度”，再基于关联确定“优先级”，最后按优先级和关联动态预测“发展路径”，并验证模型可靠性。</p>
<h1 id="数据处理"><a href="#数据处理" class="headerlink" title="数据处理"></a>数据处理</h1><p>本题的一个难点是查找数据并处理。也有的队伍使用层次分析法等主观方法去评价各个指标之间的关联性，但是结果一定不如找的客观数据的分析。</p>
<h2 id="数据选择"><a href="#数据选择" class="headerlink" title="数据选择"></a>数据选择</h2><ul>
<li>数据源：联合国全球SDG数据库（涵盖17个目标的指标与序列数据）；</li>
<li>样本选择：土耳其、墨西哥、印尼（代表三大洲，覆盖不同发展阶段）；</li>
<li>时间范围：2003-2018年（平衡样本长度与数据完整性）；</li>
<li>序列筛选：共选取95个序列（确保覆盖所有SDGs，兼顾样本量与代表性）。  </li>
</ul>
<h2 id="数据清洗"><a href="#数据清洗" class="headerlink" title="数据清洗"></a>数据清洗</h2><p>针对数据缺失问题，采用线性回归填补，公式如下：</p>
<script type="math/tex; mode=display">b=\frac{n \sum_{i=1}^{n} x_{i} y_{i}-\left(\sum_{i=1}^{n} x_{i}\right)\left(\sum_{i=1}^{n} y_{i}\right)}{n \sum_{i=1}^{n} x_{i}^{2}-\left(\sum_{i=1}^{n} x_{i}\right)^{2}}</script><p>其中 $\hat{y}$ 为缺失值填补结果，x为缺失值对应的年份；若 $\hat{y}&lt;0$ （非负指标），则用0填补。</p>
<h2 id="数据归一化"><a href="#数据归一化" class="headerlink" title="数据归一化"></a>数据归一化</h2><p>为消除量纲差异，将所有指标统一转化为0-1区间，且“越大越好”：</p>
<ul>
<li>正向指标： $I<em>{i,t,c}’=\frac{I</em>{i,t,c}-min(I<em>{i,..})}{max(I</em>{i,..})-min(I_{i,..})}$</li>
<li>负向指标： $I<em>{i,t,c}’=\frac{max(I</em>{i,..})-I<em>{i,t,c}}{max(I</em>{i,..})-min(I_{i,..})}$</li>
</ul>
<h2 id="复现遇到的困难1"><a href="#复现遇到的困难1" class="headerlink" title="复现遇到的困难1"></a>复现遇到的困难1</h2><ul>
<li>要先去了解一下联合国数据库中的数据下载等问题，有一点点麻烦</li>
<li>需要从下载的数据中选择的总指标中，针对各个SDGs找出较有说服力的指标，比较花时间</li>
<li>找出指标后，需要为各个指标确认正向或负向，同样需要时间查找等</li>
<li>其中涉及多种的表格筛选，裁剪等操作，需要时间学习</li>
</ul>
<h1 id="模型1：关系图模型"><a href="#模型1：关系图模型" class="headerlink" title="模型1：关系图模型"></a>模型1：关系图模型</h1><p>量化17个SDGs之间的协同/权衡关系，为优先级排序提供基础指标。</p>
<h2 id="图结构构建"><a href="#图结构构建" class="headerlink" title="图结构构建"></a>图结构构建</h2><p>用无向图 $G(V,E,W)$ 表示SDGs关系：</p>
<ul>
<li>节点 $V_i$：第$i$个SDG目标；</li>
<li>边 $E_{ij}$：目标$i$与$j$的关联（用Spearman相关系数衡量）；</li>
<li>权重 $w_{ij}$：关联强度（正向=协同，负向=权衡）。</li>
</ul>
<p>小细节：</p>
<pre><code>这里涉及到Spearman和Pearson的选择  
皮尔逊相关性Pearson只能评估两个连续变量之间的线性相关，就是最开始学的线性相关的检验r。  
而Spearman相关性可以评估两个变量之间的单调关系——有序，它基于每个变量的排名值而不是原始数据。  
总之，就是Pearson 适用于两个变量之间的线性关系，而Spearman不仅适用线性关系，还适用于单调关系
</code></pre><h2 id="图度量：节点重要性指标"><a href="#图度量：节点重要性指标" class="headerlink" title="图度量：节点重要性指标"></a>图度量：节点重要性指标</h2><p>用3类中心性（分别计算正向边/负向边）量化目标的“网络影响力”，共6个指标：</p>
<ul>
<li>介数中心性（BC）： $BC(i)=\sum_{\substack{j&lt;k \ j \neq i \neq k}} \frac{n_i(j,k)}{n(j,k)}$（衡量节点的“桥梁作用”）；</li>
<li>接近中心性（CC）： $CC(i)=\frac{1}{\sum_{j \neq i} d(i,j)}$（衡量节点的“可达性”）；</li>
<li>特征向量中心性（EC）： $EC(i)=\alpha \sum<em>{j=1}^{n} r</em>{ij} \cdot EC(j)$（衡量节点的“核心辐射力”）。</li>
</ul>
<h2 id="论文核心发现1"><a href="#论文核心发现1" class="headerlink" title="论文核心发现1"></a>论文核心发现1</h2><ul>
<li>多数SDGs呈正相关（协同关系），符合联合国设定目标的一致性；</li>
<li>Goal7（廉价清洁能源）与多数目标呈负相关（技术投入大、资源竞争激烈）。</li>
</ul>
<h1 id="模型2：优先级模型"><a href="#模型2：优先级模型" class="headerlink" title="模型2：优先级模型"></a>模型2：优先级模型</h1><h2 id="核心目的"><a href="#核心目的" class="headerlink" title="核心目的"></a>核心目的</h2><p>基于关系图模型的6个中心性指标，客观排序17个SDGs的优先级。</p>
<h2 id="方法组合：熵权法（EWM）-加权TOPSIS"><a href="#方法组合：熵权法（EWM）-加权TOPSIS" class="headerlink" title="方法组合：熵权法（EWM）+ 加权TOPSIS"></a>方法组合：熵权法（EWM）+ 加权TOPSIS</h2><ul>
<li>熵权法：确定6个指标的客观权重（避免主观偏差）；</li>
<li>TOPSIS（加权）：基于权重计算目标与“最优解”“最劣解”的距离，排序优先级。  </li>
</ul>
<p><del>点击蓝字来看熵权法和TOPSIS细节</del></p>
<h2 id="论文核心发现2"><a href="#论文核心发现2" class="headerlink" title="论文核心发现2"></a>论文核心发现2</h2><ul>
<li>普适性结论：Goal3（健康）和Goal4（教育）在三个国家均进入前五，是全球普适性高优先级目标；</li>
<li>国别差异：土耳其优先Goal17（伙伴关系）和Goal9（产业创新），印尼/墨西哥优先Goal3（健康）。</li>
</ul>
<h1 id="模型3：时间香槟塔模型"><a href="#模型3：时间香槟塔模型" class="headerlink" title="模型3：时间香槟塔模型"></a>模型3：时间香槟塔模型</h1><h2 id="核心目的-1"><a href="#核心目的-1" class="headerlink" title="核心目的"></a>核心目的</h2><p>基于优先级和SDGs关联，预测未来10年发展进度（资源动态分配）。</p>
<h2 id="设计灵感"><a href="#设计灵感" class="headerlink" title="设计灵感"></a>设计灵感</h2><p>香槟塔的“上层满溢后流入下层”——资源优先分配给高优先级目标，完成后资源流向关联目标，同时维护已完成目标。</p>
<h2 id="核心假设"><a href="#核心假设" class="headerlink" title="核心假设"></a>核心假设</h2><p>所有SDGs的“完成难度相同”（1%完成度的资源投入等价），简化量化复杂度。</p>
<h2 id="关键变量与公式"><a href="#关键变量与公式" class="headerlink" title="关键变量与公式"></a>关键变量与公式</h2><ul>
<li>完成率：$G_t=[g_t(1),g_t(2),…,g_t(17)]$（$g_t(i)$为第$t$年第$i$个目标的完成百分比）；</li>
<li>发展队列：$S_{t+1}=S_t \cup S_t’$（$S_t’$为与当前队列关联的目标集合）；</li>
<li>协同影响：$v<em>t(i)=\frac{1}{C} \sum</em>{\substack{j \ V<em>j \in S_t}} w</em>{ij}(v<em>{t-1}(j)+d</em>{t-1}(j))$（$C=5$基于帕累托原则）；</li>
<li>资源分配：$d<em>t(i)=\begin{cases}0, if v_t(i)+g</em>{t-1}(i)≥1 \ min(1-v<em>t(i)-g</em>{t-1}(i), D-\sum<em>{s(j)&gt;s(i)} max(0,1-v_t(j)-g</em>{t-1}(j))), else \end{cases}$；</li>
<li>完成率更新：$g<em>t(i)=min(g</em>{t-1}(i)+d_t(i)+v_t(i),1)$。</li>
</ul>
<h2 id="核心结果（以印尼为例）"><a href="#核心结果（以印尼为例）" class="headerlink" title="核心结果（以印尼为例）"></a>核心结果（以印尼为例）</h2><ul>
<li>无突发事件：2033年完成5个目标（Goal3/4/6/9/13），整体完成率82%（比当前计划高4%）；</li>
<li>创新建议：新增Goal18（国际合作），利用已完成目标的协同效应帮助其他国家。</li>
</ul>
<h1 id="扩展分析：突发事件与敏感性检验"><a href="#扩展分析：突发事件与敏感性检验" class="headerlink" title="扩展分析：突发事件与敏感性检验"></a>扩展分析：突发事件与敏感性检验</h1><h2 id="1-突发事件影响分析（COVID-19为例）"><a href="#1-突发事件影响分析（COVID-19为例）" class="headerlink" title="1. 突发事件影响分析（COVID-19为例）"></a>1. 突发事件影响分析（COVID-19为例）</h2><ul>
<li>新增参数：威胁系数 $\gamma_i$（衡量目标受疫情威胁程度）、缓解/加剧系数 $MI_i$（衡量目标对疫情的影响）；</li>
<li>模型调整：资源分配公式加入 $\gamma_i$，优先级指标加入 $MI_i$；</li>
<li>结果：印尼2033年仅完成3个目标，整体完成率78%（比无疫情低4%）。</li>
</ul>
<h2 id="2-敏感性分析（验证鲁棒性）"><a href="#2-敏感性分析（验证鲁棒性）" class="headerlink" title="2. 敏感性分析（验证鲁棒性）"></a>2. 敏感性分析（验证鲁棒性）</h2><ul>
<li>分析对象：超参数$C$（协同效率常数）和$D$（年总资源）；</li>
<li>测试范围：$C$取4-6（步长0.1），$D$取0.102-0.136；</li>
<li>结果：$C$的最大影响幅度0.041，$D$的最大影响幅度0.028，模型对超参数变化不敏感，鲁棒性强。</li>
</ul>
<h1 id="代码复现"><a href="#代码复现" class="headerlink" title="代码复现"></a>代码复现</h1><h2 id="图结构构建（得到相关性矩阵）"><a href="#图结构构建（得到相关性矩阵）" class="headerlink" title="图结构构建（得到相关性矩阵）"></a>图结构构建（得到相关性矩阵）</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">import</span> seaborn <span class="keyword">as</span> sns</span><br><span class="line"><span class="keyword">from</span> sklearn.linear_model <span class="keyword">import</span> LinearRegression</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> r2_score</span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"></span><br><span class="line">PATH = <span class="string">&quot;H:/数模/2023美赛D/rdata.xlsx&quot;</span></span><br><span class="line">COUNTRY = <span class="string">&quot;Türkiye&quot;</span>  <span class="comment"># 替换国家名运行</span></span><br><span class="line">st_y = <span class="number">2003</span></span><br><span class="line">ed_y = <span class="number">2018</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 输出路径</span></span><br><span class="line">script_dir = os.path.dirname(os.path.abspath(__file__))</span><br><span class="line">corr_matrix_path = os.path.join(script_dir, <span class="string">f&quot;<span class="subst">&#123;COUNTRY&#125;</span>_SDG相关性矩阵.csv&quot;</span>)</span><br><span class="line">heatmap_path = os.path.join(script_dir, <span class="string">f&quot;<span class="subst">&#123;COUNTRY&#125;</span>_SDG相关性热力图.png&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># -------------------------- 1. 读取数据 &amp; 筛选范围 --------------------------</span></span><br><span class="line">df = pd.read_excel(PATH)</span><br><span class="line">df = df[(df[<span class="string">&quot;GeoAreaName&quot;</span>] == COUNTRY) &amp; (df[<span class="string">&quot;Time&quot;</span>] &gt;= st_y) &amp; (df[<span class="string">&quot;Time&quot;</span>] &lt;= ed_y)].copy()</span><br><span class="line"></span><br><span class="line"><span class="comment"># -------------------------- 2. 预处理 --------------------------</span></span><br><span class="line">df[<span class="string">&quot;Value&quot;</span>] = pd.to_numeric(df[<span class="string">&quot;Value&quot;</span>], errors=<span class="string">&quot;coerce&quot;</span>)  <span class="comment"># 处理非数值为NaN</span></span><br><span class="line">df[<span class="string">&quot;is_valid&quot;</span>] = ~df[<span class="string">&quot;Value&quot;</span>].isna()  <span class="comment"># 标记真实数据</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># -------------------------- 3. 负向指标正向化 --------------------------</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">forward_transform</span>(<span class="params">group</span>):</span><br><span class="line">    max_val = group[<span class="string">&quot;Value&quot;</span>].<span class="built_in">max</span>()</span><br><span class="line">    group[<span class="string">&quot;Value_forward&quot;</span>] = np.where(group[<span class="string">&quot;sign&quot;</span>] == <span class="string">&quot;-&quot;</span>, max_val - group[<span class="string">&quot;Value&quot;</span>], group[<span class="string">&quot;Value&quot;</span>])</span><br><span class="line">    <span class="keyword">return</span> group</span><br><span class="line"></span><br><span class="line">df = df.groupby(<span class="string">&quot;SeriesDescription&quot;</span>).apply(forward_transform).reset_index(level=<span class="number">0</span>, drop=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># -------------------------- 4. 线性回归填充 --------------------------</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">linear_reg_fill</span>(<span class="params">group</span>):</span><br><span class="line">    indicator = group[<span class="string">&quot;SeriesDescription&quot;</span>].iloc[<span class="number">0</span>]</span><br><span class="line">    valid_cnt = group[<span class="string">&quot;is_valid&quot;</span>].<span class="built_in">sum</span>()</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 真实数据&lt;3个标记为无效</span></span><br><span class="line">    <span class="keyword">if</span> valid_cnt &lt; <span class="number">3</span>:</span><br><span class="line">        group[<span class="string">&quot;Value_filled&quot;</span>] = np.nan</span><br><span class="line">        <span class="keyword">return</span> group</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 分离真实数据拟合回归</span></span><br><span class="line">    valid_data = group[group[<span class="string">&quot;is_valid&quot;</span>]].copy()</span><br><span class="line">    X = valid_data[<span class="string">&quot;Time&quot;</span>].values.reshape(-<span class="number">1</span>, <span class="number">1</span>)</span><br><span class="line">    y = valid_data[<span class="string">&quot;Value_forward&quot;</span>].values</span><br><span class="line">    model = LinearRegression()</span><br><span class="line">    model.fit(X, y)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 预测所有年份</span></span><br><span class="line">    all_X = group[<span class="string">&quot;Time&quot;</span>].values.reshape(-<span class="number">1</span>, <span class="number">1</span>)</span><br><span class="line">    all_pred = model.predict(all_X)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 回归结果非负（小于0设为0）</span></span><br><span class="line">    all_pred = np.maximum(all_pred, <span class="number">0</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 合并</span></span><br><span class="line">    group[<span class="string">&quot;Value_filled&quot;</span>] = np.where(group[<span class="string">&quot;is_valid&quot;</span>], group[<span class="string">&quot;Value_forward&quot;</span>], all_pred)</span><br><span class="line">    <span class="keyword">return</span> group</span><br><span class="line"></span><br><span class="line"><span class="comment"># 填充 + 过滤无效指标</span></span><br><span class="line">df = df.groupby(<span class="string">&quot;SeriesDescription&quot;</span>).apply(linear_reg_fill).reset_index(level=<span class="number">0</span>, drop=<span class="literal">True</span>)</span><br><span class="line">df = df.dropna(subset=[<span class="string">&quot;Value_filled&quot;</span>])</span><br><span class="line"></span><br><span class="line"><span class="comment"># -------------------------- 5. 归一化 --------------------------</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">normalize</span>(<span class="params">group</span>):</span><br><span class="line">    min_val = group[<span class="string">&quot;Value_filled&quot;</span>].<span class="built_in">min</span>()</span><br><span class="line">    max_val = group[<span class="string">&quot;Value_filled&quot;</span>].<span class="built_in">max</span>()</span><br><span class="line">    <span class="keyword">if</span> max_val - min_val == <span class="number">0</span>:</span><br><span class="line">        group[<span class="string">&quot;Value_norm&quot;</span>] = <span class="number">0.5</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        group[<span class="string">&quot;Value_norm&quot;</span>] = (group[<span class="string">&quot;Value_filled&quot;</span>] - min_val) / (max_val - min_val)</span><br><span class="line">    <span class="keyword">return</span> group</span><br><span class="line"></span><br><span class="line">df = df.groupby(<span class="string">&quot;SeriesDescription&quot;</span>).apply(normalize).reset_index(level=<span class="number">0</span>, drop=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># -------------------------- 6. 聚合 &amp; 相关性分析 --------------------------</span></span><br><span class="line"><span class="comment"># 按Goal+年份聚合（求均值）</span></span><br><span class="line">goal_year_avg = df.groupby([<span class="string">&quot;Goal&quot;</span>, <span class="string">&quot;Time&quot;</span>])[<span class="string">&quot;Value_norm&quot;</span>].mean().reset_index()</span><br><span class="line"></span><br><span class="line">wide_df = goal_year_avg.pivot_table(index=<span class="string">&quot;Time&quot;</span>, columns=<span class="string">&quot;Goal&quot;</span>, values=<span class="string">&quot;Value_norm&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 计算Spearman相关性矩阵</span></span><br><span class="line">corr_matrix = wide_df.corr(method=<span class="string">&quot;spearman&quot;</span>)</span><br><span class="line">corr_matrix.<span class="built_in">round</span>(<span class="number">2</span>).to_csv(corr_matrix_path, encoding=<span class="string">&quot;utf-8-sig&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># -------------------------- 7. 绘制热力图 --------------------------</span></span><br><span class="line">plt.rcParams[<span class="string">&quot;font.sans-serif&quot;</span>] = [<span class="string">&quot;SimHei&quot;</span>]</span><br><span class="line">plt.figure(figsize=(<span class="number">10</span>, <span class="number">8</span>))</span><br><span class="line">sns.heatmap(</span><br><span class="line">    corr_matrix, </span><br><span class="line">    annot=<span class="literal">True</span>, cmap=<span class="string">&quot;RdBu_r&quot;</span>, </span><br><span class="line">    vmin=-<span class="number">1</span>, vmax=<span class="number">1</span>, fmt=<span class="string">&quot;.2f&quot;</span></span><br><span class="line">)</span><br><span class="line">plt.title(<span class="string">f&quot;<span class="subst">&#123;COUNTRY&#125;</span> SDG Goal 相关性热力图（2003-2018）&quot;</span>, fontsize=<span class="number">14</span>)</span><br><span class="line">plt.tight_layout()</span><br><span class="line">plt.savefig(heatmap_path, dpi=<span class="number">300</span>, bbox_inches=<span class="string">&quot;tight&quot;</span>)</span><br><span class="line">plt.close()</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;<span class="subst">&#123;COUNTRY&#125;</span> 分析完成！&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;相关性矩阵已保存：<span class="subst">&#123;corr_matrix_path&#125;</span>&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;热力图已保存：<span class="subst">&#123;heatmap_path&#125;</span>&quot;</span>)</span><br></pre></td></tr></table></figure>
<h2 id="得到中心性指标"><a href="#得到中心性指标" class="headerlink" title="得到中心性指标"></a>得到中心性指标</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> networkx <span class="keyword">as</span> nx</span><br><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"></span><br><span class="line"><span class="comment"># 1. 读取SDG相关性矩阵</span></span><br><span class="line">w = pd.read_csv(</span><br><span class="line">    <span class="string">&quot;H:/数模/2023美赛D/Türkiye_SDG相关性矩阵.csv&quot;</span>,</span><br><span class="line">    index_col=<span class="number">0</span>  </span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">w.columns = w.columns.astype(<span class="built_in">int</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 拆分正负相关子图</span></span><br><span class="line">w_plus = w.where(w &gt; <span class="number">0</span>, <span class="number">0</span>)</span><br><span class="line">w_minus = w.where(w &lt; <span class="number">0</span>, <span class="number">0</span>).<span class="built_in">abs</span>()</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 定义中心性计算函数</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">calculate_centralities_with_fix</span>(<span class="params">G</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    计算图G的3种中心性（介数、接近度、特征向量），并修复孤立节点的报错</span></span><br><span class="line"><span class="string">    返回：包含3个中心性指标的DataFrame</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    <span class="comment"># 1. 介数中心性（考虑边权重）</span></span><br><span class="line">    bc = nx.betweenness_centrality(G, weight=<span class="string">&quot;weight&quot;</span>, normalized=<span class="literal">True</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 2. 接近度中心性（处理孤立节点：无连接时接近度设为0）</span></span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        cc = nx.closeness_centrality(G, distance=<span class="string">&quot;weight&quot;</span>)</span><br><span class="line">    <span class="keyword">except</span> nx.NetworkXError:  <span class="comment"># 存在孤立节点</span></span><br><span class="line">        cc = &#123;node: <span class="number">0.0</span> <span class="keyword">for</span> node <span class="keyword">in</span> G.nodes()&#125;  <span class="comment"># 孤立节点接近度设为0</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 3. 特征向量中心性（处理不连通图：孤立节点设为0）</span></span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        ec = nx.eigenvector_centrality(G, weight=<span class="string">&quot;weight&quot;</span>, tol=<span class="number">1e-6</span>, max_iter=<span class="number">1000</span>)</span><br><span class="line">    <span class="keyword">except</span> nx.NetworkXError:  <span class="comment"># 图不连通时</span></span><br><span class="line">        ec = &#123;node: <span class="number">0.0</span> <span class="keyword">for</span> node <span class="keyword">in</span> G.nodes()&#125;  <span class="comment"># 孤立节点特征向量中心性设为0</span></span><br><span class="line">    </span><br><span class="line">    centralities = pd.DataFrame(&#123;</span><br><span class="line">        <span class="string">&quot;BC&quot;</span>: bc,</span><br><span class="line">        <span class="string">&quot;CC&quot;</span>: cc,</span><br><span class="line">        <span class="string">&quot;EC&quot;</span>: ec</span><br><span class="line">    &#125;)</span><br><span class="line">    <span class="keyword">return</span> centralities</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 4. 计算正负子图的中心性指标</span></span><br><span class="line">G_plus = nx.from_pandas_adjacency(w_plus, create_using=nx.Graph())</span><br><span class="line">centralities_plus = calculate_centralities_with_fix(G_plus)</span><br><span class="line">centralities_plus.columns = [col + <span class="string">&quot;_plus&quot;</span> <span class="keyword">for</span> col <span class="keyword">in</span> centralities_plus.columns]  <span class="comment"># 重命名列（正边）</span></span><br><span class="line"></span><br><span class="line">G_minus = nx.from_pandas_adjacency(w_minus, create_using=nx.Graph())</span><br><span class="line">centralities_minus = calculate_centralities_with_fix(G_minus)</span><br><span class="line">centralities_minus.columns = [col + <span class="string">&quot;_minus&quot;</span> <span class="keyword">for</span> col <span class="keyword">in</span> centralities_minus.columns]  <span class="comment"># 重命名列（负边）</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 5. 合并中心性指标并保存（生成后续优先级分析的输入文件）</span></span><br><span class="line">centralities_df = pd.concat([centralities_plus, centralities_minus], axis=<span class="number">1</span>)</span><br><span class="line">centralities_df.index.name = <span class="string">&quot;SDG目标&quot;</span></span><br><span class="line"></span><br><span class="line">centralities_df.to_csv(</span><br><span class="line">    <span class="string">&quot;SDG中心性指标.csv&quot;</span>,</span><br><span class="line">    encoding=<span class="string">&quot;utf-8-sig&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;\n✅ 中心性指标计算完成，结果维度：&quot;</span>, centralities_df.shape)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;📊 中心性指标前5行：&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(centralities_df.head())</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;\n💾 中心性指标已保存到：SDG中心性指标.csv&quot;</span>)</span><br></pre></td></tr></table></figure>
<h2 id="优先级分析（得到排序结果）"><a href="#优先级分析（得到排序结果）" class="headerlink" title="优先级分析（得到排序结果）"></a>优先级分析（得到排序结果）</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"></span><br><span class="line"><span class="comment"># 1. 数据准备：读取中心性指标（需确保数据格式正确）</span></span><br><span class="line"></span><br><span class="line">df_centrality = pd.read_csv(</span><br><span class="line">    <span class="string">&quot;SDG中心性指标.csv&quot;</span>,  </span><br><span class="line">    index_col=<span class="string">&quot;SDG目标&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 负边中心性正向化</span></span><br><span class="line"></span><br><span class="line">negative_cols = [<span class="string">&quot;BC_minus&quot;</span>, <span class="string">&quot;CC_minus&quot;</span>, <span class="string">&quot;EC_minus&quot;</span>]</span><br><span class="line"><span class="keyword">for</span> col <span class="keyword">in</span> negative_cols:</span><br><span class="line">    df_centrality[col + <span class="string">&quot;_normalized&quot;</span>] = df_centrality[col].<span class="built_in">max</span>() - df_centrality[col]</span><br><span class="line"></span><br><span class="line">normalized_cols = [<span class="string">&quot;BC_plus&quot;</span>, <span class="string">&quot;CC_plus&quot;</span>, <span class="string">&quot;EC_plus&quot;</span>, </span><br><span class="line">                   <span class="string">&quot;BC_minus_normalized&quot;</span>, <span class="string">&quot;CC_minus_normalized&quot;</span>, <span class="string">&quot;EC_minus_normalized&quot;</span>]</span><br><span class="line">df_normalized = df_centrality[normalized_cols].copy()</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;\n✅ 负边中心性正向化完成，正向化后前5行数据：&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(df_normalized.head())</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 熵权法：计算6个指标的客观权重</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">calculate_entropy_weight</span>(<span class="params">data</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    输入：正向化后的指标矩阵（行=SDG目标，列=指标）</span></span><br><span class="line"><span class="string">    输出：各指标的熵权（总和=1）</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    <span class="comment"># Step 1: 标准化</span></span><br><span class="line">    data_std = (data - data.<span class="built_in">min</span>(axis=<span class="number">0</span>)) / (data.<span class="built_in">max</span>(axis=<span class="number">0</span>) - data.<span class="built_in">min</span>(axis=<span class="number">0</span>))</span><br><span class="line">    <span class="comment"># 避免0值导致log(0)错误，替换0为极小值1e-8</span></span><br><span class="line">    data_std = data_std.replace(<span class="number">0</span>, <span class="number">1e-8</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># Step 2: 计算各目标在指标中的占比</span></span><br><span class="line">    p = data_std / data_std.<span class="built_in">sum</span>(axis=<span class="number">0</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># Step 3: 计算信息熵e_j</span></span><br><span class="line">    n = <span class="built_in">len</span>(data)</span><br><span class="line">    e = -np.<span class="built_in">sum</span>(p * np.log(p), axis=<span class="number">0</span>) / np.log(n)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># Step 4: 计算信息效用值d_j和熵权w_j</span></span><br><span class="line">    d = <span class="number">1</span> - e  <span class="comment"># 效用值：熵越小，效用越大</span></span><br><span class="line">    w = d / d.<span class="built_in">sum</span>()  <span class="comment"># 熵权：归一化效用值</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> w, e, d</span><br><span class="line"></span><br><span class="line"><span class="comment"># 执行熵权计算</span></span><br><span class="line">weights, entropy, utility = calculate_entropy_weight(df_normalized)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 整理权重结果</span></span><br><span class="line">weight_df = pd.DataFrame(&#123;</span><br><span class="line">    <span class="string">&quot;指标名称&quot;</span>: normalized_cols,</span><br><span class="line">    <span class="string">&quot;信息熵e_j&quot;</span>: entropy.<span class="built_in">round</span>(<span class="number">4</span>),</span><br><span class="line">    <span class="string">&quot;信息效用值d_j&quot;</span>: utility.<span class="built_in">round</span>(<span class="number">4</span>),</span><br><span class="line">    <span class="string">&quot;熵权w_j&quot;</span>: weights.<span class="built_in">round</span>(<span class="number">4</span>)</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;\n✅ 熵权法计算完成，各指标权重：&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(weight_df)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;🔍 权重总和：&quot;</span>, weights.<span class="built_in">sum</span>().<span class="built_in">round</span>(<span class="number">4</span>))  <span class="comment"># 验证：权重总和应为1</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 4. 加权TOPSIS：计算SDG优先级得分与排序</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">weighted_topsis</span>(<span class="params">data, weights</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    输入：正向化后的指标矩阵、各指标熵权</span></span><br><span class="line"><span class="string">    输出：各SDG目标的TOPSIS得分、排序</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    <span class="comment"># Step 1: 指标标准化</span></span><br><span class="line">    z = data / np.sqrt(np.<span class="built_in">sum</span>(data ** <span class="number">2</span>, axis=<span class="number">0</span>))</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># Step 2: 确定正负理想解</span></span><br><span class="line">    z_plus = z.<span class="built_in">max</span>(axis=<span class="number">0</span>) </span><br><span class="line">    z_minus = z.<span class="built_in">min</span>(axis=<span class="number">0</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># Step 3: 计算加权欧氏距离</span></span><br><span class="line">    D_plus = np.sqrt(np.<span class="built_in">sum</span>(weights * (z_plus - z) ** <span class="number">2</span>, axis=<span class="number">1</span>))  <span class="comment"># 到正理想解距离</span></span><br><span class="line">    D_minus = np.sqrt(np.<span class="built_in">sum</span>(weights * (z_minus - z) ** <span class="number">2</span>, axis=<span class="number">1</span>))<span class="comment"># 到负理想解距离</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># Step 4: 计算优先级得分s_i</span></span><br><span class="line">    s = D_minus / (D_plus + D_minus)</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> s, D_plus, D_minus, z_plus, z_minus</span><br><span class="line"></span><br><span class="line"><span class="comment"># 执行TOPSIS计算</span></span><br><span class="line">scores, D_plus, D_minus, z_plus, z_minus = weighted_topsis(df_normalized, weights)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 整理优先级结果</span></span><br><span class="line">priority_df = pd.DataFrame(&#123;</span><br><span class="line">    <span class="string">&quot;SDG目标&quot;</span>: df_centrality.index,</span><br><span class="line">    <span class="string">&quot;到正理想解距离D+&quot;</span>: D_plus.<span class="built_in">round</span>(<span class="number">4</span>),</span><br><span class="line">    <span class="string">&quot;到负理想解距离D-&quot;</span>: D_minus.<span class="built_in">round</span>(<span class="number">4</span>),</span><br><span class="line">    <span class="string">&quot;优先级得分s_i&quot;</span>: scores.<span class="built_in">round</span>(<span class="number">4</span>),</span><br><span class="line">    <span class="string">&quot;优先级排名&quot;</span>: scores.rank(ascending=<span class="literal">False</span>).astype(<span class="built_in">int</span>)  <span class="comment"># 降序排名：得分高→排名前</span></span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 按排名排序</span></span><br><span class="line">priority_df_sorted = priority_df.sort_values(<span class="string">&quot;优先级排名&quot;</span>).reset_index(drop=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;\n✅ TOPSIS优先级计算完成，SDG优先级排序（前10名）：&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(priority_df_sorted.head(<span class="number">10</span>))</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 5. 结果输出</span></span><br><span class="line"><span class="comment"># 保存完整排序结果</span></span><br><span class="line">priority_df_sorted.to_csv(</span><br><span class="line">    <span class="string">&quot;SDG优先级排序结果.csv&quot;</span>,</span><br><span class="line">    index=<span class="literal">False</span>,</span><br><span class="line">    encoding=<span class="string">&quot;utf-8-sig&quot;</span>  <span class="comment"># 支持中文显示</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 保存指标权重结果</span></span><br><span class="line">weight_df.to_csv(</span><br><span class="line">    <span class="string">&quot;SDG指标熵权结果.csv&quot;</span>,</span><br><span class="line">    index=<span class="literal">False</span>,</span><br><span class="line">    encoding=<span class="string">&quot;utf-8-sig&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;\n💾 结果已保存：&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;1. SDG优先级排序结果.csv（含得分与排名）&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;2. SDG指标熵权结果.csv（含信息熵与权重）&quot;</span>)</span><br></pre></td></tr></table></figure>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>2023 美赛 D 题围绕联合国 17 个可持续发展目标（SDGs）的优先级排序与发展预测展开，核心是量化 SDGs 间的协同 / 权衡关系并落地实际预测与应用。<br>论文采用 “数据预处理→关系图模型→优先级模型→时间香槟塔模型→扩展分析” 的递进式建模框架：</p>
<ul>
<li>先通过联合国 SDG 数据库数据构建基于 Spearman 相关系数的 SDGs 关系图，用三类中心性指标量化节点影响力；</li>
<li>再结合熵权法与加权 TOPSIS 实现优先级客观排序，发现健康（Goal3）、教育（Goal4）为全球普适高优先级目标；</li>
<li>随后基于 “资源优先流向高优先级目标” 的香槟塔逻辑，预测未来 10 年发展进度；</li>
<li>最后分析 COVID-19 等突发事件影响并验证模型鲁棒性。</li>
<li>整体模型兼顾客观性与实用性，为 SDGs 资源分配与发展规划提供了可行方案。</li>
</ul>
]]></content>
      <categories>
        <category>数学建模</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>图论</tag>
      </tags>
  </entry>
  <entry>
    <title>2023美赛A题论文阅读</title>
    <url>/2025/12/10/2023%E7%BE%8E%E8%B5%9BA%E9%A2%98%E8%AE%BA%E6%96%87%E9%98%85%E8%AF%BB/</url>
    <content><![CDATA[<p>受干旱影响的植物群落<br><span id="more"></span> </p>
<h1 id="2023美赛A题：干旱下植物的群落演替"><a href="#2023美赛A题：干旱下植物的群落演替" class="headerlink" title="2023美赛A题：干旱下植物的群落演替"></a>2023美赛A题：干旱下植物的群落演替</h1><h2 id="任务简述"><a href="#任务简述" class="headerlink" title="任务简述"></a>任务简述</h2><p>美赛A题通常是“复杂生态/物理系统建模”，这篇的核心矛盾是：<strong>干旱胁迫下，植物群落的物种数量、种间关系如何影响其存活能力？</strong></p>
<p>核心任务：</p>
<ol>
<li>建一个能模拟“不规则干旱周期+种间相互作用”的群落演替模型；</li>
<li>找群落能受益的“最小物种数”，以及物种数增加的边际效应；</li>
<li>分析污染、栖息地缩减这些人类活动的影响；</li>
<li>给保护群落的实际措施提建议。</li>
</ol>
<h2 id="核心模型：用“生态位宽度”量化群落状态"><a href="#核心模型：用“生态位宽度”量化群落状态" class="headerlink" title="核心模型：用“生态位宽度”量化群落状态"></a>核心模型：用“生态位宽度”量化群落状态</h2><p>论文的灵魂是 <strong>“基于生态位理论的动态模型”</strong> ——把抽象的“群落演替”转化为“物种生态位宽度的变化”（生态位宽度越宽，物种资源利用能力越强）。</p>
<h3 id="基本假设"><a href="#基本假设" class="headerlink" title="基本假设"></a>基本假设</h3><ul>
<li>封闭系统：没新物种迁入，只看现有物种的相互作用；</li>
<li>种间关系稳定：竞争/互利关系不会随时间变；</li>
<li>干旱敏感性固定：每个物种的“怕旱程度”是定值。</li>
</ul>
<h3 id="核心方程"><a href="#核心方程" class="headerlink" title="核心方程"></a>核心方程</h3><p>论文把“繁殖、死亡、随机干扰”三个因素揉进了微分方程，核心公式：</p>
<script type="math/tex; mode=display">
\frac{dl_i(t)}{dt}=g_i l_i(t)\left(1-\frac{l_i(t)+\sum_{j\neq i} c_{ij} l_j(t)}{K(t)}\right)-f(dr) \beta_i l_i(t)+\sigma_i(t)</script><p>简理解一下各项：</p>
<ul>
<li><strong>繁殖增益</strong>：物种自身繁殖+种间关系（竞争会抑制、互利会促进）；</li>
<li><strong>死亡损耗</strong>：干旱越严重，死亡率越高（靠<code>dr</code>（干旱系数）控制）；</li>
<li><strong>随机干扰</strong>：模拟突发气候波动（比如突然降温）。</li>
</ul>
<h3 id="模拟不规则干旱"><a href="#模拟不规则干旱" class="headerlink" title="模拟不规则干旱"></a>模拟不规则干旱</h3><p>要让模型贴近现实，得先生成“随时间变化的干旱系数<code>dr</code>”——论文用了一个专门的算法：</p>
<p>核心逻辑</p>
<ol>
<li>先定规则：“上次干旱间隔越久，这次干旱概率越高”（符合现实气候规律）；</li>
<li>随机判断：每个月使用随机数，看是否触发干旱；</li>
<li>干旱期：一旦触发，持续10个月左右，<code>dr</code>维持在0.7左右（中度干旱）；</li>
<li>非干旱期：<code>dr</code>固定0.2（轻度/无干旱）。</li>
</ol>
<p>最终会得到一个“100个月的<code>dr</code>序列”，作为模型的“环境驱动因子”。</p>
<h2 id="不同物种数，群落表现差异"><a href="#不同物种数，群落表现差异" class="headerlink" title="不同物种数，群落表现差异"></a>不同物种数，群落表现差异</h2><p>论文用同一组<code>dr</code>序列，测了1/2/3/4种物种的群落，结果很直观：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>物种数</th>
<th>生态位宽度变化</th>
<th>群落状态</th>
</tr>
</thead>
<tbody>
<tr>
<td>1种</td>
<td>持续下降到0</td>
<td>直接灭绝</td>
</tr>
<tr>
<td>2种</td>
<td>剧烈波动</td>
<td>无法稳定存活</td>
</tr>
<tr>
<td>3种</td>
<td>波动上升</td>
<td>靠种间互利维持稳定</td>
</tr>
<tr>
<td>4种</td>
<td>上升后趋于平缓</td>
<td>物种太多导致生态位重叠，竞争加剧</td>
</tr>
</tbody>
</table>
</div>
<p>结论：<strong>群落受益的最小物种数是3种</strong>，多了反而会“内耗”。</p>
<h2 id="额外影响"><a href="#额外影响" class="headerlink" title="额外影响"></a>额外影响</h2><p>论文还测了“人类活动干扰”的场景，核心是通过两个方式削弱群落：</p>
<ol>
<li>压缩生态位上限：污染越重、栖息地越小，物种能利用的资源越少；</li>
<li>加剧种间竞争：资源不够时，物种从“协作”变“抢资源”。</li>
</ol>
<p>结果是“叠加式打击”：</p>
<ul>
<li>轻度干扰：生态位小幅下降，但群落能活；</li>
<li>重度干扰：生态位快速崩塌，70%物种灭绝，只剩极耐旱的“独苗”。</li>
</ul>
<h2 id="代码复现"><a href="#代码复现" class="headerlink" title="代码复现"></a>代码复现</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> scipy.integrate <span class="keyword">import</span> odeint</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> random</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">b=<span class="number">0.04</span></span><br><span class="line">n=<span class="number">100</span></span><br><span class="line">dr=[<span class="number">0.2</span>]*<span class="number">200</span></span><br><span class="line">K=<span class="number">10</span></span><br><span class="line">sig=<span class="number">0.2</span></span><br><span class="line">noise = np.random.normal(<span class="number">0</span>, sig, n+<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">g_max=[<span class="number">0.23</span>,<span class="number">0.15</span>,<span class="number">0.33</span>]</span><br><span class="line">gama=[<span class="number">0.5</span>,<span class="number">0.7</span>,<span class="number">0.2</span>]</span><br><span class="line">beta=[<span class="number">0.1</span>]*<span class="number">3</span></span><br><span class="line"></span><br><span class="line">C=[</span><br><span class="line">    [<span class="number">0</span>,-<span class="number">0.2</span>,<span class="number">0.4</span>],</span><br><span class="line">    [-<span class="number">0.2</span>,<span class="number">0</span>,<span class="number">0.6</span>],</span><br><span class="line">    [<span class="number">0.4</span>,<span class="number">0.6</span>,<span class="number">0</span>]</span><br><span class="line">]</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">P</span>(<span class="params">t</span>):</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>-np.exp(-b*t)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">f</span>(<span class="params">d</span>):</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>/(<span class="number">1</span>+np.exp(-<span class="number">12</span>*(d-<span class="number">0.5</span>)))</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">g_dr</span>():</span><br><span class="line">    nt=<span class="number">0</span></span><br><span class="line">    i=<span class="number">0</span></span><br><span class="line">    random.seed(<span class="number">42</span>)</span><br><span class="line">    np.random.seed(<span class="number">42</span>)</span><br><span class="line">    <span class="keyword">while</span> i&lt;=n :</span><br><span class="line">        r=random.random()</span><br><span class="line">        p=P(nt)</span><br><span class="line">        <span class="keyword">if</span> r&lt;=p:</span><br><span class="line">            rd=np.random.randn()</span><br><span class="line">            le=<span class="built_in">round</span>(rd*<span class="number">2</span>+<span class="number">10</span>)</span><br><span class="line">            <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(i,i+le):</span><br><span class="line">                rdj=np.random.randn()</span><br><span class="line">                dr[j]=<span class="number">0.15</span>*rdj+<span class="number">0.6</span></span><br><span class="line">            i=i+le</span><br><span class="line">            nt=<span class="number">0</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            dr[i]=dr[<span class="number">0</span>]</span><br><span class="line">            nt+=<span class="number">1</span></span><br><span class="line">            i+=<span class="number">1</span></span><br><span class="line">    <span class="keyword">pass</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">model</span>(<span class="params">l,t</span>):</span><br><span class="line">    ans=[]</span><br><span class="line">    t_idx = <span class="built_in">int</span>(np.<span class="built_in">round</span>(t))</span><br><span class="line">    t_idx = np.clip(t_idx, <span class="number">0</span>, n)</span><br><span class="line">    dr_t = dr[t_idx]</span><br><span class="line">    sigma = noise[t_idx]</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">3</span>): </span><br><span class="line">        g=g_max[i]*np.exp(-dr_t*gama[i])</span><br><span class="line">        s=<span class="number">0</span></span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">3</span>):</span><br><span class="line">            <span class="keyword">if</span> j!=i:</span><br><span class="line">                s+=C[i][j]*l[j]</span><br><span class="line">        dldt=g*l[i]*(<span class="number">1</span>-(l[i]+s)/K)-f(dr_t)*beta[i]*l[i]+sigma</span><br><span class="line">        ans.append(dldt)</span><br><span class="line">    <span class="keyword">return</span> ans</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    g_dr()</span><br><span class="line">    l=[<span class="number">2.0</span>]*<span class="number">3</span></span><br><span class="line">    t= np.arange(<span class="number">0</span>, n+<span class="number">1</span>, <span class="number">1</span>)</span><br><span class="line">    res=odeint(model,l,t)</span><br><span class="line">    l1=res[:,<span class="number">0</span>]</span><br><span class="line">    l2=res[:,<span class="number">1</span>]</span><br><span class="line">    l3=res[:,<span class="number">2</span>]</span><br><span class="line">    plt.figure(figsize=(<span class="number">12</span>, <span class="number">8</span>))</span><br><span class="line">        </span><br><span class="line">    ax1 = plt.subplot(<span class="number">211</span>)</span><br><span class="line">    ax1.plot(t, l1, color=<span class="string">&#x27;#2E86AB&#x27;</span>, linewidth=<span class="number">1.5</span>, label=<span class="string">&#x27;Species 1&#x27;</span>)</span><br><span class="line">    ax1.plot(t, l2, color=<span class="string">&#x27;#E63946&#x27;</span>, linewidth=<span class="number">1.5</span>, label=<span class="string">&#x27;Species 2&#x27;</span>)</span><br><span class="line">    ax1.plot(t, l3, color=<span class="string">&quot;#F3E304&quot;</span>, linewidth=<span class="number">1.5</span>, label=<span class="string">&#x27;Species 3&#x27;</span>)</span><br><span class="line">    ax1.set_xlabel(<span class="string">&#x27;Simulation Month&#x27;</span>, fontsize=<span class="number">12</span>)</span><br><span class="line">    ax1.set_ylabel(<span class="string">&#x27;Niche Width&#x27;</span>, fontsize=<span class="number">12</span>)</span><br><span class="line">    ax1.set_title(<span class="string">&#x27;Niche Width Change of 3 Species (100 Months)&#x27;</span>, fontsize=<span class="number">14</span>)</span><br><span class="line">    ax1.legend(loc=<span class="string">&#x27;upper right&#x27;</span>)</span><br><span class="line">    ax1.grid(alpha=<span class="number">0.3</span>)</span><br><span class="line">    ax1.set_ylim(<span class="number">0</span>, K+<span class="number">1</span>)</span><br><span class="line">    plt.show()</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>根据前面的干旱模拟算法求出<code>dr</code>，再解微分方程得到各物种生态位变化情况。<br>下面的其他问题都是在这个代码框架下修改。</p>
<h2 id="论文值得学的点"><a href="#论文值得学的点" class="headerlink" title="论文值得学的点"></a>论文值得学的点</h2><ol>
<li><strong>闭环思维</strong>：从“问题→模型→实验→措施”全程落地；</li>
<li><strong>理论+实际结合</strong>：用经典生态方程（Lotka-Volterra）打底，又加了现实的干旱、污染因子；</li>
<li><strong>对比实验清晰</strong>：控制单一变量（比如固定<code>dr</code>测不同物种数），结论说服力强。</li>
</ol>
]]></content>
      <categories>
        <category>数学建模</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>微分方程</tag>
        <tag>机理分析</tag>
      </tags>
  </entry>
  <entry>
    <title>GBDT</title>
    <url>/2025/12/01/GBDT/</url>
    <content><![CDATA[<p>简单讲讲GBDT ʕ⸝⸝⸝˙Ⱉ˙ʔ<br><span id="more"></span><br>    前置知识：决策树，集成学习</p>
<h1 id="GBDT与BDT"><a href="#GBDT与BDT" class="headerlink" title="GBDT与BDT"></a>GBDT与BDT</h1><h2 id="BDT"><a href="#BDT" class="headerlink" title="BDT"></a>BDT</h2><p>要学习GBDT，要先了解一下BDT（Boosting Decision Tree）提升树。<br>提升树的主要思想：<strong>使用残差拟合结果</strong>。  </p>
<pre><code>残差=真实值-预测值
</code></pre><p>举个简单例子，假设要预测某人年龄，其真实年龄为100岁  </p>
<ul>
<li>第一次预测80，残差为20</li>
<li>第二次以第一次的残差为输入，也就是对20预测，假设预测为16，残差为4</li>
<li>第三次以第二次的残差4为输入，假设预测为3.2</li>
<li>可以像这样进行多测预测</li>
<li>最后将每次预测加起来，最后预测结果=80+16+3.2=99.2<br>这样通过残差拟合将个弱学习器组合为强学习器，就是提升树最朴素的思想<h2 id="GBDT"><a href="#GBDT" class="headerlink" title="GBDT"></a>GBDT</h2>GBDT（Gradient Boosting Decision Tree）决策梯度提升树是集成学习Boosting（串行）算法的一种。<br>同样的Boosting算法可以看看前面的<a href="https://longnamehaha.github.io/2025/11/28/AdaBoost/" title="看看">AdaBoost</a>。   </li>
</ul>
<p>GBDT不再拟合残差，而是使用梯度下降的方法，将梯度下降的损失函数作为残差的近似值。<br>下面仅进行简单推导<br><del> 还有一些细节如用到的泰勒展开过程等此次暂时没有提到 </del><br>：<br>假设第i-1次强学习器：</p>
<script type="math/tex; mode=display">f_{i-1}(x)</script><p>其损失函数为:</p>
<script type="math/tex; mode=display">L(y,f_{i-1}(x))</script><p>第i次的弱学习器为：</p>
<script type="math/tex; mode=display">h_i(x)</script><p>第i次强学习器：</p>
<script type="math/tex; mode=display">f_i(x)=f_{i-1}(x)+h_i(x)</script><p>其损失函数为：</p>
<script type="math/tex; mode=display">L(y,f_i(x))=L(y,f_{i-1}(x)+h_i(x))=(y-f_{i-1}(x)-h_i(x))^2</script><p>梯度为：</p>
<script type="math/tex; mode=display">\dfrac{\partial L(y,f_i(x))}{\partial f_i(x)}=f_i(x)-y</script><p>负梯度为：</p>
<script type="math/tex; mode=display">-\dfrac{\partial L(y,f_i(x))}{\partial f_i(x)}=y-f_i(x)</script><p>这里的y就是真实值， $f_i(x)$ 为预测值，等式右边就是真实值-预测值，即残差。  </p>
<pre><code>负梯度=真实值-预测值=残差
</code></pre><p>也就是说，如果采用平方损失，则GBDT拟合的负梯度就是残差，GBDT实际上是BDT的一种。  </p>
<h1 id="GBDT具体过程细节"><a href="#GBDT具体过程细节" class="headerlink" title="GBDT具体过程细节"></a>GBDT具体过程细节</h1><p>此次以CART回归树为弱学习器，用平方损失作为损失函数。<br>先训练第一个弱学习器，最开始是选择同样的一个函数作为预测值，记都为 $f(x_i)$ 。<br>设所涉及的所有可能作为预测值的函数集合为 $\Gamma$ ,则我们需求的目标为：</p>
<script type="math/tex; mode=display">argmin_{f(x_i) \in \Gamma}L(y,f(x))</script><p>设</p>
<script type="math/tex; mode=display">Y=\frac{1}{2} L(y,f(x))</script><p>等价于求其最小值点，求偏导：</p>
<script type="math/tex; mode=display">\dfrac{\partial Y}{\partial f(x)}=\sum_{i=1}^n(y_i-f(x_i))(y_i-f(x_i))^{'}=-\sum_{i=1}^n(y_i-f(x_i))</script><p>令上式=0，得：</p>
<script type="math/tex; mode=display">nf(x_i)=\sum_{i=1}^ny_i</script><script type="math/tex; mode=display">f(x_i)=\frac{\sum_{i=1}^ny_i}{n}=\overline{y}</script><p>由公式可得，初始化输出值为目标均值时，平方损失最小。  </p>
<p>将目标值均值作为所有的预测值，就是第一个弱学习器。然后求出残差，残差就作为下一轮学习的目标值。  </p>
<p>对一组目标值，除了要得到下一组目标值，还需要得出这组目标值的弱学习器。  </p>
<p>此处弱学习器的训练同前面CART树一样，找到使总平方损失最小的分割点，此条件即为树结点。  </p>
<p>注意：上面以目标均值作为预测值，最开始是所有目标值的均值，接下来每次都是按照上一次决策树的结果分成两边，也就是左右子树分别计算均值做为预测值。</p>
<p>最后的预测结果，和前面的BDT一样，就是将每次预测的结果相加。流程与上面预测年龄的过程类似。</p>
<p>若为分类任务（如实战的存活预测），损失函数会改用对数损失 / 指数损失，此时负梯度不再是残差，但核心逻辑（拟合负梯度、加法集成）不变，最终通过 sigmoid/softmax 将累加结果转换为类别概率。</p>
<h1 id="实战运用"><a href="#实战运用" class="headerlink" title="实战运用"></a>实战运用</h1><p>api<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.ensemble <span class="keyword">import</span> GradientBoostingClassifier</span><br></pre></td></tr></table></figure><br>参数：<br>learning_rate：学习率<br>参数几乎没有变化，具体参考随机森林</p>
<p>以泰坦尼克号存活预测为例<br>完整代码<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> train_test_split</span><br><span class="line"><span class="keyword">from</span> sklearn.ensemble <span class="keyword">import</span> GradientBoostingClassifier</span><br><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> GridSearchCV</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> accuracy_score</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">d1</span>():</span><br><span class="line">    df=pd.read_csv(<span class="string">&quot;titanic_train.csv&quot;</span>)</span><br><span class="line">    <span class="comment"># df.info()</span></span><br><span class="line">    x=df[[<span class="string">&quot;Pclass&quot;</span>,<span class="string">&quot;Age&quot;</span>,<span class="string">&quot;Sex&quot;</span>]].copy()</span><br><span class="line">    y=df[<span class="string">&quot;Survived&quot;</span>].copy()</span><br><span class="line">    x[<span class="string">&#x27;Age&#x27;</span>].fillna(value=df[<span class="string">&quot;Age&quot;</span>].mean(),inplace=<span class="literal">True</span>)</span><br><span class="line">    x=pd.get_dummies(x) <span class="comment">#处理非数值类型</span></span><br><span class="line">    x_train,x_test,y_train,y_test = train_test_split(x,y,test_size=<span class="number">0.2</span>,random_state=<span class="number">23</span>)</span><br><span class="line">    estimator=GradientBoostingClassifier()</span><br><span class="line">    estimator.fit(x_train,y_train)</span><br><span class="line">    y_pred=estimator.predict(x_test)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&#x27;score: <span class="subst">&#123;accuracy_score(y_test,y_pred)&#125;</span>&#x27;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment">#交叉验证网格搜索</span></span><br><span class="line">    param=&#123;<span class="string">&quot;n_estimators&quot;</span>:[<span class="number">100</span>,<span class="number">150</span>,<span class="number">200</span>],<span class="string">&quot;max_depth&quot;</span>:[<span class="number">2</span>,<span class="number">4</span>,<span class="number">6</span>],<span class="string">&quot;learning_rate&quot;</span>:[<span class="number">0.03</span>,<span class="number">0.04</span>,<span class="number">0.05</span>,<span class="number">0.06</span>,<span class="number">0.1</span>]&#125;</span><br><span class="line">    tep=GradientBoostingClassifier()</span><br><span class="line">    model=GridSearchCV(tep,param_grid=param,cv=<span class="number">5</span>)</span><br><span class="line">    model.fit(x_train,y_train)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&#x27;score_2: <span class="subst">&#123;model.best_score_&#125;</span>&#x27;</span>)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&#x27;model: <span class="subst">&#123;model.best_estimator_&#125;</span>&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    d1()</span><br></pre></td></tr></table></figure></p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>BDT 是 “Boosting + 决策树” 的总称，GBDT 是其具体实现（用负梯度替代残差，更通用）；平方损失下，负梯度等价于残差，因此残差提升树是 GBDT 的特例。  </p>
<p>GBDT 的核心逻辑：<br>初始化：以目标值均值作为初始强学习器（平方损失下损失最小）；<br>迭代训练：每轮用 CART 回归树拟合前一轮的负梯度（误差减少最快的方向），得到弱学习器；<br>加法集成：最终强学习器 = 初始值 + 学习率 × 所有弱学习器输出之和（慢步累加，避免过拟合）。</p>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>机器学习</tag>
        <tag>GBDT</tag>
        <tag>Boosting</tag>
        <tag>集成学习</tag>
      </tags>
  </entry>
  <entry>
    <title>AdaBoost</title>
    <url>/2025/11/28/AdaBoost/</url>
    <content><![CDATA[<p>简单看看AdaBoost(,,・ω・,,)<br><span id="more"></span><br>    前置知识：集成学习</p>
<h1 id="什么是AdaBoost"><a href="#什么是AdaBoost" class="headerlink" title="什么是AdaBoost"></a>什么是AdaBoost</h1><p>前面的<a href="https://longnamehaha.github.io/2025/11/27/%E9%9A%8F%E6%9C%BA%E6%A3%AE%E6%9E%97/" title="看看">随机森林篇</a>已经学习过并行算法， <code>AdaBoost</code> ，从后缀Boost也可以看出来，这是集成学习中Boosting（串行）算法的一种。<br> Boosting是将原始数据经过第一个过程处理后，输入第二个过程继续…（比如数学先学习1+1，再学微积分）  </p>
<p> <code>AdaBoost</code> , 即自适应提升树，是基于集成学习中Boosting思想，来训练样本的强学习器，其核心在于<strong>逐步提高前一步被错误分类的样本的权重</strong>。没错，在训练过程中，样本不在看做等价值的，而是在训练过程中不断调整权重。  </p>
<p> 其实仔细一想也很好理解，比如当前先进行了一个分类，对于分类正确的部分，我们就不需要花更多的精力，而应该重点关注分类错误的样本，即增大错误样本的权重，减少正确样本的权重。  </p>
<p> 同样的，比如像刚刚讲的，训练了很多轮后，取最后的一个模型作为训练结果，那么，这仍然是一个模型，无非训练过程复杂些，也还是一个弱学习器。所以，在每步训练出来的模型，都要参与最后的决策。由于是串行，每一步训练出来的模型并非等价的（区别于随机森林的“平权投票”），所以每一步的模型对样本改变的程度和模型对最后的贡献，也是加权的。  </p>
<h1 id="AdaBoost算法过程"><a href="#AdaBoost算法过程" class="headerlink" title="AdaBoost算法过程"></a>AdaBoost算法过程</h1><p>刚刚已经简述了AdaBoost的工作原理，下面需要对其进行数学量化。<br>以二分类为例子, 即y取1或-1，算法流程：  </p>
<ol>
<li>初始化所有样本权重为 $\frac{1}{N}$ (N为样本数量)，记$D<em>i$=($w</em>{i1}$,$w<em>{i2}$…$w</em>{iN}$)为第 <code>i</code> 个样本集，其中，$w_{1j}$=$\frac{1}{N}$, j=1,2…N</li>
<li><p>设执行M次训练，则对应m=1. 2. 3….M</p>
<ol>
<li>先用第m次的数据集学习，得到基分类器$G_m$, 使用$G_m$对数据集分类并计算错误率$\varepsilon_m$:  <script type="math/tex; mode=display">\varepsilon_m=\sum_{i=1}^N w_{mi}I(G_m(x_i)\neq y_i)</script></li>
<li>计算得到$G_m$的系数：<script type="math/tex; mode=display">\alpha_m=\frac{1}{2}·\ln\frac{1-\varepsilon_m}{\varepsilon_m}</script></li>
<li>更新样本权重：<script type="math/tex; mode=display">w_{m+1,i}=\frac{w_{mi}}{Z_m} e^{\alpha_m t}</script>其中，<script type="math/tex; mode=display">t= \begin{cases} 
 1 & 分类错误 \\
 -1 & 分类正确 
 \end{cases}</script><script type="math/tex; mode=display">Z_m=\sum_{i=1}^N w_ie^{\alpha_m t}</script>$Z_m$为规范化因子，保证所有样本权值和为1  </li>
</ol>
</li>
<li><p>构建基分类器的线性组合<br>这一步就是与<a href="https://longnamehaha.github.io/2025/11/27/%E9%9A%8F%E6%9C%BA%E6%A3%AE%E6%9E%97/" title="看看">随机森林</a>“平权投票”不一样的地方了，此次各基学习器都是有权重的，即上面的$ \alpha_m$  </p>
</li>
</ol>
<p>线性组合：  </p>
<script type="math/tex; mode=display">f(x)= \sum_{i=1}^M \alpha_m G_m(x)</script><p>得到最终分类器：  </p>
<script type="math/tex; mode=display">G(x)=sign(f(x))</script><h1 id="应用案例——葡萄酒分类"><a href="#应用案例——葡萄酒分类" class="headerlink" title="应用案例——葡萄酒分类"></a>应用案例——葡萄酒分类</h1><h2 id="先介绍一下api："><a href="#先介绍一下api：" class="headerlink" title="先介绍一下api："></a>先介绍一下api：</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.ensemble <span class="keyword">import</span> AdaBoostClassifier</span><br><span class="line">model=AdaBoostClassifier(estimator=mytree,n_estimators=<span class="number">200</span>,learning_rate=<span class="number">0.1</span>,algorithm=<span class="string">&quot;SAMME.R&quot;</span>)</span><br></pre></td></tr></table></figure>
<ul>
<li><code>estimator</code> 是用于训练的基学习器，要先弄出来（下面用的是决策树）</li>
<li><code>n_estimators</code> 是串行训练的数量，即上面公式M</li>
<li><code>learning_rate</code> 学习率，控制每个基学习器的「话语权权重」—— 学习率越大，单个基学习器的影响越强；反之越弱</li>
<li><code>algorithm</code> 集成算法，一般使用默认 <code>SAMME.R</code></li>
</ul>
<h2 id="下面是完整代码"><a href="#下面是完整代码" class="headerlink" title="下面是完整代码"></a>下面是完整代码</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> train_test_split</span><br><span class="line"><span class="keyword">from</span> sklearn.tree <span class="keyword">import</span> DecisionTreeClassifier</span><br><span class="line"><span class="keyword">from</span> sklearn.ensemble <span class="keyword">import</span> AdaBoostClassifier</span><br><span class="line"><span class="keyword">from</span> sklearn.preprocessing <span class="keyword">import</span> LabelEncoder</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">d1</span>():</span><br><span class="line">    df = pd.read_csv(<span class="string">&quot;F:/机器学习/Adaboost/wine.data&quot;</span>, sep=<span class="string">&quot;,&quot;</span>, header=<span class="literal">None</span>)</span><br><span class="line">    df=df[df[<span class="number">0</span>]!=<span class="number">1</span>]</span><br><span class="line">    <span class="comment"># df.info()</span></span><br><span class="line">    x=df.iloc[:,[<span class="number">1</span>,-<span class="number">3</span>]] <span class="comment">#这里选择特征，具体特征可以看数据集</span></span><br><span class="line">    y=df.iloc[:,<span class="number">0</span>]</span><br><span class="line">    le = LabelEncoder()</span><br><span class="line">    y=le.fit_transform(y)</span><br><span class="line">    x_train,x_test,y_train,y_test = train_test_split(x,y,test_size=<span class="number">0.2</span>,random_state=<span class="number">23</span>)</span><br><span class="line">    mytree=DecisionTreeClassifier(max_depth=<span class="number">1</span>,random_state=<span class="number">0</span>)</span><br><span class="line">    mytree.fit(x_train,y_train)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;score1: <span class="subst">&#123;mytree.score(x_test,y_test)&#125;</span>&quot;</span>)</span><br><span class="line">    model=AdaBoostClassifier(estimator=mytree,n_estimators=<span class="number">200</span>,learning_rate=<span class="number">0.1</span>,algorithm=<span class="string">&quot;SAMME.R&quot;</span>)</span><br><span class="line">    model.fit(x_train,y_train)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;score2: <span class="subst">&#123;model.score(x_test,y_test)&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    d1()</span><br></pre></td></tr></table></figure>
<p>先构建决策树，然后以决策树为基学习器构造集成model。</p>
<h1 id="AdaBoost：基学习器组合与任务应用"><a href="#AdaBoost：基学习器组合与任务应用" class="headerlink" title="AdaBoost：基学习器组合与任务应用"></a>AdaBoost：基学习器组合与任务应用</h1><p>上面例子的决策树只是其中一种基学习器，当然还有别的组合，可以完成其他任务。</p>
<h2 id="一、可组合的基学习器（核心：弱学习器优先）"><a href="#一、可组合的基学习器（核心：弱学习器优先）" class="headerlink" title="一、可组合的基学习器（核心：弱学习器优先）"></a>一、可组合的基学习器（核心：弱学习器优先）</h2><ol>
<li>决策树桩（最推荐）<ul>
<li>原理：单层决策树，仅用1个特征分类，弱学习器代表</li>
<li>代码：<code>DecisionTreeClassifier(max_depth=1)</code></li>
<li>适用：所有分类/回归任务，训练快、泛化好</li>
</ul>
</li>
<li>逻辑回归（线性基学习器）<ul>
<li>代码：<code>LogisticRegression(max_iter=1000)</code></li>
<li>适用：线性可分数据，可解释性强</li>
</ul>
</li>
<li>线性 SVM（高维数据）<ul>
<li>代码：<code>SVC(kernel=&#39;linear&#39;, probability=True)</code></li>
<li>适用：文本、基因等高维数据分类</li>
</ul>
</li>
<li>浅层神经网络（非线性数据）<ul>
<li>代码：<code>MLPClassifier(hidden_layer_sizes=(10,))</code></li>
<li>适用：中等复杂度非线性数据</li>
</ul>
</li>
</ol>
<h2 id="二、能完成的核心任务"><a href="#二、能完成的核心任务" class="headerlink" title="二、能完成的核心任务"></a>二、能完成的核心任务</h2><ol>
<li>二分类（原生支持）<ul>
<li>应用：垃圾邮件分类、疾病诊断</li>
<li>代码：<code>AdaBoostClassifier(base_estimator=决策树桩)</code></li>
</ul>
</li>
<li>多分类（One-vs-All 策略）<ul>
<li>应用：手写数字识别、新闻分类</li>
<li>代码：直接用 <code>AdaBoostClassifier</code>，自动支持多分类</li>
</ul>
</li>
<li>回归（修改损失函数）<ul>
<li>应用：房价、销售额预测</li>
<li>代码：<code>AdaBoostRegressor(base_estimator=决策树回归)</code></li>
</ul>
</li>
<li>异常检测（聚焦少数类）<ul>
<li>应用：欺诈检测、故障检测</li>
<li>代码：<code>AdaBoostClassifier</code> + 不平衡数据权重调整</li>
</ul>
</li>
</ol>
<h2 id="三、选型建议"><a href="#三、选型建议" class="headerlink" title="三、选型建议"></a>三、选型建议</h2><ul>
<li>优先用决策树桩作为基学习器，性价比最高；</li>
<li>二分类/多分类选 <code>AdaBoostClassifier</code>，回归选 <code>AdaBoostRegressor</code>；</li>
<li>学习率建议 0.05-0.1，基学习器数量 50-200，平衡速度和效果。</li>
</ul>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>AdaBoost 作为集成学习中 Boosting 思想的经典实现，核心魅力在于「用简单弱学习器集成强模型」，其设计逻辑既直观又严谨：通过串行训练基学习器，每一轮都聚焦前一轮的错分样本（动态调整权重），同时给每个基学习器分配差异化权重（性能越好的基学习器 “话语权” 越强），最终通过加权组合实现比单一弱学习器更优的泛化能力。  </p>
<p>相较于单一模型，AdaBoost 无需复杂特征工程就能应对多数场景，且训练速度快、可解释性较强；但需注意，它对<strong>异常值较敏感</strong>（错分样本权重持续升高可能放大异常值影响），且不适合用复杂强学习器作为基学习器（易过拟合、失去集成意义）。</p>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>机器学习</tag>
        <tag>Boosting</tag>
        <tag>集成学习</tag>
        <tag>AdaBoost</tag>
      </tags>
  </entry>
  <entry>
    <title>TOPSIS法</title>
    <url>/2025/11/30/TOPSIS%E6%B3%95/</url>
    <content><![CDATA[<p>简单的TOPSIS法｡ﾟ(ﾟ´ω`ﾟ)ﾟ｡<br><span id="more"></span><br>    前置知识：最好会层次分析法</p>
<h1 id="模型引出"><a href="#模型引出" class="headerlink" title="模型引出"></a>模型引出</h1><p>本节的TOPSIS法，同前面的一样，都是是评价类模型。<br>TOPSIS法（Technique for Order Preference by Similarity to Ideal Solution）可翻译为逼近理想解排序法，或简称为优劣解距离法。<br>首先需要引入两个概念：</p>
<ul>
<li>理想解：各维度都达到所有备选方案最优秀的解</li>
<li>反理想解：各维度都达到所有备选方案最差的解  </li>
</ul>
<p>比如工厂生产，第一维表示产量，第二维表示耗资。三个工厂（备选方案）为：（100，100），（150，110），（200，300）则理想解为（200，100），反理想解为（100，300）.  </p>
<p>方案排序的规则就是根据与最优解的距离来，同时要尽量远离反理性解。</p>
<h1 id="基本步骤"><a href="#基本步骤" class="headerlink" title="基本步骤"></a>基本步骤</h1><h2 id="1-原始矩阵正向化"><a href="#1-原始矩阵正向化" class="headerlink" title="1.原始矩阵正向化"></a>1.原始矩阵正向化</h2><p>正向化，即将各指标转化为“越大越好”<br>首先将指标分为4种：极大型，极小型，中间型，区间型<br>设 $\widetilde{x_i}$ 为转换后的第i个指标</p>
<ul>
<li>极大型：指标数值越大越好，不用转换</li>
<li>极小型：指标数值越小越好，转换方法：<script type="math/tex; mode=display">\widetilde{x_i}= max(x) - x_i</script></li>
<li>中间型：即数值越接近中间一个最优值 $x_{best}$ 越好：<br>要先计算M：<script type="math/tex; mode=display">M=max(|x_i-x_{best}|)</script><script type="math/tex; mode=display">\widetilde{x_i}= 1 - \frac{|x_i-x_{best}|}{M}</script></li>
<li>区间型:数值处于某个区间 [a,b] 内最好，此时分区间讨论：<script type="math/tex; mode=display">M=max(max(x_i)-b,a-min(x_i))</script><script type="math/tex; mode=display">
\widetilde{x_i}= 
\begin{cases}
1-\frac{a-x_i}{M} \quad \text{ $x_i$ <a} \\
1\quad \text{$ a \leq x_i \leq b $} \\
1-\frac{x_i-b}{M} \quad \text{ $x_i$ >b}
\end{cases}</script><h2 id="2-正向化矩阵标准化"><a href="#2-正向化矩阵标准化" class="headerlink" title="2.正向化矩阵标准化"></a>2.正向化矩阵标准化</h2></li>
</ul>
<p>有<strong>n个待评价对象，m个指标</strong>，矩阵元素 $x_{ij}$ 表示第i个对象第j个指标的得分。则：  </p>
<script type="math/tex; mode=display">\widetilde{x_{ij}}=\frac{x_{ij}}{\sqrt{\sum_{i=1}^{n}{x_{ij}^2}}}</script><p>标准化后，默认各指标权重相同，也可以使用层次分析法，熵权法等赋予权重。  </p>
<h2 id="3-计算得分"><a href="#3-计算得分" class="headerlink" title="3.计算得分"></a>3.计算得分</h2><ul>
<li><p>定义正理想解向量 <script type="math/tex">Z^+=(Z_1^+, Z_2^+, ...,Z_m^+)</script><br>其中 $Z_i^+$ 为第i列元素最大值。  </p>
</li>
<li><p>负理想解向量 <script type="math/tex">Z^-=(Z_1^-, Z_2^-, ...,Z_m^-)</script><br>其中 $Z_i^-$ 为第i列元素最小值。  </p>
</li>
<li><p>记权重向量 $w$，$w_j$表示第j个指标的权重。  </p>
</li>
<li><p>第i个对象与正理想解距离：</p>
<script type="math/tex; mode=display">D_{i}^+=\sqrt{\sum_{j=1}^{m}{w_j(Z_i^+-\widetilde{x_{ij}})^2}}</script></li>
<li>第i个对象与负理想解距离：<script type="math/tex; mode=display">D_{i}^-=\sqrt{\sum_{j=1}^{m}{w_j(Z_i^--\widetilde{x_{ij}})^2}}</script>计算原始得分：<script type="math/tex; mode=display">S_i=\frac{D_{i}^-}{D_{i}^++D_{i}^-}</script>显然 $S_i\in [0,1]$ ,且 $S_i$ 越大，越接近正理想解。  </li>
</ul>
<p>为便于比较，再将个对象的得分归一化后*100转化为百分制。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>TOPSIS 法是基于 “距离排序” 的客观评价模型，核心逻辑是通过计算各方案与 “正理想解（最优值组合）”“负理想解（最劣值组合）” 的加权距离，量化方案的优劣程度。<br>该模型的优势是兼顾客观数据规律与指标重要性差异，可灵活结合 AHP（主观赋权）或熵权法（客观赋权），适用于多指标、多方案的排序决策（如产品评选、绩效评价、资源分配等）。</p>
]]></content>
      <categories>
        <category>数学建模</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>TOPSIS法</tag>
        <tag>评价类模型</tag>
      </tags>
  </entry>
  <entry>
    <title>层次分析法</title>
    <url>/2025/11/29/%E5%B1%82%E6%AC%A1%E5%88%86%E6%9E%90%E6%B3%95/</url>
    <content><![CDATA[<p>简单的层次分析法(ゝ∀･)<br><span id="more"></span><br>    前置知识：一点点矩阵基础</p>
<h1 id="模型引出"><a href="#模型引出" class="headerlink" title="模型引出"></a>模型引出</h1><p>在决策问题中，需要面对多种策略，选择最优。但实际的方案通常涉及多个指标，各指标之间无法之间比较。比如评选最优明星，有粉丝量，颜值，作品质量，作品数量等。<br>按照普通的想法，可能想将各指标归一化后相加，这样可以消除量纲的影响。但是各个指标的<strong>重要性</strong>也是不同的，于是需要主观的对个指标赋予不同的<strong>权重</strong>。<br>简单来说，层次分析法（ <code>Analytic Hierarchy Process</code> ，简称 <code>AHP</code> ）也是这样的，本质上和上面的<strong>为各指派赋权重</strong>一样，只是加了一个判断机制，让权重偏差不会过大。  </p>
<h1 id="模型原理"><a href="#模型原理" class="headerlink" title="模型原理"></a>模型原理</h1><h2 id="何为层次"><a href="#何为层次" class="headerlink" title="何为层次"></a>何为层次</h2><p>层次分析法，分为三层：  </p>
<ul>
<li>最高层，即最后得到的决策目标。</li>
<li>中间层，即涉及到的各个原始指标，如粉丝量，颜值，作品质量，作品数量  </li>
<li>最底层，即具体各种备选方案，如各明星  </li>
</ul>
<h2 id="基本求解步骤："><a href="#基本求解步骤：" class="headerlink" title="基本求解步骤："></a>基本求解步骤：</h2><ol>
<li>构建阶梯结构模型（上面的那些）</li>
<li>求出各层中所有<strong>判断矩阵</strong></li>
<li>一致性检验</li>
<li>求出权重后进行检验  </li>
</ol>
<p>层次分析法能够起到作用，就是依赖<strong>一致性检验</strong>。<br>构建判断矩阵过程：<br>对各指标两两进行重要性比较。  </p>
<p>其中矩阵元素 $a<em>{ij}$ 表示第 i 个指标相对于第 j 个指标的重要程度。<br>当 $i \leq j$ 时，$a</em>{ij}$ 取整数 1~9 ，$a<em>{ij}$=1，表示第i个和第j个元素同样重要，$a</em>{ij}$=3，表示i稍微比j重要。以此类推，$a_{ij}$越大，说明第i个指标对比第j个指标重要性越大。  </p>
<p>可得 $a<em>{ij} · a</em>{ji} = 1$ (称为正互反矩阵)   </p>
<p>除了主对角线元素全为1，其他元素都是需要我们去填写的。<br>由上面的定义，我们可以知道，重要性是可以传递的，即 $a<em>{ij}=a</em>{ik}·a_{kj}$，所以，我们其实只填写任一行（或列）除了主对角线外的n-1个元素，就可以递推地求出整个矩阵（此时矩阵秩为1）。但是这样就和普通的去赋予权重一样了。<br>层次分析法中，要求我们为每个空缺元素都根据主观赋值，显然，此时再按照上面的递推式计算，最后构建的矩阵会产生矛盾。而一致性检验，就是我们对这个矛盾的<strong>容忍程度</strong>。  </p>
<h2 id="具体讲讲一致性检验"><a href="#具体讲讲一致性检验" class="headerlink" title="具体讲讲一致性检验"></a>具体讲讲一致性检验</h2><h3 id="什么是一致性检验"><a href="#什么是一致性检验" class="headerlink" title="什么是一致性检验"></a>什么是一致性检验</h3><p>上面通过递推构建出来的秩1矩阵，记作<strong>一致矩阵</strong>。<br>一致性检验就是检验我们自己写的矩阵和一致矩阵的差别大不大。  </p>
<pre><code>关于一致矩阵秩为什么为1：按照递推构成的矩阵，每行每列都是成比例的，由矩阵知识可以知道其秩为1。其一个特征值为n，其余特征值为0等矩阵相关性质。
</code></pre><p>引理：n阶正互反矩阵为一致矩阵当且仅当其最大特征值 $\lambda<em>{max} = n$ ,且当矩阵非一致矩阵时，一定有 $\lambda</em>{max} \geq n$，判断矩阵越不一致，其最大特征值与n差距越大。  </p>
<h3 id="一致性检验具体步骤"><a href="#一致性检验具体步骤" class="headerlink" title="一致性检验具体步骤"></a>一致性检验具体步骤</h3><ol>
<li>计算一致性指标 $CI$ :<script type="math/tex; mode=display">CI=\frac{\lambda_{max}-n}{n-1}</script></li>
<li>查找平均随机一致性指标 $RI$ ：</li>
</ol>
<div class="table-container">
<table>
<thead>
<tr>
<th>判断矩阵阶数（n）</th>
<th>1</th>
<th>2</th>
<th>3</th>
<th>4</th>
<th>5</th>
<th>6</th>
<th>7</th>
<th>8</th>
<th>9</th>
<th>10</th>
</tr>
</thead>
<tbody>
<tr>
<td>平均随机一致性指标（RI）</td>
<td>0.00</td>
<td>0.00</td>
<td>0.58</td>
<td>0.90</td>
<td>1.12</td>
<td>1.24</td>
<td>1.32</td>
<td>1.41</td>
<td>1.45</td>
<td>1.49</td>
</tr>
</tbody>
</table>
</div>
<p>只需要会查表即可。  </p>
<ol>
<li>计算一致性比例 $CR$ <script type="math/tex; mode=display">
CR= \frac{CI}{RI} 
\begin{cases}
= 0 \quad \text{判断矩阵为一致性矩阵} \\
< 0.1 \quad \text{判断矩阵一致} \\
\geq 0.1 \quad \text{判断矩阵不一致}
\end{cases}</script></li>
</ol>
<h2 id="求权重的方法"><a href="#求权重的方法" class="headerlink" title="求权重的方法"></a>求权重的方法</h2><ul>
<li>算术平均值：  <ol>
<li>每列（各个特征）归一化</li>
<li>按行求和</li>
<li>再次归一化</li>
</ol>
</li>
<li><p>几何平均值：  </p>
<ol>
<li>按行求几何平均值</li>
<li>归一化</li>
</ol>
</li>
<li><p>特征值法：  </p>
<p>  以最大特征值对应的特征向量作为权重</p>
</li>
</ul>
<p>求出权重后，在带回原来各方案，求出各个得分。  </p>
<h1 id="简单总结"><a href="#简单总结" class="headerlink" title="简单总结"></a>简单总结</h1><p>层次分析法是评价类的模型，依旧无法避免主观性的影响，但可以通过一致性检验来限制主观因素在一个较合理的范围。所以说，<strong>一致性检验</strong>才是层次分析法的灵魂所在。  </p>
<p>当然也有很多其他评价类模型，实际情况往往需要综合考量。</p>
]]></content>
      <categories>
        <category>数学建模</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>评价类模型</tag>
        <tag>层次分析法</tag>
      </tags>
  </entry>
  <entry>
    <title>matlab基础</title>
    <url>/2026/01/17/matlab%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<p>部分语法基础<br><span id="more"></span></p>
<h1 id="排序"><a href="#排序" class="headerlink" title="排序"></a>排序</h1><h2 id="一维排序和排名"><a href="#一维排序和排名" class="headerlink" title="一维排序和排名"></a>一维排序和排名</h2><p>默认升序，descend表示降<br><figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line">v=[<span class="number">5</span>,<span class="number">5</span>,<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>];</span><br><span class="line">[sorted,ind]=<span class="built_in">sort</span>(v,<span class="string">&#x27;descend&#x27;</span>)</span><br><span class="line">mx=sorted(<span class="number">1</span>:<span class="number">3</span>)</span><br><span class="line">id=ind(<span class="number">1</span>:<span class="number">3</span>)<span class="comment">%最大的前3个</span></span><br><span class="line">[~,indd]=<span class="built_in">sort</span>(ind)<span class="comment">%对ind再次找按升序的索引，即可得到原数组各数排名</span></span><br></pre></td></tr></table></figure></p>
<h2 id="多维"><a href="#多维" class="headerlink" title="多维"></a>多维</h2><p>matlab中使用dim参数表示维度，二维使用最多，<code>dim=1</code> 表示行（默认），从上到下，<code>dim=2</code> 表示列，从左到右<br><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">a=randi(10,5,6)</span><br><span class="line">sort1=sort(a,1)</span><br><span class="line">sort2=sort(a,2)</span><br><span class="line">sort3=sort(a,2,&#x27;descend&#x27;)</span><br><span class="line">[sorted,ind]=sort(a)</span><br></pre></td></tr></table></figure></p>
<h2 id="按某列"><a href="#按某列" class="headerlink" title="按某列"></a>按某列</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sorted=sortrows(a,[1,3])</span><br></pre></td></tr></table></figure>
<ul>
<li>二维矩阵，将行看成一个整体，共同排序</li>
<li>这里表示先按第一列，若相同再按照第三列排序（升序），再相同按照原始出现的顺序</li>
</ul>
<p>可以简写为 <code>sorted=sortrows(a)</code> 表示先按照第一列，然后第二列…<br>比如各列是不同的指标，列号越小指标越重要</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[sorted,ind]=sortrows(a,[1,3],&#123;&#x27;descend&#x27;,&#x27;ascend&#x27;&#125;)</span><br></pre></td></tr></table></figure>
<p>也可以先按某列降，再按照某列升等，可以用于负向指标<br>ind 同上<br>这里用 <code>sorted</code> 和 <code>a(ind,:)</code> 结果一样</p>
<h1 id="反转与旋转"><a href="#反转与旋转" class="headerlink" title="反转与旋转"></a>反转与旋转</h1><p>对向量或者矩阵反转<br><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">flip(a)%上下</span><br><span class="line">flip(a,2)%左右</span><br></pre></td></tr></table></figure></p>
<p>旋转<br><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">b=rot90(a,2)</span><br></pre></td></tr></table></figure><br>表示逆时针转k*90度，k取负数表示顺时针旋转<br>2·3矩阵操作一次变成3·2</p>
<h1 id="集合操作"><a href="#集合操作" class="headerlink" title="集合操作"></a>集合操作</h1><h2 id="去重"><a href="#去重" class="headerlink" title="去重"></a>去重</h2><p> <code>unique(a)</code> 去重（自动排序）  </p>
<p>可以接受更多参数<br><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[c,ia,ic]=unique(a)</span><br></pre></td></tr></table></figure><br>表示结果在另一个向量中的索引，同前<br>即有 <code>a(ic)==c&amp;&amp;c(ia)==a</code>  </p>
<p>当然也可以不排序，稳定的方法如下<br><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">c=unique(a,&#x27;stable&#x27;)</span><br></pre></td></tr></table></figure><br>按照原顺序</p>
<p>作用于矩阵<br><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">a = randi(2,5,2)</span><br><span class="line">a(:)</span><br><span class="line">unique(a)</span><br><span class="line">unique(a(:))%二者等价，表示先将a拆为一维列向量后去重</span><br><span class="line">unique(a,&#x27;rows&#x27;,&#x27;stable&#x27;)%表示将行作为整体，同前sortrows</span><br></pre></td></tr></table></figure></p>
<h2 id="查找"><a href="#查找" class="headerlink" title="查找"></a>查找</h2><p>使用ismember函数判断集合中有没有目标值<br><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">a=[2,1,2,3,4]</span><br><span class="line">ismember(a,[1,2,3])</span><br></pre></td></tr></table></figure><br>前面的a是待查找向量，后面的为目标，<br>返回 <code>1 1 1 1 0</code> 表示只要是值为目标中的一个就为真  </p>
<p>当然也可以用于二维矩阵，效果一样<br>或者将行作为一个整体，如<br><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">a=[1,2;1,2;1,3;4,5]</span><br><span class="line">ismember(a,[1,2],&#x27;rows&#x27;)</span><br></pre></td></tr></table></figure><br>作用同前类似</p>
<h2 id="其他集合运算"><a href="#其他集合运算" class="headerlink" title="其他集合运算"></a>其他集合运算</h2><p>常见的一些运算：</p>
<ul>
<li><code>intersect</code> 表示交集</li>
<li><code>union</code> 表示并集</li>
<li><code>setdiff</code> 表示差集，<code>setdiff(A,B)==A-B</code> </li>
<li><code>setxor</code> 表示异或</li>
</ul>
<h1 id="文件"><a href="#文件" class="headerlink" title="文件"></a>文件</h1><h2 id="读入"><a href="#读入" class="headerlink" title="读入"></a>读入</h2><p>不需要用代码读文件，直接使用导入功能<br>导入时可以选择导入格式，列表名等  </p>
<h2 id="处理"><a href="#处理" class="headerlink" title="处理"></a>处理</h2><p>常用的是将xlsx，csv等导入为table<br>table常用相关函数：</p>
<ul>
<li>summary：直接得到各列最值，均值等</li>
<li>size</li>
<li>head/tail：看几个头/尾元素</li>
<li>disp：完整打印  </li>
<li>table2array：转为常用的数值矩阵</li>
<li>table2cell：转为元胞，主要是兼容不同类型</li>
<li>rmmissing：样本充足时使用，去除包含NaN的行或列</li>
<li>fillmissing：填报缺失值，可以用均值或线性填报  </li>
</ul>
<h2 id="导出"><a href="#导出" class="headerlink" title="导出"></a>导出</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">% 1. 导出清洗后的表格（带表头，方便论文查看）</span><br><span class="line">writetable(tt_clean, &#x27;t1.csv&#x27;, &#x27;Encoding&#x27;, &#x27;GBK&#x27;);</span><br><span class="line"></span><br><span class="line">% 2. 导出数值数组（无表头，用于后续建模计算）</span><br><span class="line">writematrix(table2array(tt_clean), &#x27;t2.csv&#x27;);</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>数学建模</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>matlab</tag>
      </tags>
  </entry>
  <entry>
    <title>k-means</title>
    <url>/2025/12/18/k-means/</url>
    <content><![CDATA[<p>无监督学习<br><span id="more"></span></p>
<h1 id="聚类算法"><a href="#聚类算法" class="headerlink" title="聚类算法"></a>聚类算法</h1><h2 id="简单概念"><a href="#简单概念" class="headerlink" title="简单概念"></a>简单概念</h2><p>聚类算法是机器学习中<strong>无监督学习</strong>的一种，前面提到的有监督学习，是已经有数据和标签，然后需要我们预测等。而无监督学习，一开始没有标签，需要我们自己发掘数据之间的<strong>相似性</strong><br>衡量样本相似性有许多方法，通常是使用距离。常用的距离有欧式距离，曼哈顿距离，切比雪夫距离等。</p>
<h2 id="简单类别"><a href="#简单类别" class="headerlink" title="简单类别"></a>简单类别</h2><p>按照颗粒度划分：</p>
<ul>
<li>分的类别越多，颗粒度越小，称为细聚类</li>
<li>分的类别越少，颗粒度越大，称为粗聚类  </li>
</ul>
<p>按照实现方法，有K-means，层次聚类，DBSCAN等。<br>本文涉及的K-means，也是聚类算法最常用，最普遍的一种。</p>
<h2 id="简单应用"><a href="#简单应用" class="headerlink" title="简单应用"></a>简单应用</h2><p>聚类算法一般在某工程初期，缺少标签时会使用到，使用聚类算法等构建数据标签后，就向监督学习的各算法转换。<br>用户画像，搜索推荐，新闻聚类等都用到了聚类算法。</p>
<h1 id="k-means流程"><a href="#k-means流程" class="headerlink" title="k-means流程"></a>k-means流程</h1><h2 id="核心算法"><a href="#核心算法" class="headerlink" title="核心算法"></a>核心算法</h2><p>k-means作为聚类算法最普遍使用的一种，其流程也是十分简单：</p>
<ol>
<li>事先确定常数k，表示最后的类别数（通常也称为 ‘簇’ ，有几个类别就说几簇）</li>
<li>随机选择k个样本点作为初始聚类中心</li>
<li>逐个计算各个数据点与各聚类中心的距离，该数据点属于距离最近的聚类中心</li>
<li>在分类完成后，计算各类别的<strong>质心</strong>（此次使用所有样本点各指标的平均值）</li>
<li>以质心作为新的聚类中心，重复步骤3，4直至聚类中心不在变化<br>由该流程也可以看出此算法的收敛性，最后各个聚类中心会稳定于某状态。<br>聚类中心不一定是某个确定的样本点，就比如物体的质心可以不在物体上。<h2 id="核心代码"><a href="#核心代码" class="headerlink" title="核心代码"></a>核心代码</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.cluster <span class="keyword">import</span> KMeans</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">from</span> sklearn.datasets <span class="keyword">import</span> make_blobs</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> calinski_harabasz_score  </span><br><span class="line"></span><br><span class="line">x,y=make_blobs(n_samples=<span class="number">100</span>,n_features=<span class="number">2</span>,centers=[[<span class="number">1</span>,<span class="number">1</span>],[<span class="number">2</span>,<span class="number">2</span>],[<span class="number">3</span>,<span class="number">3</span>],[<span class="number">4</span>,<span class="number">4</span>]],cluster_std=[<span class="number">0.4</span>,<span class="number">0.2</span>,<span class="number">0.3</span>,<span class="number">0.1</span>],random_state=<span class="number">23</span>)</span><br><span class="line"></span><br><span class="line">y_pred=KMeans(n_clusters=<span class="number">4</span>,random_state=<span class="number">23</span>).fit_predict(x)</span><br><span class="line"></span><br><span class="line">plt.figure()</span><br><span class="line">plt.scatter(x[:,<span class="number">0</span>],x[:,<span class="number">1</span>],c=y_pred)</span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure>
API参数介绍：<br><code>make_blobs</code> 用于生成符合高斯分布的二维随机点，可以用 <code>centers</code> 来指定生成的中心，用 <code>cluster_std</code> 来指定方差。<br><code>KMeans</code> 是核心函数：其中 <code>n_clusters</code> 表示最终分类的簇数</li>
</ol>
<p>从该函数的参数我们会想到一个问题：如何确定 <code>n_clusters</code> ？<br>就像我们前面提到的，聚类算法的使用往往是因为没有先验的知识，我们有的仅仅是数据，并不知道簇数。所有，我们需要下面的这些方法。</p>
<h1 id="SSE聚类评估指标"><a href="#SSE聚类评估指标" class="headerlink" title="SSE聚类评估指标"></a>SSE聚类评估指标</h1><p>SSE（The sum of squares due to error，误差平方和）是评价聚类算法优劣的指标。这种计算方法想必大家已经见过无数次了，就不在细讲，公式也十分简单：</p>
<script type="math/tex; mode=display">
\sum_{i=1}^k \sum_{p \in C_i}|p-m_i|^2</script><p>式中，</p>
<ul>
<li>$C_i$表示簇</li>
<li>k为聚类中心个数</li>
<li>p表示某个样本内部样本点</li>
<li>m表示质心点</li>
</ul>
<p>易得，SSE越小，表示数据距离各自质心越近，聚类效果越好。<br>SSE不仅仅是用来评估的，更重要的是用于确定簇数。单独的SSE还不够，需要结合‘肘’方法</p>
<h2 id="‘肘’方法"><a href="#‘肘’方法" class="headerlink" title="‘肘’方法"></a>‘肘’方法</h2><p>由SSE定义易得，当k越大，SSE越小。‘肘’方法就是找到一个临界点，在此临界点之前，SSE减小很快，之后SSE减小变慢。<br>从图像来说，就是负斜率快速减少的临界点对应的簇数作为分类的k，图像上就想我们的胳膊，临界点就是在肘部位置。</p>
<h2 id="代码过程"><a href="#代码过程" class="headerlink" title="代码过程"></a>代码过程</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.cluster <span class="keyword">import</span> KMeans</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">from</span> sklearn.datasets <span class="keyword">import</span> make_blobs</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> calinski_harabasz_score</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">SSE</span>():</span><br><span class="line">    sse_list = []</span><br><span class="line"></span><br><span class="line">    x, y = make_blobs(n_samples=<span class="number">1000</span>, n_features=<span class="number">2</span>, centers=[[-<span class="number">1</span>, -<span class="number">1</span>], [<span class="number">0</span>, <span class="number">0</span>], [<span class="number">1</span>, <span class="number">1</span>], [<span class="number">2</span>, <span class="number">2</span>]],cluster_std=[<span class="number">0.4</span>, <span class="number">0.2</span>, <span class="number">0.2</span>, <span class="number">0.2</span>], random_state=<span class="number">22</span>)</span><br><span class="line">    <span class="keyword">for</span> clu_num <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">1</span>, <span class="number">100</span>):</span><br><span class="line">        my_kmeans = KMeans(n_clusters=clu_num, max_iter=<span class="number">100</span>, random_state=<span class="number">0</span>)</span><br><span class="line">        my_kmeans.fit(x)</span><br><span class="line">        sse_list.append(my_kmeans.inertia_ ) <span class="comment"># 获取sse的值</span></span><br><span class="line">    </span><br><span class="line">    plt.figure(figsize=(<span class="number">18</span>, <span class="number">8</span>), dpi=<span class="number">100</span>)</span><br><span class="line">    plt.xticks(<span class="built_in">range</span>(<span class="number">0</span>, <span class="number">100</span>, <span class="number">3</span>), labels=<span class="built_in">range</span>(<span class="number">0</span>, <span class="number">100</span>, <span class="number">3</span>))</span><br><span class="line">    plt.grid()</span><br><span class="line">    plt.title(<span class="string">&#x27;sse&#x27;</span>)</span><br><span class="line">    plt.plot(<span class="built_in">range</span>(<span class="number">1</span>, <span class="number">100</span>), sse_list, <span class="string">&#x27;or-&#x27;</span>)</span><br><span class="line">    plt.show()</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    SSE()</span><br></pre></td></tr></table></figure>
<p>代码非常简单，就是遍历要划分的簇数，然后求出SSE并画图，最后从图像上选择出临界点。</p>
<h1 id="SC轮廓系数"><a href="#SC轮廓系数" class="headerlink" title="SC轮廓系数"></a>SC轮廓系数</h1><p>距离指标，分为内聚和分离。<br>前面的SSE，仅考虑了簇内的距离，即内聚。而没有考虑簇间的距离。<br>下面的SC指标加入的分离的因素<br>公式</p>
<script type="math/tex; mode=display">
s(i) = \frac{b(i) - a(i)}{\max\left\{a(i), b(i)\right\}}</script><p>其中：</p>
<ul>
<li><p>$a(i)$：<strong>簇内紧密度</strong> → 样本 $i$ 到其所属簇内所有其他样本的<strong>平均欧氏距离</strong>；</p>
<script type="math/tex; mode=display">a(i) = \frac{1}{n_k - 1} \sum_{j \in C_k, j \neq i} d(i, j)</script><p>$C_k$ 是样本 $i$ 所属的簇，$n_k$ 是簇 $C_k$ 的样本数，$d(i,j)$ 是样本 $i$ 和 $j$ 的欧氏距离。</p>
</li>
<li><p>$b(i)$：<strong>簇间分离度</strong> → 样本 $i$ 到「所有其他簇」的平均距离中<strong>最小的那个值</strong>；</p>
<script type="math/tex; mode=display">b(i) = \min_{m \neq k} \left\{ \frac{1}{n_m} \sum_{j \in C_m} d(i, j) \right\}</script><p>$C_m$ 是除 $C_k$ 外的任意簇，$n_m$ 是簇 $C_m$ 的样本数。</p>
</li>
</ul>
<p>整体轮廓系数（SC）<br>所有样本的轮廓系数的算术平均值：</p>
<script type="math/tex; mode=display">
SC = \frac{1}{n} \sum_{i=1}^n s(i)</script><p>$n$ 是样本总数。</p>
<h2 id="代码演示"><a href="#代码演示" class="headerlink" title="代码演示"></a>代码演示</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> os </span><br><span class="line">os.environ[<span class="string">&#x27;OMP_NUM_THREADS&#x27;</span>]=<span class="string">&#x27;4&#x27;</span></span><br><span class="line"><span class="keyword">from</span> sklearn.cluster <span class="keyword">import</span> KMeans</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">from</span> sklearn.datasets <span class="keyword">import</span> make_blobs</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> calinski_harabasz_score</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> silhouette_score  <span class="comment"># 核心导入（必须加，否则代码报错）</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">SC</span>():</span><br><span class="line">    sc_list = []   </span><br><span class="line"></span><br><span class="line">    x, y = make_blobs(n_samples=<span class="number">1000</span>, n_features=<span class="number">2</span>, centers=[[-<span class="number">1</span>, -<span class="number">1</span>], [<span class="number">0</span>, <span class="number">0</span>], [<span class="number">1</span>, <span class="number">1</span>], [<span class="number">2</span>, <span class="number">2</span>]],cluster_std=[<span class="number">0.4</span>, <span class="number">0.2</span>, <span class="number">0.2</span>, <span class="number">0.2</span>], random_state=<span class="number">22</span>)</span><br><span class="line">    <span class="keyword">for</span> clu_num <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">2</span>, <span class="number">100</span>):</span><br><span class="line">        my_kmeans = KMeans(n_clusters=clu_num, max_iter=<span class="number">100</span>, random_state=<span class="number">0</span>)</span><br><span class="line">        my_kmeans.fit(x)</span><br><span class="line">        y_pred=my_kmeans.predict(x)</span><br><span class="line">        sc_value=silhouette_score(x,y_pred)</span><br><span class="line">        sc_list.append(sc_value) </span><br><span class="line">    </span><br><span class="line">    plt.figure(figsize=(<span class="number">18</span>, <span class="number">8</span>), dpi=<span class="number">100</span>)</span><br><span class="line">    plt.xticks(<span class="built_in">range</span>(<span class="number">0</span>, <span class="number">100</span>, <span class="number">3</span>), labels=<span class="built_in">range</span>(<span class="number">0</span>, <span class="number">100</span>, <span class="number">3</span>))</span><br><span class="line">    plt.grid()</span><br><span class="line">    plt.title(<span class="string">&#x27;sc&#x27;</span>)</span><br><span class="line">    plt.plot(<span class="built_in">range</span>(<span class="number">2</span>, <span class="number">100</span>), sc_list, <span class="string">&#x27;or-&#x27;</span>)</span><br><span class="line">    plt.show()</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    SC()</span><br></pre></td></tr></table></figure>
<h1 id="CH指标"><a href="#CH指标" class="headerlink" title="CH指标"></a>CH指标</h1><p>CH指标（卡林斯基-哈拉巴斯指数，Calinski-Harabasz Index）是<strong>全局层面</strong>的聚类评估指标，基于「簇间方差」和「簇内方差」的比值，取值越大表示聚类效果越好（簇内越紧凑、簇间越分离）。</p>
<h2 id="公式"><a href="#公式" class="headerlink" title="公式"></a>公式</h2><script type="math/tex; mode=display">
CH = \frac{\frac{B_k}{k - 1}}{\frac{W_k}{n - k}}</script><p>其中：</p>
<ul>
<li>$k$：聚类的簇数；</li>
<li>$n$：样本总数；</li>
<li>$B_k$：<strong>簇间离均差平方和</strong>（Between-cluster Sum of Squares）；</li>
<li>$W_k$：<strong>簇内离均差平方和</strong>（Within-cluster Sum of Squares）。</li>
</ul>
<h2 id="关键项的计算"><a href="#关键项的计算" class="headerlink" title="关键项的计算"></a>关键项的计算</h2><h5 id="1-簇内离均差平方和-W-k"><a href="#1-簇内离均差平方和-W-k" class="headerlink" title="1. 簇内离均差平方和 $W_k$"></a>1. 簇内离均差平方和 $W_k$</h5><p>所有样本到其所属簇中心的欧氏距离平方和：</p>
<script type="math/tex; mode=display">
W_k = \sum_{m=1}^k \sum_{i \in C_m} \| x_i - \bar{x}_m \|^2</script><ul>
<li>$\bar{x}_m$：第 $m$ 个簇的中心（该簇所有样本的均值向量）；</li>
<li>$| x_i - \bar{x}_m |^2$：样本 $i$ 到其簇中心的欧氏距离平方。</li>
</ul>
<h5 id="2-簇间离均差平方和-B-k"><a href="#2-簇间离均差平方和-B-k" class="headerlink" title="2. 簇间离均差平方和 $B_k$"></a>2. 簇间离均差平方和 $B_k$</h5><p>各簇中心到<strong>全局样本中心</strong>的加权平方和（权重为簇内样本数）：</p>
<script type="math/tex; mode=display">
B_k = \sum_{m=1}^k n_m \cdot \| \bar{x}_m - \bar{x} \|^2</script><ul>
<li>$\bar{x}$：所有样本的全局中心（整体均值向量）；</li>
<li>$n_m$：第 $m$ 个簇的样本数；</li>
<li>$| \bar{x}_m - \bar{x} |^2$：第 $m$ 个簇中心到全局中心的欧氏距离平方。</li>
</ul>
<p>CH代码演示与SC类似，不再赘述。</p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><div class="table-container">
<table>
<thead>
<tr>
<th>指标</th>
<th>计算粒度</th>
<th>取值范围</th>
<th>优化目标</th>
</tr>
</thead>
<tbody>
<tr>
<td>SC（轮廓系数）</td>
<td>先样本→后全局均值</td>
<td>[-1, 1]</td>
<td>趋近1</td>
</tr>
<tr>
<td>CH（卡林斯基-哈拉巴斯）</td>
<td>全局层面</td>
<td>[0, +∞)</td>
<td>越大越好</td>
</tr>
</tbody>
</table>
</div>
<p>注：CH指标对「球形簇」的聚类效果评估更有效，若聚类形状非凸（如环形），CH指标可能失效；SC指标适用性更广，但计算成本高于CH（需计算样本间两两距离）。</p>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>机器学习</tag>
        <tag>聚类算法</tag>
        <tag>k-means</tag>
        <tag>SSE系数</tag>
        <tag>CH轮廓法</tag>
        <tag>SC轮廓法</tag>
        <tag>无监督学习</tag>
      </tags>
  </entry>
  <entry>
    <title>模糊综合评价</title>
    <url>/2025/12/29/%E6%A8%A1%E7%B3%8A%E7%BB%BC%E5%90%88%E8%AF%84%E4%BB%B7/</url>
    <content><![CDATA[<p>常用的主观评价类模型<br><span id="more"></span></p>
<h1 id="“模糊”概念"><a href="#“模糊”概念" class="headerlink" title="“模糊”概念"></a>“模糊”概念</h1><p>模糊综合评价的核心思想就是：用模糊数学的方法，把这些模糊的定性描述，转化为可计算的定量结果。关键是两个核心概念：</p>
<ol>
<li>模糊集：比如“满意”就是一个模糊集，不是非黑即白的“满意”或“不满意”，而是有程度差异。</li>
<li>隶属度：衡量某个元素属于某个模糊集的程度，用 $ \mu_A(x) $ 表示（A是模糊集，x是评价对象），取值范围是 $ 0 \leq \mu_A(x) \leq 1 $。比如用户对产品外观的“满意隶属度”是0.8，就说明接近“满意”但还没到满分；0.3则说明偏向“不满意”。</li>
</ol>
<h1 id="核心步骤"><a href="#核心步骤" class="headerlink" title="核心步骤"></a>核心步骤</h1><p>以“评价新产品用户满意度”为例：</p>
<h2 id="步骤1：确定评价对象、因素集和评语集"><a href="#步骤1：确定评价对象、因素集和评语集" class="headerlink" title="步骤1：确定评价对象、因素集和评语集"></a>步骤1：确定评价对象、因素集和评语集</h2><ul>
<li><p>评价对象：某新款蓝牙耳机</p>
</li>
<li><p>因素集U（要评价的指标）：比如 U = {外观设计, 性价比, 使用手感, 续航能力}（用 $ U = {u_1, u_2, …, u_n} $ 表示，n是指标个数）</p>
</li>
<li><p>评语集V（评价结果等级）：比如 V = {很满意, 满意, 一般, 不满意}（用 $ V = {v_1, v_2, …, v_m} $ 表示，m是等级个数）</p>
</li>
</ul>
<h2 id="步骤2：确定各评价因素的权重"><a href="#步骤2：确定各评价因素的权重" class="headerlink" title="步骤2：确定各评价因素的权重"></a>步骤2：确定各评价因素的权重</h2><p>层次分析法，熵权法等。</p>
<h2 id="步骤3：构建模糊评价矩阵R"><a href="#步骤3：构建模糊评价矩阵R" class="headerlink" title="步骤3：构建模糊评价矩阵R"></a>步骤3：构建模糊评价矩阵R</h2><p>这一步是把“每个指标对应不同评语的隶属度”整理成矩阵。比如邀请50个用户打分，对“外观设计”：25人很满意、20人满意、5人一般、0人不满意，那么外观的隶属度就是 [25/50, 20/50, 5/50, 0/50] = [0.5, 0.4, 0.1, 0.0]。</p>
<p>把所有指标的隶属度整理成矩阵R（n行m列，n是指标数，m是评语数）：</p>
<p>$ R = \begin{bmatrix} 0.5 &amp; 0.4 &amp; 0.1 &amp; 0.0 \ 0.3 &amp; 0.5 &amp; 0.2 &amp; 0.0 \ 0.4 &amp; 0.3 &amp; 0.2 &amp; 0.1 \ 0.2 &amp; 0.4 &amp; 0.3 &amp; 0.1 \end{bmatrix} $</p>
<p>解释：第一行是外观的隶属度，第二行是性价比，第三行是手感，第四行是续航。</p>
<h2 id="步骤4：模糊合成运算（核心计算）"><a href="#步骤4：模糊合成运算（核心计算）" class="headerlink" title="步骤4：模糊合成运算（核心计算）"></a>步骤4：模糊合成运算（核心计算）</h2><p>用权重集W和评价矩阵R做合成运算，得到综合评价结果B，公式是 $ B = W \circ R $（“∘”是模糊合成算子，常用“加权平均法”，适合需要量化结果的场景，也有主因素决定型算子，如取大取小等，有些会运用于需要一票否决的地方）。</p>
<p>计算过程（以加权平均为例）：每个评语的得分 = 权重1×对应隶属度1 + 权重2×对应隶属度2 + … + 权重n×对应隶属度n</p>
<p>代入数值计算：</p>
<ul>
<li><p>很满意得分：0.2×0.5 + 0.3×0.3 + 0.2×0.4 + 0.3×0.2 = 0.1 + 0.09 + 0.08 + 0.06 = 0.33</p>
</li>
<li><p>满意得分：0.2×0.4 + 0.3×0.5 + 0.2×0.3 + 0.3×0.4 = 0.08 + 0.15 + 0.06 + 0.12 = 0.41</p>
</li>
<li><p>一般得分：0.2×0.1 + 0.3×0.2 + 0.2×0.2 + 0.3×0.3 = 0.02 + 0.06 + 0.04 + 0.09 = 0.21</p>
</li>
<li><p>不满意得分：0.2×0.0 + 0.3×0.0 + 0.2×0.1 + 0.3×0.1 = 0 + 0 + 0.02 + 0.03 = 0.05</p>
</li>
</ul>
<p>所以综合评价结果B = [0.33, 0.41, 0.21, 0.05]</p>
<h2 id="步骤5：得出评价结论"><a href="#步骤5：得出评价结论" class="headerlink" title="步骤5：得出评价结论"></a>步骤5：得出评价结论</h2><p>找B中最大值对应的评语！这里“满意”的得分0.41最高，所以结论是：这款蓝牙耳机的用户满意度为“满意”。</p>
<h1 id="矩阵运算"><a href="#矩阵运算" class="headerlink" title="矩阵运算"></a>矩阵运算</h1><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"></span><br><span class="line">W = np.array([<span class="number">0.5</span>, <span class="number">0.3</span>, <span class="number">0.2</span>])  <span class="comment"># 权重集（已归一化）</span></span><br><span class="line">R = np.array([</span><br><span class="line">    [<span class="number">0.6</span>, <span class="number">0.3</span>, <span class="number">0.1</span>, <span class="number">0</span>],</span><br><span class="line">    [<span class="number">0.4</span>, <span class="number">0.4</span>, <span class="number">0.2</span>, <span class="number">0</span>],</span><br><span class="line">    [<span class="number">0.3</span>, <span class="number">0.5</span>, <span class="number">0.2</span>, <span class="number">0</span>]</span><br><span class="line">])  <span class="comment"># 3指标×4评语的隶属度矩阵</span></span><br><span class="line">V = np.array([<span class="number">90</span>, <span class="number">80</span>, <span class="number">70</span>, <span class="number">60</span>])  <span class="comment"># 评语量化分值</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 模糊合成运算</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">fuzzy_synthesis</span>(<span class="params">W, R</span>):</span><br><span class="line">    B = np.dot(W, R) </span><br><span class="line">    B = B / np.<span class="built_in">sum</span>(B) <span class="keyword">if</span> np.<span class="built_in">sum</span>(B) != <span class="number">0</span> <span class="keyword">else</span> B</span><br><span class="line">    <span class="keyword">return</span> B</span><br><span class="line"></span><br><span class="line"><span class="comment"># 综合得分</span></span><br><span class="line">B = fuzzy_synthesis(W, R)</span><br><span class="line">score = np.dot(B, V.T)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;综合评价向量 B:&quot;</span>, B)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;综合得分:&quot;</span>, <span class="built_in">round</span>(score, <span class="number">2</span>))</span><br></pre></td></tr></table></figure>
<h1 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h1><ol>
<li><p>优势：比传统打分更贴合实际场景，结果也更客观，比如不会因为某个指标满分就掩盖其他指标的不足。</p>
</li>
<li><p>注意点：权重的确定很关键，如果权重设置不合理，结果会偏差很大。</p>
</li>
</ol>
]]></content>
      <categories>
        <category>数学建模</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>评价类模型</tag>
        <tag>模糊综合评价</tag>
      </tags>
  </entry>
  <entry>
    <title>微分方程模型应用</title>
    <url>/2025/12/08/%E5%BE%AE%E5%88%86%E6%96%B9%E7%A8%8B%E6%A8%A1%E5%9E%8B%E5%BA%94%E7%94%A8/</url>
    <content><![CDATA[<p>一些简单的微分方程机理模型 ( ´•̥×•̥` )<br><span id="more"></span><br>    前置知识：微分方程</p>
<h1 id="求解微分方程"><a href="#求解微分方程" class="headerlink" title="求解微分方程"></a>求解微分方程</h1><p>这里主要使用python求解  </p>
<h2 id="1-odeint函数"><a href="#1-odeint函数" class="headerlink" title="1.odeint函数"></a>1.odeint函数</h2><p>odeint函数是scipy中的一个简单函数，用于快速求解简单微分方程<br>函数接口：</p>
<ul>
<li>func：微分方程函数或方程组，它必须是一个函数，该函数有至少两个入参，第一个参数为一个数组，表示当前因变量值。第二个参数是一个标量，表示当前自变量值（通常是时间）</li>
<li>y0：一个数组，表示初始条件</li>
<li>t：一个数组，表示积分的时间点，第一个元素为积分起点  </li>
</ul>
<h2 id="2-solve-ivp函数"><a href="#2-solve-ivp函数" class="headerlink" title="2.solve_ivp函数"></a>2.solve_ivp函数</h2><p>这是一个更强大的ODE<br>函数接口：</p>
<ul>
<li>func：同上</li>
<li>y0：同上</li>
<li>t_span：二元组，起始时间和结束时间</li>
<li>method（可选）：表示积分方法</li>
</ul>
<h1 id="马尔萨斯人口模型"><a href="#马尔萨斯人口模型" class="headerlink" title="马尔萨斯人口模型"></a>马尔萨斯人口模型</h1><h2 id="原始模型假设："><a href="#原始模型假设：" class="headerlink" title="原始模型假设："></a>原始模型假设：</h2><ul>
<li>人口数设为 $x(t)$ 对时间连续可微</li>
<li>人口变化仅由于出生和死亡，每个个体生育能力相等</li>
<li>人口增长率r为<strong>常数</strong>（r=出生率-死亡率）  </li>
</ul>
<p>可得：</p>
<script type="math/tex; mode=display">
\begin{cases}
\frac{dx}{dt}=rx  \\
x(0)=x_0 
\end{cases}</script><p>解得</p>
<script type="math/tex; mode=display">x(t)=x_0e^{rt}</script><h2 id="求解过程："><a href="#求解过程：" class="headerlink" title="求解过程："></a>求解过程：</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> scipy.integrate <span class="keyword">import</span> odeint</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">model</span>(<span class="params">x,t,r</span>):</span><br><span class="line">    dxdt=r*x</span><br><span class="line">    <span class="keyword">return</span> dxdt</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    x0=<span class="number">100</span></span><br><span class="line">    t=np.linspace(<span class="number">0</span>,<span class="number">1000</span>,<span class="number">1000</span>)</span><br><span class="line">    r=<span class="number">0.001</span></span><br><span class="line">    X=odeint(model,x0,t,args=(r,))</span><br><span class="line">    plt.plot(t,X)</span><br><span class="line">    plt.show()</span><br></pre></td></tr></table></figure>
<h2 id="模型评价"><a href="#模型评价" class="headerlink" title="模型评价"></a>模型评价</h2><p>从求解结果可以看出来，这是指数增长。自然界很多像这样的例子，比如最后一天长满一半水池的荷花，生长的竹子等。18-20世纪的数据确实也符合这个规律，这就是最理想情况下生物的生长，高中生物学中称之为“J型曲线”<br>但是，20世纪以后的人口数据就和该预测结果相差愈来愈大。由指数增长特点，地球人口会爆炸的增长，这不符合常理，因为地球的资源是有限的，由高中生物学，环境有最大环境容纳量，因此需要对模型进行修正。  </p>
<h1 id="阻滞增长模型"><a href="#阻滞增长模型" class="headerlink" title="阻滞增长模型"></a>阻滞增长模型</h1><h2 id="模型优化"><a href="#模型优化" class="headerlink" title="模型优化"></a>模型优化</h2><p>需要对人口增长速度进行限制，由此我们可以得到一些很自然的假设：</p>
<ol>
<li>人口增长率不是一个常数，而是一个随 x 变化的减函数 $r(x)$ </li>
<li>$r(x)=r-sx$ (优先采用线性假设) </li>
<li>环境存在最大容量 $x_m$ ,即当 $x=x_m$ 时，x增长率为0<br>由2，3可得<script type="math/tex; mode=display">r(x)=r(1-\frac{x}{x_m})</script>则前面微分方程可优化为：<script type="math/tex; mode=display">
\begin{cases}
\frac{dx}{dt}=r(1-\frac{x}{x_m})x  \\
x(0)=x_0 
\end{cases}</script>可以解得：<script type="math/tex; mode=display">x(t)=\frac{x_m}{1+(\frac{x_m}{x_0}-1)e^{-r(t-t_0)}}</script><h2 id="模型解释"><a href="#模型解释" class="headerlink" title="模型解释"></a>模型解释</h2>对上面的结果求二阶导，由凹凸函数的知识可以知道：  </li>
</ol>
<ul>
<li>首先当 $0&lt;x&lt;x_m$ 时，函数单调递增。</li>
<li>当 $0&lt;x&lt; \frac{x_m}{2}$ 时，为凹函数</li>
<li>当 $\frac{x_m}{2}&lt;x&lt;x_m$ 时，为凸函数  </li>
</ul>
<p>即在 $x= \frac{x_m}{2}$ 时，函数增长最快。<br>从高中生物学也可以知道，这就是“S型增长”，即有环境容纳量上限的增长。  </p>
<h2 id="部分核心代码"><a href="#部分核心代码" class="headerlink" title="部分核心代码"></a>部分核心代码</h2><p>结合前面的原始代码，下面几个地方进行修正即可<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">model</span>(<span class="params">x,t,r,K</span>):</span><br><span class="line">    dxdt=x*r*(<span class="number">1</span>-x/K)</span><br><span class="line">    <span class="keyword">return</span> dxdt</span><br><span class="line"></span><br><span class="line">X=odeint(model,x0,t,args=(r,K))</span><br></pre></td></tr></table></figure></p>
<h1 id="沃尔泰拉模型"><a href="#沃尔泰拉模型" class="headerlink" title="沃尔泰拉模型"></a>沃尔泰拉模型</h1><h2 id="模型引入"><a href="#模型引入" class="headerlink" title="模型引入"></a>模型引入</h2><p>沃尔泰拉模型（Volterra模型），即<strong>捕食者-猎物模型</strong>，是数学家沃尔泰拉在研究一战时期地中海鲨鱼比例异常变化是提出的模型。于此相关的就是经典的地中海鲨鱼问题：<br>意大利生物学家Ancona曾致力于鱼类种群相互制约关系的研究，他从第一次世界大战期间，地中海各港口捕获的几种鱼类捕获量百分比的资料中，发现鲨鱼等的比例有明显增加，而供其捕食的食用鱼的百分比却明显下降。显然战争使捕鱼量下降，食用鱼增加，鲨鱼等也随之增加，但为何鲨鱼的比例大幅增加呢？<br>他无法解释这个现象，于是求助于著名的意大利数学家V.Volterra，希望建立一个<strong>食饵—捕食</strong>系统的数学模型，定量地回答这个问题。</p>
<h2 id="模型构建"><a href="#模型构建" class="headerlink" title="模型构建"></a>模型构建</h2><p>设t时刻猎物数量为 $x_1(t)$ ,捕食者数量为 $x_2(t)$  </p>
<p>借用前面的模型，仅考虑被捕食者自由生长，有：</p>
<script type="math/tex; mode=display">\frac{dx_1}{dt}=r_1x_1</script><p>由于捕食者出现，食饵数量下降，且捕食者数量越多，消耗食饵数量越大，设：</p>
<script type="math/tex; mode=display">\frac{dx_1}{dt}=x_1(r_1- \lambda_ 1 x_2)</script><p>其中 $\lambda_ 1&gt;0$ 表示捕食者捕食食饵的能力。  </p>
<p>对捕食者来说，若仅有捕食者存在，没有食饵，捕食者数量会不断减少至0，即：</p>
<script type="math/tex; mode=display">\frac{dx_2}{dt}=-r_2x_2</script><p>由于食饵出现，对捕食者有供养作用，且食饵数量越多，对捕食者的供养能力越大，设：</p>
<script type="math/tex; mode=display">\frac{dx_2}{dt}=x_2(-r_2+ \lambda_ 2 x_1)</script><p>其中 $\lambda_ 2&gt;0$ 表示食饵对捕食者的供养能力。  </p>
<p>由上面就可以得到最简单的方程组。回到问题，我们考虑的是一战期间鲨鱼（捕食者）比例异常升高。一战期间，人类捕杀作用减少，因此该期间二者比例会改变，所以我们需要在前面的方程加上人类因素：  </p>
<script type="math/tex; mode=display">
\begin{cases}
\frac{dx_1}{dt}=x_1((r_1-e)- \lambda_ 1 x_2)  \\
\frac{dx_2}{dt}=x_2(-(r_2+e)+ \lambda_ 2 x_1)
\end{cases}</script><p>式中，e表示人类捕杀的影响。在人类影响下，食饵自然增长率降低e，捕食者死亡率增加e（这里也可以给二者赋权，表示人类捕杀的偏好）<br>用代码绘图可以清楚说明当e下降，$x_2$比例上升。<br>绘制 $x_1$ 与 $x_2$ 图像，发现是一个圈，也符合高中生物的捕食者——猎物模型。</p>
<h2 id="求解代码"><a href="#求解代码" class="headerlink" title="求解代码"></a>求解代码</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> scipy.integrate <span class="keyword">import</span> odeint</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">model</span>(<span class="params">P,t,r1,r2,la1,la2,e</span>):</span><br><span class="line">    x1,x2=P</span><br><span class="line">    dx1dt=x1*((r1-e)-la1*x2)</span><br><span class="line">    dx2dt=x2*(-(r2+e)+la2*x1)</span><br><span class="line">    <span class="keyword">return</span> [dx1dt,dx2dt]</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    x1=<span class="number">100</span></span><br><span class="line">    x2=<span class="number">20</span></span><br><span class="line">    P=[x1,x2]</span><br><span class="line">    t=np.linspace(<span class="number">0</span>,<span class="number">100</span>,<span class="number">1000</span>)</span><br><span class="line">    r1=<span class="number">1.0</span></span><br><span class="line">    r2=<span class="number">1.0</span></span><br><span class="line">    la1=<span class="number">0.1</span></span><br><span class="line">    la2=<span class="number">0.1</span></span><br><span class="line">    e=<span class="number">0.05</span></span><br><span class="line">    X=odeint(model,P,t,args=(r1,r2,la1,la2,e))</span><br><span class="line">    X1=X[:,<span class="number">0</span>]</span><br><span class="line">    X2=X[:,<span class="number">1</span>]</span><br><span class="line">    plt.plot(X1,X2)</span><br><span class="line">    plt.show()</span><br></pre></td></tr></table></figure>
<h1 id="种间竞争模型"><a href="#种间竞争模型" class="headerlink" title="种间竞争模型"></a>种间竞争模型</h1><p>考虑在有限资源中，单个物种有：  </p>
<script type="math/tex; mode=display">\frac{dx_1}{dt}=r_1x_1(1-\frac{x_1}{N_1})</script><p>N表示环境最大容量。<br>加上种群2的影响，有：</p>
<script type="math/tex; mode=display">\frac{dx_1}{dt}=r_1x_1(1-\frac{x_1}{N_1}-\sigma_ 1\frac{x_2}{N_2})</script><p>种群2数量越多，即 $\frac{x<em>2}{N_2}$ 越大，种群1增长数量越慢。<br>式中加入影响因子 $\sigma</em> 1$ 表示单位数量的2种群消耗1种群的食物的速度的单位数量1种群的倍数。<br>比如，$\sigma_ 1&lt;1$ 表示在供养1种群的食物的竞争中，种群2弱于1。<br>同理可得：</p>
<script type="math/tex; mode=display">\frac{dx_2}{dt}=r_2x_2(1-\frac{x_2}{N_2}-\sigma_ 2\frac{x_1}{N_1})</script><p>若种群1和种群2选择食物的偏好完全相同，则上面的 $\sigma<em> 1$ 和 $\sigma</em> 2$ 有： $\sigma<em> 1 \sigma</em> 2=1$<br>通常情况下，$\sigma<em> 1 \sigma</em> 2 \neq 1$ 需要通过实际情况确定。<br>其中求解方程即绘图过程于上面类似，不在赘述。  </p>
<h1 id="传染病模型（SIR-SIS）"><a href="#传染病模型（SIR-SIS）" class="headerlink" title="传染病模型（SIR/SIS）"></a>传染病模型（SIR/SIS）</h1><p>传染病模型通过微分方程刻画人群中“易感-感染-康复”的动态传播过程，核心是量化不同人群的数量变化关系。</p>
<h2 id="1-SIR模型（易感-感染-康复）"><a href="#1-SIR模型（易感-感染-康复）" class="headerlink" title="1. SIR模型（易感-感染-康复）"></a>1. SIR模型（易感-感染-康复）</h2><h3 id="模型假设"><a href="#模型假设" class="headerlink" title="模型假设"></a>模型假设</h3><ul>
<li>总人口固定为 $N$，分为三类人群：<ul>
<li>$S(t)$ ：易感者（未感染但可被传染的人群）；</li>
<li>$I(t)$ ：感染者（已感染且具传染性的人群）；</li>
<li>$R(t)$ ：康复者（已康复且获持久免疫力的人群）；<br>满足 $N = S(t) + I(t) + R(t)$ 。</li>
</ul>
</li>
<li>易感者与感染者接触后，以速率 $\beta$ （单位时间内单个感染者接触的易感者数）被感染；</li>
<li>感染者以速率 $\gamma$ （单位时间内康复的感染者比例）康复；</li>
<li>无出生、死亡、人口迁移，康复者不再参与传播。</li>
</ul>
<h3 id="模型构建-1"><a href="#模型构建-1" class="headerlink" title="模型构建"></a>模型构建</h3><p>微分方程组为：</p>
<script type="math/tex; mode=display">
\begin{cases}
\frac{dS}{dt} = -\frac{\beta S I}{N} \\
\frac{dI}{dt} = \frac{\beta S I}{N} - \gamma I \\
\frac{dR}{dt} = \gamma I
\end{cases}</script><ul>
<li>方程解释：<ul>
<li>$\frac{dS}{dt}$ ：易感者数量因感染减少，速率与易感者、感染者的接触频率成正比；</li>
<li>$\frac{dI}{dt}$ ：感染者数量变化 = 新感染人数 - 康复人数；</li>
<li>$\frac{dR}{dt}$ ：康复者数量随感染者康复增加。</li>
</ul>
</li>
</ul>
<h3 id="核心参数：基本再生数-R-0"><a href="#核心参数：基本再生数-R-0" class="headerlink" title="核心参数：基本再生数 $R_0$"></a>核心参数：基本再生数 $R_0$</h3><p>定义 $R_0 = \frac{\beta}{\gamma}$ （单个感染者平均传染的人数）：</p>
<ul>
<li>$ R_0 &gt; 1$ ：传染病会扩散流行；</li>
<li>$ R_0 &lt; 1$ ：传染病会逐渐消亡。</li>
</ul>
<h2 id="2-SIS模型（易感-感染-易感）"><a href="#2-SIS模型（易感-感染-易感）" class="headerlink" title="2. SIS模型（易感-感染-易感）"></a>2. SIS模型（易感-感染-易感）</h2><h3 id="模型假设-1"><a href="#模型假设-1" class="headerlink" title="模型假设"></a>模型假设</h3><p>适用于康复后无免疫力、会再次成为易感者的传染病（如普通流感），人群仅分 $ S(t) $ （易感者）和 $ I(t) $ （感染者），满足 $ N = S(t) + I(t) $ ，其余假设同SIR模型。</p>
<h3 id="模型构建-2"><a href="#模型构建-2" class="headerlink" title="模型构建"></a>模型构建</h3><p>微分方程组为：</p>
<script type="math/tex; mode=display">
\begin{cases}
\frac{dS}{dt} = -\frac{\beta S I}{N} + \gamma I \\
\frac{dI}{dt} = \frac{\beta S I}{N} - \gamma I
\end{cases}</script><ul>
<li>方程解释：康复者重新回到易感者群体（ $ \frac{dS}{dt} $ 增加 $ \gamma I $ 项），传染病可能呈现持续循环状态。</li>
</ul>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>本文围绕<strong>微分方程的机理建模应用</strong>，通过“现实假设→变量关系提炼→方程构建”的思路，介绍了多个经典模型：</p>
<ol>
<li><strong>马尔萨斯人口模型</strong>：以“增长率恒定”为假设，刻画无约束的指数增长（J型曲线），是单种群动态的基础模型，但忽略了资源限制；</li>
<li><strong>阻滞增长（Logistic）模型</strong>：引入“环境最大容纳量”，修正为S型非线性增长，更贴合资源有限的种群变化规律；</li>
<li><strong>沃尔泰拉模型</strong>：耦合捕食者-猎物的互动关系，刻画了种群数量的周期性振荡，解释了生态系统的此消彼长；</li>
<li><strong>种间竞争模型</strong>：扩展Logistic模型，加入种群间资源竞争项，量化了不同种群的资源消耗效率差异；</li>
<li><strong>传染病模型（SIR/SIS）</strong>：划分人群类型，刻画“感染-康复”传播过程，核心参数 $ R_0 $ 可指导疫情防控。</li>
</ol>
<p>这些模型的共性是从“机理假设”出发，用微分方程描述变量的瞬时变化率，通过求解方程得到动态趋势；模型的优化过程，本质是逐步加入现实约束（资源限制、种间互动、免疫特性等），让预测更贴合实际。</p>
<p>微分方程建模的价值在于：不仅能拟合现有数据，更能通过参数调整预测系统长期趋势，为生态调控、人口规划、疫情防控等问题提供量化依据。</p>
]]></content>
      <categories>
        <category>数学建模</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>微分方程</tag>
        <tag>机理分析</tag>
      </tags>
  </entry>
  <entry>
    <title>其他回归类</title>
    <url>/2025/12/13/%E5%85%B6%E4%BB%96%E5%9B%9E%E5%BD%92%E7%B1%BB/</url>
    <content><![CDATA[<p>一些其他回归类<br><span id="more"></span><br>    前置知识：一元线性回归</p>
<h1 id="多元线性回归"><a href="#多元线性回归" class="headerlink" title="多元线性回归"></a>多元线性回归</h1><p>多元线性回归是<strong>科学计算、数学建模</strong>中最基础且常用的预测模型，核心是通过多个自变量（特征）的线性组合预测因变量（目标值）。以下从「理论基础→编程实现→建模技巧」三个维度展开，结合 Python/C++ 代码（可直接运行），适配数学建模、数据分析场景。</p>
<h2 id="一、核心理论（简洁版，够用建模）"><a href="#一、核心理论（简洁版，够用建模）" class="headerlink" title="一、核心理论（简洁版，够用建模）"></a>一、核心理论（简洁版，够用建模）</h2><h3 id="1-模型定义"><a href="#1-模型定义" class="headerlink" title="1. 模型定义"></a>1. 模型定义</h3><p>假设存在 $k$ 个自变量 $x_1, x_2, …, x_k$ 和 1 个因变量 $y$，多元线性回归模型为：</p>
<script type="math/tex; mode=display">y = \beta_0 + \beta_1 x_1 + \beta_2 x_2 + ... + \beta_k x_k + \epsilon</script><ul>
<li>$\beta_0$：截距项（常数项）</li>
<li>$\beta_1, …, \beta_k$：回归系数（表示自变量对因变量的影响程度）</li>
<li>$\epsilon$：随机误差项（满足正态分布 $\epsilon \sim N(0, \sigma^2)$）</li>
</ul>
<p>用矩阵形式表示（更便于编程计算）：</p>
<script type="math/tex; mode=display">Y = X\beta + \epsilon</script><ul>
<li>$Y \in \mathbb{R}^{n \times 1}$：因变量向量（$n$ 为样本数）</li>
<li>$X \in \mathbb{R}^{n \times (k+1)}$：设计矩阵（第一列为全 1，对应截距项 $\beta_0$）</li>
<li>$\beta \in \mathbb{R}^{(k+1) \times 1}$：系数向量（$[\beta_0, \beta_1, …, \beta_k]^T$）</li>
<li>$\epsilon \in \mathbb{R}^{n \times 1}$：误差向量</li>
</ul>
<h3 id="2-参数估计（最小二乘法）"><a href="#2-参数估计（最小二乘法）" class="headerlink" title="2. 参数估计（最小二乘法）"></a>2. 参数估计（最小二乘法）</h3><p>通过最小化误差平方和 $\sum_{i=1}^n \epsilon_i^2 = | Y - X\beta |^2$，求得系数 $\beta$ 的最优解：</p>
<script type="math/tex; mode=display">\hat{\beta} = (X^T X)^{-1} X^T Y</script><ul>
<li>要求 $X^T X$ 可逆（即自变量无完全多重共线性）</li>
</ul>
<h3 id="3-模型评估指标"><a href="#3-模型评估指标" class="headerlink" title="3. 模型评估指标"></a>3. 模型评估指标</h3><ul>
<li><strong>决定系数 $R^2$</strong>：衡量模型拟合优度，取值范围 $[0,1]$，越接近 1 拟合越好。<script type="math/tex; mode=display">R^2 = 1 - \frac{\sum (y_i - \hat{y}_i)^2}{\sum (y_i - \bar{y})^2}</script></li>
<li><strong>调整 $R^2$</strong>：修正多重共线性导致的 $R^2$ 虚高（自变量增多时优先参考）。</li>
<li><strong>均方误差（MSE）</strong>：衡量预测误差大小，越小越好：$MSE = \frac{1}{n} \sum (y_i - \hat{y}_i)^2$</li>
</ul>
<h2 id="二、Python-实现（高效建模，适配-sklearn）"><a href="#二、Python-实现（高效建模，适配-sklearn）" class="headerlink" title="二、Python 实现（高效建模，适配 sklearn）"></a>二、Python 实现（高效建模，适配 sklearn）</h2><p>Python 中使用 <code>sklearn.linear_model.LinearRegression</code> 可快速实现，支持批量数据处理、特征缩放（可选）、模型评估，适合数学建模中的快速迭代。</p>
<h3 id="完整可运行代码（含数据生成、训练、预测、评估）"><a href="#完整可运行代码（含数据生成、训练、预测、评估）" class="headerlink" title="完整可运行代码（含数据生成、训练、预测、评估）"></a>完整可运行代码（含数据生成、训练、预测、评估）</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">from</span> sklearn.linear_model <span class="keyword">import</span> LinearRegression</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> r2_score, mean_squared_error</span><br><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> train_test_split</span><br><span class="line"></span><br><span class="line"><span class="comment"># 1. 生成模拟数据（模拟 3 个自变量的回归问题）</span></span><br><span class="line">np.random.seed(<span class="number">42</span>)  <span class="comment"># 固定随机种子，结果可复现</span></span><br><span class="line">n = <span class="number">1000</span>  <span class="comment"># 样本数</span></span><br><span class="line">k = <span class="number">3</span>     <span class="comment"># 自变量个数</span></span><br><span class="line">X = np.random.randn(n, k)  <span class="comment"># 设计矩阵（n行k列，正态分布）</span></span><br><span class="line">true_beta = np.array([<span class="number">2.5</span>, -<span class="number">1.8</span>, <span class="number">3.2</span>])  <span class="comment"># 真实系数</span></span><br><span class="line">true_intercept = <span class="number">1.0</span>  <span class="comment"># 真实截距</span></span><br><span class="line">y = true_intercept + X @ true_beta + np.random.randn(n) * <span class="number">0.5</span>  <span class="comment"># 生成因变量（含轻微噪声）</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 划分训练集/测试集（7:3 分割）</span></span><br><span class="line">X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=<span class="number">0.3</span>, random_state=<span class="number">42</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 训练多元线性回归模型</span></span><br><span class="line">model = LinearRegression()</span><br><span class="line">model.fit(X_train, y_train)  <span class="comment"># 自动学习截距和系数</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 4. 模型参数输出</span></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;=== 模型参数 ===&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;截距项 β₀: <span class="subst">&#123;model.intercept_:<span class="number">.4</span>f&#125;</span>&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;回归系数 β₁~βₖ: <span class="subst">&#123;model.coef_&#125;</span>&quot;</span>)  <span class="comment"># 对应 3 个自变量的系数</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 5. 预测与模型评估</span></span><br><span class="line">y_pred_train = model.predict(X_train)</span><br><span class="line">y_pred_test = model.predict(X_test)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;\n=== 模型评估 ===&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;训练集 R²: <span class="subst">&#123;r2_score(y_train, y_pred_train):<span class="number">.4</span>f&#125;</span>&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;测试集 R²: <span class="subst">&#123;r2_score(y_test, y_pred_test):<span class="number">.4</span>f&#125;</span>&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;训练集 MSE: <span class="subst">&#123;mean_squared_error(y_train, y_pred_train):<span class="number">.4</span>f&#125;</span>&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;测试集 MSE: <span class="subst">&#123;mean_squared_error(y_test, y_pred_test):<span class="number">.4</span>f&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 6. 实际预测（输入新数据）</span></span><br><span class="line">new_data = np.array([[<span class="number">1.2</span>, -<span class="number">0.5</span>, <span class="number">3.0</span>], [<span class="number">0.8</span>, <span class="number">2.1</span>, -<span class="number">1.5</span>]])  <span class="comment"># 2 个新样本</span></span><br><span class="line">new_pred = model.predict(new_data)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;\n=== 新样本预测结果 ===&quot;</span>)</span><br><span class="line"><span class="keyword">for</span> i, data <span class="keyword">in</span> <span class="built_in">enumerate</span>(new_data):</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;样本 <span class="subst">&#123;data&#125;</span>: 预测值 = <span class="subst">&#123;new_pred[i]:<span class="number">.4</span>f&#125;</span>&quot;</span>)</span><br></pre></td></tr></table></figure>
<h3 id="代码说明"><a href="#代码说明" class="headerlink" title="代码说明"></a>代码说明</h3><ul>
<li><strong>数据生成</strong>：用正态分布模拟自变量，加入轻微噪声模拟真实场景，确保模型可复现。</li>
<li><strong>模型训练</strong>：<code>LinearRegression</code> 自动处理截距项（无需手动添加全 1 列），支持大规模数据。</li>
<li><strong>评估指标</strong>：同时输出训练集和测试集的 $R^2$ 和 $MSE$，避免过拟合。</li>
<li><strong>预测应用</strong>：直接输入新样本即可得到预测值，适配建模中的实际需求。</li>
</ul>
<h3 id="运行结果示例"><a href="#运行结果示例" class="headerlink" title="运行结果示例"></a>运行结果示例</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">=== 模型参数 ===</span><br><span class="line">截距项 β₀: 1.0234</span><br><span class="line">回归系数 β₁~βₖ: [2.4876 -1.7932  3.1895]</span><br><span class="line">=== 模型评估 ===</span><br><span class="line">训练集 R²: 0.9876</span><br><span class="line">测试集 R²: 0.9861</span><br><span class="line">训练集 MSE: 0.2451</span><br><span class="line">测试集 MSE: 0.2532</span><br><span class="line">=== 新样本预测结果 ===</span><br><span class="line">样本 [1.2 -0.5 3.0]: 预测值 = 11.8765</span><br><span class="line">样本 [0.8 2.1 -1.5]: 预测值 = -2.3456</span><br></pre></td></tr></table></figure>
<h2 id="四、数学建模中的关键技巧（避坑指南）"><a href="#四、数学建模中的关键技巧（避坑指南）" class="headerlink" title="四、数学建模中的关键技巧（避坑指南）"></a>四、数学建模中的关键技巧（避坑指南）</h2><ol>
<li><p><strong>特征预处理</strong></p>
<ul>
<li>标准化/归一化：当自变量量纲差异大（如身高cm、体重kg），需用 <code>StandardScaler</code> 标准化（Python），避免系数受量纲影响。</li>
<li>代码示例（Python）：<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.preprocessing <span class="keyword">import</span> StandardScaler</span><br><span class="line">scaler = StandardScaler()</span><br><span class="line">X_train_scaled = scaler.fit_transform(X_train)</span><br><span class="line">X_test_scaled = scaler.transform(X_test)</span><br><span class="line">model.fit(X_train_scaled, y_train)</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p><strong>多重共线性处理</strong></p>
<ul>
<li>问题：自变量间高度相关（如“面积”和“建筑面积”），导致 $X^T X$ 接近奇异，系数不稳定。</li>
<li>解决：<ul>
<li>计算方差膨胀因子（VIF），剔除 VIF&gt;10 的自变量；</li>
<li>用岭回归（Ridge）替代普通回归，加入 L2 正则化：<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.linear_model <span class="keyword">import</span> Ridge</span><br><span class="line">ridge_model = Ridge(alpha=<span class="number">1.0</span>)  <span class="comment"># alpha为正则化强度</span></span><br><span class="line">ridge_model.fit(X_train, y_train)</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
</ul>
</li>
<li><p><strong>过拟合与模型选择</strong></p>
<ul>
<li>避免过拟合：用交叉验证（CV）选择最优参数，如 Ridge 的 alpha：<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> cross_val_score</span><br><span class="line">alphas = [<span class="number">0.1</span>, <span class="number">1</span>, <span class="number">10</span>, <span class="number">100</span>]</span><br><span class="line"><span class="keyword">for</span> alpha <span class="keyword">in</span> alphas:</span><br><span class="line">    ridge = Ridge(alpha=alpha)</span><br><span class="line">    cv_scores = cross_val_score(ridge, X_train, y_train, cv=<span class="number">5</span>, scoring=<span class="string">&#x27;r2&#x27;</span>)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;alpha=<span class="subst">&#123;alpha&#125;</span>, 平均CV R²: <span class="subst">&#123;cv_scores.mean():<span class="number">.4</span>f&#125;</span>&quot;</span>)</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p><strong>异常值处理</strong></p>
<ul>
<li>用箱线图或 Z-score（|Z|&gt;3）识别异常值，剔除或替换为均值/中位数，避免异常值主导回归结果。</li>
</ul>
</li>
</ol>
<h2 id="五、总结"><a href="#五、总结" class="headerlink" title="五、总结"></a>五、总结</h2><ul>
<li><strong>Python</strong>：适合快速建模、数据分析、数学建模比赛，用 sklearn 高效实现，支持预处理、评估、正则化等功能。</li>
<li><strong>C++</strong>：适合底层开发、算法竞赛、嵌入式场景，用 Eigen 手动实现最小二乘法，理解回归本质。</li>
<li><strong>建模核心</strong>：预处理→避坑（多重共线性、异常值）→正则化→交叉验证，确保模型稳健性和泛化能力。</li>
</ul>
<p>如果需要针对具体建模场景（如预测房价、销量、科研数据拟合）调整代码，或需要推导更深入的理论（如置信区间、假设检验），可以随时补充需求！</p>
]]></content>
      <categories>
        <category>数学建模</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>多元线性回归</tag>
        <tag>非线性回归</tag>
        <tag>预测类</tag>
      </tags>
  </entry>
  <entry>
    <title>熵权法</title>
    <url>/2025/12/04/%E7%86%B5%E6%9D%83%E6%B3%95/</url>
    <content><![CDATA[<p>水一篇<br><span id="more"></span><br>    前置知识：最好会层次分析法</p>
<h1 id="模型引出"><a href="#模型引出" class="headerlink" title="模型引出"></a>模型引出</h1><p>熵权法和前面的层次分析法一样，是一种赋权方法。但不一样的是，熵权法是完全客观的方法，仅依赖数据本身的分布。<br>先来看个例子：<br>两个人比较身材，他们的身高只差一厘米，但体重差10千克，那么我们重点需要比较两人的体重而不是身高。在这个例子中，身高的权重就比较低，体重的权重比较高，并且根据数据是可以具体算出二者权重的。</p>
<h1 id="模型原理"><a href="#模型原理" class="headerlink" title="模型原理"></a>模型原理</h1><p>由信息论基本原理：</p>
<ul>
<li>信息是系统有序程度的度量</li>
<li>熵是系统无序程度的度量  </li>
</ul>
<p>信息熵是衡量随机变量不确定性的量化指标——不确定性越高，熵值越大；确定性越高，熵值越小。  </p>
<p>简单的例子：</p>
<ul>
<li>太阳从东边升起。这是必然时间，没有不确定性，信息熵为0</li>
<li>掷一枚均匀硬币，结果是正面。有不确定性，熵不为0</li>
<li>掷一次六面骰子，结果是 3 点。不确定性比掷硬币更高，熵更大  </li>
</ul>
<p>在熵权法中，使用信息熵值来判断某个指标的离散程度。离散程度越大，权重越高，反之权重小。  </p>
<h1 id="主要过程"><a href="#主要过程" class="headerlink" title="主要过程"></a>主要过程</h1><h2 id="1-数据标准化"><a href="#1-数据标准化" class="headerlink" title="1.数据标准化"></a>1.数据标准化</h2><script type="math/tex; mode=display">
 \widetilde{x_{ij}}=\frac{x_{ij}}{\sqrt{\sum_{i=1}^{n}{x_{ij}^2}}}</script><p>如果 $x_{ij}$ 中有负数，也可以用：</p>
<script type="math/tex; mode=display">
 \widetilde{x_{ij}}=\frac{x_{ij}-min(x_{1j},x_{2j},...,x_{nj})}{max(x_{1j},x_{2j},...,x_{nj})-min(x_{1j},x_{2j},...,x_{nj})}</script><h2 id="2-归一化"><a href="#2-归一化" class="headerlink" title="2.归一化"></a>2.归一化</h2><script type="math/tex; mode=display">
 p_{ij}=\frac{\widetilde{x_{ij}}}{\sum_{i=1}^{n}{\widetilde {x_{ij}}}}</script><h2 id="3-计算熵权"><a href="#3-计算熵权" class="headerlink" title="3.计算熵权"></a>3.计算熵权</h2><script type="math/tex; mode=display">e_j=-\frac{1}{ln(n)}\sum_{i=1}^{n}{p_{ij}}ln(p_{ij})</script><p>注意，此处定义 $ln(0)=0$   </p>
<script type="math/tex; mode=display">d_j=1-e_j</script><p>然后再归一化：</p>
<script type="math/tex; mode=display">W_j=\frac{d_j}{\sum_{j=1}^{m}{d_j}}</script><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>熵权法是一种基于数据客观分布的评价类模型赋权方法。但是，绝对的客观有时也是不行的，比如上面评价身材的例子，如果身高都一样，那么权重为0，说明比较中身材完全与身高无关，但实际上身材和身高是密切相关的。我们在评价类问题中，往往需要综合多种方法，比如使用层次分析法的主观方法和客观的熵权法的组合来更精确的评价目标。</p>
]]></content>
      <categories>
        <category>数学建模</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>评价类模型</tag>
        <tag>熵权法</tag>
      </tags>
  </entry>
  <entry>
    <title>随机森林</title>
    <url>/2025/11/27/%E9%9A%8F%E6%9C%BA%E6%A3%AE%E6%9E%97/</url>
    <content><![CDATA[<p>简单的随机森林(๑´ڡ`๑)<br><span id="more"></span><br>    前置知识：决策树</p>
<h1 id="一点背景"><a href="#一点背景" class="headerlink" title="一点背景"></a>一点背景</h1><h2 id="集成学习"><a href="#集成学习" class="headerlink" title="集成学习"></a>集成学习</h2><h3 id="什么是集成学习"><a href="#什么是集成学习" class="headerlink" title="什么是集成学习"></a>什么是集成学习</h3><p>普通模型将数据训练，得到结果。集成学习是一种机器学习的思想，将多个模型组合成一个精度更高的模型（强学习器）。被组合的小模型成为弱学习器（基学习器）<br>通过多个模型集成来提升整体精度，而不是专门培养一个模型  </p>
<ul>
<li>一个单一强大模型：能力很强，但有过拟合风险，而且抗风险能力弱</li>
<li>多个小模型组合：也可以有很强的整体能力，但是减少了过拟合风险，抗风险能力也提高了  <h3 id="集成学习的分类"><a href="#集成学习的分类" class="headerlink" title="集成学习的分类"></a>集成学习的分类</h3>Bagging（并行）和Boosting（串行）<br>Bagging是将原始数据进行多个流程训练综合得到结果，多个过程可以并行（比如在学校一学期同时学数学和英语）<br>Boosting是将原始数据经过第一个过程处理后，输入第二个过程继续…（比如数学先学习1+1，再学微积分）  </li>
</ul>
<p>本文探讨的随机森林就是Bagging中的一个<br>Boosting主要有：Adaboost，GBDT，XGBoost等  </p>
<h2 id="决策树回顾"><a href="#决策树回顾" class="headerlink" title="决策树回顾"></a>决策树回顾</h2><p>决策树是一颗二叉树，是监督学习中经典的分类/回归模型，它通过模拟人类“分而治之”的决策过程，将复杂问题拆解为一系列简单判断。简单来说，各结点就是各判断条件，根据判断结果的真伪细分到下面的子树继续判断。  </p>
<p>决策树有多种，ID3，C4.5，CART等，随机森林主要用到CART决策树  </p>
<p>决策树有剪枝操作，比如限制最大深度，结点继续划分的条件等，这些在随机森林api中会再看到  </p>
<p>从这两个的名字也可以猜出来，其实随机森林的 ‘森林’ 就是由一个个决策树构成的。  </p>
<h1 id="Bagging思想"><a href="#Bagging思想" class="headerlink" title="Bagging思想"></a>Bagging思想</h1><p>随机森林是一种基于Bagging思想的集成学习方法，以决策树作为弱学习器。<br>具体过程：</p>
<ul>
<li>将训练集通过随机抽样（有放回）分为几个小训练集</li>
<li>各个小训练集再各自随机挑选一些特征进行训练  </li>
<li>构建出n个决策树，然后这些决策树进行平权投票，选出最终结果  </li>
</ul>
<p>这里注意是要有放回的随机抽样，而且抽样比例最好不要太少。如果不抽样，那同样的训练集训练出来的决策树都是一样的。有放回保证了各决策树有交叉相连的关系，因此每棵树不会有较大偏差，既有交集又有差异，更好发挥投票的效果。  </p>
<h1 id="实战训练——泰坦尼克号生存预测"><a href="#实战训练——泰坦尼克号生存预测" class="headerlink" title="实战训练——泰坦尼克号生存预测"></a>实战训练——泰坦尼克号生存预测</h1><h2 id="api介绍"><a href="#api介绍" class="headerlink" title="api介绍"></a>api介绍</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.ensemble <span class="keyword">import</span> RandomForestClassifier</span><br><span class="line">model=RandomForestClassifier()</span><br></pre></td></tr></table></figure>
<p>主要用到的参数：  </p>
<ul>
<li>n_estimators: 决策树数量</li>
<li>max_features=”auto”: 决策树构建时使用的最大特征数量（列数），默认 <code>max_features</code> =sqrt（n_features），也可以自己调整，有线性，对数等</li>
<li>bootstrap: 是否放回。一般都是使用默认True</li>
<li>max_depth: 最大深度（同决策树）</li>
<li>…其他参数（沿用决策树的参数，这里就不写完了）</li>
</ul>
<h2 id="完整代码"><a href="#完整代码" class="headerlink" title="完整代码"></a>完整代码</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> train_test_split</span><br><span class="line"><span class="keyword">from</span> sklearn.ensemble <span class="keyword">import</span> RandomForestClassifier</span><br><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> GridSearchCV</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">d1</span>():</span><br><span class="line">    df=pd.read_csv(<span class="string">&quot;titanic_train.csv&quot;</span>)</span><br><span class="line">    df.info()</span><br><span class="line">    x=df[[<span class="string">&quot;Pclass&quot;</span>,<span class="string">&quot;Age&quot;</span>,<span class="string">&quot;Sex&quot;</span>]].copy()</span><br><span class="line">    y=df[<span class="string">&quot;Survived&quot;</span>]</span><br><span class="line">    x[<span class="string">&#x27;Age&#x27;</span>].fillna(value=df[<span class="string">&quot;Age&quot;</span>].mean(),inplace=<span class="literal">True</span>)</span><br><span class="line">    x=pd.get_dummies(x) <span class="comment">#处理非数值类型</span></span><br><span class="line">    x_train,x_test,y_train,y_test = train_test_split(x,y,test_size=<span class="number">0.2</span>,random_state=<span class="number">23</span>)</span><br><span class="line">    rfc=RandomForestClassifier(max_depth=<span class="number">6</span>,random_state=<span class="number">10</span>)</span><br><span class="line">    rfc.fit(x_train,y_train)</span><br><span class="line">    y_pred=rfc.predict(x_test)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&#x27;score: <span class="subst">&#123;rfc.score(x_test,y_test)&#125;</span>&#x27;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment">#交叉验证网格搜索</span></span><br><span class="line">    model=RandomForestClassifier()</span><br><span class="line">    param=&#123;<span class="string">&quot;n_estimators&quot;</span>:[<span class="number">40</span>,<span class="number">50</span>,<span class="number">60</span>,<span class="number">70</span>],<span class="string">&quot;max_depth&quot;</span>:[<span class="number">2</span>,<span class="number">4</span>,<span class="number">6</span>,<span class="number">8</span>,<span class="number">10</span>],<span class="string">&quot;random_state&quot;</span>:[<span class="number">10</span>]&#125;</span><br><span class="line">    grid_search=GridSearchCV(model,param_grid=param,cv=<span class="number">2</span>)</span><br><span class="line">    grid_search.fit(x_train,y_train)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&#x27;score_2: <span class="subst">&#123;grid_search.score(x_test,y_test)&#125;</span>&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    d1()</span><br></pre></td></tr></table></figure>
<p>流程都是一样的，只是换个模型  </p>
<p>这里仅举分类的例子，同样的，随机森林也可以做回归，具体差异和决策树的分类与回归一样，这里就不赘述。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>随机森林是基于 Bagging 思想的集成学习方法，核心是通过 “样本随机 + 特征随机” 构建多棵独立的 CART 决策树，最终通过投票（分类）或平均（回归）得到结果，是机器学习中 “开箱即食” 的强学习器。  </p>
<p>关键要理解其具体过程，即先对样本随机，然后再对特征随机。</p>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>机器学习</tag>
        <tag>集成学习</tag>
        <tag>随机森林</tag>
      </tags>
  </entry>
  <entry>
    <title>逻辑回归</title>
    <url>/2025/11/25/%E9%80%BB%E8%BE%91%E5%9B%9E%E5%BD%92/</url>
    <content><![CDATA[<p>简单的逻辑回归和样例ヾ(<em>´∀ ˋ</em>)ﾉ<br><span id="more"></span>  <!-- 就加这行！前面的内容（标题+前置知识）首页不显示，只显文章标题 --><br><del>这篇文章是本蒟蒻第一个博客，各处表述难免欠妥，希望各位佬指出</del></p>
<pre><code>前置知识：线性回归，简单概率论，一点点机器学习基础
</code></pre><h1 id="什么是逻辑回归"><a href="#什么是逻辑回归" class="headerlink" title="什么是逻辑回归"></a>什么是逻辑回归</h1><p>逻辑回归 <code>logistic regression</code> 是一种经典分类算法，通常用于二分类。逻辑回归是一种对数线性模型(从下面的公式也可以看出来)  </p>
<h1 id="逻辑回归基础"><a href="#逻辑回归基础" class="headerlink" title="逻辑回归基础"></a>逻辑回归基础</h1><p>先来了解一下逻辑斯谛分布, 设 <code>X</code> 为连续随机变量, 逻辑斯谛分布的累积分布函数为：</p>
<script type="math/tex; mode=display">F(x) = P(X \leq x) = \frac{1}{1 + e^{-\frac{x - \mu}{\gamma}}}</script><p>对应的概率密度函数（CDF 的导数）为：</p>
<script type="math/tex; mode=display">f(x) = F'(x) = \frac{e^{-\frac{x - \mu}{\gamma}}}{\gamma \cdot \left(1 + e^{-\frac{x - \mu}{\gamma}}\right)^2}</script><p>式中 $\mu$ 为位置参数,  $\gamma$ 为形状参数  </p>
<p><del>好复杂看不懂</del><br>先看看简单的。<br>当逻辑斯谛分布的位置参数 $\mu$、形状参数 $\gamma$ 时，其累积分布函数（CDF）就是 sigmoid 函数（激活函数）：</p>
<script type="math/tex; mode=display">sigmoid(x) = \frac{1}{1 + e^{-x}}</script><p>作用：将($-\infty, +\infty$) 映射到 (0, 1)</p>
<p>再结合线性回归，就可以做到逻辑回归最简单的应用: 二分类  </p>
<p>就是<strong>将线性回归的输出作为逻辑回归的输入</strong><br>具体一点，就是将线性回归的值，带入上面的激活函数，映射到 (0-1) 后，再根据阈值分类<br>选定阈值（0-1）后大于阈值为正样本， 小于为负样本</p>
<script type="math/tex; mode=display">h(x)=sigmoid(w^T·x+b)</script><h2 id="下面介绍逻辑回归损失函数"><a href="#下面介绍逻辑回归损失函数" class="headerlink" title="下面介绍逻辑回归损失函数"></a>下面介绍逻辑回归损失函数</h2><script type="math/tex; mode=display">Loss(L) = -y_i \log p - (1 - y_i) \log(1 - p)</script><p>式中，$y_i$ 为逻辑回归的预估的类别，A类为1，B类为0，即正样本只看前半部分，负样本只看后半部分。<br> $p$ 为上面 h(x) 的返回值，即概率。<br>损失函数的工作原理：每个样本有A，B两个类别，<strong>真实类别对应的位置，概率值越大越好</strong>  </p>
<p>下面具体讲讲概率分布  </p>
<h3 id="对于一个样本"><a href="#对于一个样本" class="headerlink" title="对于一个样本"></a>对于一个样本</h3><p>假设有0，1两个类别，显然有：</p>
<script type="math/tex; mode=display">
L= 
\begin{cases}
p & y = 1 \quad (\text{样本为正类}) \\
1-p & y = 0 \quad (\text{样本为负类})
\end{cases}</script><p>等价于</p>
<script type="math/tex; mode=display">L=p^y(1-p)^{1-y}</script><h3 id="推广到n个样本"><a href="#推广到n个样本" class="headerlink" title="推广到n个样本"></a>推广到n个样本</h3><script type="math/tex; mode=display">P=\displaystyle \prod_{i=1}^{n}{p^{y_i}(1-p)^{1-{y_i}}}</script><p>这个就是所有样本都预测正确的概率。</p>
<p>问题转换为：当上述联合概率取最大时，估计 $w$ 和 $b$ ，就变成了极大似然估计。</p>
<h3 id="极大似然函数转对数似然函数"><a href="#极大似然函数转对数似然函数" class="headerlink" title="极大似然函数转对数似然函数"></a>极大似然函数转对数似然函数</h3><p>对上述式子取对数，有:</p>
<script type="math/tex; mode=display">H(L)=\sum_{i=1}^n y_i \log p + (1 - y_i) \log(1 - p)</script><p>因为是损失函数，所以应该取最小值，只需加个负号。</p>
<script type="math/tex; mode=display">Loss(L)=-\sum_{i=1}^n y_i \log p + (1 - y_i) \log(1 - p)</script><p>也就得到了之前是式子。<br>通过这个转换，将最小化损失函数的任务交给了极大似然函数，所以逻辑回归本质上没有新的算法。  </p>
<p>总结即是：逻辑回归损失函数= - 极大似然估计函数  </p>
<h1 id="实战训练——癌症预测"><a href="#实战训练——癌症预测" class="headerlink" title="实战训练——癌症预测"></a>实战训练——癌症预测</h1><p>逻辑回归的包<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.linear_model <span class="keyword">import</span> LogisticRegression</span><br></pre></td></tr></table></figure></p>
<p>简单介绍一下api<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">LogisticRegression(solver=<span class="string">&#x27;liblinear&#x27;</span>,penalty=<span class="string">&#x27;l2&#x27;</span>,C=<span class="number">1.0</span>)</span><br></pre></td></tr></table></figure><br>其中 <code>liblinear</code> 在数据集较小是比较快，对大数据集也可以换成 <code>sag</code> or <code>saga</code><br> <code>penalty</code> 表示正则化种类，l1或者l2<br>C 表示正则化力度<br>默认将数量少的类别作为正例  </p>
<p>下面是完整代码，只是换成了 <code>LogisticRegression</code> ,其他和别的方法一样</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> train_test_split</span><br><span class="line"><span class="keyword">from</span> sklearn.linear_model <span class="keyword">import</span> LogisticRegression</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> accuracy_score</span><br><span class="line"><span class="keyword">from</span> sklearn.preprocessing <span class="keyword">import</span> MinMaxScaler</span><br><span class="line"><span class="keyword">from</span> sklearn.preprocessing <span class="keyword">import</span> StandardScaler</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">d1</span>():</span><br><span class="line">    df = pd.read_csv(<span class="string">&quot;breast-cancer-wisconsin.data&quot;</span>, sep=<span class="string">&quot;,&quot;</span>, header=<span class="literal">None</span>)</span><br><span class="line">    <span class="comment"># df.info()</span></span><br><span class="line">    df=df.replace(to_replace=<span class="string">&#x27;?&#x27;</span>,value=np.NaN)</span><br><span class="line">    df=df.dropna()</span><br><span class="line">    df.info()</span><br><span class="line">    x=df.iloc[:,<span class="number">1</span>:-<span class="number">1</span>]</span><br><span class="line">    y=df.iloc[:,-<span class="number">1</span>]</span><br><span class="line">    x_train,x_test,y_train,y_test = train_test_split(x,y,test_size=<span class="number">0.2</span>,random_state=<span class="number">22</span>)</span><br><span class="line">    transfer=StandardScaler()</span><br><span class="line">    x_train=transfer.fit_transform(x_train)</span><br><span class="line">    x_test=transfer.transform(x_test)</span><br><span class="line">    model=LogisticRegression()</span><br><span class="line">    model.fit(x_train,y_train)</span><br><span class="line">    y_pred=model.predict(x_test)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&#x27;score: <span class="subst">&#123;model.score(x_test,y_test)&#125;</span>&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    d1()</span><br></pre></td></tr></table></figure>
<h1 id="模型的评估"><a href="#模型的评估" class="headerlink" title="模型的评估"></a>模型的评估</h1><p>像上面那样，仅使用 ‘准确率’ 进行估计，事实上是不精确，甚至是不可取的  </p>
<p>比如：1000 人中仅 10 人患癌（正例占 1%，健康人占 99%，极度不平衡）若模型 “躺平” 全预测 “健康”，准确率 = 990/1000=99%（看似优秀），但漏诊了所有 10 名患者（相当于最坏情况下根本没测出来）。<br>而如果说继续提升这个准确率，确实可以提高测出来的比例，但是这在精度上的要求将是巨大的，现实情况样本容量巨大，实现是不可能的。  </p>
<p>在正负样例容量差距较大时，需要进行进一步的评估，下面介绍一些方法  </p>
<h2 id="混淆矩阵"><a href="#混淆矩阵" class="headerlink" title="混淆矩阵"></a>混淆矩阵</h2><div class="table-container">
<table>
<thead>
<tr>
<th style="text-align:center"></th>
<th style="text-align:center">正例（预测）</th>
<th style="text-align:center">反例（预测）</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">正例（真实）</td>
<td style="text-align:center">真正例（TP）</td>
<td style="text-align:center">伪反例（FN）</td>
</tr>
<tr>
<td style="text-align:center">反例（真实）</td>
<td style="text-align:center">伪正例（FP）</td>
<td style="text-align:center">真反例（TN）</td>
</tr>
</tbody>
</table>
</div>
<p>其中，T/F表示真/假，P/N表示正/负（样例）<br>比如伪反例，就是本来应该是正例，被错误预测为反例，即假的反例 <code>False Negative</code>  </p>
<p>下面的评估需要用到混淆矩阵  </p>
<ul>
<li>精确率 <code>precision</code> <script type="math/tex; mode=display">P= \frac{TP}{TP + FP}</script></li>
<li>召回率（查全率） <code>recall</code><script type="math/tex; mode=display">P= \frac{TP}{TP + FN}</script></li>
<li><code>F1 score</code> (用于综合上面两个)  <script type="math/tex; mode=display">P= \frac{2·precision·recall}{precision + recall}</script></li>
</ul>
<p>从各公式就可以明显看出来这三个值的含义了，再简单解释一下，精确率就是预测出来的正例确实是正例的概率，查全率就是有多少正例被预测出来了。这两个指标当然越大越好，F1值即综合上面两个指标，也就是可以用F1值作为模型好坏的评价，<strong>F1值越大模型越好</strong>。  </p>
<p>代码也很简单，先加个包<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> accuracy_score, f1_score,confusion_matrix</span><br></pre></td></tr></table></figure><br>其他的和上面的代码一样，得分改成F1值<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="built_in">print</span>(<span class="string">f&#x27;F1 Score (pos_label=4): <span class="subst">&#123;f1_score(y_test, y_pred, pos_label=<span class="number">4</span>):<span class="number">.4</span>f&#125;</span>&#x27;</span>)</span><br></pre></td></tr></table></figure><br>当然这里也可以不加 <code>pos_label</code> ，默认样本少的为正例<br>也可以打印出混淆矩阵来看看<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">cm = confusion_matrix(y_test, y_pred)</span><br><span class="line">cm_df = pd.DataFrame(</span><br><span class="line">    cm,</span><br><span class="line">    index=[<span class="string">&quot;真实_良性(2)&quot;</span>, <span class="string">&quot;真实_恶性(4)&quot;</span>],</span><br><span class="line">    columns=[<span class="string">&quot;预测_良性(2)&quot;</span>, <span class="string">&quot;预测_恶性(4)&quot;</span>]</span><br><span class="line">)</span><br><span class="line"><span class="built_in">print</span>(cm_df)</span><br></pre></td></tr></table></figure><br>至此，就可以得到更加精准的模型评估  </p>
<h2 id="ROC曲线和AUC指标"><a href="#ROC曲线和AUC指标" class="headerlink" title="ROC曲线和AUC指标"></a>ROC曲线和AUC指标</h2><p>这两个就是进一步拓展了，这里简单讲一点  </p>
<p>ROC <code>Receiver Operating Characteristic Curve</code> 曲线可以就是上面混淆矩阵的可视化  </p>
<ul>
<li><p>横轴（FPR）：假阳性率 = 假阳性样本数/(假阳性+真阴性样本数) → 模型误将负样本判为正样本的比例（反映“误诊”风险）  </p>
</li>
<li><p>纵轴（TPR）：真阳性率 = 真阳性样本数/(真阳性+假阴性样本数) → 模型正确识别正样本的比例（即召回率，反映“漏诊”风险）  </p>
</li>
</ul>
<p>同一数据集下，性能更优的模型ROC曲线会更靠近左上角（TPR高且FPR低） </p>
<p>AUC <code>Area Under the ROC Curve</code> 是ROC曲线下方的面积，取值范围为0~1：</p>
<p>核心意义：代表模型在随机抽取一个正样本和一个负样本时，正样本被预测为正类的概率大于负样本的概率（即模型区分正/负样本的能力, 也可以作为模型评分）<br>使用AUC可以直接比较多个模型的整体性能  </p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>逻辑回归看着带“回归”二字，其实是做分类的，尤其适合分二类（比如判断是不是癌症）。</p>
<p>核心思路很简单：先拿线性回归算出一个结果，再用sigmoid函数把这个结果变成0到1之间的概率，定个阈值（比如0.5），概率超阈值就是正例，反之就是负例。</p>
<p>实战里要注意：</p>
<ul>
<li>先给特征做标准化（不然模型不准）；</li>
<li>遇到像癌症预测这种“少数人患病、多数人健康”的情况，别只看准确率（容易骗人），用混淆矩阵、F1分数或者AUC这些指标才靠谱，能避免漏诊、误诊。</li>
</ul>
<p>它的好处就是模型简单、训练快，还能看懂每个特征的影响（比如哪个指标对判断癌症更重要），是新手入门机器学习的好选择</p>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>机器学习</tag>
        <tag>逻辑回归</tag>
      </tags>
  </entry>
  <entry>
    <title>插值算法</title>
    <url>/2025/12/23/%E6%8F%92%E5%80%BC%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<p>就讲讲最简单的应用，不涉及过多证明<br><span id="more"></span></p>
<h1 id="算法引入"><a href="#算法引入" class="headerlink" title="算法引入"></a>算法引入</h1><p>插值算法，简单来讲，就是通过已经有的离散的样本点来得到该样本点符合的函数式。<br>数模中有时用于数据点较少的情况，通过插值来获得更多估算的样本点或者得出样本点之间关系等</p>
<ul>
<li>内插：估算 x 在已知区间 $[x_0,x_n]$ 内的值</li>
<li>外插：估算 x 超出已知区间的值（容易失真）  </li>
</ul>
<p>一般多使用内插。</p>
<h1 id="各类插值算法"><a href="#各类插值算法" class="headerlink" title="各类插值算法"></a>各类插值算法</h1><h2 id="线性插值"><a href="#线性插值" class="headerlink" title="线性插值"></a>线性插值</h2><p>直接将各个样本点用线段相连。<br>最简单的插值方法，当然有很多缺点，插值误差大等。</p>
<h2 id="多项式插值"><a href="#多项式插值" class="headerlink" title="多项式插值"></a>多项式插值</h2><p>若有n+1个样本点，可以假设插值函数为n次多项式。<br>由此可以列出n+1个方程。<br>由线性代数知识，可得该方程组秩为n+1（范德蒙德行列式），求出多项式各系数。</p>
<h2 id="牛顿插值法"><a href="#牛顿插值法" class="headerlink" title="牛顿插值法"></a>牛顿插值法</h2><p>从两个点开始，得到第一次插值多项式，然后增加一个点，结合上一次结果得到第二次插值多项式（根据插值函数各样本点函数值已知得到）。以此迭代得到最终结果。</p>
<h2 id="三次埃尔米特插值多项式"><a href="#三次埃尔米特插值多项式" class="headerlink" title="三次埃尔米特插值多项式"></a>三次埃尔米特插值多项式</h2><h3 id="龙格现象"><a href="#龙格现象" class="headerlink" title="龙格现象"></a>龙格现象</h3><p>前面的多项式插值和牛顿插值不可避免的涉及到多项式，而高阶多项式在数据较大时是发散的，尤其在外插时，等距节点下高次多项式插值，在区间边界剧烈振荡, 误差较大。</p>
<h3 id="埃尔米特插值"><a href="#埃尔米特插值" class="headerlink" title="埃尔米特插值"></a>埃尔米特插值</h3><p>埃尔米特插值对每两个数据点之间都采用不同的多项式进行插值，有效避免了仅使用一个合成的多项式插值产生的龙格现象（当然在外插时也会有误差）。<br>次数较小时，会退化为多项式或者线段插值，次数较大时，计算复杂其可能产生过拟合，综合考虑，一般采用三次多项式进行两个数据点之间的插值，即三次埃尔米插值。</p>
<h2 id="三次样条插值"><a href="#三次样条插值" class="headerlink" title="三次样条插值"></a>三次样条插值</h2><h3 id="更平滑"><a href="#更平滑" class="headerlink" title="更平滑"></a>更平滑</h3><p>前面的各种插值方法，都只考虑了函数在样本点处的值，没有考虑样本点实际表现出来的变化趋势等。<br>要更充分的挖掘样本点内信息，加上变化趋势的考虑等，可以加上导数。<br>即样本点得出的插值函数，需要存在导数，m次样条插值意味着样本点存在m-1阶连续导数。限制了可导的因素，插值函数相较于前面的方法更加平滑。<br>更高阶的导数要求，对样本点和计算的规范也越多。综合考虑，一般取m=3，即三次样条插值</p>
<h1 id="代码演示1"><a href="#代码演示1" class="headerlink" title="代码演示1"></a>代码演示1</h1><figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line">x=<span class="number">1</span>:<span class="number">10</span>;</span><br><span class="line">y=<span class="built_in">log</span>(x);</span><br><span class="line"><span class="built_in">plot</span>(x,y,<span class="string">&#x27;o&#x27;</span>)</span><br><span class="line"><span class="built_in">hold</span> on;</span><br><span class="line">nx=<span class="number">0.01</span>:<span class="number">0.1</span>:<span class="number">10</span>;</span><br><span class="line">p=pchip(x,y,nx);</span><br><span class="line"><span class="built_in">plot</span>(nx,p)</span><br><span class="line"><span class="built_in">hold</span> on;</span><br><span class="line">p=spline(x,y,nx);</span><br><span class="line"><span class="built_in">plot</span>(nx,p,<span class="string">&#x27;b-&#x27;</span>)</span><br><span class="line"><span class="built_in">legend</span>(<span class="string">&#x27;样本点&#x27;</span>,<span class="string">&#x27;三次埃尔米插值&#x27;</span>,<span class="string">&#x27;三次样条插值&#x27;</span>,<span class="string">&#x27;Location&#x27;</span>,<span class="string">&#x27;SouthEast&#x27;</span>);</span><br></pre></td></tr></table></figure>
<h1 id="更高维度"><a href="#更高维度" class="headerlink" title="更高维度"></a>更高维度</h1><h2 id="api"><a href="#api" class="headerlink" title="api"></a>api</h2><p>当然，样本点不一定只有x，y，可能涉及更高维度，不够原理和二维的近似。<br>使用函数 <code>interp2</code> 表示样本点为二维的，<code>interpn</code> 表示样本点为n维的。</p>
<h2 id="代码演示2"><a href="#代码演示2" class="headerlink" title="代码演示2"></a>代码演示2</h2><figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line">x=<span class="number">-1</span>:<span class="number">0.5</span>:<span class="number">1</span>;</span><br><span class="line">y=x;</span><br><span class="line">[x,y]=<span class="built_in">meshgrid</span>(x,y);</span><br><span class="line">z=<span class="number">1</span>-x.^<span class="number">2</span>-y.^<span class="number">2</span>;</span><br><span class="line">nx=<span class="number">-1</span>:<span class="number">0.1</span>:<span class="number">1</span>;</span><br><span class="line">ny=nx;</span><br><span class="line">[nx,ny]=<span class="built_in">meshgrid</span>(nx,ny);</span><br><span class="line">p=interp2(x,y,z,nx,ny,<span class="string">&#x27;spline&#x27;</span>);</span><br><span class="line">mesh(nx,ny,p)</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>数学建模</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>插值算法</tag>
        <tag>三次样条插值</tag>
      </tags>
  </entry>
  <entry>
    <title>灰色预测</title>
    <url>/2025/12/14/%E7%81%B0%E8%89%B2%E9%A2%84%E6%B5%8B/</url>
    <content><![CDATA[<p>用于数据量较少的模型<br><span id="more"></span></p>
<h1 id="模型引入"><a href="#模型引入" class="headerlink" title="模型引入"></a>模型引入</h1><h2 id="什么是灰色"><a href="#什么是灰色" class="headerlink" title="什么是灰色"></a>什么是灰色</h2><p>一个系统通常分为黑，白，灰。白色系统，即数据完全清楚已知的系统。黑色系统完全未知，而灰色系统就是介于二者之间，仅有部分是已知的。</p>
<h2 id="时间序列"><a href="#时间序列" class="headerlink" title="时间序列"></a>时间序列</h2><p>灰色模型通常用于<a href="https://longnamehaha.github.io/2025/12/15/ARIMA/" title="看看">时间序列</a>的预测，但是灰色预测更加适合已知的历史数据较少的情况</p>
<h1 id="核心过程"><a href="#核心过程" class="headerlink" title="核心过程"></a>核心过程</h1><p>灰色预测模型中，<strong>GM(1,1)（Gray Model (1,1)）</strong> 是最基础、最常用的单变量一阶灰色模型，适用于“单指标、短期、小样本”预测。括号中“1”分别表示“1个变量”和“1阶微分方程”。</p>
<h2 id="模型的核心假设"><a href="#模型的核心假设" class="headerlink" title="模型的核心假设"></a>模型的核心假设</h2><p>原始数据序列需满足“光滑性”和“指数增长趋势”（可通过级比检验验证），否则需进行数据预处理（如平移变换、对数变换）。</p>
<h2 id="数学推导"><a href="#数学推导" class="headerlink" title="数学推导"></a>数学推导</h2><p>设原始非负数据序列为：  </p>
<script type="math/tex; mode=display">X^{(0)} = \{x^{(0)}(1), x^{(0)}(2), ..., x^{(0)}(n)\}, \quad n \geq 4 \quad (n为样本数)</script><h3 id="累加生成（AGO）"><a href="#累加生成（AGO）" class="headerlink" title="累加生成（AGO）"></a>累加生成（AGO）</h3><p>对原始序列进行<strong>一次累加生成</strong>（1-AGO），得到生成序列 $ X^{(1)} $，目的是增强数据的规律性（让杂乱数据呈现指数增长趋势）：  </p>
<script type="math/tex; mode=display">X^{(1)} = {x^{(1)}_1, x^{(1)}_2, ..., x^{(1)}_n}</script><p>其中，$ x^{(1)}(k) = \sum_{i=1}^k x^{(0)}(i) \quad (k=1,2,…,n) $ (前缀和)</p>
<h3 id="构建白化微分方程"><a href="#构建白化微分方程" class="headerlink" title="构建白化微分方程"></a>构建白化微分方程</h3><p>假设 $ X^{(1)} $ 满足一阶线性微分方程（白化方程）：  </p>
<script type="math/tex; mode=display">\frac{dx^{(1)}(t)}{dt} + a x^{(1)}(t) = b</script><ul>
<li>$ a $：发展系数（反映序列增长趋势的强弱，$ |a|$ 越小，趋势越平稳）；  </li>
<li>$ b $：灰作用量（反映系统的外部输入或固有特性）。</li>
</ul>
<h3 id="最小二乘估计参数"><a href="#最小二乘估计参数" class="headerlink" title="最小二乘估计参数"></a>最小二乘估计参数</h3><p>将微分方程离散化，得到矩阵形式 $ Y = B\hat{\alpha} $，其中：  </p>
<ul>
<li>观测向量 $ Y = \begin{bmatrix} x^{(0)}(2) \ x^{(0)}(3) \ … \ x^{(0)}(n) \end{bmatrix} $  </li>
<li>数据矩阵 $ B = \begin{bmatrix} -\frac{1}{2}[x^{(1)}(1)+x^{(1)}(2)] &amp; 1 \ -\frac{1}{2}[x^{(1)}(2)+x^{(1)}(3)] &amp; 1 \ … &amp; … \ -\frac{1}{2}[x^{(1)}(n-1)+x^{(1)}(n)] &amp; 1 \end{bmatrix} $  </li>
<li>参数向量 $ \hat{\alpha} = \begin{bmatrix} a \ b \end{bmatrix} $  </li>
</ul>
<p>通过最小二乘法求解参数：  </p>
<script type="math/tex; mode=display">\hat{\alpha} = (B^T B)^{-1} B^T Y</script><h3 id="求解白化方程与还原预测"><a href="#求解白化方程与还原预测" class="headerlink" title="求解白化方程与还原预测"></a>求解白化方程与还原预测</h3><p>白化方程的解析解（时间响应函数）为：  </p>
<script type="math/tex; mode=display">x^{(1)}(t) = \left( x^{(0)}(1) - \frac{b}{a} \right) e^{-a(t-1)} + \frac{b}{a}</script><p>对 $ x^{(1)}(t) $ 进行<strong>累减生成</strong>（差分）,（IG），还原得到原始序列的预测值 $ \hat{x}^{(0)}(k) $：  </p>
<script type="math/tex; mode=display">\hat{x}^{(0)}(1) = x^{(0)}(1) \quad</script><script type="math/tex; mode=display">\hat{x}^{(0)}(k) = \hat{x}^{(1)}(k) - \hat{x}^{(1)}(k-1) \quad (k \geq 2)</script><h3 id="精度检验"><a href="#精度检验" class="headerlink" title="精度检验"></a>精度检验</h3><p>通过以下指标验证模型可靠性：  </p>
<ol>
<li><strong>平均相对误差（MAPE）</strong>：$ \text{MAPE} = \frac{1}{n} \sum_{k=1}^n \left| \frac{x^{(0)}(k) - \hat{x}^{(0)}(k)}{x^{(0)}(k)} \right| \times 100\% $  <ul>
<li>一级精度：$ \text{MAPE} &lt; 10\% $；二级精度：$ 10\% \leq \text{MAPE} &lt; 20\% $；三级精度：$ 20\% \leq \text{MAPE} &lt; 30\% $  </li>
</ul>
</li>
<li><strong>后验差检验</strong>：  <ul>
<li>原始序列标准差 $ S<em>1 = \sqrt{\frac{1}{n} \sum</em>{k=1}^n (x^{(0)}(k) - \bar{x}^{(0)})^2} $  </li>
<li>残差序列 $ \varepsilon(k) = x^{(0)}(k) - \hat{x}^{(0)}(k) $，残差标准差 $ S<em>2 = \sqrt{\frac{1}{n} \sum</em>{k=1}^n (\varepsilon(k) - \bar{\varepsilon})^2} $  </li>
<li>后验差比 $ C = \frac{S_2}{S_1} $，小误差概率 $ P = P(|\varepsilon(k) - \bar{\varepsilon}| &lt; 0.6745 S_1) $  </li>
<li>一级精度：$ C &lt; 0.35, P &gt; 0.95 $；二级精度：$ C &lt; 0.5, P &gt; 0.8 $；三级精度：$ C &lt; 0.65, P &gt; 0.7 $</li>
</ul>
</li>
</ol>
<h1 id="完整步骤"><a href="#完整步骤" class="headerlink" title="完整步骤"></a>完整步骤</h1><h2 id="数据预处理与检验"><a href="#数据预处理与检验" class="headerlink" title="数据预处理与检验"></a>数据预处理与检验</h2><ul>
<li><strong>数据要求</strong>：原始序列 $ X^{(0)} $ 为非负序列（若有负数，进行平移变换 $ x’(0)(k) = x^{(0)}(k) + c $，$ c $ 为常数使所有数据非负）。  </li>
<li><strong>级比检验</strong>：验证数据是否适合GM(1,1)模型，计算级比 $ \lambda(k) = \frac{x^{(0)}(k-1)}{x^{(0)}(k)} \quad (k=2,…,n) $，若所有 $ \lambda(k) \in (e^{-2/(n+1)}, e^{2/(n+1)}) $，则适合建模；否则需进行数据变换（如对数变换、平移变换）。</li>
</ul>
<h2 id="累加生成（1-AGO）"><a href="#累加生成（1-AGO）" class="headerlink" title="累加生成（1-AGO）"></a>累加生成（1-AGO）</h2><p>计算 $ X^{(1)} $，增强数据规律性。</p>
<h2 id="构建矩阵-B-和-Y"><a href="#构建矩阵-B-和-Y" class="headerlink" title="构建矩阵 $ B $ 和 $ Y $"></a>构建矩阵 $ B $ 和 $ Y $</h2><p>按公式构造矩阵，注意维度匹配（$ B $ 为 $ (n-1) \times 2 $ 矩阵，$ Y $ 为 $ (n-1) \times 1 $ 向量）。</p>
<h2 id="参数估计（求解-a-b-）"><a href="#参数估计（求解-a-b-）" class="headerlink" title="参数估计（求解 $ a, b $）"></a>参数估计（求解 $ a, b $）</h2><p>通过最小二乘法计算 $ \hat{\alpha} = (B^T B)^{-1} B^T Y $。</p>
<h2 id="预测与还原"><a href="#预测与还原" class="headerlink" title="预测与还原"></a>预测与还原</h2><ul>
<li>短期预测：计算 $ \hat{x}^{(1)}(n+1), \hat{x}^{(1)}(n+2), … $（竞赛中预测步长建议≤3，步长越长误差越大）。  </li>
<li>累减还原：得到原始序列的预测值 $ \hat{x}^{(0)}(n+1), … $。</li>
</ul>
<h2 id="精度检验-1"><a href="#精度检验-1" class="headerlink" title="精度检验"></a>精度检验</h2><p>计算MAPE、后验差比 $ C $、小误差概率 $ P $，若精度不达标，需优化模型（如残差修正、组合模型）。</p>
<h1 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h1><h2 id="Python版本"><a href="#Python版本" class="headerlink" title="Python版本"></a>Python版本</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">gm11</span>(<span class="params">x0, predict_num=<span class="number">1</span></span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    GM(1,1)灰色预测模型</span></span><br><span class="line"><span class="string">    :param x0: 原始序列（list或array，非负）</span></span><br><span class="line"><span class="string">    :param predict_num: 预测步数（默认1步，竞赛建议≤3）</span></span><br><span class="line"><span class="string">    :return: 原始序列预测值、未来预测值、模型参数(a,b)、平均相对误差(mape)</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    x0 = np.array(x0).reshape(-<span class="number">1</span>, <span class="number">1</span>)</span><br><span class="line">    n = <span class="built_in">len</span>(x0)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 1. 累加生成（1-AGO）</span></span><br><span class="line">    x1 = np.cumsum(x0, axis=<span class="number">0</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 2. 构建矩阵B和Y</span></span><br><span class="line">    B = np.zeros((n-<span class="number">1</span>, <span class="number">2</span>))</span><br><span class="line">    Y = np.zeros((n-<span class="number">1</span>, <span class="number">1</span>))</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(n-<span class="number">1</span>):</span><br><span class="line">        B[i, <span class="number">0</span>] = -<span class="number">0.5</span> * (x1[i] + x1[i+<span class="number">1</span>])</span><br><span class="line">        B[i, <span class="number">1</span>] = <span class="number">1</span></span><br><span class="line">        Y[i] = x0[i+<span class="number">1</span>]</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 3. 最小二乘法估计参数a, b</span></span><br><span class="line">    alpha = np.dot(np.dot(np.linalg.inv(np.dot(B.T, B)), B.T), Y)</span><br><span class="line">    a, b = alpha[<span class="number">0</span>, <span class="number">0</span>], alpha[<span class="number">1</span>, <span class="number">0</span>]</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 4. 时间响应函数与预测（x1_hat）</span></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">x1_hat</span>(<span class="params">k</span>):</span><br><span class="line">        <span class="keyword">return</span> (x0[<span class="number">0</span>, <span class="number">0</span>] - b/a) * np.exp(-a*(k-<span class="number">1</span>)) + b/a</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 5. 还原预测值（x0_hat）</span></span><br><span class="line">    x0_hat = np.zeros((n, <span class="number">1</span>))</span><br><span class="line">    x0_hat[<span class="number">0</span>] = x0[<span class="number">0</span>, <span class="number">0</span>]  <span class="comment"># 初始值不变</span></span><br><span class="line">    <span class="keyword">for</span> k <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">1</span>, n):</span><br><span class="line">        x0_hat[k] = x1_hat(k) - x1_hat(k-<span class="number">1</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 6. 未来预测</span></span><br><span class="line">    future_x0 = []</span><br><span class="line">    <span class="keyword">for</span> k <span class="keyword">in</span> <span class="built_in">range</span>(n+<span class="number">1</span>, n+<span class="number">1</span>+predict_num):</span><br><span class="line">        future_x0.append(x1_hat(k) - x1_hat(k-<span class="number">1</span>))</span><br><span class="line">    future_x0 = np.array(future_x0).reshape(-<span class="number">1</span>, <span class="number">1</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 7. 精度检验（平均相对误差MAPE）</span></span><br><span class="line">    mape = np.mean(np.<span class="built_in">abs</span>((x0 - x0_hat)/x0)) * <span class="number">100</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> x0_hat, future_x0, (a, b), mape</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&quot;__main__&quot;</span>:</span><br><span class="line">    <span class="comment"># 原始数据：某地区2018-2022年GDP（单位：亿元）</span></span><br><span class="line">    x0 = [<span class="number">500</span>, <span class="number">550</span>, <span class="number">610</span>, <span class="number">680</span>, <span class="number">760</span>]</span><br><span class="line">    predict_num = <span class="number">2</span>  <span class="comment"># 预测2023、2024年GDP</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 建模预测</span></span><br><span class="line">    x0_hat, future_x0, (a, b), mape = gm11(x0, predict_num)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 结果输出</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;模型参数：a=<span class="subst">&#123;a:<span class="number">.4</span>f&#125;</span>, b=<span class="subst">&#123;b:<span class="number">.4</span>f&#125;</span>&quot;</span>)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;平均相对误差MAPE：<span class="subst">&#123;mape:<span class="number">.2</span>f&#125;</span>%&quot;</span>)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;原始序列预测值：<span class="subst">&#123;x0_hat.flatten().<span class="built_in">round</span>(<span class="number">2</span>)&#125;</span>&quot;</span>)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;2023年预测GDP：<span class="subst">&#123;future_x0[<span class="number">0</span>,<span class="number">0</span>]:<span class="number">.2</span>f&#125;</span>亿元&quot;</span>)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;2024年预测GDP：<span class="subst">&#123;future_x0[<span class="number">1</span>,<span class="number">0</span>]:<span class="number">.2</span>f&#125;</span>亿元&quot;</span>)</span><br><span class="line">    </span><br><span class="line">    plt.rcParams[<span class="string">&#x27;font.sans-serif&#x27;</span>] = [<span class="string">&#x27;SimHei&#x27;</span>]  <span class="comment"># 支持中文</span></span><br><span class="line">    plt.figure(figsize=(<span class="number">10</span>, <span class="number">6</span>))</span><br><span class="line">    years = np.arange(<span class="number">2018</span>, <span class="number">2018</span>+<span class="built_in">len</span>(x0))</span><br><span class="line">    predict_years = np.arange(<span class="number">2018</span>+<span class="built_in">len</span>(x0), <span class="number">2018</span>+<span class="built_in">len</span>(x0)+predict_num)</span><br><span class="line">    </span><br><span class="line">    plt.plot(years, x0, <span class="string">&#x27;o-&#x27;</span>, label=<span class="string">&#x27;原始GDP&#x27;</span>, linewidth=<span class="number">2</span>, markersize=<span class="number">8</span>)</span><br><span class="line">    plt.plot(years, x0_hat.flatten(), <span class="string">&#x27;s-&#x27;</span>, label=<span class="string">&#x27;拟合GDP&#x27;</span>, linewidth=<span class="number">2</span>, markersize=<span class="number">8</span>)</span><br><span class="line">    plt.plot(predict_years, future_x0.flatten(), <span class="string">&#x27;^-&#x27;</span>, label=<span class="string">&#x27;预测GDP&#x27;</span>, linewidth=<span class="number">2</span>, markersize=<span class="number">8</span>, color=<span class="string">&#x27;red&#x27;</span>)</span><br><span class="line">    </span><br><span class="line">    plt.xlabel(<span class="string">&#x27;年份&#x27;</span>, fontsize=<span class="number">12</span>)</span><br><span class="line">    plt.ylabel(<span class="string">&#x27;GDP（亿元）&#x27;</span>, fontsize=<span class="number">12</span>)</span><br><span class="line">    plt.title(<span class="string">f&#x27;GM(1,1)灰色预测模型：某地区GDP预测（MAPE=<span class="subst">&#123;mape:<span class="number">.2</span>f&#125;</span>%）&#x27;</span>, fontsize=<span class="number">14</span>)</span><br><span class="line">    plt.legend(fontsize=<span class="number">10</span>)</span><br><span class="line">    plt.grid(<span class="literal">True</span>, alpha=<span class="number">0.3</span>)</span><br><span class="line">    plt.show()</span><br></pre></td></tr></table></figure>
<h2 id="Matlab版本"><a href="#Matlab版本" class="headerlink" title="Matlab版本"></a>Matlab版本</h2><figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="params">[x0_hat, future_x0, a, b, mape]</span> = <span class="title">gm11</span><span class="params">(x0, predict_num)</span></span></span><br><span class="line">    <span class="comment">% GM(1,1)灰色预测模型</span></span><br><span class="line">    <span class="comment">% 输入：x0-原始序列（行向量），predict_num-预测步数</span></span><br><span class="line">    <span class="comment">% 输出：x0_hat-原始序列预测值，future_x0-未来预测值，a-发展系数，b-灰作用量，mape-平均相对误差</span></span><br><span class="line">    </span><br><span class="line">    x0 = x0(:);  <span class="comment">% 转为列向量</span></span><br><span class="line">    n = <span class="built_in">length</span>(x0);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">% 1. 累加生成（1-AGO）</span></span><br><span class="line">    x1 = cumsum(x0);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">% 2. 构建矩阵B和Y</span></span><br><span class="line">    B = <span class="built_in">zeros</span>(n<span class="number">-1</span>, <span class="number">2</span>);</span><br><span class="line">    Y = <span class="built_in">zeros</span>(n<span class="number">-1</span>, <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">for</span> <span class="built_in">i</span> = <span class="number">1</span>:n<span class="number">-1</span></span><br><span class="line">        B(<span class="built_in">i</span>, <span class="number">1</span>) = <span class="number">-0.5</span> * (x1(<span class="built_in">i</span>) + x1(<span class="built_in">i</span>+<span class="number">1</span>));</span><br><span class="line">        B(<span class="built_in">i</span>, <span class="number">2</span>) = <span class="number">1</span>;</span><br><span class="line">        Y(<span class="built_in">i</span>) = x0(<span class="built_in">i</span>+<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">% 3. 最小二乘法估计参数</span></span><br><span class="line">    alpha = (B&#x27; * B) \ B&#x27; * Y;</span><br><span class="line">    a = alpha(<span class="number">1</span>);</span><br><span class="line">    b = alpha(<span class="number">2</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">% 4. 时间响应函数与还原预测</span></span><br><span class="line">    x0_hat = <span class="built_in">zeros</span>(n, <span class="number">1</span>);</span><br><span class="line">    x0_hat(<span class="number">1</span>) = x0(<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">for</span> k = <span class="number">2</span>:n</span><br><span class="line">        x1_hat_k = (x0(<span class="number">1</span>) - b/a) * <span class="built_in">exp</span>(-a*(k<span class="number">-1</span>)) + b/a;</span><br><span class="line">        x1_hat_k_1 = (x0(<span class="number">1</span>) - b/a) * <span class="built_in">exp</span>(-a*(k<span class="number">-2</span>)) + b/a;</span><br><span class="line">        x0_hat(k) = x1_hat_k - x1_hat_k_1;</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">% 5. 未来预测</span></span><br><span class="line">    future_x0 = <span class="built_in">zeros</span>(predict_num, <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">for</span> k = <span class="number">1</span>:predict_num</span><br><span class="line">        t = n + k;</span><br><span class="line">        x1_hat_t = (x0(<span class="number">1</span>) - b/a) * <span class="built_in">exp</span>(-a*(t<span class="number">-1</span>)) + b/a;</span><br><span class="line">        x1_hat_t_1 = (x0(<span class="number">1</span>) - b/a) * <span class="built_in">exp</span>(-a*(t<span class="number">-2</span>)) + b/a;</span><br><span class="line">        future_x0(k) = x1_hat_t - x1_hat_t_1;</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">% 6. 计算MAPE</span></span><br><span class="line">    mape = <span class="built_in">mean</span>(<span class="built_in">abs</span>((x0 - x0_hat) ./ x0)) * <span class="number">100</span>;</span><br><span class="line"><span class="keyword">end</span></span><br><span class="line"></span><br><span class="line"><span class="comment">% ---------------------- 测试代码 ----------------------</span></span><br><span class="line">x0 = [<span class="number">500</span>, <span class="number">550</span>, <span class="number">610</span>, <span class="number">680</span>, <span class="number">760</span>];</span><br><span class="line">predict_num = <span class="number">2</span>;</span><br><span class="line">[x0_hat, future_x0, a, b, mape] = gm11(x0, predict_num);</span><br><span class="line"></span><br><span class="line">fprintf(<span class="string">&#x27;模型参数：a=%.4f, b=%.4f\n&#x27;</span>, a, b);</span><br><span class="line">fprintf(<span class="string">&#x27;平均相对误差MAPE：%.2f%%\n&#x27;</span>, mape);</span><br><span class="line">fprintf(<span class="string">&#x27;原始序列预测值：&#x27;</span>);</span><br><span class="line"><span class="built_in">disp</span>(x0_hat&#x27;);</span><br><span class="line">fprintf(<span class="string">&#x27;2023年预测GDP：%.2f亿元\n&#x27;</span>, future_x0(<span class="number">1</span>));</span><br><span class="line">fprintf(<span class="string">&#x27;2024年预测GDP：%.2f亿元\n&#x27;</span>, future_x0(<span class="number">2</span>));</span><br><span class="line"></span><br><span class="line"><span class="comment">% 可视化</span></span><br><span class="line">years = <span class="number">2018</span>:<span class="number">2022</span>;</span><br><span class="line">predict_years = <span class="number">2023</span>:<span class="number">2024</span>;</span><br><span class="line"><span class="built_in">plot</span>(years, x0, <span class="string">&#x27;o-&#x27;</span>, <span class="string">&#x27;LineWidth&#x27;</span>, <span class="number">2</span>, <span class="string">&#x27;MarkerSize&#x27;</span>, <span class="number">8</span>, <span class="string">&#x27;DisplayName&#x27;</span>, <span class="string">&#x27;原始GDP&#x27;</span>);</span><br><span class="line"><span class="built_in">hold</span> on;</span><br><span class="line"><span class="built_in">plot</span>(years, x0_hat&#x27;, <span class="string">&#x27;s-&#x27;</span>, <span class="string">&#x27;LineWidth&#x27;</span>, <span class="number">2</span>, <span class="string">&#x27;MarkerSize&#x27;</span>, <span class="number">8</span>, <span class="string">&#x27;DisplayName&#x27;</span>, <span class="string">&#x27;拟合GDP&#x27;</span>);</span><br><span class="line"><span class="built_in">plot</span>(predict_years, future_x0&#x27;, <span class="string">&#x27;^-&#x27;</span>, <span class="string">&#x27;LineWidth&#x27;</span>, <span class="number">2</span>, <span class="string">&#x27;MarkerSize&#x27;</span>, <span class="number">8</span>, <span class="string">&#x27;Color&#x27;</span>, <span class="string">&#x27;red&#x27;</span>, <span class="string">&#x27;DisplayName&#x27;</span>, <span class="string">&#x27;预测GDP&#x27;</span>);</span><br><span class="line">xlabel(<span class="string">&#x27;年份&#x27;</span>, <span class="string">&#x27;FontSize&#x27;</span>, <span class="number">12</span>);</span><br><span class="line">ylabel(<span class="string">&#x27;GDP（亿元）&#x27;</span>, <span class="string">&#x27;FontSize&#x27;</span>, <span class="number">12</span>);</span><br><span class="line">title(sprintf(<span class="string">&#x27;GM(1,1)灰色预测模型：某地区GDP预测（MAPE=%.2f%%）&#x27;</span>, mape), <span class="string">&#x27;FontSize&#x27;</span>, <span class="number">14</span>);</span><br><span class="line"><span class="built_in">legend</span>(<span class="string">&#x27;FontSize&#x27;</span>, <span class="number">10</span>);</span><br><span class="line">grid on;</span><br><span class="line"><span class="built_in">hold</span> off;</span><br></pre></td></tr></table></figure>
<h1 id="总结与应用场景"><a href="#总结与应用场景" class="headerlink" title="总结与应用场景"></a>总结与应用场景</h1><p>灰色预测的核心优势是“小样本、贫信息”，在数学建模中常用于以下场景：  </p>
<ol>
<li>短期经济预测（如GDP、居民收入、物价指数）；  </li>
<li>公共卫生预测（如疫情初期确诊人数、疫苗接种需求）；  </li>
<li>工程预测（如设备故障时间、原材料消耗、能耗预测）；  </li>
<li>环境预测（如空气质量指数、污染物浓度短期预测）。</li>
</ol>
<p>在竞赛中，若遇到“数据少、无明确分布、短期预测”需求，可优先考虑GM(1,1)模型，并通过“残差修正+组合模型”提升精度。</p>
]]></content>
      <categories>
        <category>数学建模</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>灰色预测</tag>
      </tags>
  </entry>
</search>

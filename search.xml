<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>2023美赛A题论文阅读</title>
    <url>/2025/12/10/2023%E7%BE%8E%E8%B5%9BA%E9%A2%98%E8%AE%BA%E6%96%87%E9%98%85%E8%AF%BB/</url>
    <content><![CDATA[<p>受干旱影响的植物群落</p>
<span id="more"></span> 
<h1 id="2023美赛A题：干旱下植物的群落演替"><a href="#2023美赛A题：干旱下植物的群落演替" class="headerlink" title="2023美赛A题：干旱下植物的群落演替"></a>2023美赛A题：干旱下植物的群落演替</h1><h2 id="任务简述"><a href="#任务简述" class="headerlink" title="任务简述"></a>任务简述</h2><p>美赛A题通常是“复杂生态&#x2F;物理系统建模”，这篇的核心矛盾是：<strong>干旱胁迫下，植物群落的物种数量、种间关系如何影响其存活能力？</strong></p>
<p>核心任务：</p>
<ol>
<li>建一个能模拟“不规则干旱周期+种间相互作用”的群落演替模型；</li>
<li>找群落能受益的“最小物种数”，以及物种数增加的边际效应；</li>
<li>分析污染、栖息地缩减这些人类活动的影响；</li>
<li>给保护群落的实际措施提建议。</li>
</ol>
<h2 id="核心模型：用“生态位宽度”量化群落状态"><a href="#核心模型：用“生态位宽度”量化群落状态" class="headerlink" title="核心模型：用“生态位宽度”量化群落状态"></a>核心模型：用“生态位宽度”量化群落状态</h2><p>论文的灵魂是 <strong>“基于生态位理论的动态模型”</strong> ——把抽象的“群落演替”转化为“物种生态位宽度的变化”（生态位宽度越宽，物种资源利用能力越强）。</p>
<h3 id="基本假设"><a href="#基本假设" class="headerlink" title="基本假设"></a>基本假设</h3><ul>
<li>封闭系统：没新物种迁入，只看现有物种的相互作用；</li>
<li>种间关系稳定：竞争&#x2F;互利关系不会随时间变；</li>
<li>干旱敏感性固定：每个物种的“怕旱程度”是定值。</li>
</ul>
<h3 id="核心方程"><a href="#核心方程" class="headerlink" title="核心方程"></a>核心方程</h3><p>论文把“繁殖、死亡、随机干扰”三个因素揉进了微分方程，核心公式：<br>$$<br>\frac{dl_i(t)}{dt}&#x3D;g_i l_i(t)\left(1-\frac{l_i(t)+\sum_{j\neq i} c_{ij} l_j(t)}{K(t)}\right)-f(dr) \beta_i l_i(t)+\sigma_i(t)<br>$$<br>简理解一下各项：</p>
<ul>
<li><strong>繁殖增益</strong>：物种自身繁殖+种间关系（竞争会抑制、互利会促进）；</li>
<li><strong>死亡损耗</strong>：干旱越严重，死亡率越高（靠<code>dr</code>（干旱系数）控制）；</li>
<li><strong>随机干扰</strong>：模拟突发气候波动（比如突然降温）。</li>
</ul>
<h3 id="模拟不规则干旱"><a href="#模拟不规则干旱" class="headerlink" title="模拟不规则干旱"></a>模拟不规则干旱</h3><p>要让模型贴近现实，得先生成“随时间变化的干旱系数<code>dr</code>”——论文用了一个专门的算法：</p>
<p>核心逻辑</p>
<ol>
<li>先定规则：“上次干旱间隔越久，这次干旱概率越高”（符合现实气候规律）；</li>
<li>随机判断：每个月使用随机数，看是否触发干旱；</li>
<li>干旱期：一旦触发，持续10个月左右，<code>dr</code>维持在0.7左右（中度干旱）；</li>
<li>非干旱期：<code>dr</code>固定0.2（轻度&#x2F;无干旱）。</li>
</ol>
<p>最终会得到一个“100个月的<code>dr</code>序列”，作为模型的“环境驱动因子”。</p>
<h2 id="不同物种数，群落表现差异"><a href="#不同物种数，群落表现差异" class="headerlink" title="不同物种数，群落表现差异"></a>不同物种数，群落表现差异</h2><p>论文用同一组<code>dr</code>序列，测了1&#x2F;2&#x2F;3&#x2F;4种物种的群落，结果很直观：</p>
<table>
<thead>
<tr>
<th>物种数</th>
<th>生态位宽度变化</th>
<th>群落状态</th>
</tr>
</thead>
<tbody><tr>
<td>1种</td>
<td>持续下降到0</td>
<td>直接灭绝</td>
</tr>
<tr>
<td>2种</td>
<td>剧烈波动</td>
<td>无法稳定存活</td>
</tr>
<tr>
<td>3种</td>
<td>波动上升</td>
<td>靠种间互利维持稳定</td>
</tr>
<tr>
<td>4种</td>
<td>上升后趋于平缓</td>
<td>物种太多导致生态位重叠，竞争加剧</td>
</tr>
</tbody></table>
<p>结论：<strong>群落受益的最小物种数是3种</strong>，多了反而会“内耗”。</p>
<h2 id="额外影响"><a href="#额外影响" class="headerlink" title="额外影响"></a>额外影响</h2><p>论文还测了“人类活动干扰”的场景，核心是通过两个方式削弱群落：</p>
<ol>
<li>压缩生态位上限：污染越重、栖息地越小，物种能利用的资源越少；</li>
<li>加剧种间竞争：资源不够时，物种从“协作”变“抢资源”。</li>
</ol>
<p>结果是“叠加式打击”：</p>
<ul>
<li>轻度干扰：生态位小幅下降，但群落能活；</li>
<li>重度干扰：生态位快速崩塌，70%物种灭绝，只剩极耐旱的“独苗”。</li>
</ul>
<h2 id="代码复现"><a href="#代码复现" class="headerlink" title="代码复现"></a>代码复现</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> scipy.integrate <span class="keyword">import</span> odeint</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> random</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">b=<span class="number">0.04</span></span><br><span class="line">n=<span class="number">100</span></span><br><span class="line">dr=[<span class="number">0.2</span>]*<span class="number">200</span></span><br><span class="line">K=<span class="number">10</span></span><br><span class="line">sig=<span class="number">0.2</span></span><br><span class="line">noise = np.random.normal(<span class="number">0</span>, sig, n+<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">g_max=[<span class="number">0.23</span>,<span class="number">0.15</span>,<span class="number">0.33</span>]</span><br><span class="line">gama=[<span class="number">0.5</span>,<span class="number">0.7</span>,<span class="number">0.2</span>]</span><br><span class="line">beta=[<span class="number">0.1</span>]*<span class="number">3</span></span><br><span class="line"></span><br><span class="line">C=[</span><br><span class="line">    [<span class="number">0</span>,-<span class="number">0.2</span>,<span class="number">0.4</span>],</span><br><span class="line">    [-<span class="number">0.2</span>,<span class="number">0</span>,<span class="number">0.6</span>],</span><br><span class="line">    [<span class="number">0.4</span>,<span class="number">0.6</span>,<span class="number">0</span>]</span><br><span class="line">]</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">P</span>(<span class="params">t</span>):</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>-np.exp(-b*t)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">f</span>(<span class="params">d</span>):</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>/(<span class="number">1</span>+np.exp(-<span class="number">12</span>*(d-<span class="number">0.5</span>)))</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">g_dr</span>():</span><br><span class="line">    nt=<span class="number">0</span></span><br><span class="line">    i=<span class="number">0</span></span><br><span class="line">    random.seed(<span class="number">42</span>)</span><br><span class="line">    np.random.seed(<span class="number">42</span>)</span><br><span class="line">    <span class="keyword">while</span> i&lt;=n :</span><br><span class="line">        r=random.random()</span><br><span class="line">        p=P(nt)</span><br><span class="line">        <span class="keyword">if</span> r&lt;=p:</span><br><span class="line">            rd=np.random.randn()</span><br><span class="line">            le=<span class="built_in">round</span>(rd*<span class="number">2</span>+<span class="number">10</span>)</span><br><span class="line">            <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(i,i+le):</span><br><span class="line">                rdj=np.random.randn()</span><br><span class="line">                dr[j]=<span class="number">0.15</span>*rdj+<span class="number">0.6</span></span><br><span class="line">            i=i+le</span><br><span class="line">            nt=<span class="number">0</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            dr[i]=dr[<span class="number">0</span>]</span><br><span class="line">            nt+=<span class="number">1</span></span><br><span class="line">            i+=<span class="number">1</span></span><br><span class="line">    <span class="keyword">pass</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">model</span>(<span class="params">l,t</span>):</span><br><span class="line">    ans=[]</span><br><span class="line">    t_idx = <span class="built_in">int</span>(np.<span class="built_in">round</span>(t))</span><br><span class="line">    t_idx = np.clip(t_idx, <span class="number">0</span>, n)</span><br><span class="line">    dr_t = dr[t_idx]</span><br><span class="line">    sigma = noise[t_idx]</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">3</span>): </span><br><span class="line">        g=g_max[i]*np.exp(-dr_t*gama[i])</span><br><span class="line">        s=<span class="number">0</span></span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">3</span>):</span><br><span class="line">            <span class="keyword">if</span> j!=i:</span><br><span class="line">                s+=C[i][j]*l[j]</span><br><span class="line">        dldt=g*l[i]*(<span class="number">1</span>-(l[i]+s)/K)-f(dr_t)*beta[i]*l[i]+sigma</span><br><span class="line">        ans.append(dldt)</span><br><span class="line">    <span class="keyword">return</span> ans</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    g_dr()</span><br><span class="line">    l=[<span class="number">2.0</span>]*<span class="number">3</span></span><br><span class="line">    t= np.arange(<span class="number">0</span>, n+<span class="number">1</span>, <span class="number">1</span>)</span><br><span class="line">    res=odeint(model,l,t)</span><br><span class="line">    l1=res[:,<span class="number">0</span>]</span><br><span class="line">    l2=res[:,<span class="number">1</span>]</span><br><span class="line">    l3=res[:,<span class="number">2</span>]</span><br><span class="line">    plt.figure(figsize=(<span class="number">12</span>, <span class="number">8</span>))</span><br><span class="line">        </span><br><span class="line">    ax1 = plt.subplot(<span class="number">211</span>)</span><br><span class="line">    ax1.plot(t, l1, color=<span class="string">&#x27;#2E86AB&#x27;</span>, linewidth=<span class="number">1.5</span>, label=<span class="string">&#x27;Species 1&#x27;</span>)</span><br><span class="line">    ax1.plot(t, l2, color=<span class="string">&#x27;#E63946&#x27;</span>, linewidth=<span class="number">1.5</span>, label=<span class="string">&#x27;Species 2&#x27;</span>)</span><br><span class="line">    ax1.plot(t, l3, color=<span class="string">&quot;#F3E304&quot;</span>, linewidth=<span class="number">1.5</span>, label=<span class="string">&#x27;Species 3&#x27;</span>)</span><br><span class="line">    ax1.set_xlabel(<span class="string">&#x27;Simulation Month&#x27;</span>, fontsize=<span class="number">12</span>)</span><br><span class="line">    ax1.set_ylabel(<span class="string">&#x27;Niche Width&#x27;</span>, fontsize=<span class="number">12</span>)</span><br><span class="line">    ax1.set_title(<span class="string">&#x27;Niche Width Change of 3 Species (100 Months)&#x27;</span>, fontsize=<span class="number">14</span>)</span><br><span class="line">    ax1.legend(loc=<span class="string">&#x27;upper right&#x27;</span>)</span><br><span class="line">    ax1.grid(alpha=<span class="number">0.3</span>)</span><br><span class="line">    ax1.set_ylim(<span class="number">0</span>, K+<span class="number">1</span>)</span><br><span class="line">    plt.show()</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>根据前面的干旱模拟算法求出<code>dr</code>，再解微分方程得到各物种生态位变化情况。<br>下面的其他问题都是在这个代码框架下修改。</p>
<h2 id="论文值得学的点"><a href="#论文值得学的点" class="headerlink" title="论文值得学的点"></a>论文值得学的点</h2><ol>
<li><strong>闭环思维</strong>：从“问题→模型→实验→措施”全程落地；</li>
<li><strong>理论+实际结合</strong>：用经典生态方程（Lotka-Volterra）打底，又加了现实的干旱、污染因子；</li>
<li><strong>对比实验清晰</strong>：控制单一变量（比如固定<code>dr</code>测不同物种数），结论说服力强。</li>
</ol>
]]></content>
      <categories>
        <category>数学建模</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>微分方程</tag>
        <tag>机理分析</tag>
      </tags>
  </entry>
  <entry>
    <title>2023美赛D题论文阅读</title>
    <url>/2025/12/14/2023%E7%BE%8E%E8%B5%9BD%E9%A2%98%E8%AE%BA%E6%96%87%E9%98%85%E8%AF%BB/</url>
    <content><![CDATA[<p>联合国17个可持续发展目标的优先级排序与发展预测</p>
<span id="more"></span>

<h1 id="问题综述"><a href="#问题综述" class="headerlink" title="问题综述"></a>问题综述</h1><h2 id="核心需求"><a href="#核心需求" class="headerlink" title="核心需求"></a>核心需求</h2><p>SDGs并非孤立存在，存在协同关系（相互促进）和权衡关系（资源竞争），论文需解决5大核心问题：</p>
<ul>
<li>构建SDGs间的关系网络（量化目标间关联）；</li>
<li>制定SDGs的优先级排序标准；</li>
<li>基于优先级预测未来10年SDGs发展；</li>
<li>量化COVID-19等突发事件的影响；</li>
<li>拓展模型在企业&#x2F;组织中的应用。</li>
</ul>
<h2 id="整体建模框架"><a href="#整体建模框架" class="headerlink" title="整体建模框架"></a>整体建模框架</h2><ol>
<li>数据预处理 → 关系图模型（揭示SDGs关联）</li>
<li>关系图模型 → 优先级模型（多指标排序）</li>
<li>优先级模型 → 时间香槟塔模型（未来10年预测）</li>
<li>时间香槟塔模型 → 突发事件分析（COVID-19影响）</li>
<li>时间香槟塔模型 → 敏感性分析（验证鲁棒性）</li>
</ol>
<p>核心逻辑：先量化SDGs的“关联强度”，再基于关联确定“优先级”，最后按优先级和关联动态预测“发展路径”，并验证模型可靠性。</p>
<h1 id="数据处理"><a href="#数据处理" class="headerlink" title="数据处理"></a>数据处理</h1><p>本题的一个难点是查找数据并处理。也有的队伍使用层次分析法等主观方法去评价各个指标之间的关联性，但是结果一定不如找的客观数据的分析。</p>
<h2 id="数据选择"><a href="#数据选择" class="headerlink" title="数据选择"></a>数据选择</h2><ul>
<li>数据源：联合国全球SDG数据库（涵盖17个目标的指标与序列数据）；</li>
<li>样本选择：土耳其、墨西哥、印尼（代表三大洲，覆盖不同发展阶段）；</li>
<li>时间范围：2003-2018年（平衡样本长度与数据完整性）；</li>
<li>序列筛选：共选取95个序列（确保覆盖所有SDGs，兼顾样本量与代表性）。</li>
</ul>
<h2 id="数据清洗"><a href="#数据清洗" class="headerlink" title="数据清洗"></a>数据清洗</h2><p>针对数据缺失问题，采用线性回归填补，公式如下：<br>$$b&#x3D;\frac{n \sum_{i&#x3D;1}^{n} x_{i} y_{i}-\left(\sum_{i&#x3D;1}^{n} x_{i}\right)\left(\sum_{i&#x3D;1}^{n} y_{i}\right)}{n \sum_{i&#x3D;1}^{n} x_{i}^{2}-\left(\sum_{i&#x3D;1}^{n} x_{i}\right)^{2}}$$<br>其中 $\hat{y}$ 为缺失值填补结果，x为缺失值对应的年份；若 $\hat{y}&lt;0$ （非负指标），则用0填补。</p>
<h2 id="数据归一化"><a href="#数据归一化" class="headerlink" title="数据归一化"></a>数据归一化</h2><p>为消除量纲差异，将所有指标统一转化为0-1区间，且“越大越好”：</p>
<ul>
<li>正向指标： $I_{i,t,c}’&#x3D;\frac{I_{i,t,c}-min(I_{i,..})}{max(I_{i,..})-min(I_{i,..})}$</li>
<li>负向指标： $I_{i,t,c}’&#x3D;\frac{max(I_{i,..})-I_{i,t,c}}{max(I_{i,..})-min(I_{i,..})}$</li>
</ul>
<h2 id="复现遇到的困难1"><a href="#复现遇到的困难1" class="headerlink" title="复现遇到的困难1"></a>复现遇到的困难1</h2><ul>
<li>要先去了解一下联合国数据库中的数据下载等问题，有一点点麻烦</li>
<li>需要从下载的数据中选择的总指标中，针对各个SDGs找出较有说服力的指标，比较花时间</li>
<li>找出指标后，需要为各个指标确认正向或负向，同样需要时间查找等</li>
<li>其中涉及多种的表格筛选，裁剪等操作，需要时间学习</li>
</ul>
<h1 id="模型1：关系图模型"><a href="#模型1：关系图模型" class="headerlink" title="模型1：关系图模型"></a>模型1：关系图模型</h1><p>量化17个SDGs之间的协同&#x2F;权衡关系，为优先级排序提供基础指标。</p>
<h2 id="图结构构建"><a href="#图结构构建" class="headerlink" title="图结构构建"></a>图结构构建</h2><p>用无向图 $G(V,E,W)$ 表示SDGs关系：</p>
<ul>
<li>节点 $V_i$：第$i$个SDG目标；</li>
<li>边 $E_{ij}$：目标$i$与$j$的关联（用Spearman相关系数衡量）；</li>
<li>权重 $w_{ij}$：关联强度（正向&#x3D;协同，负向&#x3D;权衡）。</li>
</ul>
<p>小细节：</p>
<pre><code>这里涉及到Spearman和Pearson的选择  
皮尔逊相关性Pearson只能评估两个连续变量之间的线性相关，就是最开始学的线性相关的检验r。  
而Spearman相关性可以评估两个变量之间的单调关系——有序，它基于每个变量的排名值而不是原始数据。  
总之，就是Pearson 适用于两个变量之间的线性关系，而Spearman不仅适用线性关系，还适用于单调关系
</code></pre>
<h2 id="图度量：节点重要性指标"><a href="#图度量：节点重要性指标" class="headerlink" title="图度量：节点重要性指标"></a>图度量：节点重要性指标</h2><p>用3类中心性（分别计算正向边&#x2F;负向边）量化目标的“网络影响力”，共6个指标：</p>
<ul>
<li>介数中心性（BC）： $BC(i)&#x3D;\sum_{\substack{j&lt;k \ j \neq i \neq k}} \frac{n_i(j,k)}{n(j,k)}$（衡量节点的“桥梁作用”）；</li>
<li>接近中心性（CC）： $CC(i)&#x3D;\frac{1}{\sum_{j \neq i} d(i,j)}$（衡量节点的“可达性”）；</li>
<li>特征向量中心性（EC）： $EC(i)&#x3D;\alpha \sum_{j&#x3D;1}^{n} r_{ij} \cdot EC(j)$（衡量节点的“核心辐射力”）。</li>
</ul>
<h2 id="论文核心发现1"><a href="#论文核心发现1" class="headerlink" title="论文核心发现1"></a>论文核心发现1</h2><ul>
<li>多数SDGs呈正相关（协同关系），符合联合国设定目标的一致性；</li>
<li>Goal7（廉价清洁能源）与多数目标呈负相关（技术投入大、资源竞争激烈）。</li>
</ul>
<h1 id="模型2：优先级模型"><a href="#模型2：优先级模型" class="headerlink" title="模型2：优先级模型"></a>模型2：优先级模型</h1><h2 id="核心目的"><a href="#核心目的" class="headerlink" title="核心目的"></a>核心目的</h2><p>基于关系图模型的6个中心性指标，客观排序17个SDGs的优先级。</p>
<h2 id="方法组合：熵权法（EWM）-加权TOPSIS"><a href="#方法组合：熵权法（EWM）-加权TOPSIS" class="headerlink" title="方法组合：熵权法（EWM）+ 加权TOPSIS"></a>方法组合：熵权法（EWM）+ 加权TOPSIS</h2><ul>
<li>熵权法：确定6个指标的客观权重（避免主观偏差）；</li>
<li>TOPSIS（加权）：基于权重计算目标与“最优解”“最劣解”的距离，排序优先级。</li>
</ul>
<p><del>点击蓝字来看熵权法和TOPSIS细节</del></p>
<h2 id="论文核心发现2"><a href="#论文核心发现2" class="headerlink" title="论文核心发现2"></a>论文核心发现2</h2><ul>
<li>普适性结论：Goal3（健康）和Goal4（教育）在三个国家均进入前五，是全球普适性高优先级目标；</li>
<li>国别差异：土耳其优先Goal17（伙伴关系）和Goal9（产业创新），印尼&#x2F;墨西哥优先Goal3（健康）。</li>
</ul>
<h1 id="模型3：时间香槟塔模型"><a href="#模型3：时间香槟塔模型" class="headerlink" title="模型3：时间香槟塔模型"></a>模型3：时间香槟塔模型</h1><h2 id="核心目的-1"><a href="#核心目的-1" class="headerlink" title="核心目的"></a>核心目的</h2><p>基于优先级和SDGs关联，预测未来10年发展进度（资源动态分配）。</p>
<h2 id="设计灵感"><a href="#设计灵感" class="headerlink" title="设计灵感"></a>设计灵感</h2><p>香槟塔的“上层满溢后流入下层”——资源优先分配给高优先级目标，完成后资源流向关联目标，同时维护已完成目标。</p>
<h2 id="核心假设"><a href="#核心假设" class="headerlink" title="核心假设"></a>核心假设</h2><p>所有SDGs的“完成难度相同”（1%完成度的资源投入等价），简化量化复杂度。</p>
<h2 id="关键变量与公式"><a href="#关键变量与公式" class="headerlink" title="关键变量与公式"></a>关键变量与公式</h2><ul>
<li>完成率：$G_t&#x3D;[g_t(1),g_t(2),…,g_t(17)]$（$g_t(i)$为第$t$年第$i$个目标的完成百分比）；</li>
<li>发展队列：$S_{t+1}&#x3D;S_t \cup S_t’$（$S_t’$为与当前队列关联的目标集合）；</li>
<li>协同影响：$v_t(i)&#x3D;\frac{1}{C} \sum_{\substack{j \ V_j \in S_t}} w_{ij}(v_{t-1}(j)+d_{t-1}(j))$（$C&#x3D;5$基于帕累托原则）；</li>
<li>资源分配：$d_t(i)&#x3D;\begin{cases}0, if v_t(i)+g_{t-1}(i)≥1 \ min(1-v_t(i)-g_{t-1}(i), D-\sum_{s(j)&gt;s(i)} max(0,1-v_t(j)-g_{t-1}(j))), else \end{cases}$；</li>
<li>完成率更新：$g_t(i)&#x3D;min(g_{t-1}(i)+d_t(i)+v_t(i),1)$。</li>
</ul>
<h2 id="核心结果（以印尼为例）"><a href="#核心结果（以印尼为例）" class="headerlink" title="核心结果（以印尼为例）"></a>核心结果（以印尼为例）</h2><ul>
<li>无突发事件：2033年完成5个目标（Goal3&#x2F;4&#x2F;6&#x2F;9&#x2F;13），整体完成率82%（比当前计划高4%）；</li>
<li>创新建议：新增Goal18（国际合作），利用已完成目标的协同效应帮助其他国家。</li>
</ul>
<h1 id="扩展分析：突发事件与敏感性检验"><a href="#扩展分析：突发事件与敏感性检验" class="headerlink" title="扩展分析：突发事件与敏感性检验"></a>扩展分析：突发事件与敏感性检验</h1><h2 id="1-突发事件影响分析（COVID-19为例）"><a href="#1-突发事件影响分析（COVID-19为例）" class="headerlink" title="1. 突发事件影响分析（COVID-19为例）"></a>1. 突发事件影响分析（COVID-19为例）</h2><ul>
<li>新增参数：威胁系数 $\gamma_i$（衡量目标受疫情威胁程度）、缓解&#x2F;加剧系数 $MI_i$（衡量目标对疫情的影响）；</li>
<li>模型调整：资源分配公式加入 $\gamma_i$，优先级指标加入 $MI_i$；</li>
<li>结果：印尼2033年仅完成3个目标，整体完成率78%（比无疫情低4%）。</li>
</ul>
<h2 id="2-敏感性分析（验证鲁棒性）"><a href="#2-敏感性分析（验证鲁棒性）" class="headerlink" title="2. 敏感性分析（验证鲁棒性）"></a>2. 敏感性分析（验证鲁棒性）</h2><ul>
<li>分析对象：超参数$C$（协同效率常数）和$D$（年总资源）；</li>
<li>测试范围：$C$取4-6（步长0.1），$D$取0.102-0.136；</li>
<li>结果：$C$的最大影响幅度0.041，$D$的最大影响幅度0.028，模型对超参数变化不敏感，鲁棒性强。</li>
</ul>
<h1 id="代码复现"><a href="#代码复现" class="headerlink" title="代码复现"></a>代码复现</h1><h2 id="图结构构建（得到相关性矩阵）"><a href="#图结构构建（得到相关性矩阵）" class="headerlink" title="图结构构建（得到相关性矩阵）"></a>图结构构建（得到相关性矩阵）</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">import</span> seaborn <span class="keyword">as</span> sns</span><br><span class="line"><span class="keyword">from</span> sklearn.linear_model <span class="keyword">import</span> LinearRegression</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> r2_score</span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"></span><br><span class="line">PATH = <span class="string">&quot;H:/数模/2023美赛D/rdata.xlsx&quot;</span></span><br><span class="line">COUNTRY = <span class="string">&quot;Türkiye&quot;</span>  <span class="comment"># 替换国家名运行</span></span><br><span class="line">st_y = <span class="number">2003</span></span><br><span class="line">ed_y = <span class="number">2018</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 输出路径</span></span><br><span class="line">script_dir = os.path.dirname(os.path.abspath(__file__))</span><br><span class="line">corr_matrix_path = os.path.join(script_dir, <span class="string">f&quot;<span class="subst">&#123;COUNTRY&#125;</span>_SDG相关性矩阵.csv&quot;</span>)</span><br><span class="line">heatmap_path = os.path.join(script_dir, <span class="string">f&quot;<span class="subst">&#123;COUNTRY&#125;</span>_SDG相关性热力图.png&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># -------------------------- 1. 读取数据 &amp; 筛选范围 --------------------------</span></span><br><span class="line">df = pd.read_excel(PATH)</span><br><span class="line">df = df[(df[<span class="string">&quot;GeoAreaName&quot;</span>] == COUNTRY) &amp; (df[<span class="string">&quot;Time&quot;</span>] &gt;= st_y) &amp; (df[<span class="string">&quot;Time&quot;</span>] &lt;= ed_y)].copy()</span><br><span class="line"></span><br><span class="line"><span class="comment"># -------------------------- 2. 预处理 --------------------------</span></span><br><span class="line">df[<span class="string">&quot;Value&quot;</span>] = pd.to_numeric(df[<span class="string">&quot;Value&quot;</span>], errors=<span class="string">&quot;coerce&quot;</span>)  <span class="comment"># 处理非数值为NaN</span></span><br><span class="line">df[<span class="string">&quot;is_valid&quot;</span>] = ~df[<span class="string">&quot;Value&quot;</span>].isna()  <span class="comment"># 标记真实数据</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># -------------------------- 3. 负向指标正向化 --------------------------</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">forward_transform</span>(<span class="params">group</span>):</span><br><span class="line">    max_val = group[<span class="string">&quot;Value&quot;</span>].<span class="built_in">max</span>()</span><br><span class="line">    group[<span class="string">&quot;Value_forward&quot;</span>] = np.where(group[<span class="string">&quot;sign&quot;</span>] == <span class="string">&quot;-&quot;</span>, max_val - group[<span class="string">&quot;Value&quot;</span>], group[<span class="string">&quot;Value&quot;</span>])</span><br><span class="line">    <span class="keyword">return</span> group</span><br><span class="line"></span><br><span class="line">df = df.groupby(<span class="string">&quot;SeriesDescription&quot;</span>).apply(forward_transform).reset_index(level=<span class="number">0</span>, drop=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># -------------------------- 4. 线性回归填充 --------------------------</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">linear_reg_fill</span>(<span class="params">group</span>):</span><br><span class="line">    indicator = group[<span class="string">&quot;SeriesDescription&quot;</span>].iloc[<span class="number">0</span>]</span><br><span class="line">    valid_cnt = group[<span class="string">&quot;is_valid&quot;</span>].<span class="built_in">sum</span>()</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 真实数据&lt;3个标记为无效</span></span><br><span class="line">    <span class="keyword">if</span> valid_cnt &lt; <span class="number">3</span>:</span><br><span class="line">        group[<span class="string">&quot;Value_filled&quot;</span>] = np.nan</span><br><span class="line">        <span class="keyword">return</span> group</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 分离真实数据拟合回归</span></span><br><span class="line">    valid_data = group[group[<span class="string">&quot;is_valid&quot;</span>]].copy()</span><br><span class="line">    X = valid_data[<span class="string">&quot;Time&quot;</span>].values.reshape(-<span class="number">1</span>, <span class="number">1</span>)</span><br><span class="line">    y = valid_data[<span class="string">&quot;Value_forward&quot;</span>].values</span><br><span class="line">    model = LinearRegression()</span><br><span class="line">    model.fit(X, y)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 预测所有年份</span></span><br><span class="line">    all_X = group[<span class="string">&quot;Time&quot;</span>].values.reshape(-<span class="number">1</span>, <span class="number">1</span>)</span><br><span class="line">    all_pred = model.predict(all_X)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 回归结果非负（小于0设为0）</span></span><br><span class="line">    all_pred = np.maximum(all_pred, <span class="number">0</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 合并</span></span><br><span class="line">    group[<span class="string">&quot;Value_filled&quot;</span>] = np.where(group[<span class="string">&quot;is_valid&quot;</span>], group[<span class="string">&quot;Value_forward&quot;</span>], all_pred)</span><br><span class="line">    <span class="keyword">return</span> group</span><br><span class="line"></span><br><span class="line"><span class="comment"># 填充 + 过滤无效指标</span></span><br><span class="line">df = df.groupby(<span class="string">&quot;SeriesDescription&quot;</span>).apply(linear_reg_fill).reset_index(level=<span class="number">0</span>, drop=<span class="literal">True</span>)</span><br><span class="line">df = df.dropna(subset=[<span class="string">&quot;Value_filled&quot;</span>])</span><br><span class="line"></span><br><span class="line"><span class="comment"># -------------------------- 5. 归一化 --------------------------</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">normalize</span>(<span class="params">group</span>):</span><br><span class="line">    min_val = group[<span class="string">&quot;Value_filled&quot;</span>].<span class="built_in">min</span>()</span><br><span class="line">    max_val = group[<span class="string">&quot;Value_filled&quot;</span>].<span class="built_in">max</span>()</span><br><span class="line">    <span class="keyword">if</span> max_val - min_val == <span class="number">0</span>:</span><br><span class="line">        group[<span class="string">&quot;Value_norm&quot;</span>] = <span class="number">0.5</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        group[<span class="string">&quot;Value_norm&quot;</span>] = (group[<span class="string">&quot;Value_filled&quot;</span>] - min_val) / (max_val - min_val)</span><br><span class="line">    <span class="keyword">return</span> group</span><br><span class="line"></span><br><span class="line">df = df.groupby(<span class="string">&quot;SeriesDescription&quot;</span>).apply(normalize).reset_index(level=<span class="number">0</span>, drop=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># -------------------------- 6. 聚合 &amp; 相关性分析 --------------------------</span></span><br><span class="line"><span class="comment"># 按Goal+年份聚合（求均值）</span></span><br><span class="line">goal_year_avg = df.groupby([<span class="string">&quot;Goal&quot;</span>, <span class="string">&quot;Time&quot;</span>])[<span class="string">&quot;Value_norm&quot;</span>].mean().reset_index()</span><br><span class="line"></span><br><span class="line">wide_df = goal_year_avg.pivot_table(index=<span class="string">&quot;Time&quot;</span>, columns=<span class="string">&quot;Goal&quot;</span>, values=<span class="string">&quot;Value_norm&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 计算Spearman相关性矩阵</span></span><br><span class="line">corr_matrix = wide_df.corr(method=<span class="string">&quot;spearman&quot;</span>)</span><br><span class="line">corr_matrix.<span class="built_in">round</span>(<span class="number">2</span>).to_csv(corr_matrix_path, encoding=<span class="string">&quot;utf-8-sig&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># -------------------------- 7. 绘制热力图 --------------------------</span></span><br><span class="line">plt.rcParams[<span class="string">&quot;font.sans-serif&quot;</span>] = [<span class="string">&quot;SimHei&quot;</span>]</span><br><span class="line">plt.figure(figsize=(<span class="number">10</span>, <span class="number">8</span>))</span><br><span class="line">sns.heatmap(</span><br><span class="line">    corr_matrix, </span><br><span class="line">    annot=<span class="literal">True</span>, cmap=<span class="string">&quot;RdBu_r&quot;</span>, </span><br><span class="line">    vmin=-<span class="number">1</span>, vmax=<span class="number">1</span>, fmt=<span class="string">&quot;.2f&quot;</span></span><br><span class="line">)</span><br><span class="line">plt.title(<span class="string">f&quot;<span class="subst">&#123;COUNTRY&#125;</span> SDG Goal 相关性热力图（2003-2018）&quot;</span>, fontsize=<span class="number">14</span>)</span><br><span class="line">plt.tight_layout()</span><br><span class="line">plt.savefig(heatmap_path, dpi=<span class="number">300</span>, bbox_inches=<span class="string">&quot;tight&quot;</span>)</span><br><span class="line">plt.close()</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;<span class="subst">&#123;COUNTRY&#125;</span> 分析完成！&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;相关性矩阵已保存：<span class="subst">&#123;corr_matrix_path&#125;</span>&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;热力图已保存：<span class="subst">&#123;heatmap_path&#125;</span>&quot;</span>)</span><br></pre></td></tr></table></figure>
<h2 id="得到中心性指标"><a href="#得到中心性指标" class="headerlink" title="得到中心性指标"></a>得到中心性指标</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> networkx <span class="keyword">as</span> nx</span><br><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"></span><br><span class="line"><span class="comment"># 1. 读取SDG相关性矩阵</span></span><br><span class="line">w = pd.read_csv(</span><br><span class="line">    <span class="string">&quot;H:/数模/2023美赛D/Türkiye_SDG相关性矩阵.csv&quot;</span>,</span><br><span class="line">    index_col=<span class="number">0</span>  </span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">w.columns = w.columns.astype(<span class="built_in">int</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 拆分正负相关子图</span></span><br><span class="line">w_plus = w.where(w &gt; <span class="number">0</span>, <span class="number">0</span>)</span><br><span class="line">w_minus = w.where(w &lt; <span class="number">0</span>, <span class="number">0</span>).<span class="built_in">abs</span>()</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 定义中心性计算函数</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">calculate_centralities_with_fix</span>(<span class="params">G</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    计算图G的3种中心性（介数、接近度、特征向量），并修复孤立节点的报错</span></span><br><span class="line"><span class="string">    返回：包含3个中心性指标的DataFrame</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    <span class="comment"># 1. 介数中心性（考虑边权重）</span></span><br><span class="line">    bc = nx.betweenness_centrality(G, weight=<span class="string">&quot;weight&quot;</span>, normalized=<span class="literal">True</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 2. 接近度中心性（处理孤立节点：无连接时接近度设为0）</span></span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        cc = nx.closeness_centrality(G, distance=<span class="string">&quot;weight&quot;</span>)</span><br><span class="line">    <span class="keyword">except</span> nx.NetworkXError:  <span class="comment"># 存在孤立节点</span></span><br><span class="line">        cc = &#123;node: <span class="number">0.0</span> <span class="keyword">for</span> node <span class="keyword">in</span> G.nodes()&#125;  <span class="comment"># 孤立节点接近度设为0</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 3. 特征向量中心性（处理不连通图：孤立节点设为0）</span></span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        ec = nx.eigenvector_centrality(G, weight=<span class="string">&quot;weight&quot;</span>, tol=<span class="number">1e-6</span>, max_iter=<span class="number">1000</span>)</span><br><span class="line">    <span class="keyword">except</span> nx.NetworkXError:  <span class="comment"># 图不连通时</span></span><br><span class="line">        ec = &#123;node: <span class="number">0.0</span> <span class="keyword">for</span> node <span class="keyword">in</span> G.nodes()&#125;  <span class="comment"># 孤立节点特征向量中心性设为0</span></span><br><span class="line">    </span><br><span class="line">    centralities = pd.DataFrame(&#123;</span><br><span class="line">        <span class="string">&quot;BC&quot;</span>: bc,</span><br><span class="line">        <span class="string">&quot;CC&quot;</span>: cc,</span><br><span class="line">        <span class="string">&quot;EC&quot;</span>: ec</span><br><span class="line">    &#125;)</span><br><span class="line">    <span class="keyword">return</span> centralities</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 4. 计算正负子图的中心性指标</span></span><br><span class="line">G_plus = nx.from_pandas_adjacency(w_plus, create_using=nx.Graph())</span><br><span class="line">centralities_plus = calculate_centralities_with_fix(G_plus)</span><br><span class="line">centralities_plus.columns = [col + <span class="string">&quot;_plus&quot;</span> <span class="keyword">for</span> col <span class="keyword">in</span> centralities_plus.columns]  <span class="comment"># 重命名列（正边）</span></span><br><span class="line"></span><br><span class="line">G_minus = nx.from_pandas_adjacency(w_minus, create_using=nx.Graph())</span><br><span class="line">centralities_minus = calculate_centralities_with_fix(G_minus)</span><br><span class="line">centralities_minus.columns = [col + <span class="string">&quot;_minus&quot;</span> <span class="keyword">for</span> col <span class="keyword">in</span> centralities_minus.columns]  <span class="comment"># 重命名列（负边）</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 5. 合并中心性指标并保存（生成后续优先级分析的输入文件）</span></span><br><span class="line">centralities_df = pd.concat([centralities_plus, centralities_minus], axis=<span class="number">1</span>)</span><br><span class="line">centralities_df.index.name = <span class="string">&quot;SDG目标&quot;</span></span><br><span class="line"></span><br><span class="line">centralities_df.to_csv(</span><br><span class="line">    <span class="string">&quot;SDG中心性指标.csv&quot;</span>,</span><br><span class="line">    encoding=<span class="string">&quot;utf-8-sig&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;\n✅ 中心性指标计算完成，结果维度：&quot;</span>, centralities_df.shape)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;📊 中心性指标前5行：&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(centralities_df.head())</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;\n💾 中心性指标已保存到：SDG中心性指标.csv&quot;</span>)</span><br></pre></td></tr></table></figure>

<h2 id="优先级分析（得到排序结果）"><a href="#优先级分析（得到排序结果）" class="headerlink" title="优先级分析（得到排序结果）"></a>优先级分析（得到排序结果）</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"></span><br><span class="line"><span class="comment"># 1. 数据准备：读取中心性指标（需确保数据格式正确）</span></span><br><span class="line"></span><br><span class="line">df_centrality = pd.read_csv(</span><br><span class="line">    <span class="string">&quot;SDG中心性指标.csv&quot;</span>,  </span><br><span class="line">    index_col=<span class="string">&quot;SDG目标&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 负边中心性正向化</span></span><br><span class="line"></span><br><span class="line">negative_cols = [<span class="string">&quot;BC_minus&quot;</span>, <span class="string">&quot;CC_minus&quot;</span>, <span class="string">&quot;EC_minus&quot;</span>]</span><br><span class="line"><span class="keyword">for</span> col <span class="keyword">in</span> negative_cols:</span><br><span class="line">    df_centrality[col + <span class="string">&quot;_normalized&quot;</span>] = df_centrality[col].<span class="built_in">max</span>() - df_centrality[col]</span><br><span class="line"></span><br><span class="line">normalized_cols = [<span class="string">&quot;BC_plus&quot;</span>, <span class="string">&quot;CC_plus&quot;</span>, <span class="string">&quot;EC_plus&quot;</span>, </span><br><span class="line">                   <span class="string">&quot;BC_minus_normalized&quot;</span>, <span class="string">&quot;CC_minus_normalized&quot;</span>, <span class="string">&quot;EC_minus_normalized&quot;</span>]</span><br><span class="line">df_normalized = df_centrality[normalized_cols].copy()</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;\n✅ 负边中心性正向化完成，正向化后前5行数据：&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(df_normalized.head())</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 熵权法：计算6个指标的客观权重</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">calculate_entropy_weight</span>(<span class="params">data</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    输入：正向化后的指标矩阵（行=SDG目标，列=指标）</span></span><br><span class="line"><span class="string">    输出：各指标的熵权（总和=1）</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    <span class="comment"># Step 1: 标准化</span></span><br><span class="line">    data_std = (data - data.<span class="built_in">min</span>(axis=<span class="number">0</span>)) / (data.<span class="built_in">max</span>(axis=<span class="number">0</span>) - data.<span class="built_in">min</span>(axis=<span class="number">0</span>))</span><br><span class="line">    <span class="comment"># 避免0值导致log(0)错误，替换0为极小值1e-8</span></span><br><span class="line">    data_std = data_std.replace(<span class="number">0</span>, <span class="number">1e-8</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># Step 2: 计算各目标在指标中的占比</span></span><br><span class="line">    p = data_std / data_std.<span class="built_in">sum</span>(axis=<span class="number">0</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># Step 3: 计算信息熵e_j</span></span><br><span class="line">    n = <span class="built_in">len</span>(data)</span><br><span class="line">    e = -np.<span class="built_in">sum</span>(p * np.log(p), axis=<span class="number">0</span>) / np.log(n)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># Step 4: 计算信息效用值d_j和熵权w_j</span></span><br><span class="line">    d = <span class="number">1</span> - e  <span class="comment"># 效用值：熵越小，效用越大</span></span><br><span class="line">    w = d / d.<span class="built_in">sum</span>()  <span class="comment"># 熵权：归一化效用值</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> w, e, d</span><br><span class="line"></span><br><span class="line"><span class="comment"># 执行熵权计算</span></span><br><span class="line">weights, entropy, utility = calculate_entropy_weight(df_normalized)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 整理权重结果</span></span><br><span class="line">weight_df = pd.DataFrame(&#123;</span><br><span class="line">    <span class="string">&quot;指标名称&quot;</span>: normalized_cols,</span><br><span class="line">    <span class="string">&quot;信息熵e_j&quot;</span>: entropy.<span class="built_in">round</span>(<span class="number">4</span>),</span><br><span class="line">    <span class="string">&quot;信息效用值d_j&quot;</span>: utility.<span class="built_in">round</span>(<span class="number">4</span>),</span><br><span class="line">    <span class="string">&quot;熵权w_j&quot;</span>: weights.<span class="built_in">round</span>(<span class="number">4</span>)</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;\n✅ 熵权法计算完成，各指标权重：&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(weight_df)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;🔍 权重总和：&quot;</span>, weights.<span class="built_in">sum</span>().<span class="built_in">round</span>(<span class="number">4</span>))  <span class="comment"># 验证：权重总和应为1</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 4. 加权TOPSIS：计算SDG优先级得分与排序</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">weighted_topsis</span>(<span class="params">data, weights</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    输入：正向化后的指标矩阵、各指标熵权</span></span><br><span class="line"><span class="string">    输出：各SDG目标的TOPSIS得分、排序</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    <span class="comment"># Step 1: 指标标准化</span></span><br><span class="line">    z = data / np.sqrt(np.<span class="built_in">sum</span>(data ** <span class="number">2</span>, axis=<span class="number">0</span>))</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># Step 2: 确定正负理想解</span></span><br><span class="line">    z_plus = z.<span class="built_in">max</span>(axis=<span class="number">0</span>) </span><br><span class="line">    z_minus = z.<span class="built_in">min</span>(axis=<span class="number">0</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># Step 3: 计算加权欧氏距离</span></span><br><span class="line">    D_plus = np.sqrt(np.<span class="built_in">sum</span>(weights * (z_plus - z) ** <span class="number">2</span>, axis=<span class="number">1</span>))  <span class="comment"># 到正理想解距离</span></span><br><span class="line">    D_minus = np.sqrt(np.<span class="built_in">sum</span>(weights * (z_minus - z) ** <span class="number">2</span>, axis=<span class="number">1</span>))<span class="comment"># 到负理想解距离</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># Step 4: 计算优先级得分s_i</span></span><br><span class="line">    s = D_minus / (D_plus + D_minus)</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> s, D_plus, D_minus, z_plus, z_minus</span><br><span class="line"></span><br><span class="line"><span class="comment"># 执行TOPSIS计算</span></span><br><span class="line">scores, D_plus, D_minus, z_plus, z_minus = weighted_topsis(df_normalized, weights)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 整理优先级结果</span></span><br><span class="line">priority_df = pd.DataFrame(&#123;</span><br><span class="line">    <span class="string">&quot;SDG目标&quot;</span>: df_centrality.index,</span><br><span class="line">    <span class="string">&quot;到正理想解距离D+&quot;</span>: D_plus.<span class="built_in">round</span>(<span class="number">4</span>),</span><br><span class="line">    <span class="string">&quot;到负理想解距离D-&quot;</span>: D_minus.<span class="built_in">round</span>(<span class="number">4</span>),</span><br><span class="line">    <span class="string">&quot;优先级得分s_i&quot;</span>: scores.<span class="built_in">round</span>(<span class="number">4</span>),</span><br><span class="line">    <span class="string">&quot;优先级排名&quot;</span>: scores.rank(ascending=<span class="literal">False</span>).astype(<span class="built_in">int</span>)  <span class="comment"># 降序排名：得分高→排名前</span></span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 按排名排序</span></span><br><span class="line">priority_df_sorted = priority_df.sort_values(<span class="string">&quot;优先级排名&quot;</span>).reset_index(drop=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;\n✅ TOPSIS优先级计算完成，SDG优先级排序（前10名）：&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(priority_df_sorted.head(<span class="number">10</span>))</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 5. 结果输出</span></span><br><span class="line"><span class="comment"># 保存完整排序结果</span></span><br><span class="line">priority_df_sorted.to_csv(</span><br><span class="line">    <span class="string">&quot;SDG优先级排序结果.csv&quot;</span>,</span><br><span class="line">    index=<span class="literal">False</span>,</span><br><span class="line">    encoding=<span class="string">&quot;utf-8-sig&quot;</span>  <span class="comment"># 支持中文显示</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 保存指标权重结果</span></span><br><span class="line">weight_df.to_csv(</span><br><span class="line">    <span class="string">&quot;SDG指标熵权结果.csv&quot;</span>,</span><br><span class="line">    index=<span class="literal">False</span>,</span><br><span class="line">    encoding=<span class="string">&quot;utf-8-sig&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;\n💾 结果已保存：&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;1. SDG优先级排序结果.csv（含得分与排名）&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;2. SDG指标熵权结果.csv（含信息熵与权重）&quot;</span>)</span><br></pre></td></tr></table></figure>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>2023 美赛 D 题围绕联合国 17 个可持续发展目标（SDGs）的优先级排序与发展预测展开，核心是量化 SDGs 间的协同 &#x2F; 权衡关系并落地实际预测与应用。<br>论文采用 “数据预处理→关系图模型→优先级模型→时间香槟塔模型→扩展分析” 的递进式建模框架：</p>
<ul>
<li>先通过联合国 SDG 数据库数据构建基于 Spearman 相关系数的 SDGs 关系图，用三类中心性指标量化节点影响力；</li>
<li>再结合熵权法与加权 TOPSIS 实现优先级客观排序，发现健康（Goal3）、教育（Goal4）为全球普适高优先级目标；</li>
<li>随后基于 “资源优先流向高优先级目标” 的香槟塔逻辑，预测未来 10 年发展进度；</li>
<li>最后分析 COVID-19 等突发事件影响并验证模型鲁棒性。</li>
<li>整体模型兼顾客观性与实用性，为 SDGs 资源分配与发展规划提供了可行方案。</li>
</ul>
]]></content>
      <categories>
        <category>数学建模</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>图论</tag>
      </tags>
  </entry>
  <entry>
    <title>CNN卷积神经网络</title>
    <url>/2026/03/18/CNN%E5%8D%B7%E7%A7%AF%E7%A5%9E%E7%BB%8F%E7%BD%91%E7%BB%9C/</url>
    <content><![CDATA[<p>CNN可以处理基础图像</p>
<span id="more"></span>
<pre><code>前置：ANN
</code></pre>
<h1 id="图像"><a href="#图像" class="headerlink" title="图像"></a>图像</h1><h2 id="图像概念"><a href="#图像概念" class="headerlink" title="图像概念"></a>图像概念</h2><p>在计算机中，按照颜色和灰度的多少可以将图像分为四种基本类型。</p>
<ul>
<li><p><strong>二值图像</strong></p>
<p>一幅二值图像的二维矩阵仅由0、1两个值构成，<strong>“0”代表黑色，“1”代白色</strong>。由于每一像素（矩阵中每一元素）取值仅有0、1两种可能，所以计算机中二值图像的数据类型通常为1个二进制位。二值图像通常用于文字、线条图的扫描识别（OCR）和掩膜图像的存储。</p>
</li>
<li><p><strong>灰度图像</strong></p>
<p>灰度图像矩阵元素的取值范围通常为[0，255]。因此其数据类型一般为8位无符号整数的（int8），这就是人们经常提到的256灰度图像。“0”表示纯黑色，“255”表示纯白色，中间的数字从小到大表示由黑到白的过渡色。二值图像可以看成是灰度图像的一个特例。</p>
</li>
<li><p><strong>索引图像</strong></p>
<p>索引图像的文件结构比较复杂，除了<strong>存放图像的二维矩阵</strong>外，还包括一个称之为<strong>颜色索引矩阵MAP的二维数组</strong>。MAP的大小由存放图像的矩阵元素值域决定，如矩阵元素值域为[0，255]，则MAP矩阵的大小为256Ⅹ3，<strong>用MAP&#x3D;[RGB]表示</strong>。<strong>MAP中每一行的三个元素分别指定该行对应颜色的红、绿、蓝单色值，MAP中每一行对应图像矩阵像素的一个灰度值</strong>，如某一像素的灰度值为64，则该像素就与MAP中的第64行建立了映射关系，该像素在屏幕上的实际颜色由第64行的[RGB]组合决定。也就是说，图像在屏幕上显示时，每一像素的颜色由存放在矩阵中该像素的灰度值作为索引通过检索颜色索引矩阵MAP得到。</p>
</li>
<li><p><strong>真彩色RGB图像</strong></p>
<p>RGB图像与索引图像一样都可以用来表示彩色图像。与索引图像一样，它分别用红（R）、绿（G）、蓝（B）三原色的组合来表示每个像素的颜色。但与索引图像不同的是，RGB图像每一个像素的颜色值（由RGB三原色表示）直接存放在图像矩阵中，由于每一像素的颜色需由R、G、B三个分量来表示，M、N分别表示图像的行列数，三个M x N的二维矩阵分别表示各个像素的R、G、B三个颜色分量。RGB图像的数据类型一般为8位无符号整形。</p>
<p>  <strong>注意：通道的顺序是 BGR 而不是 RGB。</strong></p>
<table>
<thead>
<tr>
<th>图像类型</th>
<th>通道数</th>
<th>像素值范围</th>
<th>主要特点</th>
<th>常见用途</th>
</tr>
</thead>
<tbody><tr>
<td><strong>二值图像</strong></td>
<td>1通道</td>
<td>0 或 1</td>
<td>每个像素只有黑与白两种值</td>
<td>形态学操作、二值化、轮廓检测</td>
</tr>
<tr>
<td><strong>灰度图像</strong></td>
<td>1通道</td>
<td>0 到 255</td>
<td>每个像素表示灰度（亮度）</td>
<td>图像预处理、物体检测、人脸识别</td>
</tr>
<tr>
<td><strong>索引图像</strong></td>
<td>1通道</td>
<td>0 到 255（索引）</td>
<td>像素值为颜色表的索引，颜色表决定实际颜色</td>
<td>存储压缩、较少颜色的图像表示</td>
</tr>
<tr>
<td><strong>RGB图像</strong></td>
<td>3通道（R、G、B）</td>
<td>0 到 255</td>
<td>每个像素由红、绿、蓝三个通道组成</td>
<td>普通彩色图像显示、图像处理与分析</td>
</tr>
</tbody></table>
<p>简单的讲：<strong>图像是由像素点组成的，每个像素点的取值范围为: [0, 255] 。像素值越接近于0，颜色越暗，接近于黑色；像素值越接近于255，颜色越亮，接近于白色。</strong></p>
<p>在深度学习中，我们使用的图像大多是彩色图，彩色图由RGB3个通道组成</p>
</li>
</ul>
<h2 id="图像加载"><a href="#图像加载" class="headerlink" title="图像加载"></a>图像加载</h2><p>前面提到，图像其实也就是个矩阵，可以用imshow将numpy的矩阵转换为对应的图像进行显示<br>api：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">img = np.zeros([<span class="number">200</span>, <span class="number">200</span>, <span class="number">3</span>])</span><br><span class="line">plt.imshow(img)</span><br><span class="line">plt.imread(<span class="string">&quot;&quot;</span>) <span class="comment"># 加路径</span></span><br></pre></td></tr></table></figure>

<h1 id="卷积神经网络概述"><a href="#卷积神经网络概述" class="headerlink" title="卷积神经网络概述"></a>卷积神经网络概述</h1><p>卷积神经网络（Convolutional Neural Network，CNN）是一种专门为处理网格结构数据（尤其是图像，二维像素网格）设计的深度学习模型。</p>
<p>在计算机视觉领域, 往往我们输入的图像都很大，使用全连接网络的话，计算的代价较高。另外图像也很难保留原有的特征，导致图像处理的准确率不高。</p>
<p>CNN是<strong>含有卷积层的神经网络</strong>。卷积层的<strong>作用就是用来自动学习、提取图像的特征</strong>。</p>
<p>CNN网络主要由三部分构成：<strong>卷积层、池化层和全连接层</strong>构成：</p>
<ol>
<li><p>卷积层负责提取图像中的局部特征</p>
</li>
<li><p>池化层用来大幅降低参数量级(降维)</p>
</li>
<li><p>全连接层，类似ANN的部分，用来输出想要的结果</p>
</li>
</ol>
<p>完整CNN：</p>
<ul>
<li>输入层<ul>
<li>数据输入层：对数据做一些处理，比如去均值（各维度都减对应维度的均值，使得输入数据各个维度都中心化为0，避免数据过多偏差，影响训练效果）、归一化（把所有的数据都归一到同样的范围）、PCA等等。CNN只对训练集做“去均值”这一步。</li>
</ul>
</li>
<li>隐藏层<ul>
<li>卷积层(CONV)：线性乘积求和，提取图像中的局部特征</li>
<li>激励层(RELU)：ReLU激活函数,输入数据转换成输出数据</li>
<li>池化层(POOL)：取区域平均值或最大值，大幅降低参数量级(降维)</li>
</ul>
</li>
<li>输出层<ul>
<li>全连接层(FC)：接收二维数据集，输出CNN模型预测结果</li>
</ul>
</li>
</ul>
<h2 id="卷积神经网络应用"><a href="#卷积神经网络应用" class="headerlink" title="卷积神经网络应用"></a>卷积神经网络应用</h2><p><strong>图像分类</strong>：最常见的应用，例如识别图片中的物体类别</p>
<p><strong>目标检测</strong>：检测图像中物体的位置和类别</p>
<p><strong>图像分割</strong>：将图像分成多个区域，用于语义分割</p>
<p><strong>人脸识别</strong>：识别图像中的人脸</p>
<p><strong>医学图像分析</strong>：用于检测医学图像中的异常（如癌症检测、骨折检测等）</p>
<p><strong>自动驾驶</strong>：用于识别交通标志、车辆、行人</p>
<h1 id="卷积层"><a href="#卷积层" class="headerlink" title="卷积层"></a>卷积层</h1><p>卷积层（Convolutional Layer）通过卷积操作提取输入数据中的特征（例如图像中的边缘、纹理、形状等）。<br>卷积层利用卷积核（滤波器）对输入进行处理，从而生成特征图（feature map），并且每个卷积层能够提取不同层次的特征，从低级特征（如边缘）到高级特征（如物体的形状）。</p>
<h2 id="卷积计算"><a href="#卷积计算" class="headerlink" title="卷积计算"></a>卷积计算</h2><ol>
<li><p>input 表示输入的图像</p>
</li>
<li><p>filter 表示卷积核, 也叫做滤波器(滤波矩阵)</p>
<ul>
<li>一组固定的权重，因为每个神经元的多个权重固定，所以又可以看做一个恒定的滤波器filter</li>
<li>非严格意义上来讲，一个滤波器可以理解为<strong>一组固定权重的神经元</strong>。多个滤波器叠加便成了卷积层</li>
<li>一个卷积核就是一个神经元</li>
</ul>
</li>
<li><p>input 经过 filter 得到输出为最右侧的图像，该图叫做特征图</p>
</li>
</ol>
<p><strong>卷积运算本质上就是在滤波器和输入数据的局部区域间做点积。</strong><br><img src="/../images/convolution.jpg" alt="卷积核计算示意图"><br>即逐元素相乘，再求和，得到第一个元素。<br>之后卷积核移动一步，继续计算下一个元素…</p>
<p>从结果上来说，左边是图像输入，中间部分就是滤波器filter（带着一组固定权重的神经元），不同的滤波器filter会得到不同的输出数据，比如颜色深浅、轮廓。相当于如果想提取图像的不同特征，则用不同的滤波器filter，提取想要的关于图像的特定信息：颜色深浅或轮廓。</p>
<h2 id="Padding（填充）"><a href="#Padding（填充）" class="headerlink" title="Padding（填充）"></a>Padding（填充）</h2><p>通过上面的卷积计算过程，最终的特征图比原始图像小很多，如果想要改变经过卷积后的图像大小, 可以在<strong>原图周围</strong>添加 Padding 来实现。</p>
<p>Padding（填充）操作用于<strong>在输入特征图的边界周围添加额外像素</strong>（通常为0）。</p>
<p><strong>Padding的主要作用：</strong></p>
<ul>
<li>保持空间维度：如果不使用 padding，每次卷积操作后，特征图的尺寸都会缩小。多次卷积后，特征图会变得非常小，可能会丢失重要的<strong>边缘信息</strong>。Padding可以帮助维持输出特征图的尺寸与输入相同或接近相同。</li>
<li>保留边缘信息：图像边缘的像素在卷积过程中参与的计算次数较少，这意味着边缘信息在特征提取过程中容易丢失。Padding通过在边缘添加额外的像素，增加了边缘像素的参与度，从而更好地保留了边缘信息。</li>
<li>提高性能：Padding有助于避免由于特征图尺寸快速缩小而导致的信息丢失，从而提高模型的性能，尤其是在处理较小的图像或需要进行多层卷积时。</li>
</ul>
<p><strong>Padding的选择：</strong></p>
<ul>
<li><strong>Valid Padding (No Padding):</strong> 不进行任何填充。卷积核只在输入图像的有效区域内滑动。输出尺寸会缩小。适用于不需要保持输出尺寸的场景，或者输入图像足够大，边缘信息丢失不重要的情况。 </li>
<li><strong>Same Padding:</strong> 添加足够的填充，使得输出特征图的尺寸与输入相同。广泛应用于各种CNN架构中，因为它可以保持特征图的尺寸，方便网络设计和计算。 </li>
<li><strong>Full Padding:</strong> 尽可能多地添加填充，使得卷积核的每个元素都至少在输入图像上滑动一次。输出尺寸会增大。较少使用，因为它会增加计算量，并且可能会在边缘引入一些伪影。</li>
</ul>
<h2 id="Stride（步长）"><a href="#Stride（步长）" class="headerlink" title="Stride（步长）"></a>Stride（步长）</h2><p>Stride（步长）指的是<strong>卷积核在图像上滑动时的步伐大小</strong>，即每次卷积时卷积核在图像中向右（或向下）移动的像素数。步长直接影响卷积操作后输出特征图的尺寸，以及计算量和模型的特征提取能力。</p>
<p><strong>Stride的作用:</strong></p>
<ul>
<li>降低计算复杂度：更大的步长意味着卷积核移动的次数更少，从而减少了计算量，并加快了训练和推理速度。</li>
<li>步长越大，生成的特征图尺寸越小。这类似于池化的降维效果。</li>
<li>增大感受野：虽然更大的步长会减小特征图的尺寸，但它同时也会增大每个神经元在输入数据上的感受野。这意味着每个神经元能够捕捉到更大范围的输入信息。</li>
</ul>
<p>Stride的选择：取决于具体的应用场景和网络架构</p>
<ul>
<li><strong>Stride &#x3D; 1:</strong> 这是最常见的设置，尤其是在网络的早期层。它允许保留更多的空间细节。</li>
<li><strong>Stride &gt; 1:</strong> 通常用于减小特征图的尺寸和增大感受野，例如在网络的后期层或需要进行快速降维时。 常见的设置包括 stride&#x3D;2 或 stride&#x3D;4。</li>
</ul>
<h2 id="多通道卷积计算"><a href="#多通道卷积计算" class="headerlink" title="多通道卷积计算"></a>多通道卷积计算</h2><p>实际中的图像都是多个通道组成的<br><img src="/../images/convolutions.jpg" alt="多通道卷积核计算示意图"></p>
<p>计算方法如下：</p>
<ol>
<li>当输入有多个通道(Channel), 例如 RGB 三个通道, 此时要求卷积核需要拥有相同的通道数（图像有多少通道，每个卷积核就有多少通道）.</li>
<li>每个卷积核通道与对应的输入图像的各个通道进行卷积.</li>
<li>将每个通道的卷积结果按位<strong>相加</strong>得到最终的特征图.</li>
</ol>
<h2 id="多卷积核卷积计算"><a href="#多卷积核卷积计算" class="headerlink" title="多卷积核卷积计算"></a>多卷积核卷积计算</h2><p>上面的例子里我们只使用一个卷积核进行特征提取, 实际对图像进行特征提取时, 我们需要使用多个卷积核进行特征提取. 这个多个卷积核可以理解为从不同到的视角、不同的角度对图像特征进行提取.</p>
<p>通过以下例子查看多卷积核卷积计算流程:</p>
<p><img src="/../images/muti_convolutions.jpg" alt="多通道卷积核计算示意图"></p>
<p>可以看到：</p>
<ul>
<li>两个神经元，意味着有两个滤波器</li>
<li>数据窗口每次移动两个步长取3*3的局部数据，即stride&#x3D;2</li>
<li>zero-padding&#x3D;1。输入数据由<code>5*5*3</code>变为<code>7*7*3</code></li>
<li>左边是输入（<strong>7*7*3</strong>中，7*7代表图像的像素&#x2F;长宽，3代表R、G、B 三个颜色通道）</li>
<li>中间部分是两个不同的滤波器Filter w0、Filter w1</li>
<li>最右边则是两个不同的输出</li>
</ul>
<h2 id="特征图大小计算"><a href="#特征图大小计算" class="headerlink" title="特征图大小计算"></a>特征图大小计算</h2><p>输出特征图的大小与以下参数息息相关:</p>
<ol>
<li>size: 卷积核&#x2F;过滤器大小，一般会选择为奇数，比如有 <code>1*1</code>, <code>3*3</code>， <code>5*5</code></li>
<li>Padding: 零填充的方式</li>
<li>Stride: 步长</li>
</ol>
<p>那计算方法如下图所示:</p>
<ol>
<li>输入图像大小: W x W</li>
<li>卷积核大小: F x F</li>
<li>Stride: S</li>
<li>Padding: P</li>
<li>输出图像大小: N x N</li>
</ol>
<p>$$<br>N&#x3D;\frac{W-F+2P}{S}+1<br>$$</p>
<h2 id="PyTorch卷积层API"><a href="#PyTorch卷积层API" class="headerlink" title="PyTorch卷积层API"></a>PyTorch卷积层API</h2><p>在PyTorch中进行卷积的API是：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">conv = nn.Conv2d(in_channels, out_channels, kernel_size, stride, padding)</span><br><span class="line"></span><br><span class="line"><span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">参数说明：</span></span><br><span class="line"><span class="string">in_channels: 输入通道数，RGB图片一般是3</span></span><br><span class="line"><span class="string">out_channels: 输出通道，也可以理解为卷积核kernel的数量</span></span><br><span class="line"><span class="string">kernel_size：卷积核的高和宽设置，一般为3,5,7...</span></span><br><span class="line"><span class="string">stride：卷积核移动的步长</span></span><br><span class="line"><span class="string">    整数stride：表示在所有维度上使用相同的步长 stride=2 表示在水平和垂直方向上每次移动2个像素</span></span><br><span class="line"><span class="string">    元组stride: 允许在不同维度上设置不同的步长 stride=(2, 1) 表示在水平方向上步长为2，在垂直方向上步长为1</span></span><br><span class="line"><span class="string">padding：在四周加入padding的数量，默认补0</span></span><br><span class="line"><span class="string">    padding=0：不进行填充。</span></span><br><span class="line"><span class="string">    padding=1：在每个维度上填充 1 个像素（常用于保持输出尺寸与输入相同 padding=输入形状大小-输出形状大小）。</span></span><br><span class="line"><span class="string">    padding=&#x27;same&#x27;（从 PyTorch 1.9+ 开始支持）：让输出特征图的尺寸与输入保持一致。PyTorch会自动计算需要的填充量。stride必须等于1，不支持跨行，因为计算padding时可能出现小数</span></span><br><span class="line"><span class="string">    padding=kernel_size-1：Full Padding 完全填充</span></span><br><span class="line"><span class="string">&quot;&quot;&quot;</span></span><br></pre></td></tr></table></figure>
<h1 id="池化层"><a href="#池化层" class="headerlink" title="池化层"></a>池化层</h1><p>池化层（Pooling Layer）是用于降低输入数据的空间维度（例如图像的高度和宽度），从而减少计算量、减少内存消耗，并提高模型的鲁棒性。</p>
<p>池化层通常位于卷积层之后，它通过对卷积层输出的特征图进行下采样，保留最重要的特征信息，同时丢弃一些不重要的细节。<br><strong>池化层的主要作用如下:</strong></p>
<ul>
<li><strong>降维和计算量减少</strong>：池化层通过减少特征图的尺寸，从而降低了计算量，特别是在多层网络中，随着层数的增加，池化能够显著减少计算资源的消耗。</li>
<li><strong>提高鲁棒性</strong>：池化操作可以使得特征对小的变换、平移和旋转变得更加不敏感。这样，模型在面对噪声或图像的轻微变化时，依然能够稳定工作。</li>
<li><strong>防止过拟合</strong>：通过池化减少了特征图的大小，减少了模型的复杂度，从而有助于防止过拟合，尤其是在较小的数据集上。</li>
<li><strong>抽象特征</strong>：通过池化层的操作，可以提取更为抽象和高层次的特征，使得网络能够学习到更具泛化能力的表示。</li>
</ul>
<h3 id="池化层计算"><a href="#池化层计算" class="headerlink" title="池化层计算"></a>池化层计算</h3><p><img src="/../images/pooling.jpg" alt="池化计算"></p>
<ul>
<li><p>最大池化(Max Pooling) ：通过池化窗口进行最大池化，<strong>取窗口中的最大值作为输出</strong></p>
</li>
<li><p>平均池化(Avg Pooling) ：<strong>取窗口内的所有值的均值作为输出</strong></p>
</li>
</ul>
<h3 id="多通道池化计算"><a href="#多通道池化计算" class="headerlink" title="多通道池化计算"></a>多通道池化计算</h3><p>在处理多通道输入数据时，池化层对每个输入通道分别池化，而不是像卷积层那样将各个通道的输入相加。这意味着</p>
<blockquote>
<p>池化层的输出和输入的通道数是相等。</p>
</blockquote>
<p><strong>池化只在宽高维度上池化</strong>，<strong>在通道上是不发生池化</strong>（池化前后，多少个通道还是多少个通道）</p>
<h3 id="PyTorch池化层API"><a href="#PyTorch池化层API" class="headerlink" title="PyTorch池化层API"></a>PyTorch池化层API</h3><p>在PyTorch中进行池化的API是：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 最大池化</span></span><br><span class="line">nn.MaxPool2d(kernel_size=<span class="number">2</span>, stride=<span class="number">2</span>, padding=<span class="number">1</span>)</span><br><span class="line"><span class="comment"># 平均池化</span></span><br><span class="line">nn.AvgPool2d(kernel_size=<span class="number">2</span>, stride=<span class="number">1</span>, padding=<span class="number">0</span>)</span><br><span class="line"><span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">参数说明：</span></span><br><span class="line"><span class="string">kernel_size：核的高和宽设置，一般为3,5,7...</span></span><br><span class="line"><span class="string">stride：核移动的步长</span></span><br><span class="line"><span class="string">padding：在四周加入padding的数量，默认补0</span></span><br><span class="line"><span class="string">&quot;&quot;&quot;</span></span><br></pre></td></tr></table></figure>

<h1 id="代码实现"><a href="#代码实现" class="headerlink" title="代码实现"></a>代码实现</h1><p>CNN代码流程与ANN大同小异，仅添加图像处理部分，训练时增加卷积层和池化层。每个卷积层后面都要跟上池化层用于精简信息，降低时间复杂度  </p>
<pre><code>需要注意的是图像数据信息较大，运行时间久
</code></pre>
<h2 id="数据集介绍"><a href="#数据集介绍" class="headerlink" title="数据集介绍"></a>数据集介绍</h2><p>CIFAR-10数据集5万张训练图像、1万张测试图像、10个类别、每个类别有6k个图像，图像大小32×32×3。<br>PyTorch 中的 torchvision.datasets 计算机视觉模块封装了 CIFAR10 数据集, 使用前需要先安装torchvision:</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">pip install torchvision -i https://mirrors.aliyun.com/pypi/simple/</span><br></pre></td></tr></table></figure>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1. 数据集基本信息</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">create_dataset</span>():</span><br><span class="line">    <span class="comment"># 加载数据集:训练集数据和测试数据</span></span><br><span class="line">    <span class="comment"># ToTensor: 将image（一个PIL.Image对象）转换为一个Tensor</span></span><br><span class="line">    train = CIFAR10(root=<span class="string">&#x27;data&#x27;</span>, train=<span class="literal">True</span>, transform=ToTensor())</span><br><span class="line">    valid = CIFAR10(root=<span class="string">&#x27;data&#x27;</span>, train=<span class="literal">False</span>, transform=ToTensor())</span><br><span class="line">    <span class="comment"># 返回数据集结果</span></span><br><span class="line">    <span class="keyword">return</span> train, valid</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    <span class="comment"># 数据集加载</span></span><br><span class="line">    train_dataset, valid_dataset = create_dataset()</span><br><span class="line">    <span class="comment"># 数据集类别</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;数据集类别:&quot;</span>, train_dataset.class_to_idx)</span><br><span class="line">    <span class="comment"># 数据集中的图像数据</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;训练集数据集:&quot;</span>, train_dataset.data.shape)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;测试集数据集:&quot;</span>, valid_dataset.data.shape)</span><br><span class="line">    <span class="comment"># 图像展示</span></span><br><span class="line">    plt.figure(figsize=(<span class="number">2</span>, <span class="number">2</span>))</span><br><span class="line">    plt.imshow(train_dataset.data[<span class="number">1</span>])</span><br><span class="line">    plt.title(train_dataset.targets[<span class="number">1</span>])</span><br><span class="line">    plt.show()</span><br></pre></td></tr></table></figure>
<h2 id="完整代码"><a href="#完整代码" class="headerlink" title="完整代码"></a>完整代码</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"><span class="keyword">import</span> torch.nn <span class="keyword">as</span> nn</span><br><span class="line"><span class="keyword">from</span> torchvision.datasets <span class="keyword">import</span> CIFAR10</span><br><span class="line"><span class="keyword">from</span> torchvision.transforms <span class="keyword">import</span> ToTensor</span><br><span class="line"><span class="keyword">import</span> torch.optim <span class="keyword">as</span> optim</span><br><span class="line"><span class="keyword">from</span> torch.utils.data <span class="keyword">import</span> DataLoader</span><br><span class="line"><span class="keyword">import</span> time</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">from</span> torchsummary <span class="keyword">import</span> summary</span><br><span class="line"></span><br><span class="line">BATCH_SIZE=<span class="number">8</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">create_dataset</span>():</span><br><span class="line">    train = CIFAR10(root=<span class="string">&#x27;data&#x27;</span>, train=<span class="literal">True</span>, download=<span class="literal">True</span>,transform=ToTensor())</span><br><span class="line">    valid = CIFAR10(root=<span class="string">&#x27;data&#x27;</span>, train=<span class="literal">False</span>, download=<span class="literal">True</span>,transform=ToTensor())</span><br><span class="line">    <span class="keyword">return</span> train, valid</span><br><span class="line"></span><br><span class="line"><span class="comment"># 模型构建</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ImageClassification</span>(nn.Module):</span><br><span class="line">    <span class="comment"># 定义网络结构</span></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="built_in">super</span>(ImageClassification, <span class="variable language_">self</span>).__init__()</span><br><span class="line">        <span class="comment"># 定义网络层：卷积层+池化层</span></span><br><span class="line">        <span class="comment"># 第一个卷积层, 输入图像为3通道,输出特征图为6通道,卷积核3*3</span></span><br><span class="line">        <span class="variable language_">self</span>.conv1 = nn.Conv2d(<span class="number">3</span>, <span class="number">6</span>, stride=<span class="number">1</span>, kernel_size=<span class="number">3</span>)</span><br><span class="line">        <span class="comment"># 第一个池化层, 核宽高2*2</span></span><br><span class="line">        <span class="variable language_">self</span>.pool1 = nn.MaxPool2d(kernel_size=<span class="number">2</span>, stride=<span class="number">2</span>)</span><br><span class="line">        <span class="comment"># 第二个卷积层, 输入图像为6通道,输出特征图为16通道,卷积核3*3</span></span><br><span class="line">        <span class="variable language_">self</span>.conv2 = nn.Conv2d(<span class="number">6</span>, <span class="number">16</span>, stride=<span class="number">1</span>, kernel_size=<span class="number">3</span>)</span><br><span class="line">        <span class="comment"># 第二个池化层, 核宽高2*2</span></span><br><span class="line">        <span class="variable language_">self</span>.pool2 = nn.MaxPool2d(kernel_size=<span class="number">2</span>, stride=<span class="number">2</span>)</span><br><span class="line">        <span class="comment"># 全连接层</span></span><br><span class="line">        <span class="comment"># 第一个隐藏层 输入特征576个(一张图像为16*6*6), 输出特征120个</span></span><br><span class="line">        <span class="variable language_">self</span>.linear1 = nn.Linear(<span class="number">576</span>, <span class="number">120</span>)</span><br><span class="line">        <span class="comment"># 第二个隐藏层</span></span><br><span class="line">        <span class="variable language_">self</span>.linear2 = nn.Linear(<span class="number">120</span>, <span class="number">84</span>)</span><br><span class="line">        <span class="comment"># 输出层</span></span><br><span class="line">        <span class="variable language_">self</span>.out = nn.Linear(<span class="number">84</span>, <span class="number">10</span>)</span><br><span class="line">        </span><br><span class="line">	<span class="comment"># 定义前向传播</span></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x</span>):</span><br><span class="line">        <span class="comment"># 卷积+relu+池化</span></span><br><span class="line">        x = torch.relu(<span class="variable language_">self</span>.conv1(x))</span><br><span class="line">        x = <span class="variable language_">self</span>.pool1(x)</span><br><span class="line">        <span class="comment"># 卷积+relu+池化</span></span><br><span class="line">        x = torch.relu(<span class="variable language_">self</span>.conv2(x))</span><br><span class="line">        x = <span class="variable language_">self</span>.pool2(x)</span><br><span class="line">        <span class="comment"># 将特征图做成以为向量的形式：相当于特征向量 全连接层只能接收二维数据集</span></span><br><span class="line">        <span class="comment"># 由于最后一个批次可能不够8，所以需要根据批次数量来改变形状</span></span><br><span class="line">        <span class="comment"># x[8, 16, 6, 6] --&gt; [8, 576] --&gt;8个样本,576个特征</span></span><br><span class="line">        <span class="comment"># x.size(0): 第1个值是样本数 行数</span></span><br><span class="line">        <span class="comment"># -1：第2个值由原始x剩余3个维度值相乘计算得到 列数(特征个数)</span></span><br><span class="line">        x = x.reshape(x.size(<span class="number">0</span>), -<span class="number">1</span>)</span><br><span class="line">        <span class="comment"># 全连接层</span></span><br><span class="line">        x = torch.relu(<span class="variable language_">self</span>.linear1(x))</span><br><span class="line">        x = torch.relu(<span class="variable language_">self</span>.linear2(x))</span><br><span class="line">        <span class="comment"># 返回输出结果</span></span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">self</span>.out(x)</span><br><span class="line">    </span><br><span class="line"><span class="keyword">def</span> <span class="title function_">train</span>(<span class="params">model, train_dataset</span>):</span><br><span class="line">    <span class="comment"># 构建数据加载器</span></span><br><span class="line">    dataloader = DataLoader(train_dataset, batch_size=BATCH_SIZE, shuffle=<span class="literal">True</span>)</span><br><span class="line">    criterion = nn.CrossEntropyLoss() <span class="comment"># 构建损失函数</span></span><br><span class="line">    optimizer = optim.Adam(model.parameters(), lr=<span class="number">1e-3</span>) <span class="comment"># 构建优化方法</span></span><br><span class="line">    epoch = <span class="number">100</span>  <span class="comment"># 训练轮数</span></span><br><span class="line">    <span class="keyword">for</span> epoch_idx <span class="keyword">in</span> <span class="built_in">range</span>(epoch):</span><br><span class="line">        sum_num = <span class="number">0</span>   <span class="comment"># 样本数量</span></span><br><span class="line">        total_loss = <span class="number">0.0</span>  <span class="comment"># 损失总和</span></span><br><span class="line">        correct = <span class="number">0</span>  <span class="comment"># 预测正确样本数</span></span><br><span class="line">        start = time.time()  <span class="comment"># 开始时间</span></span><br><span class="line">        <span class="comment"># 遍历数据进行网络训练</span></span><br><span class="line">        <span class="keyword">for</span> x, y <span class="keyword">in</span> dataloader:</span><br><span class="line">            model.train()</span><br><span class="line">            output = model(x)</span><br><span class="line">            loss = criterion(output, y)  <span class="comment"># 计算损失</span></span><br><span class="line">            optimizer.zero_grad()  <span class="comment"># 梯度清零</span></span><br><span class="line">            loss.backward()  <span class="comment"># 反向传播</span></span><br><span class="line">            optimizer.step()  <span class="comment"># 参数更新</span></span><br><span class="line">            correct += (torch.argmax(output, dim=-<span class="number">1</span>) == y).<span class="built_in">sum</span>()  <span class="comment"># 计算预测正确样本数</span></span><br><span class="line">            <span class="comment"># 计算每次训练模型的总损失值 loss是每批样本平均损失值</span></span><br><span class="line">            total_loss += loss.item()*<span class="built_in">len</span>(y)  <span class="comment"># 统计损失和</span></span><br><span class="line">            sum_num += <span class="built_in">len</span>(y)</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&#x27;epoch:%2s loss:%.5f acc:%.2f time:%.2fs&#x27;</span> %(epoch_idx + <span class="number">1</span>,total_loss / sum_num,correct / sum_num,time.time() - start))</span><br><span class="line">    <span class="comment"># 模型保存</span></span><br><span class="line">    torch.save(model.state_dict(), <span class="string">&#x27;model/image_classification.pth&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">test</span>(<span class="params">valid_dataset</span>):</span><br><span class="line">    <span class="comment"># 构建数据加载器</span></span><br><span class="line">    dataloader = DataLoader(valid_dataset, batch_size=BATCH_SIZE, shuffle=<span class="literal">False</span>)</span><br><span class="line">    <span class="comment"># 加载模型并加载训练好的权重</span></span><br><span class="line">    model = ImageClassification()</span><br><span class="line">    model.load_state_dict(torch.load(<span class="string">&#x27;model/image_classification.pth&#x27;</span>))</span><br><span class="line">    <span class="comment"># 模型切换评估模式, 如果网络模型中有dropout/BN等层, 评估阶段不进行相应操作</span></span><br><span class="line">    model.<span class="built_in">eval</span>()</span><br><span class="line">    <span class="comment"># 计算精度</span></span><br><span class="line">    total_correct = <span class="number">0</span></span><br><span class="line">    total_samples = <span class="number">0</span></span><br><span class="line">    <span class="comment"># 遍历每个batch的数据，获取预测结果，计算精度</span></span><br><span class="line">    <span class="keyword">for</span> x, y <span class="keyword">in</span> dataloader:</span><br><span class="line">        output = model(x)</span><br><span class="line">        total_correct += (torch.argmax(output, dim=-<span class="number">1</span>) == y).<span class="built_in">sum</span>()</span><br><span class="line">        total_samples += <span class="built_in">len</span>(y)</span><br><span class="line">    <span class="comment"># 打印精度</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;Acc: %.2f&#x27;</span> % (total_correct / total_samples))</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    <span class="comment"># 数据集加载</span></span><br><span class="line">    train_dataset, valid_dataset = create_dataset()</span><br><span class="line">    <span class="comment"># # 数据集类别</span></span><br><span class="line">    <span class="comment"># print(&quot;数据集类别:&quot;, train_dataset.class_to_idx)</span></span><br><span class="line">    <span class="comment"># # 数据集中的图像数据</span></span><br><span class="line">    <span class="comment"># print(&quot;训练集数据集:&quot;, train_dataset.data.shape)</span></span><br><span class="line">    <span class="comment"># print(&quot;测试集数据集:&quot;, valid_dataset.data.shape)</span></span><br><span class="line">    <span class="comment"># # 图像展示</span></span><br><span class="line">    <span class="comment"># plt.figure(figsize=(2, 2))</span></span><br><span class="line">    <span class="comment"># plt.imshow(train_dataset.data[1])</span></span><br><span class="line">    <span class="comment"># plt.title(train_dataset.targets[1])</span></span><br><span class="line">    <span class="comment"># plt.show()</span></span><br><span class="line">    <span class="comment"># 模型实例化</span></span><br><span class="line">    <span class="comment"># model = ImageClassification()</span></span><br><span class="line">    <span class="comment"># # 模型训练</span></span><br><span class="line">    <span class="comment"># train(model,train_dataset)</span></span><br><span class="line">    test(valid_dataset)</span><br></pre></td></tr></table></figure>
<h2 id="优化"><a href="#优化" class="headerlink" title="优化"></a>优化</h2><p>CNN网络模型在训练集样本上的准确率远远高于测试集,说明模型产生了过拟合问题,我们把学习率由1e-3修改为1e-4、增加网络参数量和增加dropout正则化</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">ImageClassification</span>(nn.Module):</span><br><span class="line">	<span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self</span>):</span><br><span class="line">		<span class="built_in">super</span>(ImageClassification, <span class="variable language_">self</span>).__init__()</span><br><span class="line">		<span class="variable language_">self</span>.conv1 = nn.Conv2d(<span class="number">3</span>, <span class="number">32</span>, stride=<span class="number">1</span>, kernel_size=<span class="number">3</span>)</span><br><span class="line">		<span class="variable language_">self</span>.pool1 = nn.MaxPool2d(kernel_size=<span class="number">2</span>, stride=<span class="number">2</span>)</span><br><span class="line">		<span class="variable language_">self</span>.conv2 = nn.Conv2d(<span class="number">32</span>, <span class="number">128</span>, stride=<span class="number">1</span>, kernel_size=<span class="number">3</span>)</span><br><span class="line">		<span class="variable language_">self</span>.pool2 = nn.MaxPool2d(kernel_size=<span class="number">2</span>, stride=<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line">		<span class="variable language_">self</span>.linear1 = nn.Linear(<span class="number">128</span> * <span class="number">6</span> * <span class="number">6</span>, <span class="number">2048</span>)</span><br><span class="line">		<span class="variable language_">self</span>.linear2 = nn.Linear(<span class="number">2048</span>, <span class="number">2048</span>)</span><br><span class="line">		<span class="variable language_">self</span>.out = nn.Linear(<span class="number">2048</span>, <span class="number">10</span>)</span><br><span class="line">		<span class="comment"># Dropout层，p表示神经元被丢弃的概率</span></span><br><span class="line">		<span class="variable language_">self</span>.dropout = nn.Dropout(p=<span class="number">0.5</span>)</span><br><span class="line"></span><br><span class="line">	<span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x</span>):</span><br><span class="line">		x = torch.relu(<span class="variable language_">self</span>.conv1(x))</span><br><span class="line">		x = <span class="variable language_">self</span>.pool1(x)</span><br><span class="line">		x = torch.relu(<span class="variable language_">self</span>.conv2(x))</span><br><span class="line">		x = <span class="variable language_">self</span>.pool2(x)</span><br><span class="line">		<span class="comment"># 由于最后一个批次可能不够 32，所以需要根据批次数量来 flatten</span></span><br><span class="line">		x = x.reshape(x.size(<span class="number">0</span>), -<span class="number">1</span>)</span><br><span class="line">		x = torch.relu(<span class="variable language_">self</span>.linear1(x))</span><br><span class="line">		<span class="comment"># dropout正则化</span></span><br><span class="line">		<span class="comment"># 训练集准确率远远高于测试准确率,模型产生了过拟合</span></span><br><span class="line">		x = <span class="variable language_">self</span>.dropout(x)</span><br><span class="line">		x = torch.relu(<span class="variable language_">self</span>.linear2(x))</span><br><span class="line">		x = <span class="variable language_">self</span>.dropout(x)</span><br><span class="line">		<span class="keyword">return</span> <span class="variable language_">self</span>.out(x)</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>深度学习</category>
      </categories>
      <tags>
        <tag>深度学习</tag>
        <tag>张量</tag>
        <tag>神经网络</tag>
        <tag>卷积</tag>
        <tag>池化层</tag>
        <tag>CNN</tag>
      </tags>
  </entry>
  <entry>
    <title>k-means</title>
    <url>/2025/12/18/k-means/</url>
    <content><![CDATA[<p>无监督学习</p>
<span id="more"></span>
<h1 id="聚类算法"><a href="#聚类算法" class="headerlink" title="聚类算法"></a>聚类算法</h1><h2 id="简单概念"><a href="#简单概念" class="headerlink" title="简单概念"></a>简单概念</h2><p>聚类算法是机器学习中<strong>无监督学习</strong>的一种，前面提到的有监督学习，是已经有数据和标签，然后需要我们预测等。而无监督学习，一开始没有标签，需要我们自己发掘数据之间的<strong>相似性</strong><br>衡量样本相似性有许多方法，通常是使用距离。常用的距离有欧式距离，曼哈顿距离，切比雪夫距离等。</p>
<h2 id="简单类别"><a href="#简单类别" class="headerlink" title="简单类别"></a>简单类别</h2><p>按照颗粒度划分：</p>
<ul>
<li>分的类别越多，颗粒度越小，称为细聚类</li>
<li>分的类别越少，颗粒度越大，称为粗聚类</li>
</ul>
<p>按照实现方法，有K-means，层次聚类，DBSCAN等。<br>本文涉及的K-means，也是聚类算法最常用，最普遍的一种。</p>
<h2 id="简单应用"><a href="#简单应用" class="headerlink" title="简单应用"></a>简单应用</h2><p>聚类算法一般在某工程初期，缺少标签时会使用到，使用聚类算法等构建数据标签后，就向监督学习的各算法转换。<br>用户画像，搜索推荐，新闻聚类等都用到了聚类算法。</p>
<h1 id="k-means流程"><a href="#k-means流程" class="headerlink" title="k-means流程"></a>k-means流程</h1><h2 id="核心算法"><a href="#核心算法" class="headerlink" title="核心算法"></a>核心算法</h2><p>k-means作为聚类算法最普遍使用的一种，其流程也是十分简单：</p>
<ol>
<li>事先确定常数k，表示最后的类别数（通常也称为 ‘簇’ ，有几个类别就说几簇）</li>
<li>随机选择k个样本点作为初始聚类中心</li>
<li>逐个计算各个数据点与各聚类中心的距离，该数据点属于距离最近的聚类中心</li>
<li>在分类完成后，计算各类别的<strong>质心</strong>（此次使用所有样本点各指标的平均值）</li>
<li>以质心作为新的聚类中心，重复步骤3，4直至聚类中心不在变化<br>由该流程也可以看出此算法的收敛性，最后各个聚类中心会稳定于某状态。<br>聚类中心不一定是某个确定的样本点，就比如物体的质心可以不在物体上。</li>
</ol>
<h2 id="核心代码"><a href="#核心代码" class="headerlink" title="核心代码"></a>核心代码</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.cluster <span class="keyword">import</span> KMeans</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">from</span> sklearn.datasets <span class="keyword">import</span> make_blobs</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> calinski_harabasz_score  </span><br><span class="line"></span><br><span class="line">x,y=make_blobs(n_samples=<span class="number">100</span>,n_features=<span class="number">2</span>,centers=[[<span class="number">1</span>,<span class="number">1</span>],[<span class="number">2</span>,<span class="number">2</span>],[<span class="number">3</span>,<span class="number">3</span>],[<span class="number">4</span>,<span class="number">4</span>]],cluster_std=[<span class="number">0.4</span>,<span class="number">0.2</span>,<span class="number">0.3</span>,<span class="number">0.1</span>],random_state=<span class="number">23</span>)</span><br><span class="line"></span><br><span class="line">y_pred=KMeans(n_clusters=<span class="number">4</span>,random_state=<span class="number">23</span>).fit_predict(x)</span><br><span class="line"></span><br><span class="line">plt.figure()</span><br><span class="line">plt.scatter(x[:,<span class="number">0</span>],x[:,<span class="number">1</span>],c=y_pred)</span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure>
<p>API参数介绍：<br> <code>make_blobs</code> 用于生成符合高斯分布的二维随机点，可以用 <code>centers</code> 来指定生成的中心，用 <code>cluster_std</code> 来指定方差。<br> <code>KMeans</code> 是核心函数：其中 <code>n_clusters</code> 表示最终分类的簇数</p>
<p>从该函数的参数我们会想到一个问题：如何确定 <code>n_clusters</code> ？<br>就像我们前面提到的，聚类算法的使用往往是因为没有先验的知识，我们有的仅仅是数据，并不知道簇数。所有，我们需要下面的这些方法。</p>
<h1 id="SSE聚类评估指标"><a href="#SSE聚类评估指标" class="headerlink" title="SSE聚类评估指标"></a>SSE聚类评估指标</h1><p>SSE（The sum of squares due to error，误差平方和）是评价聚类算法优劣的指标。这种计算方法想必大家已经见过无数次了，就不在细讲，公式也十分简单：<br>$$<br>\sum_{i&#x3D;1}^k \sum_{p \in C_i}|p-m_i|^2<br>$$<br>式中，</p>
<ul>
<li>$C_i$表示簇</li>
<li>k为聚类中心个数</li>
<li>p表示某个样本内部样本点</li>
<li>m表示质心点</li>
</ul>
<p>易得，SSE越小，表示数据距离各自质心越近，聚类效果越好。<br>SSE不仅仅是用来评估的，更重要的是用于确定簇数。单独的SSE还不够，需要结合‘肘’方法</p>
<h2 id="‘肘’方法"><a href="#‘肘’方法" class="headerlink" title="‘肘’方法"></a>‘肘’方法</h2><p>由SSE定义易得，当k越大，SSE越小。‘肘’方法就是找到一个临界点，在此临界点之前，SSE减小很快，之后SSE减小变慢。<br>从图像来说，就是负斜率快速减少的临界点对应的簇数作为分类的k，图像上就想我们的胳膊，临界点就是在肘部位置。</p>
<h2 id="代码过程"><a href="#代码过程" class="headerlink" title="代码过程"></a>代码过程</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.cluster <span class="keyword">import</span> KMeans</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">from</span> sklearn.datasets <span class="keyword">import</span> make_blobs</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> calinski_harabasz_score</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">SSE</span>():</span><br><span class="line">    sse_list = []</span><br><span class="line"></span><br><span class="line">    x, y = make_blobs(n_samples=<span class="number">1000</span>, n_features=<span class="number">2</span>, centers=[[-<span class="number">1</span>, -<span class="number">1</span>], [<span class="number">0</span>, <span class="number">0</span>], [<span class="number">1</span>, <span class="number">1</span>], [<span class="number">2</span>, <span class="number">2</span>]],cluster_std=[<span class="number">0.4</span>, <span class="number">0.2</span>, <span class="number">0.2</span>, <span class="number">0.2</span>], random_state=<span class="number">22</span>)</span><br><span class="line">    <span class="keyword">for</span> clu_num <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">1</span>, <span class="number">100</span>):</span><br><span class="line">        my_kmeans = KMeans(n_clusters=clu_num, max_iter=<span class="number">100</span>, random_state=<span class="number">0</span>)</span><br><span class="line">        my_kmeans.fit(x)</span><br><span class="line">        sse_list.append(my_kmeans.inertia_ ) <span class="comment"># 获取sse的值</span></span><br><span class="line">    </span><br><span class="line">    plt.figure(figsize=(<span class="number">18</span>, <span class="number">8</span>), dpi=<span class="number">100</span>)</span><br><span class="line">    plt.xticks(<span class="built_in">range</span>(<span class="number">0</span>, <span class="number">100</span>, <span class="number">3</span>), labels=<span class="built_in">range</span>(<span class="number">0</span>, <span class="number">100</span>, <span class="number">3</span>))</span><br><span class="line">    plt.grid()</span><br><span class="line">    plt.title(<span class="string">&#x27;sse&#x27;</span>)</span><br><span class="line">    plt.plot(<span class="built_in">range</span>(<span class="number">1</span>, <span class="number">100</span>), sse_list, <span class="string">&#x27;or-&#x27;</span>)</span><br><span class="line">    plt.show()</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    SSE()</span><br></pre></td></tr></table></figure>
<p>代码非常简单，就是遍历要划分的簇数，然后求出SSE并画图，最后从图像上选择出临界点。</p>
<h1 id="SC轮廓系数"><a href="#SC轮廓系数" class="headerlink" title="SC轮廓系数"></a>SC轮廓系数</h1><p>距离指标，分为内聚和分离。<br>前面的SSE，仅考虑了簇内的距离，即内聚。而没有考虑簇间的距离。<br>下面的SC指标加入的分离的因素<br>公式<br>$$<br>s(i) &#x3D; \frac{b(i) - a(i)}{\max\left{a(i), b(i)\right}}<br>$$<br>其中：</p>
<ul>
<li><p>$a(i)$：<strong>簇内紧密度</strong> → 样本 $i$ 到其所属簇内所有其他样本的<strong>平均欧氏距离</strong>；<br>$$a(i) &#x3D; \frac{1}{n_k - 1} \sum_{j \in C_k, j \neq i} d(i, j)$$<br>$C_k$ 是样本 $i$ 所属的簇，$n_k$ 是簇 $C_k$ 的样本数，$d(i,j)$ 是样本 $i$ 和 $j$ 的欧氏距离。</p>
</li>
<li><p>$b(i)$：<strong>簇间分离度</strong> → 样本 $i$ 到「所有其他簇」的平均距离中<strong>最小的那个值</strong>；<br>$$b(i) &#x3D; \min_{m \neq k} \left{ \frac{1}{n_m} \sum_{j \in C_m} d(i, j) \right}$$<br>$C_m$ 是除 $C_k$ 外的任意簇，$n_m$ 是簇 $C_m$ 的样本数。</p>
</li>
</ul>
<p>整体轮廓系数（SC）<br>所有样本的轮廓系数的算术平均值：<br>$$<br>SC &#x3D; \frac{1}{n} \sum_{i&#x3D;1}^n s(i)<br>$$<br>$n$ 是样本总数。</p>
<h2 id="代码演示"><a href="#代码演示" class="headerlink" title="代码演示"></a>代码演示</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> os </span><br><span class="line">os.environ[<span class="string">&#x27;OMP_NUM_THREADS&#x27;</span>]=<span class="string">&#x27;4&#x27;</span></span><br><span class="line"><span class="keyword">from</span> sklearn.cluster <span class="keyword">import</span> KMeans</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">from</span> sklearn.datasets <span class="keyword">import</span> make_blobs</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> calinski_harabasz_score</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> silhouette_score  <span class="comment"># 核心导入（必须加，否则代码报错）</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">SC</span>():</span><br><span class="line">    sc_list = []   </span><br><span class="line"></span><br><span class="line">    x, y = make_blobs(n_samples=<span class="number">1000</span>, n_features=<span class="number">2</span>, centers=[[-<span class="number">1</span>, -<span class="number">1</span>], [<span class="number">0</span>, <span class="number">0</span>], [<span class="number">1</span>, <span class="number">1</span>], [<span class="number">2</span>, <span class="number">2</span>]],cluster_std=[<span class="number">0.4</span>, <span class="number">0.2</span>, <span class="number">0.2</span>, <span class="number">0.2</span>], random_state=<span class="number">22</span>)</span><br><span class="line">    <span class="keyword">for</span> clu_num <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">2</span>, <span class="number">100</span>):</span><br><span class="line">        my_kmeans = KMeans(n_clusters=clu_num, max_iter=<span class="number">100</span>, random_state=<span class="number">0</span>)</span><br><span class="line">        my_kmeans.fit(x)</span><br><span class="line">        y_pred=my_kmeans.predict(x)</span><br><span class="line">        sc_value=silhouette_score(x,y_pred)</span><br><span class="line">        sc_list.append(sc_value) </span><br><span class="line">    </span><br><span class="line">    plt.figure(figsize=(<span class="number">18</span>, <span class="number">8</span>), dpi=<span class="number">100</span>)</span><br><span class="line">    plt.xticks(<span class="built_in">range</span>(<span class="number">0</span>, <span class="number">100</span>, <span class="number">3</span>), labels=<span class="built_in">range</span>(<span class="number">0</span>, <span class="number">100</span>, <span class="number">3</span>))</span><br><span class="line">    plt.grid()</span><br><span class="line">    plt.title(<span class="string">&#x27;sc&#x27;</span>)</span><br><span class="line">    plt.plot(<span class="built_in">range</span>(<span class="number">2</span>, <span class="number">100</span>), sc_list, <span class="string">&#x27;or-&#x27;</span>)</span><br><span class="line">    plt.show()</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    SC()</span><br></pre></td></tr></table></figure>

<h1 id="CH指标"><a href="#CH指标" class="headerlink" title="CH指标"></a>CH指标</h1><p>CH指标（卡林斯基-哈拉巴斯指数，Calinski-Harabasz Index）是<strong>全局层面</strong>的聚类评估指标，基于「簇间方差」和「簇内方差」的比值，取值越大表示聚类效果越好（簇内越紧凑、簇间越分离）。</p>
<h2 id="公式"><a href="#公式" class="headerlink" title="公式"></a>公式</h2><p>$$<br>CH &#x3D; \frac{\frac{B_k}{k - 1}}{\frac{W_k}{n - k}}<br>$$<br>其中：</p>
<ul>
<li>$k$：聚类的簇数；</li>
<li>$n$：样本总数；</li>
<li>$B_k$：<strong>簇间离均差平方和</strong>（Between-cluster Sum of Squares）；</li>
<li>$W_k$：<strong>簇内离均差平方和</strong>（Within-cluster Sum of Squares）。</li>
</ul>
<h2 id="关键项的计算"><a href="#关键项的计算" class="headerlink" title="关键项的计算"></a>关键项的计算</h2><h5 id="1-簇内离均差平方和-W-k"><a href="#1-簇内离均差平方和-W-k" class="headerlink" title="1. 簇内离均差平方和 $W_k$"></a>1. 簇内离均差平方和 $W_k$</h5><p>所有样本到其所属簇中心的欧氏距离平方和：<br>$$<br>W_k &#x3D; \sum_{m&#x3D;1}^k \sum_{i \in C_m} | x_i - \bar{x}_m |^2<br>$$</p>
<ul>
<li>$\bar{x}_m$：第 $m$ 个簇的中心（该簇所有样本的均值向量）；</li>
<li>$| x_i - \bar{x}_m |^2$：样本 $i$ 到其簇中心的欧氏距离平方。</li>
</ul>
<h5 id="2-簇间离均差平方和-B-k"><a href="#2-簇间离均差平方和-B-k" class="headerlink" title="2. 簇间离均差平方和 $B_k$"></a>2. 簇间离均差平方和 $B_k$</h5><p>各簇中心到<strong>全局样本中心</strong>的加权平方和（权重为簇内样本数）：<br>$$<br>B_k &#x3D; \sum_{m&#x3D;1}^k n_m \cdot | \bar{x}_m - \bar{x} |^2<br>$$</p>
<ul>
<li>$\bar{x}$：所有样本的全局中心（整体均值向量）；</li>
<li>$n_m$：第 $m$ 个簇的样本数；</li>
<li>$| \bar{x}_m - \bar{x} |^2$：第 $m$ 个簇中心到全局中心的欧氏距离平方。</li>
</ul>
<p>CH代码演示与SC类似，不再赘述。</p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><table>
<thead>
<tr>
<th>指标</th>
<th>计算粒度</th>
<th>取值范围</th>
<th>优化目标</th>
</tr>
</thead>
<tbody><tr>
<td>SC（轮廓系数）</td>
<td>先样本→后全局均值</td>
<td>[-1, 1]</td>
<td>趋近1</td>
</tr>
<tr>
<td>CH（卡林斯基-哈拉巴斯）</td>
<td>全局层面</td>
<td>[0, +∞)</td>
<td>越大越好</td>
</tr>
</tbody></table>
<p>注：CH指标对「球形簇」的聚类效果评估更有效，若聚类形状非凸（如环形），CH指标可能失效；SC指标适用性更广，但计算成本高于CH（需计算样本间两两距离）。</p>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>聚类算法</tag>
        <tag>机器学习</tag>
        <tag>k-means</tag>
        <tag>SSE系数</tag>
        <tag>CH轮廓法</tag>
        <tag>SC轮廓法</tag>
        <tag>无监督学习</tag>
      </tags>
  </entry>
  <entry>
    <title>matlab基础</title>
    <url>/2026/01/17/matlab%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<p>部分语法基础</p>
<span id="more"></span>
<h1 id="排序"><a href="#排序" class="headerlink" title="排序"></a>排序</h1><h2 id="一维排序和排名"><a href="#一维排序和排名" class="headerlink" title="一维排序和排名"></a>一维排序和排名</h2><p>默认升序，descend表示降</p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line">v=[<span class="number">5</span>,<span class="number">5</span>,<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>];</span><br><span class="line">[sorted,ind]=<span class="built_in">sort</span>(v,<span class="string">&#x27;descend&#x27;</span>)</span><br><span class="line">mx=sorted(<span class="number">1</span>:<span class="number">3</span>)</span><br><span class="line">id=ind(<span class="number">1</span>:<span class="number">3</span>)<span class="comment">%最大的前3个</span></span><br><span class="line">[~,indd]=<span class="built_in">sort</span>(ind)<span class="comment">%对ind再次找按升序的索引，即可得到原数组各数排名</span></span><br></pre></td></tr></table></figure>
<h2 id="多维"><a href="#多维" class="headerlink" title="多维"></a>多维</h2><p>matlab中使用dim参数表示维度，二维使用最多，<code>dim=1</code> 表示行（默认），从上到下，<code>dim=2</code> 表示列，从左到右</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">a=randi(10,5,6)</span><br><span class="line">sort1=sort(a,1)</span><br><span class="line">sort2=sort(a,2)</span><br><span class="line">sort3=sort(a,2,&#x27;descend&#x27;)</span><br><span class="line">[sorted,ind]=sort(a)</span><br></pre></td></tr></table></figure>
<h2 id="按某列"><a href="#按某列" class="headerlink" title="按某列"></a>按某列</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sorted=sortrows(a,[1,3])</span><br></pre></td></tr></table></figure>
<ul>
<li>二维矩阵，将行看成一个整体，共同排序</li>
<li>这里表示先按第一列，若相同再按照第三列排序（升序），再相同按照原始出现的顺序</li>
</ul>
<p>可以简写为 <code>sorted=sortrows(a)</code> 表示先按照第一列，然后第二列…<br>比如各列是不同的指标，列号越小指标越重要</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[sorted,ind]=sortrows(a,[1,3],&#123;&#x27;descend&#x27;,&#x27;ascend&#x27;&#125;)</span><br></pre></td></tr></table></figure>
<p>也可以先按某列降，再按照某列升等，可以用于负向指标<br>ind 同上<br>这里用 <code>sorted</code> 和 <code>a(ind,:)</code> 结果一样</p>
<h1 id="反转与旋转"><a href="#反转与旋转" class="headerlink" title="反转与旋转"></a>反转与旋转</h1><p>对向量或者矩阵反转</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">flip(a)%上下</span><br><span class="line">flip(a,2)%左右</span><br></pre></td></tr></table></figure>

<p>旋转</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">b=rot90(a,2)</span><br></pre></td></tr></table></figure>
<p>表示逆时针转k*90度，k取负数表示顺时针旋转<br>2·3矩阵操作一次变成3·2</p>
<h1 id="集合操作"><a href="#集合操作" class="headerlink" title="集合操作"></a>集合操作</h1><h2 id="去重"><a href="#去重" class="headerlink" title="去重"></a>去重</h2><p> <code>unique(a)</code> 去重（自动排序）  </p>
<p>可以接受更多参数</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[c,ia,ic]=unique(a)</span><br></pre></td></tr></table></figure>
<p>表示结果在另一个向量中的索引，同前<br>即有 <code>a(ic)==c&amp;&amp;c(ia)==a</code>  </p>
<p>当然也可以不排序，稳定的方法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">c=unique(a,&#x27;stable&#x27;)</span><br></pre></td></tr></table></figure>
<p>按照原顺序</p>
<p>作用于矩阵</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">a = randi(2,5,2)</span><br><span class="line">a(:)</span><br><span class="line">unique(a)</span><br><span class="line">unique(a(:))%二者等价，表示先将a拆为一维列向量后去重</span><br><span class="line">unique(a,&#x27;rows&#x27;,&#x27;stable&#x27;)%表示将行作为整体，同前sortrows</span><br></pre></td></tr></table></figure>

<h2 id="查找"><a href="#查找" class="headerlink" title="查找"></a>查找</h2><p>使用ismember函数判断集合中有没有目标值</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">a=[2,1,2,3,4]</span><br><span class="line">ismember(a,[1,2,3])</span><br></pre></td></tr></table></figure>
<p>前面的a是待查找向量，后面的为目标，<br>返回 <code>1 1 1 1 0</code> 表示只要是值为目标中的一个就为真  </p>
<p>当然也可以用于二维矩阵，效果一样<br>或者将行作为一个整体，如</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">a=[1,2;1,2;1,3;4,5]</span><br><span class="line">ismember(a,[1,2],&#x27;rows&#x27;)</span><br></pre></td></tr></table></figure>
<p>作用同前类似</p>
<h2 id="其他集合运算"><a href="#其他集合运算" class="headerlink" title="其他集合运算"></a>其他集合运算</h2><p>常见的一些运算：</p>
<ul>
<li><code>intersect</code> 表示交集</li>
<li><code>union</code> 表示并集</li>
<li><code>setdiff</code> 表示差集，<code>setdiff(A,B)==A-B</code> </li>
<li><code>setxor</code> 表示异或</li>
</ul>
<h1 id="文件"><a href="#文件" class="headerlink" title="文件"></a>文件</h1><h2 id="读入"><a href="#读入" class="headerlink" title="读入"></a>读入</h2><p>不需要用代码读文件，直接使用导入功能<br>导入时可以选择导入格式，列表名等  </p>
<h2 id="处理"><a href="#处理" class="headerlink" title="处理"></a>处理</h2><p>常用的是将xlsx，csv等导入为table<br>table常用相关函数：</p>
<ul>
<li>summary：直接得到各列最值，均值等</li>
<li>size</li>
<li>head&#x2F;tail：看几个头&#x2F;尾元素</li>
<li>disp：完整打印  </li>
<li>table2array：转为常用的数值矩阵</li>
<li>table2cell：转为元胞，主要是兼容不同类型</li>
<li>rmmissing：样本充足时使用，去除包含NaN的行或列</li>
<li>fillmissing：填报缺失值，可以用均值或线性填报</li>
</ul>
<h2 id="导出"><a href="#导出" class="headerlink" title="导出"></a>导出</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">% 1. 导出清洗后的表格（带表头，方便论文查看）</span><br><span class="line">writetable(tt_clean, &#x27;t1.csv&#x27;, &#x27;Encoding&#x27;, &#x27;GBK&#x27;);</span><br><span class="line"></span><br><span class="line">% 2. 导出数值数组（无表头，用于后续建模计算）</span><br><span class="line">writematrix(table2array(tt_clean), &#x27;t2.csv&#x27;);</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>数学建模</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>matlab</tag>
      </tags>
  </entry>
  <entry>
    <title>TOPSIS法</title>
    <url>/2025/11/30/TOPSIS%E6%B3%95/</url>
    <content><![CDATA[<p>简单的TOPSIS法｡ﾟ(ﾟ´ω&#96;ﾟ)ﾟ｡</p>
<span id="more"></span>
<pre><code>前置知识：最好会层次分析法
</code></pre>
<h1 id="模型引出"><a href="#模型引出" class="headerlink" title="模型引出"></a>模型引出</h1><p>本节的TOPSIS法，同前面的一样，都是是评价类模型。<br>TOPSIS法（Technique for Order Preference by Similarity to Ideal Solution）可翻译为逼近理想解排序法，或简称为优劣解距离法。<br>首先需要引入两个概念：</p>
<ul>
<li>理想解：各维度都达到所有备选方案最优秀的解</li>
<li>反理想解：各维度都达到所有备选方案最差的解</li>
</ul>
<p>比如工厂生产，第一维表示产量，第二维表示耗资。三个工厂（备选方案）为：（100，100），（150，110），（200，300）则理想解为（200，100），反理想解为（100，300）.  </p>
<p>方案排序的规则就是根据与最优解的距离来，同时要尽量远离反理性解。</p>
<h1 id="基本步骤"><a href="#基本步骤" class="headerlink" title="基本步骤"></a>基本步骤</h1><h2 id="1-原始矩阵正向化"><a href="#1-原始矩阵正向化" class="headerlink" title="1.原始矩阵正向化"></a>1.原始矩阵正向化</h2><p>正向化，即将各指标转化为“越大越好”<br>首先将指标分为4种：极大型，极小型，中间型，区间型<br>设 $\widetilde{x_i}$ 为转换后的第i个指标</p>
<ul>
<li>极大型：指标数值越大越好，不用转换</li>
<li>极小型：指标数值越小越好，转换方法：<br>$$ \widetilde{x_i}&#x3D; max(x) - x_i<br>$$</li>
<li>中间型：即数值越接近中间一个最优值 $x_{best}$ 越好：<br>要先计算M：<br>$$ M&#x3D;max(|x_i-x_{best}|)$$<br>$$ \widetilde{x_i}&#x3D; 1 - \frac{|x_i-x_{best}|}{M}<br>$$</li>
<li>区间型:数值处于某个区间 [a,b] 内最好，此时分区间讨论：<br>$$ M&#x3D;max(max(x_i)-b,a-min(x_i))$$<br>$$<br>\widetilde{x_i}&#x3D;<br>\begin{cases}<br>1-\frac{a-x_i}{M} \quad \text{ $x_i$ &lt;a} \<br>1\quad \text{$ a \leq x_i \leq b $} \<br>1-\frac{x_i-b}{M} \quad \text{ $x_i$ &gt;b}<br>\end{cases}<br>$$</li>
</ul>
<h2 id="2-正向化矩阵标准化"><a href="#2-正向化矩阵标准化" class="headerlink" title="2.正向化矩阵标准化"></a>2.正向化矩阵标准化</h2><p>有<strong>n个待评价对象，m个指标</strong>，矩阵元素 $x_{ij}$ 表示第i个对象第j个指标的得分。则：<br> $$ \widetilde{x_{ij}}&#x3D;\frac{x_{ij}}{\sqrt{\sum_{i&#x3D;1}^{n}{x_{ij}^2}}}<br> $$<br>标准化后，默认各指标权重相同，也可以使用层次分析法，熵权法等赋予权重。  </p>
<h2 id="3-计算得分"><a href="#3-计算得分" class="headerlink" title="3.计算得分"></a>3.计算得分</h2><ul>
<li><p>定义正理想解向量 $$Z^+&#x3D;(Z_1^+, Z_2^+, …,Z_m^+)$$<br>其中 $Z_i^+$ 为第i列元素最大值。  </p>
</li>
<li><p>负理想解向量 $$Z^-&#x3D;(Z_1^-, Z_2^-, …,Z_m^-)$$<br>其中 $Z_i^-$ 为第i列元素最小值。  </p>
</li>
<li><p>记权重向量 $w$，$w_j$表示第j个指标的权重。  </p>
</li>
<li><p>第i个对象与正理想解距离：<br>$$D_{i}^+&#x3D;\sqrt{\sum_{j&#x3D;1}^{m}{w_j(Z_i^+-\widetilde{x_{ij}})^2}}<br>$$</p>
</li>
<li><p>第i个对象与负理想解距离：<br>$$D_{i}^-&#x3D;\sqrt{\sum_{j&#x3D;1}^{m}{w_j(Z_i^–\widetilde{x_{ij}})^2}}<br>$$<br>计算原始得分：<br>$$ S_i&#x3D;\frac{D_{i}^-}{D_{i}^++D_{i}^-}<br>$$<br>显然 $S_i\in [0,1]$ ,且 $S_i$ 越大，越接近正理想解。</p>
</li>
</ul>
<p>为便于比较，再将个对象的得分归一化后*100转化为百分制。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>TOPSIS 法是基于 “距离排序” 的客观评价模型，核心逻辑是通过计算各方案与 “正理想解（最优值组合）”“负理想解（最劣值组合）” 的加权距离，量化方案的优劣程度。<br>该模型的优势是兼顾客观数据规律与指标重要性差异，可灵活结合 AHP（主观赋权）或熵权法（客观赋权），适用于多指标、多方案的排序决策（如产品评选、绩效评价、资源分配等）。</p>
]]></content>
      <categories>
        <category>数学建模</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>TOPSIS法</tag>
        <tag>评价类模型</tag>
      </tags>
  </entry>
  <entry>
    <title>ARIMA</title>
    <url>/2025/12/15/ARIMA/</url>
    <content><![CDATA[<p>这么火的ARIMA，来看看( ´•̥̥̥ω•̥̥̥&#96; )</p>
<span id="more"></span>
<pre><code>说在前面：本文讲到的都属于时间序列相关模型，本质任务就是使用过去的数据预测未来的数据。
</code></pre>
<p>篇幅有点长，可以分部分选择阅读</p>
<h1 id="时间序列模型"><a href="#时间序列模型" class="headerlink" title="时间序列模型"></a>时间序列模型</h1><h2 id="简单概念"><a href="#简单概念" class="headerlink" title="简单概念"></a>简单概念</h2><p>从专业定义上来讲：时间序列是按时间顺序排列的数据集合。这些数据可以在任何时间间隔（例如，每秒、每分钟、每小时、每天、每月、每年等）观察和收集。<br>时间序列可以简单理解为一串与时间相关的数字，比如每月的销售额，每年的GDP，每天的气温等。</p>
<h2 id="时间序列分类"><a href="#时间序列分类" class="headerlink" title="时间序列分类"></a>时间序列分类</h2><p>时间序列分为两类：  </p>
<ul>
<li>时期时间序列：结果表示一段时间总和，比如国家今年GDP，就是这一年的总和。</li>
<li>时点时间序列：结果表示目标某一个时刻达到的值，比如今天的温度，这个月你的体重。</li>
</ul>
<p>简单的区分方法：时期时间序列的累加是有意义的，比如2024年和2025年GDP累加，就表示这两年的GDP之和；而时点时间序列的累加是没有意义的，比如今天的气温+明天的气温，或者你这个月的体重+上个月的体重，这些值没有意义。  </p>
<h2 id="单变量和多变量"><a href="#单变量和多变量" class="headerlink" title="单变量和多变量"></a>单变量和多变量</h2><p>单变量时间序列只涉及一个变量随时间的变化。比如上面的每月的销售额变化，只看了销售额这一个变量。<br>既然有单变量，就会有多变量。多变量时间序列描述一个事件多个指标随时间的变化，而且由于是同一个事件，各个指标之间也会有关系。<br>比如出售商品，涉及到销售额，广告费，产品数量等指标，这些指标之间会产生联系。  </p>
<p>从预测结果的角度，单变量的预测只需要考虑该变量前面时间的数据；而多变量中，每一个变量的预测都需要考虑前面时间每个变量的数据，并不是简单的多个单变量组合起来。  </p>
<h2 id="时间序列与机器学习"><a href="#时间序列与机器学习" class="headerlink" title="时间序列与机器学习"></a>时间序列与机器学习</h2><p>从前面可以了解到，时间序列就是通过过去数据来预测未来数据，容易联想到机器学习。机器学习也是从已知的数据训练出模型去预测未知的数据。这样看来，是不是用机器学习就可以解除时间序列的预测问题呢？  </p>
<p>答案是否定的。  </p>
<p>机器学习和时间序列模型的内在不同，</p>
<ul>
<li>对于机器学习，其训练数据直接往往没有联系，只是单纯组合并训练出模型结果。</li>
<li>对于时间序列模型，其数据是严格依赖时间的，因此无法忽略数据之间的联系，其数据之间存在相互影响，需要额外考虑。</li>
</ul>
<p>这里我将机器学习和时间序列模型放在同等地位理解，下面的内容也和机器学习过程有很多相似。</p>
<h2 id="时间序列模型建模过程"><a href="#时间序列模型建模过程" class="headerlink" title="时间序列模型建模过程"></a>时间序列模型建模过程</h2><p>时间序列模型也是预测未知数据的模型，各步骤类似机器学习。</p>
<h3 id="划分数据集"><a href="#划分数据集" class="headerlink" title="划分数据集"></a>划分数据集</h3><p>前面说过，时间序列相关模型，本质任务就是使用过去的数据预测未来的数据。所以训练集一定是用历史数据，而测试集要预测的时间一定在训练集之后。  </p>
<h3 id="训练方式"><a href="#训练方式" class="headerlink" title="训练方式"></a>训练方式</h3><h4 id="单步预测"><a href="#单步预测" class="headerlink" title="单步预测"></a>单步预测</h4><p>仅预测需要的某一个时间点的值，比如通过1月-8月的销售量，预测9月的。<br>这里，我们只关心这一个点的预测结果，而不考虑后面的结果。</p>
<h4 id="多步预测"><a href="#多步预测" class="headerlink" title="多步预测"></a>多步预测</h4><p>预测需要的某一段时间的值，比如通过1月-8月的销售量，预测9月-12月的。<br>这里，我们不只关心一个点的预测结果。很明显，离现在越远的未来越难预测，因此应该要尽量压缩训练集与测试集之间的时间差。<br>一种预测方法是按照时间进行，将先预测到的值作为后面预测的参考。比如使用6-8月预测出9月的值，然后使用7-8月的真实值+9月的预测值预测出10月的，以此类推。<br>当然，对于这样的预测结果，缺陷也是很明显的，因为误差会逐步累计。如果第一步预测有问题，那么后面误差会越来越大。</p>
<h4 id="逐点预测与分布预测"><a href="#逐点预测与分布预测" class="headerlink" title="逐点预测与分布预测"></a>逐点预测与分布预测</h4><p>逐点预测是指在需要的时间点，预测一个最可能的值。这是我们最常见的预测形式，例如在天气预测中，我们可能会预测明天的最高气温为28度。<br>分布预测，顾名思义，即预测需要的点，在该点各个结果可能出现的分别情况。例如在天气预测中，我们可能会预测明天的最高气温为27-29度的可能性为70%，25-26以及30-31的可能性为10%等。</p>
<h2 id="时序任务分类"><a href="#时序任务分类" class="headerlink" title="时序任务分类"></a>时序任务分类</h2><h3 id="Forecasting"><a href="#Forecasting" class="headerlink" title="Forecasting"></a>Forecasting</h3><p>即前面常举例的预测任务，这里的数据一般需要满足：</p>
<ul>
<li>数据样本由时间顺序排列</li>
<li>各个样本之间的时间间隔保持一致</li>
<li>数据样本之间存在关联</li>
<li>过去的样本数据影响未来的预测值</li>
</ul>
<h3 id="Time-Supervision"><a href="#Time-Supervision" class="headerlink" title="Time Supervision"></a>Time Supervision</h3><p>类似机器学习，<br>时序有监督学习包括时间序列回归和时间序列分类两种有监督学习方法（Time Series Regression&#x2F; Time Series Classification），指的是在<strong>多变量</strong>时间序列上，基于时间或时间相关的数据，完成回归或分类任务，这类任务对样本与样本之间的时间顺序、相关等都没有要求、反而更关注特征之间的关系，因此这类任务要求：</p>
<ul>
<li>数据中必须存在除时间之外的其他特征，且特征越多越好</li>
<li>可以完成“去时序化”处理，将时序数据彻底变为一般机器学习数据</li>
<li>这一类任务是学习特征值、预测标签值，并不关心样本。</li>
</ul>
<p>时间序列模型同机器学习一样有很多应用，也有很多实例算法，比如下面即将讲到的：</p>
<h1 id="AR模型"><a href="#AR模型" class="headerlink" title="AR模型"></a>AR模型</h1><h2 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h2><p>AR模型，即自回归模型（Autoregressive Model），是统计学中处理时间序列的常用方法，广泛应用于经济学、信息学、自然现象预测及计算机视觉等领域。  </p>
<h2 id="公式："><a href="#公式：" class="headerlink" title="公式："></a>公式：</h2><p>$$X_t &#x3D; \phi_ 0 + \phi_ 1 X_{t-1} + \phi_ 2 X_{t-2} + \dots + \phi_ p X_{t-p} + \varepsilon_t$$<br>其中， $X_t$ 为t时刻的预测值， $\phi_ 0$ 为常数项， $X_t$ 到 $X_{t-p}$ 为历史数据，阶数 $p$ 表示考虑前p个的历史值，$\phi_ 1$ 到 $\phi_ p$ 表示权重。</p>
<h2 id="重要理解"><a href="#重要理解" class="headerlink" title="重要理解"></a>重要理解</h2><p>AR模型当前的结果一定依赖于过去的积累，因此AR模型相信：一个时间点上的标签值一定是依赖于之前的时间点上的标签值而存在的。</p>
<h1 id="MA模型"><a href="#MA模型" class="headerlink" title="MA模型"></a>MA模型</h1><h2 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h2><p>MA模型，即移动平均模型，是时间序列分析中的一种模型，它描述的是当前时间点的数据与过去<strong>噪声</strong>的关系。<br>严格定义上来讲：其模型的定义是基于白噪声序列的假设。白噪声是一种特殊的时间序列模型，每个时间点的数据都是独立且服从相同分布的，且具有常数的均值和方差。</p>
<h2 id="公式"><a href="#公式" class="headerlink" title="公式"></a>公式</h2><p>$$ X_t &#x3D; \mu + \varepsilon_t - \theta_ 1 \varepsilon_{t-1} - \theta_ 2 \varepsilon_{t-2} - \dots - \theta_q \varepsilon_{t-q}$$<br>其中，</p>
<ul>
<li>$X_t$ 为t时刻的预测值（此处实际上是误差的预测），</li>
<li>$\varepsilon_t$ 为当前误差，</li>
<li>$\mu$ 为常数项, 是时间序列的均值或期望值</li>
<li>$\varepsilon_{t-1}$ 到 $\varepsilon_{t-q}$ 为历史的误差，</li>
<li>q表示考虑前q个，</li>
<li>$\theta_ 1$ 到 $\theta_ q$ 表示权重。</li>
</ul>
<h2 id="重要理解-1"><a href="#重要理解-1" class="headerlink" title="重要理解"></a>重要理解</h2><p>从定义上来看：MA模型的基本思想和基本假设与AR模型大不相同。MA模型的基本思想是：大部分时候时间序列应当是相对稳定的。在稳定的基础上，每个时间点上的标签值受过去一段时间内、不可预料的各种偶然事件影响而波动。<br>也就是说，AR模型是考虑了数据从历史以来的稳定性，而MA模型考虑这种稳定性上的突发情况。因此，二者相辅相成，组成ARMA模型。</p>
<h1 id="ARMA-p-q-模型（自回归移动平均模型）"><a href="#ARMA-p-q-模型（自回归移动平均模型）" class="headerlink" title="ARMA(p,q) 模型（自回归移动平均模型）"></a>ARMA(p,q) 模型（自回归移动平均模型）</h1><p>$$X_t &#x3D; \phi_0 + \phi_1 X_{t-1} + \dots + \phi_p X_{t-p} + \varepsilon_t - \theta_1 \varepsilon_{t-1} - \dots - \theta_q \varepsilon_{t-q}$$</p>
<h1 id="ARIMA模型"><a href="#ARIMA模型" class="headerlink" title="ARIMA模型"></a>ARIMA模型</h1><h2 id="组成"><a href="#组成" class="headerlink" title="组成"></a>组成</h2><p>ARIMA模型全称为自回归差分移动平均模型（Autoregressive Integrated Moving Average Model）。ARIMA模型主要由三部分构成，分别为自回归模型（AR）、差分过程（I）和移动平均模型（MA）。</p>
<ul>
<li>使用差分将时间序列变平稳</li>
<li>使用AR考虑过去历史值的影响</li>
<li>使用MA考虑过去误差，突发事件的影响</li>
</ul>
<p>在ARIMA模型（$ARIMA(p,d,q)$）中，参数选择的核心逻辑是 <strong>“先定d（差分阶数）→ 再选p（AR阶数）和q（MA阶数）”</strong>，其中：</p>
<ul>
<li>$d$ 依赖 <strong>ADF平稳性检验</strong>（确保序列平稳）；</li>
<li>$p$ 和 $q$ 结合 <strong>ACF&#x2F;PACF图形特征</strong>（直观判断）和 <strong>AIC&#x2F;BIC准则</strong>（客观择优）；</li>
<li>最终通过 <strong>残差检验</strong> 验证模型合理性。</li>
</ul>
<h2 id="差分"><a href="#差分" class="headerlink" title="差分"></a>差分</h2><p>1阶差分：$\nabla X_t &#x3D; X_t - X_{t-1}$<br>d阶差分：$\nabla^d X_t &#x3D; \nabla(\nabla^{d-1} X_t)$  </p>
<p>一个序列要平稳，关键是「统计特性不随时间变化」，核心需要满足：</p>
<ol>
<li>均值恒定：不管在哪个时间点（比如 t&#x3D;10 或 t&#x3D;100），序列的平均水平不变；</li>
<li>方差恒定：序列的波动幅度（离散程度）不随时间变大或变小；</li>
</ol>
<p>于是，对于各种非平稳的序列，比如线性增长或者二次增长等，就可以通过一次或多次差分使序列变平稳，序列平稳也是AR和MA模型使用的前提。</p>
<h2 id="模型核心公式"><a href="#模型核心公式" class="headerlink" title="模型核心公式"></a>模型核心公式</h2><p>$$\nabla^d X_t &#x3D; \phi_0 + \phi_1 \nabla^d X_{t-1} + \dots + \phi_p \nabla^d X_{t-p} + \varepsilon_t - \theta_1 \varepsilon_{t-1} - \dots - \theta_q \varepsilon_{t-q}$$<br>这个公式基本上是将AR模型和MA模型的公式组合在一起，其中有重复的常数项就合并了，各项的定义同前面的公式。</p>
<h2 id="ACF与PACF"><a href="#ACF与PACF" class="headerlink" title="ACF与PACF"></a>ACF与PACF</h2><h3 id="概念铺垫"><a href="#概念铺垫" class="headerlink" title="概念铺垫"></a>概念铺垫</h3><ol>
<li><strong>时间序列平稳性</strong>：以下所有公式均基于「宽平稳序列」（均值恒定、方差恒定、自协方差仅与时间间隔有关）——非平稳序列需先差分（如ARIMA的d步），再对差分后的平稳序列分析ACF&#x2F;PACF；</li>
<li><strong>滞后阶数</strong>：用 $k$ 表示「时间间隔」（ $k&#x3D;0,1,2,…$ ），$k&#x3D;0$表示序列与自身的相关（恒为1），$k&#x3D;1$表示 $y_t$ 与 $y_{t-1}$ 的相关，$k&#x3D;2$ 表示 $y_t$ 与 $y_{t-2}$ 的相关，以此类推。</li>
</ol>
<h3 id="ACF"><a href="#ACF" class="headerlink" title="ACF"></a>ACF</h3><h4 id="1-核心定义"><a href="#1-核心定义" class="headerlink" title="1. 核心定义"></a>1. 核心定义</h4><p>ACF（自相关函数，Auto-Correlation Function）是描述时间序列中「相隔$k$期的两个观测值 $y_t$ 和 $y_{t-k}$ 之间的线性相关程度」，反映的是「总相关关系」（包含直接相关和通过中间变量的间接相关）。</p>
<h4 id="2-数学公式"><a href="#2-数学公式" class="headerlink" title="2. 数学公式"></a>2. 数学公式</h4><h5 id="（1）总体自相关函数（理论值）"><a href="#（1）总体自相关函数（理论值）" class="headerlink" title="（1）总体自相关函数（理论值）"></a>（1）总体自相关函数（理论值）</h5><p>设平稳序列 $y_t$ 的均值为 $\mu &#x3D; E(y_t)$，方差为 $\sigma^2 &#x3D; Var(y_t)$，则滞后 $k$ 阶的ACF记为 $\rho_k$：<br>$$<br>\rho_k &#x3D; \frac{Cov(y_t, y_{t-k})}{\sqrt{Var(y_t)} \cdot \sqrt{Var(y_{t-k})}} &#x3D; \frac{\gamma_k}{\gamma_0}<br>$$<br>其中：</p>
<ul>
<li>$\gamma_k &#x3D; Cov(y_t, y_{t-k}) &#x3D; E[(y_t - \mu)(y_{t-k} - \mu)]$：滞后$k$阶的自协方差（衡量$y_t$与$y_{t-k}$的线性关联强度）；</li>
<li>$\gamma_0 &#x3D; Cov(y_t, y_t) &#x3D; Var(y_t) &#x3D; \sigma^2$：滞后0阶的自协方差（即序列本身的方差）。</li>
</ul>
<h5 id="（2）样本自相关函数（实际计算值）"><a href="#（2）样本自相关函数（实际计算值）" class="headerlink" title="（2）样本自相关函数（实际计算值）"></a>（2）样本自相关函数（实际计算值）</h5><p>建模时需用样本数据估计ACF，记为$\hat{\rho}<em>k$：<br>$$<br>\hat{\rho}<em>k &#x3D; \frac{\sum</em>{t&#x3D;k+1}^T (y_t - \bar{y})(y</em>{t-k} - \bar{y})}{\sum_{t&#x3D;1}^T (y_t - \bar{y})^2}<br>$$<br>其中：</p>
<ul>
<li>$T$：样本量；</li>
<li>$\bar{y} &#x3D; \frac{1}{T}\sum_{t&#x3D;1}^T y_t$：样本均值；</li>
<li>分子：样本自协方差（滞后$k$阶）；</li>
<li>分母：样本方差（滞后0阶自协方差）。</li>
</ul>
<h4 id="3-关键性质"><a href="#3-关键性质" class="headerlink" title="3. 关键性质"></a>3. 关键性质</h4><ul>
<li>对称性：$\rho_k &#x3D; \rho_{-k}$（$y_t$与$y_{t-k}$的相关和$y_{t-k}$与$y_t$的相关相同）；</li>
<li>取值范围：$-1 \leq \rho_k \leq 1$（与普通相关系数一致，越接近±1，相关越强）；</li>
<li>$\rho_0 &#x3D; 1$（序列与自身完全相关）。</li>
</ul>
<h3 id="PACF"><a href="#PACF" class="headerlink" title="PACF"></a>PACF</h3><h4 id="1-核心定义-1"><a href="#1-核心定义-1" class="headerlink" title="1. 核心定义"></a>1. 核心定义</h4><p>PACF（偏自相关函数，Partial Auto-Correlation Function）是描述时间序列中「相隔 $k$ 期的两个观测值 $y_t$ 和 $y_{t-k}$，在剔除了中间 $k-1$ 个观测值（$y_{t-1}, y_{t-2}, …, y_{t-k+1}$）的干扰后，剩余的直接线性相关程度」——简单说，PACF“剥离了间接相关”，只保留「直接相关关系」。</p>
<h4 id="2-数学公式-1"><a href="#2-数学公式-1" class="headerlink" title="2. 数学公式"></a>2. 数学公式</h4><h5 id="（1）直观定义（条件相关视角）"><a href="#（1）直观定义（条件相关视角）" class="headerlink" title="（1）直观定义（条件相关视角）"></a>（1）直观定义（条件相关视角）</h5><p>滞后$k$阶的PACF记为 $\phi_{kk}$ ，本质是以下线性回归模型中的系数 $\beta_k$ ：<br>$$<br>y_t &#x3D; \beta_1 y_{t-1} + \beta_2 y_{t-2} + … + \beta_k y_{t-k} + \epsilon_t<br>$$<br>即 $\phi_{kk} &#x3D; \beta_k$——表示在控制了 $y_{t-1}$ 到 $y_{t-k+1}$ 的影响后， $y_{t-k}$ 对 $y_t$ 的直接影响系数。</p>
<h5 id="（2）严格公式（Yule-Walker方程推导）"><a href="#（2）严格公式（Yule-Walker方程推导）" class="headerlink" title="（2）严格公式（Yule-Walker方程推导）"></a>（2）严格公式（Yule-Walker方程推导）</h5><p>$$<br>\phi_{kk} &#x3D; \frac{\rho_k - \sum_{i&#x3D;1}^{k-1} \phi_{k-1,i} \rho_{k-i}}{1 - \sum_{i&#x3D;1}^{k-1} \phi_{k-1,i} \rho_i}<br>$$<br>其中：</p>
<ul>
<li>$\phi_{k-1,i}$：滞后$k-1$阶PACF模型中的系数；</li>
<li>$\rho_k$：滞后$k$阶的ACF值。</li>
</ul>
<h5 id="（3）样本偏自相关函数（实际计算值）"><a href="#（3）样本偏自相关函数（实际计算值）" class="headerlink" title="（3）样本偏自相关函数（实际计算值）"></a>（3）样本偏自相关函数（实际计算值）</h5><p>建模时用样本数据估计，记为 $\hat{\phi}<em>{kk}$ ，通常通过「递推法」计算（避免直接解高阶方程组），核心递推公式：<br>$$<br>\hat{\phi}</em>{11} &#x3D; \hat{\rho}<em>1<br>$$<br>$$<br>\hat{\phi}</em>{kk} &#x3D; \hat{\rho}<em>k - \sum</em>{i&#x3D;1}^{k-1} \hat{\phi}<em>{k-1,i} \hat{\rho}</em>{k-i} \quad (k \geq 2)<br>$$</p>
<h4 id="3-关键性质-1"><a href="#3-关键性质-1" class="headerlink" title="3. 关键性质"></a>3. 关键性质</h4><ul>
<li>取值范围：$-1 \leq \phi_{kk} \leq 1$；</li>
<li>$\phi_{11} &#x3D; \rho_1$（滞后1阶时，无中间变量，ACF&#x3D;PACF）；</li>
<li>高阶滞后时，PACF会快速衰减或截尾（核心用于AR模型定阶）。</li>
</ul>
<h3 id="ACF与PACF的区别"><a href="#ACF与PACF的区别" class="headerlink" title="ACF与PACF的区别"></a>ACF与PACF的区别</h3><p>简单理解</p>
<ul>
<li>假设序列是 $y_t$ （今天的气温）、$y_{t-1}$（昨天的气温）、$y_{t-2}$（前天的气温）；</li>
<li>ACF($k&#x3D;2$)： $\rho_2$ 衡量今天和前天的「总相关」——包含“前天→昨天→今天”的间接相关（前天影响昨天，昨天再影响今天）和“前天→今天”的直接相关；</li>
<li>PACF($k&#x3D;2$)： $\phi_{22}$ 衡量今天和前天的「直接相关」——剔除了昨天的干扰，只看前天是否直接影响今天（比如前天的冷空气是否直接持续到今天，不通过昨天传递）。</li>
</ul>
<h2 id="ADF检验"><a href="#ADF检验" class="headerlink" title="ADF检验"></a>ADF检验</h2><p>$d$ 是使非平稳序列变为平稳序列所需的 <strong>最小差分次数</strong>，核心工具是 <strong>ADF检验</strong>（Augmented Dickey-Fuller Test）。</p>
<h3 id="核心原理"><a href="#核心原理" class="headerlink" title="核心原理"></a>核心原理</h3><p>ADF检验的目的是 <strong>检验序列是否存在单位根</strong>：</p>
<ul>
<li>原假设 $H_0$：序列存在单位根 → 非平稳；</li>
<li>备择假设 $H_1$：序列不存在单位根 → 平稳；</li>
<li>判定规则：计算ADF统计量，与临界值（1%&#x2F;5%&#x2F;10%）对比：<ul>
<li>若 <strong>ADF统计量 &lt; 临界值</strong> → 拒绝 $H_0$ → 序列平稳；</li>
<li>若 <strong>ADF统计量 ≥ 临界值</strong> → 接受 $H_0$ → 序列非平稳，需差分。</li>
</ul>
</li>
</ul>
<h3 id="步骤流程"><a href="#步骤流程" class="headerlink" title="步骤流程"></a>步骤流程</h3><ol>
<li>对原始序列 $y_t$ 做ADF检验：<ul>
<li>若平稳（ADF统计量 &lt; 5%临界值）→ $d&#x3D;0$；</li>
<li>若不平稳 → 做1阶差分（ $d^1_t &#x3D; y_t - y_{t-1}$ ），对 $d^1$ 做ADF检验；</li>
</ul>
</li>
<li>对1阶差分序列 $d^1$ 做ADF检验：<ul>
<li>若平稳 → $d&#x3D;1$；</li>
<li>若不平稳 → 做2阶差分（$d^2_t &#x3D; d^1_t - d^1_{t-1}$），对 $d^2_t$ 做ADF检验；</li>
</ul>
</li>
<li>对2阶差分序列 $d^2_t$ 做ADF检验：<ul>
<li>若平稳 → $d&#x3D;2$；</li>
<li>若仍不平稳 → 优先检查序列是否有季节性（需用SARIMA模型），而非继续增加 $d$（$d≥3$ 时模型易过拟合，现实中极少用）。</li>
</ul>
</li>
</ol>
<h3 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h3><ul>
<li>$d$ 通常取0、1、2，超过2时模型解释性差，且易出现过拟合；</li>
<li>若原始序列本身平稳（如白噪声），则 $d&#x3D;0$（此时ARIMA退化为ARMA模型）；</li>
<li>差分过度会导致序列信息丢失（如2阶差分可能把平稳序列变成“过度差分”，方差变大），因此需用ADF检验严格验证，而非盲目差分。</li>
</ul>
<h2 id="确定-p-和-q：AR阶数和MA阶数"><a href="#确定-p-和-q：AR阶数和MA阶数" class="headerlink" title="确定 p 和 q：AR阶数和MA阶数"></a>确定 p 和 q：AR阶数和MA阶数</h2><p>确定 $d$ 后，基于 <strong>差分后的平稳序列</strong>（记为 $y_{diff}^d$），通过“ACF&#x2F;PACF图形特征”初步判断 $p$ 和 $q$，再用“AIC&#x2F;BIC准则”客观择优。</p>
<h3 id="ACF-PACF图形直观判断（快速定范围）"><a href="#ACF-PACF图形直观判断（快速定范围）" class="headerlink" title="ACF&#x2F;PACF图形直观判断（快速定范围）"></a>ACF&#x2F;PACF图形直观判断（快速定范围）</h3><p>ACF（自相关函数）反映“总相关”，PACF（偏自相关函数）反映“直接相关”，两者的 <strong>截尾&#x2F;拖尾特征</strong> 是选择 $p$ 和 $q$ 的核心依据（仅适用于平稳序列）：</p>
<table>
<thead>
<tr>
<th>模型类型</th>
<th>ACF（自相关函数）</th>
<th>PACF（偏自相关函数）</th>
<th>核心逻辑</th>
</tr>
</thead>
<tbody><tr>
<td>AR(p)</td>
<td>拖尾（缓慢衰减至0）</td>
<td>截尾（$k&#x3D;p$后落入置信区间）</td>
<td>PACF“截尾”对应AR的阶数p：比如PACF在k&#x3D;2后显著为0，说明p&#x3D;2</td>
</tr>
<tr>
<td>MA(q)</td>
<td>截尾（$k&#x3D;q$后落入置信区间）</td>
<td>拖尾（缓慢衰减至0）</td>
<td>ACF“截尾”对应MA的阶数q：比如ACF在k&#x3D;3后显著为0，说明q&#x3D;3</td>
</tr>
<tr>
<td>ARMA(p,q)</td>
<td>拖尾</td>
<td>拖尾</td>
<td>无法仅通过图形确定，需结合AIC&#x2F;BIC遍历搜索</td>
</tr>
</tbody></table>
<p>其实实际问题，通常采用逐个检验的傻瓜方法来确定p和q，通常都是较小的数字。</p>
<h2 id="AIC和BIC"><a href="#AIC和BIC" class="headerlink" title="AIC和BIC"></a>AIC和BIC</h2><p>在 ARIMA 模型参数选择中，AIC（赤池信息准则）和 BIC（贝叶斯信息准则）是客观判断最优模型的核心工具—— 本质是通过 “平衡模型拟合效果” 和 “模型复杂度”，避免欠拟合或过拟合，最终选出 “性价比最高” 的模型。  </p>
<p>在ARIMA模型参数选择中，<strong>AIC（赤池信息准则）和BIC（贝叶斯信息准则）是客观判断最优模型的核心工具</strong>——本质是通过“平衡模型拟合效果”和“模型复杂度”，避免欠拟合或过拟合，最终选出“性价比最高”的模型。以下从「核心作用、公式含义、判断规则、关键差异、实操步骤」全维度讲解，结合ARIMA场景落地：</p>
<h3 id="核心作用：解决“主观判断的不足”"><a href="#核心作用：解决“主观判断的不足”" class="headerlink" title="核心作用：解决“主观判断的不足”"></a>核心作用：解决“主观判断的不足”</h3><p>之前用ACF&#x2F;PACF图判断p、q时，“拖尾&#x2F;截尾”的界定有主观性（比如有人认为k&#x3D;2截尾，有人认为k&#x3D;3）；而AIC&#x2F;BIC通过<strong>量化指标</strong>，从“所有候选模型”中选出最优，核心目标是：</p>
<blockquote>
<p>在“拟合得好”和“参数少（简单）”之间找最优解——既不因为参数太少导致欠拟合，也不因为参数太多导致过拟合。</p>
</blockquote>
<h3 id="公式与核心含义"><a href="#公式与核心含义" class="headerlink" title="公式与核心含义"></a>公式与核心含义</h3><p>AIC和BIC的公式都围绕“似然值（拟合度）”和“参数个数（复杂度）”展开，先记住核心公式和每个符号的意义：</p>
<table>
<thead>
<tr>
<th>准则</th>
<th>公式（简化版）</th>
<th>各部分含义</th>
</tr>
</thead>
<tbody><tr>
<td>AIC</td>
<td>$AIC &#x3D; 2k - 2\ln(L)$</td>
<td>- $k$：模型的参数个数（ARIMA中$k &#x3D; p + q + 1$，+1是常数项；比如ARIMA(2,1,1)的$k&#x3D;2+1+1&#x3D;4$）<br>- $\ln(L)$：模型的对数似然值（$L$是似然值，越大表示模型对数据的拟合效果越好；$\ln(L)$越大，$-2\ln(L)$越小）<br>- $2k$：复杂度惩罚项（参数越多，惩罚越重）</td>
</tr>
<tr>
<td>BIC</td>
<td>$BIC &#x3D; k\ln(n) - 2\ln(L)$</td>
<td>- $k$：同AIC（参数个数）<br>- $n$：样本量（数据行数）<br>- $\ln(n)$：样本量越大，对参数的惩罚越重<br>- $-2\ln(L)$：同AIC（拟合度项）</td>
</tr>
</tbody></table>
<h4 id="公式的核心逻辑："><a href="#公式的核心逻辑：" class="headerlink" title="公式的核心逻辑："></a>公式的核心逻辑：</h4><ul>
<li><strong>拟合度越好</strong>（$\ln(L)$越大）→ $-2\ln(L)$越小 → AIC&#x2F;BIC越小（越优）；</li>
<li><strong>参数越多</strong>（$k$越大）→ 惩罚项（$2k$或$k\ln(n)$）越大 → AIC&#x2F;BIC越大（越差）；</li>
<li>AIC&#x2F;BIC的最终值，是“拟合度”和“复杂度”的综合得分——<strong>得分越低，模型越优</strong>。</li>
</ul>
<h3 id="AIC-vs-BIC："><a href="#AIC-vs-BIC：" class="headerlink" title="AIC vs BIC："></a>AIC vs BIC：</h3><h4 id="对比"><a href="#对比" class="headerlink" title="对比"></a>对比</h4><p>两者的核心区别是<strong>对“模型复杂度（参数个数）”的惩罚力度不同</strong>，直接决定了适用场景：</p>
<table>
<thead>
<tr>
<th>特征</th>
<th>AIC（赤池信息准则）</th>
<th>BIC（贝叶斯信息准则）</th>
</tr>
</thead>
<tbody><tr>
<td>惩罚力度</td>
<td>轻（惩罚项是$2k$，与样本量无关）</td>
<td>重（惩罚项是$k\ln(n)$，样本量越大，惩罚越重）</td>
</tr>
<tr>
<td>核心倾向</td>
<td>优先保证拟合效果，允许稍多参数（易选复杂模型）</td>
<td>优先保证模型简单，严控参数个数（易选简单模型）</td>
</tr>
<tr>
<td>适用场景</td>
<td>1. 样本量小（n&lt;100）<br>2. 希望模型拟合精度高<br>3. 允许少量过拟合</td>
<td>1. 样本量大（n≥100）<br>2. 追求模型解释性（参数少）<br>3. 避免过拟合</td>
</tr>
<tr>
<td>极端情况</td>
<td>样本量再大，也可能选参数多的模型</td>
<td>样本量足够大时，一定会选参数最少的“有效模型”</td>
</tr>
</tbody></table>
<h4 id="实操选择技巧："><a href="#实操选择技巧：" class="headerlink" title="实操选择技巧："></a>实操选择技巧：</h4><ul>
<li>若AIC和BIC指向同一个模型 → 直接选这个模型（最优解）；</li>
<li>若AIC和BIC指向不同模型：<ul>
<li>样本量小（比如n&lt;50）→ 选AIC最优（避免欠拟合）；</li>
<li>样本量大（比如n≥100）→ 选BIC最优（避免过拟合，模型更简洁）；</li>
<li>折中方案：选两个模型中“参数更少、AIC&#x2F;BIC接近最小值”的（比如AIC最小是162（p&#x3D;1,q&#x3D;1），次小是163（p&#x3D;1,q&#x3D;0），选p&#x3D;1,q&#x3D;0）。</li>
</ul>
</li>
</ul>
<h3 id="常见误区"><a href="#常见误区" class="headerlink" title="常见误区"></a>常见误区</h3><ol>
<li><strong>误区1</strong>：AIC&#x2F;BIC越小越好 → 错！如果参数搜索范围过大（比如p&#x3D;0<del>10），可能选到过拟合模型（AIC小但泛化能力差），需结合ACF&#x2F;PACF限定搜索范围（通常0</del>3&#x2F;0~4）；</li>
<li><strong>误区2</strong>：只看AIC&#x2F;BIC，忽略残差检验 → 错！AIC&#x2F;BIC是“择优”，但不能保证模型“有效”（比如序列有季节性，ARIMA本身不适用，AIC&#x2F;BIC再小也没用）；</li>
<li><strong>误区3</strong>：不同模型的AIC&#x2F;BIC可直接对比 → 错！只有“同一数据集、同一d值”的ARIMA模型，AIC&#x2F;BIC才可比（比如d&#x3D;1的模型和d&#x3D;2的模型，AIC不可比）；</li>
<li><strong>误区4</strong>：BIC一定比AIC好 → 错！样本量小时，BIC的强惩罚可能导致欠拟合（比如明明p&#x3D;2更好，BIC却选p&#x3D;1），需结合场景。</li>
</ol>
<h3 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h3><p>AIC和BIC的核心判断逻辑是：<strong>在限定的p&#x2F;q范围内，选准则值最小的模型</strong>，其中：</p>
<ul>
<li>AIC：“拟合优先”，适合小样本；</li>
<li>BIC：“简洁优先”，适合大样本；</li>
<li>最终必须用残差检验验证，避免“最优但无效”的模型。</li>
</ul>
<p>这两个准则的价值是“把主观判断变成客观量化”，是ARIMA参数选择中最核心的“兜底工具”——结合ACF&#x2F;PACF的直观判断+AIC&#x2F;BIC的客观择优，才能选出最优的p、q、d。</p>
<h2 id="ARIMA模型小结"><a href="#ARIMA模型小结" class="headerlink" title="ARIMA模型小结"></a>ARIMA模型小结</h2><table>
<thead>
<tr>
<th>步骤</th>
<th>核心工具</th>
<th>目标</th>
<th>输出</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td>ADF检验</td>
<td>使序列平稳</td>
<td>确定d</td>
</tr>
<tr>
<td>2</td>
<td>ACF&#x2F;PACF图</td>
<td>初步判断p和q的范围</td>
<td>p_range, q_range</td>
</tr>
<tr>
<td>3</td>
<td>AIC&#x2F;BIC准则</td>
<td>客观选择最优p和q</td>
<td>确定p, q</td>
</tr>
<tr>
<td>4</td>
<td>残差检验</td>
<td>验证模型合理性</td>
<td>确认参数或调整</td>
</tr>
</tbody></table>
<h1 id="详细代码"><a href="#详细代码" class="headerlink" title="详细代码"></a>详细代码</h1><p>下面代码很多部分使用python <code>statsmodels</code> 库，先去下载一下。<br>使用股票价格预测举例</p>
<h2 id="差分-1"><a href="#差分-1" class="headerlink" title="差分"></a>差分</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"></span><br><span class="line">PATH=<span class="string">&#x27;ChinaBank.csv&#x27;</span></span><br><span class="line"></span><br><span class="line">ChinaBank = pd.read_csv(PATH,index_col = <span class="string">&#x27;Date&#x27;</span>,parse_dates=[<span class="string">&#x27;Date&#x27;</span>])</span><br><span class="line"></span><br><span class="line">ChinaBank.head()</span><br><span class="line">ChinaBank.index = pd.to_datetime(ChinaBank.index)</span><br><span class="line">sub = ChinaBank.loc[<span class="string">&#x27;2014-01&#x27;</span>:<span class="string">&#x27;2014-06&#x27;</span>,<span class="string">&#x27;Close&#x27;</span>]</span><br><span class="line"></span><br><span class="line">sub.head()</span><br><span class="line">train = sub.loc[<span class="string">&#x27;2014-01&#x27;</span>:<span class="string">&#x27;2014-03&#x27;</span>]</span><br><span class="line">test = sub.loc[<span class="string">&#x27;2014-04&#x27;</span>:<span class="string">&#x27;2014-06&#x27;</span>]</span><br><span class="line"><span class="comment">#查看训练集的时间序列与数据(只包含训练集)</span></span><br><span class="line">plt.figure(figsize=(<span class="number">12</span>,<span class="number">6</span>))</span><br><span class="line">plt.plot(train)</span><br><span class="line">plt.xticks(rotation=<span class="number">45</span>) <span class="comment">#旋转45度</span></span><br><span class="line">plt.show()</span><br><span class="line"><span class="comment">#.diff(1)做一个时间间隔</span></span><br><span class="line">ChinaBank[<span class="string">&#x27;diff_1&#x27;</span>] = ChinaBank[<span class="string">&#x27;Close&#x27;</span>].diff(<span class="number">1</span>) <span class="comment">#1阶差分</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#对一阶差分数据在划分时间间隔</span></span><br><span class="line">ChinaBank[<span class="string">&#x27;diff_2&#x27;</span>] = ChinaBank[<span class="string">&#x27;diff_1&#x27;</span>].diff(<span class="number">1</span>) <span class="comment">#2阶差分</span></span><br><span class="line"></span><br><span class="line">fig = plt.figure(figsize=(<span class="number">12</span>,<span class="number">10</span>))</span><br><span class="line"><span class="comment">#原数据</span></span><br><span class="line">ax1 = fig.add_subplot(<span class="number">311</span>)</span><br><span class="line">ax1.plot(ChinaBank[<span class="string">&#x27;Close&#x27;</span>])</span><br><span class="line"><span class="comment">#1阶差分</span></span><br><span class="line">ax2 = fig.add_subplot(<span class="number">312</span>)</span><br><span class="line">ax2.plot(ChinaBank[<span class="string">&#x27;diff_1&#x27;</span>])</span><br><span class="line"><span class="comment">#2阶差分</span></span><br><span class="line">ax3 = fig.add_subplot(<span class="number">313</span>)</span><br><span class="line">ax3.plot(ChinaBank[<span class="string">&#x27;diff_2&#x27;</span>])</span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure>
<p>这个代码可以直观看到差分可以将时间序列变平稳。</p>
<h2 id="ADF"><a href="#ADF" class="headerlink" title="ADF"></a>ADF</h2><p>使用ADF检验确定d</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">import</span> statsmodels.api <span class="keyword">as</span> sm</span><br><span class="line"><span class="keyword">from</span> statsmodels.tsa.seasonal <span class="keyword">import</span> seasonal_decompose</span><br><span class="line"><span class="keyword">from</span> statsmodels.tsa.stattools <span class="keyword">import</span> adfuller <span class="keyword">as</span> ADF</span><br><span class="line"></span><br><span class="line"><span class="comment"># 计算原始序列、一阶差分序列、二阶差分序列的单位根检验结果</span></span><br><span class="line">ChinaBank[<span class="string">&#x27;diff_1&#x27;</span>] = ChinaBank[<span class="string">&#x27;diff_1&#x27;</span>].fillna(<span class="number">0</span>)</span><br><span class="line">ChinaBank[<span class="string">&#x27;diff_2&#x27;</span>] = ChinaBank[<span class="string">&#x27;diff_2&#x27;</span>].fillna(<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">timeseries_adf = ADF(ChinaBank[<span class="string">&#x27;Close&#x27;</span>].tolist())</span><br><span class="line">timeseries_diff1_adf = ADF(ChinaBank[<span class="string">&#x27;diff_1&#x27;</span>].tolist())</span><br><span class="line">timeseries_diff2_adf = ADF(ChinaBank[<span class="string">&#x27;diff_2&#x27;</span>].tolist())</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 打印单位根检验结果</span></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&#x27;timeseries_adf : &#x27;</span>, timeseries_adf)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&#x27;timeseries_diff1_adf : &#x27;</span>, timeseries_diff1_adf)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&#x27;timeseries_diff2_adf : &#x27;</span>, timeseries_diff2_adf)</span><br></pre></td></tr></table></figure>
<p>下面是运行结果</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">timeseries_adf :  (0.527919808483182, 0.9856974415734416, 9, 335, &#123;&#x27;1%&#x27;: -3.4500219858626227, &#x27;5%&#x27;: -2.870206553997666, &#x27;10%&#x27;: -2.571387268879483&#125;, -734.0738716811488)</span><br><span class="line">timeseries_diff1_adf :  (-6.177185544979001, 6.58710923976123e-08, 8, 336, &#123;&#x27;1%&#x27;: -3.449962981927952, &#x27;5%&#x27;: -2.870180642420163, &#x27;10%&#x27;: -2.5713734527352607&#125;, -735.8436797171294)</span><br><span class="line">timeseries_diff2_adf :  (-9.202545123160352, 1.9841232339615405e-15, 13, 331, &#123;&#x27;1%&#x27;: -3.4502615951739393, &#x27;5%&#x27;: -2.8703117734117742, &#x27;10%&#x27;: -2.5714433728242714&#125;, -717.2833732193085)</span><br></pre></td></tr></table></figure>
<p>只需要看第二个数（P值），如果小于一个较小值（如0.05），表示接受该差分结果。具体说明看上面，其中证明过程类似高中学的相关性检验。</p>
<h2 id="p和q初步确定"><a href="#p和q初步确定" class="headerlink" title="p和q初步确定"></a>p和q初步确定</h2><p>前面代码不变，加上下面这些</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">fig = plt.figure(figsize=(<span class="number">12</span>,<span class="number">7</span>))</span><br><span class="line"></span><br><span class="line">ax1 = fig.add_subplot(<span class="number">211</span>)</span><br><span class="line">fig = sm.graphics.tsa.plot_acf(train, lags=<span class="number">20</span>,ax=ax1)</span><br><span class="line">ax1.xaxis.set_ticks_position(<span class="string">&#x27;bottom&#x27;</span>) <span class="comment"># 设置坐标轴上的数字显示的位置，top:显示在顶部  bottom:显示在底部</span></span><br><span class="line"><span class="comment">#fig.tight_layout()</span></span><br><span class="line"></span><br><span class="line">ax2 = fig.add_subplot(<span class="number">212</span>)</span><br><span class="line">fig = sm.graphics.tsa.plot_pacf(train, lags=<span class="number">20</span>, ax=ax2)</span><br><span class="line">ax2.xaxis.set_ticks_position(<span class="string">&#x27;bottom&#x27;</span>)</span><br><span class="line"><span class="comment">#fig.tight_layout()</span></span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure>
<p>其中，是使用<code>sm.graphics.tsa.plot_acf</code>帮你直接确定的拖尾，不需要自己找，详细原理就是前面ACF和PACF的公式。</p>
<h2 id="p和q的具体确定"><a href="#p和q的具体确定" class="headerlink" title="p和q的具体确定"></a>p和q的具体确定</h2><p>通过上面查找拖尾大致确定p和q范围后，就可以进一步通过遍历确定具体值。<br>遍历中检查的原理就是上面提到的AIC，BIC准则<br>下面以BIC为例</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> itertools</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> seaborn <span class="keyword">as</span> sns</span><br><span class="line"><span class="comment">#确定pq的取值范围</span></span><br><span class="line">p_min = <span class="number">0</span></span><br><span class="line">d_min = <span class="number">0</span></span><br><span class="line">q_min = <span class="number">0</span></span><br><span class="line">p_max = <span class="number">5</span></span><br><span class="line">d_max = <span class="number">0</span></span><br><span class="line">q_max = <span class="number">5</span></span><br><span class="line"><span class="comment">#Initialize a DataFrame to store the results,，以BIC准则</span></span><br><span class="line">results_bic = pd.DataFrame(index=[<span class="string">&#x27;AR&#123;&#125;&#x27;</span>.<span class="built_in">format</span>(i) <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(p_min,p_max+<span class="number">1</span>)],</span><br><span class="line">                           columns=[<span class="string">&#x27;MA&#123;&#125;&#x27;</span>.<span class="built_in">format</span>(i) <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(q_min,q_max+<span class="number">1</span>)])</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> p,d,q <span class="keyword">in</span> itertools.product(<span class="built_in">range</span>(p_min,p_max+<span class="number">1</span>),</span><br><span class="line">                               <span class="built_in">range</span>(d_min,d_max+<span class="number">1</span>),</span><br><span class="line">                               <span class="built_in">range</span>(q_min,q_max+<span class="number">1</span>)):</span><br><span class="line">    <span class="keyword">if</span> p==<span class="number">0</span> <span class="keyword">and</span> d==<span class="number">0</span> <span class="keyword">and</span> q==<span class="number">0</span>:</span><br><span class="line">        results_bic.loc[<span class="string">&#x27;AR&#123;&#125;&#x27;</span>.<span class="built_in">format</span>(p), <span class="string">&#x27;MA&#123;&#125;&#x27;</span>.<span class="built_in">format</span>(q)] = np.nan</span><br><span class="line">        <span class="keyword">continue</span></span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        model = sm.tsa.ARIMA(train, order=(p, d, q),</span><br><span class="line">                               <span class="comment">#enforce_stationarity=False,</span></span><br><span class="line">                               <span class="comment">#enforce_invertibility=False,</span></span><br><span class="line">                              )</span><br><span class="line">        results = model.fit()</span><br><span class="line">        results_bic.loc[<span class="string">&#x27;AR&#123;&#125;&#x27;</span>.<span class="built_in">format</span>(p), <span class="string">&#x27;MA&#123;&#125;&#x27;</span>.<span class="built_in">format</span>(q)] = results.bic</span><br><span class="line">    <span class="keyword">except</span>:</span><br><span class="line">        <span class="keyword">continue</span></span><br><span class="line"></span><br><span class="line">results_bic = results_bic[results_bic.columns].astype(<span class="built_in">float</span>)</span><br><span class="line"><span class="comment">#绘制热力图</span></span><br><span class="line">fig, ax = plt.subplots(figsize=(<span class="number">10</span>, <span class="number">8</span>))</span><br><span class="line">ax = sns.heatmap(results_bic,</span><br><span class="line">                 mask=results_bic.isnull(),</span><br><span class="line">                 ax=ax,</span><br><span class="line">                 annot=<span class="literal">True</span>,</span><br><span class="line">                 fmt=<span class="string">&#x27;.2f&#x27;</span>,</span><br><span class="line">                 cmap=<span class="string">&quot;Purples&quot;</span></span><br><span class="line">                 )</span><br><span class="line"></span><br><span class="line">ax.set_title(<span class="string">&#x27;BIC&#x27;</span>)</span><br><span class="line">plt.show()</span><br><span class="line"><span class="built_in">print</span>(results_bic.stack().idxmin())</span><br></pre></td></tr></table></figure>
<p>过程就是遍历并逐个训练，计算得分，得到最优值。这样的手动遍历可以看出不同的参数组合的不同影响。<br>当然，也有更加简单的方法</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">train_results = sm.tsa.arma_order_select_ic(train, ic=[<span class="string">&#x27;aic&#x27;</span>, <span class="string">&#x27;bic&#x27;</span>], trend=<span class="string">&#x27;n&#x27;</span>, max_ar=<span class="number">8</span>, max_ma=<span class="number">8</span>)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&#x27;AIC&#x27;</span>, train_results.aic_min_order)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&#x27;BIC&#x27;</span>, train_results.bic_min_order)</span><br></pre></td></tr></table></figure>
<p>直接使用statsmodels 内置的 ARMA 阶数自动选择函数，直接得到最优的p和q</p>
<h2 id="残差检验"><a href="#残差检验" class="headerlink" title="残差检验"></a>残差检验</h2><p>绘制残差的自相关函数（ACF）图，<br>这是一步关键的检验，如果这里出了大问题，那前面的参数就需要进行调整</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#根据以上求得</span></span><br><span class="line">p = <span class="number">1</span></span><br><span class="line">d = <span class="number">0</span></span><br><span class="line">q = <span class="number">0</span></span><br><span class="line">model = sm.tsa.ARIMA(train, order=(p,d,q))</span><br><span class="line">results = model.fit()</span><br><span class="line">resid = results.resid <span class="comment">#获取残差</span></span><br><span class="line"><span class="comment">#绘制</span></span><br><span class="line"><span class="comment">#查看测试集的时间序列与数据(只包含测试集)</span></span><br><span class="line">fig, ax = plt.subplots(figsize=(<span class="number">12</span>, <span class="number">5</span>))</span><br><span class="line"></span><br><span class="line">ax = sm.graphics.tsa.plot_acf(resid, lags=<span class="number">40</span>,ax=ax)</span><br><span class="line"></span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure>
<p>如果观察到除了第一个以外的点都在指定区域内，就算通过检验。</p>
<h2 id="模型预测"><a href="#模型预测" class="headerlink" title="模型预测"></a>模型预测</h2><p>经历九九八十一难，终于构建成了模型，现在使用这个模型进行预测</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">predict_sunspots = results.predict(dynamic=<span class="literal">False</span>)</span><br><span class="line">fig, ax = plt.subplots(figsize=(<span class="number">12</span>, <span class="number">6</span>))</span><br><span class="line">ax = sub.plot(ax=ax)</span><br><span class="line"><span class="comment">#预测数据</span></span><br><span class="line">predict_sunspots.plot(ax=ax)</span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure>
<p>同时绘制出原始数据和预测数据</p>
<h1 id="拓展–SARIMA"><a href="#拓展–SARIMA" class="headerlink" title="拓展–SARIMA"></a>拓展–SARIMA</h1><p>SARIMA(p,d,q)(P,D,Q)s 模型（季节性自回归积分移动平均模型）<br>该模型就是将ARIMA的差分增加滞后性，比如每周的数据，可以进行滞后7的差分，年度每月数据进行滞后12的差分等。其他方面和ARIMA相似。</p>
<h2 id="滞后差分定义"><a href="#滞后差分定义" class="headerlink" title="滞后差分定义"></a>滞后差分定义</h2><p>1阶季节性差分（周期s）：$\nabla_s X_t &#x3D; X_t - X_{t-s}$<br>D阶季节性差分：$\nabla_s^D X_t &#x3D; \nabla_s(\nabla_s^{D-1} X_t)$</p>
<h2 id="核心公式"><a href="#核心公式" class="headerlink" title="核心公式"></a>核心公式</h2><p>$\nabla^d \nabla_s^D X_t &#x3D; \phi_0 + \sum_{i&#x3D;1}^p \phi_i \nabla^d \nabla_s^D X_{t-i} + \sum_{j&#x3D;1}^P \Phi_j \nabla^d \nabla_s^D X_{t-js} + \varepsilon_t - \sum_{k&#x3D;1}^q \theta_k \varepsilon_{t-k} - \sum_{l&#x3D;1}^Q \Theta_l \varepsilon_{t-ls}$</p>
]]></content>
      <categories>
        <category>数学建模</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>机器学习</tag>
        <tag>ARIMA</tag>
        <tag>时序模型</tag>
        <tag>AR模型</tag>
        <tag>MA模型</tag>
      </tags>
  </entry>
  <entry>
    <title>AdaBoost</title>
    <url>/2025/11/28/AdaBoost/</url>
    <content><![CDATA[<p>简单看看AdaBoost(,,・ω・,,)</p>
<span id="more"></span>
<pre><code>前置知识：集成学习
</code></pre>
<h1 id="什么是AdaBoost"><a href="#什么是AdaBoost" class="headerlink" title="什么是AdaBoost"></a>什么是AdaBoost</h1><p>前面的<a href="https://longnamehaha.github.io/2025/11/27/%E9%9A%8F%E6%9C%BA%E6%A3%AE%E6%9E%97/" title="看看">随机森林篇</a>已经学习过并行算法， <code>AdaBoost</code> ，从后缀Boost也可以看出来，这是集成学习中Boosting（串行）算法的一种。<br> Boosting是将原始数据经过第一个过程处理后，输入第二个过程继续…（比如数学先学习1+1，再学微积分）  </p>
<p> <code>AdaBoost</code> , 即自适应提升树，是基于集成学习中Boosting思想，来训练样本的强学习器，其核心在于<strong>逐步提高前一步被错误分类的样本的权重</strong>。没错，在训练过程中，样本不在看做等价值的，而是在训练过程中不断调整权重。  </p>
<p> 其实仔细一想也很好理解，比如当前先进行了一个分类，对于分类正确的部分，我们就不需要花更多的精力，而应该重点关注分类错误的样本，即增大错误样本的权重，减少正确样本的权重。  </p>
<p> 同样的，比如像刚刚讲的，训练了很多轮后，取最后的一个模型作为训练结果，那么，这仍然是一个模型，无非训练过程复杂些，也还是一个弱学习器。所以，在每步训练出来的模型，都要参与最后的决策。由于是串行，每一步训练出来的模型并非等价的（区别于随机森林的“平权投票”），所以每一步的模型对样本改变的程度和模型对最后的贡献，也是加权的。  </p>
<h1 id="AdaBoost算法过程"><a href="#AdaBoost算法过程" class="headerlink" title="AdaBoost算法过程"></a>AdaBoost算法过程</h1><p>刚刚已经简述了AdaBoost的工作原理，下面需要对其进行数学量化。<br>以二分类为例子, 即y取1或-1，算法流程：  </p>
<ol>
<li><p>初始化所有样本权重为 $\frac{1}{N}$ (N为样本数量)，记$D_i$&#x3D;($w_{i1}$,$w_{i2}$…$w_{iN}$)为第 <code>i</code> 个样本集，其中，$w_{1j}$&#x3D;$\frac{1}{N}$, j&#x3D;1,2…N</p>
</li>
<li><p>设执行M次训练，则对应m&#x3D;1. 2. 3….M</p>
<ol>
<li>先用第m次的数据集学习，得到基分类器$G_m$, 使用$G_m$对数据集分类并计算错误率$\varepsilon_m$:<br> $$ \varepsilon_m&#x3D;\sum_{i&#x3D;1}^N w_{mi}I(G_m(x_i)\neq y_i)<br> $$</li>
<li>计算得到$G_m$的系数：<br> $$ \alpha_m&#x3D;\frac{1}{2}·\ln\frac{1-\varepsilon_m}{\varepsilon_m}<br> $$</li>
<li>更新样本权重：<br> $$ w_{m+1,i}&#x3D;\frac{w_{mi}}{Z_m} e^{\alpha_m t}<br> $$<br> 其中，<br> $$ t&#x3D; \begin{cases}<br> 1 &amp; 分类错误 \<br> -1 &amp; 分类正确<br> \end{cases}<br> $$<br> $$ Z_m&#x3D;\sum_{i&#x3D;1}^N w_ie^{\alpha_m t}<br> $$<br> $Z_m$为规范化因子，保证所有样本权值和为1</li>
</ol>
</li>
<li><p>构建基分类器的线性组合<br>这一步就是与<a href="https://longnamehaha.github.io/2025/11/27/%E9%9A%8F%E6%9C%BA%E6%A3%AE%E6%9E%97/" title="看看">随机森林</a>“平权投票”不一样的地方了，此次各基学习器都是有权重的，即上面的$ \alpha_m$</p>
</li>
</ol>
<p>线性组合：<br>$$ f(x)&#x3D; \sum_{i&#x3D;1}^M \alpha_m G_m(x)<br>$$</p>
<p>得到最终分类器：<br>$$ G(x)&#x3D;sign(f(x))<br>$$</p>
<h1 id="应用案例——葡萄酒分类"><a href="#应用案例——葡萄酒分类" class="headerlink" title="应用案例——葡萄酒分类"></a>应用案例——葡萄酒分类</h1><h2 id="先介绍一下api："><a href="#先介绍一下api：" class="headerlink" title="先介绍一下api："></a>先介绍一下api：</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.ensemble <span class="keyword">import</span> AdaBoostClassifier</span><br><span class="line">model=AdaBoostClassifier(estimator=mytree,n_estimators=<span class="number">200</span>,learning_rate=<span class="number">0.1</span>,algorithm=<span class="string">&quot;SAMME.R&quot;</span>)</span><br></pre></td></tr></table></figure>
<ul>
<li><code>estimator</code> 是用于训练的基学习器，要先弄出来（下面用的是决策树）</li>
<li><code>n_estimators</code> 是串行训练的数量，即上面公式M</li>
<li><code>learning_rate</code> 学习率，控制每个基学习器的「话语权权重」—— 学习率越大，单个基学习器的影响越强；反之越弱</li>
<li><code>algorithm</code> 集成算法，一般使用默认 <code>SAMME.R</code></li>
</ul>
<h2 id="下面是完整代码"><a href="#下面是完整代码" class="headerlink" title="下面是完整代码"></a>下面是完整代码</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> train_test_split</span><br><span class="line"><span class="keyword">from</span> sklearn.tree <span class="keyword">import</span> DecisionTreeClassifier</span><br><span class="line"><span class="keyword">from</span> sklearn.ensemble <span class="keyword">import</span> AdaBoostClassifier</span><br><span class="line"><span class="keyword">from</span> sklearn.preprocessing <span class="keyword">import</span> LabelEncoder</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">d1</span>():</span><br><span class="line">    df = pd.read_csv(<span class="string">&quot;F:/机器学习/Adaboost/wine.data&quot;</span>, sep=<span class="string">&quot;,&quot;</span>, header=<span class="literal">None</span>)</span><br><span class="line">    df=df[df[<span class="number">0</span>]!=<span class="number">1</span>]</span><br><span class="line">    <span class="comment"># df.info()</span></span><br><span class="line">    x=df.iloc[:,[<span class="number">1</span>,-<span class="number">3</span>]] <span class="comment">#这里选择特征，具体特征可以看数据集</span></span><br><span class="line">    y=df.iloc[:,<span class="number">0</span>]</span><br><span class="line">    le = LabelEncoder()</span><br><span class="line">    y=le.fit_transform(y)</span><br><span class="line">    x_train,x_test,y_train,y_test = train_test_split(x,y,test_size=<span class="number">0.2</span>,random_state=<span class="number">23</span>)</span><br><span class="line">    mytree=DecisionTreeClassifier(max_depth=<span class="number">1</span>,random_state=<span class="number">0</span>)</span><br><span class="line">    mytree.fit(x_train,y_train)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;score1: <span class="subst">&#123;mytree.score(x_test,y_test)&#125;</span>&quot;</span>)</span><br><span class="line">    model=AdaBoostClassifier(estimator=mytree,n_estimators=<span class="number">200</span>,learning_rate=<span class="number">0.1</span>,algorithm=<span class="string">&quot;SAMME.R&quot;</span>)</span><br><span class="line">    model.fit(x_train,y_train)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;score2: <span class="subst">&#123;model.score(x_test,y_test)&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    d1()</span><br></pre></td></tr></table></figure>
<p>先构建决策树，然后以决策树为基学习器构造集成model。</p>
<h1 id="AdaBoost：基学习器组合与任务应用"><a href="#AdaBoost：基学习器组合与任务应用" class="headerlink" title="AdaBoost：基学习器组合与任务应用"></a>AdaBoost：基学习器组合与任务应用</h1><p>上面例子的决策树只是其中一种基学习器，当然还有别的组合，可以完成其他任务。</p>
<h2 id="一、可组合的基学习器（核心：弱学习器优先）"><a href="#一、可组合的基学习器（核心：弱学习器优先）" class="headerlink" title="一、可组合的基学习器（核心：弱学习器优先）"></a>一、可组合的基学习器（核心：弱学习器优先）</h2><ol>
<li>决策树桩（最推荐）<ul>
<li>原理：单层决策树，仅用1个特征分类，弱学习器代表</li>
<li>代码：<code>DecisionTreeClassifier(max_depth=1)</code></li>
<li>适用：所有分类&#x2F;回归任务，训练快、泛化好</li>
</ul>
</li>
<li>逻辑回归（线性基学习器）<ul>
<li>代码：<code>LogisticRegression(max_iter=1000)</code></li>
<li>适用：线性可分数据，可解释性强</li>
</ul>
</li>
<li>线性 SVM（高维数据）<ul>
<li>代码：<code>SVC(kernel=&#39;linear&#39;, probability=True)</code></li>
<li>适用：文本、基因等高维数据分类</li>
</ul>
</li>
<li>浅层神经网络（非线性数据）<ul>
<li>代码：<code>MLPClassifier(hidden_layer_sizes=(10,))</code></li>
<li>适用：中等复杂度非线性数据</li>
</ul>
</li>
</ol>
<h2 id="二、能完成的核心任务"><a href="#二、能完成的核心任务" class="headerlink" title="二、能完成的核心任务"></a>二、能完成的核心任务</h2><ol>
<li>二分类（原生支持）<ul>
<li>应用：垃圾邮件分类、疾病诊断</li>
<li>代码：<code>AdaBoostClassifier(base_estimator=决策树桩)</code></li>
</ul>
</li>
<li>多分类（One-vs-All 策略）<ul>
<li>应用：手写数字识别、新闻分类</li>
<li>代码：直接用 <code>AdaBoostClassifier</code>，自动支持多分类</li>
</ul>
</li>
<li>回归（修改损失函数）<ul>
<li>应用：房价、销售额预测</li>
<li>代码：<code>AdaBoostRegressor(base_estimator=决策树回归)</code></li>
</ul>
</li>
<li>异常检测（聚焦少数类）<ul>
<li>应用：欺诈检测、故障检测</li>
<li>代码：<code>AdaBoostClassifier</code> + 不平衡数据权重调整</li>
</ul>
</li>
</ol>
<h2 id="三、选型建议"><a href="#三、选型建议" class="headerlink" title="三、选型建议"></a>三、选型建议</h2><ul>
<li>优先用决策树桩作为基学习器，性价比最高；</li>
<li>二分类&#x2F;多分类选 <code>AdaBoostClassifier</code>，回归选 <code>AdaBoostRegressor</code>；</li>
<li>学习率建议 0.05-0.1，基学习器数量 50-200，平衡速度和效果。</li>
</ul>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>AdaBoost 作为集成学习中 Boosting 思想的经典实现，核心魅力在于「用简单弱学习器集成强模型」，其设计逻辑既直观又严谨：通过串行训练基学习器，每一轮都聚焦前一轮的错分样本（动态调整权重），同时给每个基学习器分配差异化权重（性能越好的基学习器 “话语权” 越强），最终通过加权组合实现比单一弱学习器更优的泛化能力。  </p>
<p>相较于单一模型，AdaBoost 无需复杂特征工程就能应对多数场景，且训练速度快、可解释性较强；但需注意，它对<strong>异常值较敏感</strong>（错分样本权重持续升高可能放大异常值影响），且不适合用复杂强学习器作为基学习器（易过拟合、失去集成意义）。</p>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>机器学习</tag>
        <tag>AdaBoost</tag>
        <tag>Boosting</tag>
        <tag>集成学习</tag>
      </tags>
  </entry>
  <entry>
    <title>GBDT</title>
    <url>/2025/12/01/GBDT/</url>
    <content><![CDATA[<p>简单讲讲GBDT ʕ⸝⸝⸝˙Ⱉ˙ʔ</p>
<span id="more"></span>
<pre><code>前置知识：决策树，集成学习
</code></pre>
<h1 id="GBDT与BDT"><a href="#GBDT与BDT" class="headerlink" title="GBDT与BDT"></a>GBDT与BDT</h1><h2 id="BDT"><a href="#BDT" class="headerlink" title="BDT"></a>BDT</h2><p>要学习GBDT，要先了解一下BDT（Boosting Decision Tree）提升树。<br>提升树的主要思想：<strong>使用残差拟合结果</strong>。  </p>
<pre><code>残差=真实值-预测值
</code></pre>
<p>举个简单例子，假设要预测某人年龄，其真实年龄为100岁  </p>
<ul>
<li>第一次预测80，残差为20</li>
<li>第二次以第一次的残差为输入，也就是对20预测，假设预测为16，残差为4</li>
<li>第三次以第二次的残差4为输入，假设预测为3.2</li>
<li>可以像这样进行多测预测</li>
<li>最后将每次预测加起来，最后预测结果&#x3D;80+16+3.2&#x3D;99.2<br>这样通过残差拟合将个弱学习器组合为强学习器，就是提升树最朴素的思想</li>
</ul>
<h2 id="GBDT"><a href="#GBDT" class="headerlink" title="GBDT"></a>GBDT</h2><p>GBDT（Gradient Boosting Decision Tree）决策梯度提升树是集成学习Boosting（串行）算法的一种。<br>同样的Boosting算法可以看看前面的<a href="https://longnamehaha.github.io/2025/11/28/AdaBoost/" title="看看">AdaBoost</a>。   </p>
<p>GBDT不再拟合残差，而是使用梯度下降的方法，将梯度下降的损失函数作为残差的近似值。<br>下面仅进行简单推导<br><del> 还有一些细节如用到的泰勒展开过程等此次暂时没有提到 </del><br>：<br>假设第i-1次强学习器：<br>$$ f_{i-1}(x)$$<br>其损失函数为:<br>$$ L(y,f_{i-1}(x))$$<br>第i次的弱学习器为：<br>$$ h_i(x)$$<br>第i次强学习器：<br>$$ f_i(x)&#x3D;f_{i-1}(x)+h_i(x)$$<br>其损失函数为：<br>$$ L(y,f_i(x))&#x3D;L(y,f_{i-1}(x)+h_i(x))&#x3D;(y-f_{i-1}(x)-h_i(x))^2$$<br>梯度为：<br>$$ \dfrac{\partial L(y,f_i(x))}{\partial f_i(x)}&#x3D;f_i(x)-y<br>$$<br>负梯度为：<br>$$ -\dfrac{\partial L(y,f_i(x))}{\partial f_i(x)}&#x3D;y-f_i(x)<br>$$<br>这里的y就是真实值， $f_i(x)$ 为预测值，等式右边就是真实值-预测值，即残差。  </p>
<pre><code>负梯度=真实值-预测值=残差
</code></pre>
<p>也就是说，如果采用平方损失，则GBDT拟合的负梯度就是残差，GBDT实际上是BDT的一种。  </p>
<h1 id="GBDT具体过程细节"><a href="#GBDT具体过程细节" class="headerlink" title="GBDT具体过程细节"></a>GBDT具体过程细节</h1><p>此次以CART回归树为弱学习器，用平方损失作为损失函数。<br>先训练第一个弱学习器，最开始是选择同样的一个函数作为预测值，记都为 $f(x_i)$ 。<br>设所涉及的所有可能作为预测值的函数集合为 $\Gamma$ ,则我们需求的目标为：<br>$$ argmin_{f(x_i) \in \Gamma}L(y,f(x))$$<br>设<br>$$ Y&#x3D;\frac{1}{2} L(y,f(x))$$<br>等价于求其最小值点，求偏导：<br>$$ \dfrac{\partial Y}{\partial f(x)}&#x3D;\sum_{i&#x3D;1}^n(y_i-f(x_i))(y_i-f(x_i))^{‘}&#x3D;-\sum_{i&#x3D;1}^n(y_i-f(x_i))<br>$$<br>令上式&#x3D;0，得：<br>$$ nf(x_i)&#x3D;\sum_{i&#x3D;1}^ny_i<br>$$<br>$$ f(x_i)&#x3D;\frac{\sum_{i&#x3D;1}^ny_i}{n}&#x3D;\overline{y}<br>$$<br>由公式可得，初始化输出值为目标均值时，平方损失最小。  </p>
<p>将目标值均值作为所有的预测值，就是第一个弱学习器。然后求出残差，残差就作为下一轮学习的目标值。  </p>
<p>对一组目标值，除了要得到下一组目标值，还需要得出这组目标值的弱学习器。  </p>
<p>此处弱学习器的训练同前面CART树一样，找到使总平方损失最小的分割点，此条件即为树结点。  </p>
<p>注意：上面以目标均值作为预测值，最开始是所有目标值的均值，接下来每次都是按照上一次决策树的结果分成两边，也就是左右子树分别计算均值做为预测值。</p>
<p>最后的预测结果，和前面的BDT一样，就是将每次预测的结果相加。流程与上面预测年龄的过程类似。</p>
<p>若为分类任务（如实战的存活预测），损失函数会改用对数损失 &#x2F; 指数损失，此时负梯度不再是残差，但核心逻辑（拟合负梯度、加法集成）不变，最终通过 sigmoid&#x2F;softmax 将累加结果转换为类别概率。</p>
<h1 id="实战运用"><a href="#实战运用" class="headerlink" title="实战运用"></a>实战运用</h1><p>api</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.ensemble <span class="keyword">import</span> GradientBoostingClassifier</span><br></pre></td></tr></table></figure>
<p>参数：<br>learning_rate：学习率<br>参数几乎没有变化，具体参考随机森林</p>
<p>以泰坦尼克号存活预测为例<br>完整代码</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> train_test_split</span><br><span class="line"><span class="keyword">from</span> sklearn.ensemble <span class="keyword">import</span> GradientBoostingClassifier</span><br><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> GridSearchCV</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> accuracy_score</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">d1</span>():</span><br><span class="line">    df=pd.read_csv(<span class="string">&quot;titanic_train.csv&quot;</span>)</span><br><span class="line">    <span class="comment"># df.info()</span></span><br><span class="line">    x=df[[<span class="string">&quot;Pclass&quot;</span>,<span class="string">&quot;Age&quot;</span>,<span class="string">&quot;Sex&quot;</span>]].copy()</span><br><span class="line">    y=df[<span class="string">&quot;Survived&quot;</span>].copy()</span><br><span class="line">    x[<span class="string">&#x27;Age&#x27;</span>].fillna(value=df[<span class="string">&quot;Age&quot;</span>].mean(),inplace=<span class="literal">True</span>)</span><br><span class="line">    x=pd.get_dummies(x) <span class="comment">#处理非数值类型</span></span><br><span class="line">    x_train,x_test,y_train,y_test = train_test_split(x,y,test_size=<span class="number">0.2</span>,random_state=<span class="number">23</span>)</span><br><span class="line">    estimator=GradientBoostingClassifier()</span><br><span class="line">    estimator.fit(x_train,y_train)</span><br><span class="line">    y_pred=estimator.predict(x_test)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&#x27;score: <span class="subst">&#123;accuracy_score(y_test,y_pred)&#125;</span>&#x27;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment">#交叉验证网格搜索</span></span><br><span class="line">    param=&#123;<span class="string">&quot;n_estimators&quot;</span>:[<span class="number">100</span>,<span class="number">150</span>,<span class="number">200</span>],<span class="string">&quot;max_depth&quot;</span>:[<span class="number">2</span>,<span class="number">4</span>,<span class="number">6</span>],<span class="string">&quot;learning_rate&quot;</span>:[<span class="number">0.03</span>,<span class="number">0.04</span>,<span class="number">0.05</span>,<span class="number">0.06</span>,<span class="number">0.1</span>]&#125;</span><br><span class="line">    tep=GradientBoostingClassifier()</span><br><span class="line">    model=GridSearchCV(tep,param_grid=param,cv=<span class="number">5</span>)</span><br><span class="line">    model.fit(x_train,y_train)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&#x27;score_2: <span class="subst">&#123;model.best_score_&#125;</span>&#x27;</span>)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&#x27;model: <span class="subst">&#123;model.best_estimator_&#125;</span>&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    d1()</span><br></pre></td></tr></table></figure>

<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>BDT 是 “Boosting + 决策树” 的总称，GBDT 是其具体实现（用负梯度替代残差，更通用）；平方损失下，负梯度等价于残差，因此残差提升树是 GBDT 的特例。  </p>
<p>GBDT 的核心逻辑：<br>初始化：以目标值均值作为初始强学习器（平方损失下损失最小）；<br>迭代训练：每轮用 CART 回归树拟合前一轮的负梯度（误差减少最快的方向），得到弱学习器；<br>加法集成：最终强学习器 &#x3D; 初始值 + 学习率 × 所有弱学习器输出之和（慢步累加，避免过拟合）。</p>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>机器学习</tag>
        <tag>Boosting</tag>
        <tag>集成学习</tag>
        <tag>GBDT</tag>
      </tags>
  </entry>
  <entry>
    <title>其他回归类</title>
    <url>/2025/12/13/%E5%85%B6%E4%BB%96%E5%9B%9E%E5%BD%92%E7%B1%BB/</url>
    <content><![CDATA[<p>一些其他回归类</p>
<span id="more"></span> 
<pre><code>前置知识：一元线性回归
</code></pre>
<h1 id="多元线性回归"><a href="#多元线性回归" class="headerlink" title="多元线性回归"></a>多元线性回归</h1><p>多元线性回归是<strong>科学计算、数学建模</strong>中最基础且常用的预测模型，核心是通过多个自变量（特征）的线性组合预测因变量（目标值）。以下从「理论基础→编程实现→建模技巧」三个维度展开，结合 Python&#x2F;C++ 代码（可直接运行），适配数学建模、数据分析场景。</p>
<h2 id="一、核心理论（简洁版，够用建模）"><a href="#一、核心理论（简洁版，够用建模）" class="headerlink" title="一、核心理论（简洁版，够用建模）"></a>一、核心理论（简洁版，够用建模）</h2><h3 id="1-模型定义"><a href="#1-模型定义" class="headerlink" title="1. 模型定义"></a>1. 模型定义</h3><p>假设存在 $k$ 个自变量 $x_1, x_2, …, x_k$ 和 1 个因变量 $y$，多元线性回归模型为：<br>$$ y &#x3D; \beta_0 + \beta_1 x_1 + \beta_2 x_2 + … + \beta_k x_k + \epsilon $$</p>
<ul>
<li>$\beta_0$：截距项（常数项）</li>
<li>$\beta_1, …, \beta_k$：回归系数（表示自变量对因变量的影响程度）</li>
<li>$\epsilon$：随机误差项（满足正态分布 $\epsilon \sim N(0, \sigma^2)$）</li>
</ul>
<p>用矩阵形式表示（更便于编程计算）：<br>$$ Y &#x3D; X\beta + \epsilon $$</p>
<ul>
<li>$Y \in \mathbb{R}^{n \times 1}$：因变量向量（$n$ 为样本数）</li>
<li>$X \in \mathbb{R}^{n \times (k+1)}$：设计矩阵（第一列为全 1，对应截距项 $\beta_0$）</li>
<li>$\beta \in \mathbb{R}^{(k+1) \times 1}$：系数向量（$[\beta_0, \beta_1, …, \beta_k]^T$）</li>
<li>$\epsilon \in \mathbb{R}^{n \times 1}$：误差向量</li>
</ul>
<h3 id="2-参数估计（最小二乘法）"><a href="#2-参数估计（最小二乘法）" class="headerlink" title="2. 参数估计（最小二乘法）"></a>2. 参数估计（最小二乘法）</h3><p>通过最小化误差平方和 $\sum_{i&#x3D;1}^n \epsilon_i^2 &#x3D; | Y - X\beta |^2$，求得系数 $\beta$ 的最优解：<br>$$ \hat{\beta} &#x3D; (X^T X)^{-1} X^T Y $$</p>
<ul>
<li>要求 $X^T X$ 可逆（即自变量无完全多重共线性）</li>
</ul>
<h3 id="3-模型评估指标"><a href="#3-模型评估指标" class="headerlink" title="3. 模型评估指标"></a>3. 模型评估指标</h3><ul>
<li><strong>决定系数 $R^2$</strong>：衡量模型拟合优度，取值范围 $[0,1]$，越接近 1 拟合越好。<br>$$ R^2 &#x3D; 1 - \frac{\sum (y_i - \hat{y}_i)^2}{\sum (y_i - \bar{y})^2} $$</li>
<li><strong>调整 $R^2$</strong>：修正多重共线性导致的 $R^2$ 虚高（自变量增多时优先参考）。</li>
<li><strong>均方误差（MSE）</strong>：衡量预测误差大小，越小越好：$MSE &#x3D; \frac{1}{n} \sum (y_i - \hat{y}_i)^2$</li>
</ul>
<h2 id="二、Python-实现（高效建模，适配-sklearn）"><a href="#二、Python-实现（高效建模，适配-sklearn）" class="headerlink" title="二、Python 实现（高效建模，适配 sklearn）"></a>二、Python 实现（高效建模，适配 sklearn）</h2><p>Python 中使用 <code>sklearn.linear_model.LinearRegression</code> 可快速实现，支持批量数据处理、特征缩放（可选）、模型评估，适合数学建模中的快速迭代。</p>
<h3 id="完整可运行代码（含数据生成、训练、预测、评估）"><a href="#完整可运行代码（含数据生成、训练、预测、评估）" class="headerlink" title="完整可运行代码（含数据生成、训练、预测、评估）"></a>完整可运行代码（含数据生成、训练、预测、评估）</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">from</span> sklearn.linear_model <span class="keyword">import</span> LinearRegression</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> r2_score, mean_squared_error</span><br><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> train_test_split</span><br><span class="line"></span><br><span class="line"><span class="comment"># 1. 生成模拟数据（模拟 3 个自变量的回归问题）</span></span><br><span class="line">np.random.seed(<span class="number">42</span>)  <span class="comment"># 固定随机种子，结果可复现</span></span><br><span class="line">n = <span class="number">1000</span>  <span class="comment"># 样本数</span></span><br><span class="line">k = <span class="number">3</span>     <span class="comment"># 自变量个数</span></span><br><span class="line">X = np.random.randn(n, k)  <span class="comment"># 设计矩阵（n行k列，正态分布）</span></span><br><span class="line">true_beta = np.array([<span class="number">2.5</span>, -<span class="number">1.8</span>, <span class="number">3.2</span>])  <span class="comment"># 真实系数</span></span><br><span class="line">true_intercept = <span class="number">1.0</span>  <span class="comment"># 真实截距</span></span><br><span class="line">y = true_intercept + X @ true_beta + np.random.randn(n) * <span class="number">0.5</span>  <span class="comment"># 生成因变量（含轻微噪声）</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 划分训练集/测试集（7:3 分割）</span></span><br><span class="line">X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=<span class="number">0.3</span>, random_state=<span class="number">42</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 训练多元线性回归模型</span></span><br><span class="line">model = LinearRegression()</span><br><span class="line">model.fit(X_train, y_train)  <span class="comment"># 自动学习截距和系数</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 4. 模型参数输出</span></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;=== 模型参数 ===&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;截距项 β₀: <span class="subst">&#123;model.intercept_:<span class="number">.4</span>f&#125;</span>&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;回归系数 β₁~βₖ: <span class="subst">&#123;model.coef_&#125;</span>&quot;</span>)  <span class="comment"># 对应 3 个自变量的系数</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 5. 预测与模型评估</span></span><br><span class="line">y_pred_train = model.predict(X_train)</span><br><span class="line">y_pred_test = model.predict(X_test)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;\n=== 模型评估 ===&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;训练集 R²: <span class="subst">&#123;r2_score(y_train, y_pred_train):<span class="number">.4</span>f&#125;</span>&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;测试集 R²: <span class="subst">&#123;r2_score(y_test, y_pred_test):<span class="number">.4</span>f&#125;</span>&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;训练集 MSE: <span class="subst">&#123;mean_squared_error(y_train, y_pred_train):<span class="number">.4</span>f&#125;</span>&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;测试集 MSE: <span class="subst">&#123;mean_squared_error(y_test, y_pred_test):<span class="number">.4</span>f&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 6. 实际预测（输入新数据）</span></span><br><span class="line">new_data = np.array([[<span class="number">1.2</span>, -<span class="number">0.5</span>, <span class="number">3.0</span>], [<span class="number">0.8</span>, <span class="number">2.1</span>, -<span class="number">1.5</span>]])  <span class="comment"># 2 个新样本</span></span><br><span class="line">new_pred = model.predict(new_data)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;\n=== 新样本预测结果 ===&quot;</span>)</span><br><span class="line"><span class="keyword">for</span> i, data <span class="keyword">in</span> <span class="built_in">enumerate</span>(new_data):</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;样本 <span class="subst">&#123;data&#125;</span>: 预测值 = <span class="subst">&#123;new_pred[i]:<span class="number">.4</span>f&#125;</span>&quot;</span>)</span><br></pre></td></tr></table></figure>

<h3 id="代码说明"><a href="#代码说明" class="headerlink" title="代码说明"></a>代码说明</h3><ul>
<li><strong>数据生成</strong>：用正态分布模拟自变量，加入轻微噪声模拟真实场景，确保模型可复现。</li>
<li><strong>模型训练</strong>：<code>LinearRegression</code> 自动处理截距项（无需手动添加全 1 列），支持大规模数据。</li>
<li><strong>评估指标</strong>：同时输出训练集和测试集的 $R^2$ 和 $MSE$，避免过拟合。</li>
<li><strong>预测应用</strong>：直接输入新样本即可得到预测值，适配建模中的实际需求。</li>
</ul>
<h3 id="运行结果示例"><a href="#运行结果示例" class="headerlink" title="运行结果示例"></a>运行结果示例</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">=== 模型参数 ===</span><br><span class="line">截距项 β₀: 1.0234</span><br><span class="line">回归系数 β₁~βₖ: [2.4876 -1.7932  3.1895]</span><br><span class="line">=== 模型评估 ===</span><br><span class="line">训练集 R²: 0.9876</span><br><span class="line">测试集 R²: 0.9861</span><br><span class="line">训练集 MSE: 0.2451</span><br><span class="line">测试集 MSE: 0.2532</span><br><span class="line">=== 新样本预测结果 ===</span><br><span class="line">样本 [1.2 -0.5 3.0]: 预测值 = 11.8765</span><br><span class="line">样本 [0.8 2.1 -1.5]: 预测值 = -2.3456</span><br></pre></td></tr></table></figure>

<h2 id="四、数学建模中的关键技巧（避坑指南）"><a href="#四、数学建模中的关键技巧（避坑指南）" class="headerlink" title="四、数学建模中的关键技巧（避坑指南）"></a>四、数学建模中的关键技巧（避坑指南）</h2><ol>
<li><p><strong>特征预处理</strong></p>
<ul>
<li>标准化&#x2F;归一化：当自变量量纲差异大（如身高cm、体重kg），需用 <code>StandardScaler</code> 标准化（Python），避免系数受量纲影响。</li>
<li>代码示例（Python）：<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.preprocessing <span class="keyword">import</span> StandardScaler</span><br><span class="line">scaler = StandardScaler()</span><br><span class="line">X_train_scaled = scaler.fit_transform(X_train)</span><br><span class="line">X_test_scaled = scaler.transform(X_test)</span><br><span class="line">model.fit(X_train_scaled, y_train)</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>多重共线性处理</strong></p>
<ul>
<li>问题：自变量间高度相关（如“面积”和“建筑面积”），导致 $X^T X$ 接近奇异，系数不稳定。</li>
<li>解决：<ul>
<li>计算方差膨胀因子（VIF），剔除 VIF&gt;10 的自变量；</li>
<li>用岭回归（Ridge）替代普通回归，加入 L2 正则化：<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.linear_model <span class="keyword">import</span> Ridge</span><br><span class="line">ridge_model = Ridge(alpha=<span class="number">1.0</span>)  <span class="comment"># alpha为正则化强度</span></span><br><span class="line">ridge_model.fit(X_train, y_train)</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
</li>
<li><p><strong>过拟合与模型选择</strong></p>
<ul>
<li>避免过拟合：用交叉验证（CV）选择最优参数，如 Ridge 的 alpha：<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> cross_val_score</span><br><span class="line">alphas = [<span class="number">0.1</span>, <span class="number">1</span>, <span class="number">10</span>, <span class="number">100</span>]</span><br><span class="line"><span class="keyword">for</span> alpha <span class="keyword">in</span> alphas:</span><br><span class="line">    ridge = Ridge(alpha=alpha)</span><br><span class="line">    cv_scores = cross_val_score(ridge, X_train, y_train, cv=<span class="number">5</span>, scoring=<span class="string">&#x27;r2&#x27;</span>)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;alpha=<span class="subst">&#123;alpha&#125;</span>, 平均CV R²: <span class="subst">&#123;cv_scores.mean():<span class="number">.4</span>f&#125;</span>&quot;</span>)</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>异常值处理</strong></p>
<ul>
<li>用箱线图或 Z-score（|Z|&gt;3）识别异常值，剔除或替换为均值&#x2F;中位数，避免异常值主导回归结果。</li>
</ul>
</li>
</ol>
<h2 id="五、总结"><a href="#五、总结" class="headerlink" title="五、总结"></a>五、总结</h2><ul>
<li><strong>Python</strong>：适合快速建模、数据分析、数学建模比赛，用 sklearn 高效实现，支持预处理、评估、正则化等功能。</li>
<li><strong>C++</strong>：适合底层开发、算法竞赛、嵌入式场景，用 Eigen 手动实现最小二乘法，理解回归本质。</li>
<li><strong>建模核心</strong>：预处理→避坑（多重共线性、异常值）→正则化→交叉验证，确保模型稳健性和泛化能力。</li>
</ul>
<p>如果需要针对具体建模场景（如预测房价、销量、科研数据拟合）调整代码，或需要推导更深入的理论（如置信区间、假设检验），可以随时补充需求！</p>
]]></content>
      <categories>
        <category>数学建模</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>多元线性回归</tag>
        <tag>非线性回归</tag>
        <tag>预测类</tag>
      </tags>
  </entry>
  <entry>
    <title>学习笔记1</title>
    <url>/2026/03/19/%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B01/</url>
    <content><![CDATA[<p>物理实验相关软件开发1</p>
<span id="more"></span>
<pre><code>物理基础，python绘制图谱，python数据校验
</code></pre>
<h1 id="物理基础"><a href="#物理基础" class="headerlink" title="物理基础"></a>物理基础</h1><p>X射线衍射（X-ray Diffraction，XRD），是一种快速、准确、高效的材料无损检测技术。作为一种表征晶体结构及其变化规律的手段，其应用遍及材料、化学、生物、医药、陶瓷、冶金、矿产等诸多领域。  </p>
<h2 id="X射线"><a href="#X射线" class="headerlink" title="X射线"></a>X射线</h2>]]></content>
      <categories>
        <category>开发</category>
      </categories>
      <tags>
        <tag>python</tag>
        <tag>XRD</tag>
        <tag>晶体</tag>
        <tag>X射线衍射</tag>
        <tag>大学物理</tag>
        <tag>物理实验</tag>
      </tags>
  </entry>
  <entry>
    <title>层次分析法</title>
    <url>/2025/11/29/%E5%B1%82%E6%AC%A1%E5%88%86%E6%9E%90%E6%B3%95/</url>
    <content><![CDATA[<p>简单的层次分析法(ゝ∀･)</p>
<span id="more"></span>
<pre><code>前置知识：一点点矩阵基础
</code></pre>
<h1 id="模型引出"><a href="#模型引出" class="headerlink" title="模型引出"></a>模型引出</h1><p>在决策问题中，需要面对多种策略，选择最优。但实际的方案通常涉及多个指标，各指标之间无法之间比较。比如评选最优明星，有粉丝量，颜值，作品质量，作品数量等。<br>按照普通的想法，可能想将各指标归一化后相加，这样可以消除量纲的影响。但是各个指标的<strong>重要性</strong>也是不同的，于是需要主观的对个指标赋予不同的<strong>权重</strong>。<br>简单来说，层次分析法（ <code>Analytic Hierarchy Process</code> ，简称 <code>AHP</code> ）也是这样的，本质上和上面的<strong>为各指派赋权重</strong>一样，只是加了一个判断机制，让权重偏差不会过大。  </p>
<h1 id="模型原理"><a href="#模型原理" class="headerlink" title="模型原理"></a>模型原理</h1><h2 id="何为层次"><a href="#何为层次" class="headerlink" title="何为层次"></a>何为层次</h2><p>层次分析法，分为三层：  </p>
<ul>
<li>最高层，即最后得到的决策目标。</li>
<li>中间层，即涉及到的各个原始指标，如粉丝量，颜值，作品质量，作品数量  </li>
<li>最底层，即具体各种备选方案，如各明星</li>
</ul>
<h2 id="基本求解步骤："><a href="#基本求解步骤：" class="headerlink" title="基本求解步骤："></a>基本求解步骤：</h2><ol>
<li>构建阶梯结构模型（上面的那些）</li>
<li>求出各层中所有<strong>判断矩阵</strong></li>
<li>一致性检验</li>
<li>求出权重后进行检验</li>
</ol>
<p>层次分析法能够起到作用，就是依赖<strong>一致性检验</strong>。<br>构建判断矩阵过程：<br>对各指标两两进行重要性比较。  </p>
<p>其中矩阵元素 $a_{ij}$ 表示第 i 个指标相对于第 j 个指标的重要程度。<br>当 $i \leq j$ 时，$a_{ij}$ 取整数 1~9 ，$a_{ij}$&#x3D;1，表示第i个和第j个元素同样重要，$a_{ij}$&#x3D;3，表示i稍微比j重要。以此类推，$a_{ij}$越大，说明第i个指标对比第j个指标重要性越大。  </p>
<p>可得 $a_{ij} · a_{ji} &#x3D; 1$ (称为正互反矩阵)   </p>
<p>除了主对角线元素全为1，其他元素都是需要我们去填写的。<br>由上面的定义，我们可以知道，重要性是可以传递的，即 $a_{ij}&#x3D;a_{ik}·a_{kj}$，所以，我们其实只填写任一行（或列）除了主对角线外的n-1个元素，就可以递推地求出整个矩阵（此时矩阵秩为1）。但是这样就和普通的去赋予权重一样了。<br>层次分析法中，要求我们为每个空缺元素都根据主观赋值，显然，此时再按照上面的递推式计算，最后构建的矩阵会产生矛盾。而一致性检验，就是我们对这个矛盾的<strong>容忍程度</strong>。  </p>
<h2 id="具体讲讲一致性检验"><a href="#具体讲讲一致性检验" class="headerlink" title="具体讲讲一致性检验"></a>具体讲讲一致性检验</h2><h3 id="什么是一致性检验"><a href="#什么是一致性检验" class="headerlink" title="什么是一致性检验"></a>什么是一致性检验</h3><p>上面通过递推构建出来的秩1矩阵，记作<strong>一致矩阵</strong>。<br>一致性检验就是检验我们自己写的矩阵和一致矩阵的差别大不大。  </p>
<pre><code>关于一致矩阵秩为什么为1：按照递推构成的矩阵，每行每列都是成比例的，由矩阵知识可以知道其秩为1。其一个特征值为n，其余特征值为0等矩阵相关性质。
</code></pre>
<p>引理：n阶正互反矩阵为一致矩阵当且仅当其最大特征值 $\lambda_{max} &#x3D; n$ ,且当矩阵非一致矩阵时，一定有 $\lambda_{max} \geq n$，判断矩阵越不一致，其最大特征值与n差距越大。  </p>
<h3 id="一致性检验具体步骤"><a href="#一致性检验具体步骤" class="headerlink" title="一致性检验具体步骤"></a>一致性检验具体步骤</h3><ol>
<li>计算一致性指标 $CI$ :<br>$$ CI&#x3D;\frac{\lambda_{max}-n}{n-1}<br>$$</li>
<li>查找平均随机一致性指标 $RI$ ：</li>
</ol>
<table>
<thead>
<tr>
<th>判断矩阵阶数（n）</th>
<th>1</th>
<th>2</th>
<th>3</th>
<th>4</th>
<th>5</th>
<th>6</th>
<th>7</th>
<th>8</th>
<th>9</th>
<th>10</th>
</tr>
</thead>
<tbody><tr>
<td>平均随机一致性指标（RI）</td>
<td>0.00</td>
<td>0.00</td>
<td>0.58</td>
<td>0.90</td>
<td>1.12</td>
<td>1.24</td>
<td>1.32</td>
<td>1.41</td>
<td>1.45</td>
<td>1.49</td>
</tr>
</tbody></table>
<p>只需要会查表即可。<br>3. 计算一致性比例 $CR$<br>$$<br>CR&#x3D; \frac{CI}{RI}<br>\begin{cases}<br>&#x3D; 0 \quad \text{判断矩阵为一致性矩阵} \<br>&lt; 0.1 \quad \text{判断矩阵一致} \<br>\geq 0.1 \quad \text{判断矩阵不一致}<br>\end{cases}<br>$$</p>
<h2 id="求权重的方法"><a href="#求权重的方法" class="headerlink" title="求权重的方法"></a>求权重的方法</h2><ul>
<li><p>算术平均值：  </p>
<ol>
<li>每列（各个特征）归一化</li>
<li>按行求和</li>
<li>再次归一化</li>
</ol>
</li>
<li><p>几何平均值：  </p>
<ol>
<li>按行求几何平均值</li>
<li>归一化</li>
</ol>
</li>
<li><p>特征值法：  </p>
<p>  以最大特征值对应的特征向量作为权重</p>
</li>
</ul>
<p>求出权重后，在带回原来各方案，求出各个得分。  </p>
<h1 id="简单总结"><a href="#简单总结" class="headerlink" title="简单总结"></a>简单总结</h1><p>层次分析法是评价类的模型，依旧无法避免主观性的影响，但可以通过一致性检验来限制主观因素在一个较合理的范围。所以说，<strong>一致性检验</strong>才是层次分析法的灵魂所在。  </p>
<p>当然也有很多其他评价类模型，实际情况往往需要综合考量。</p>
]]></content>
      <categories>
        <category>数学建模</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>评价类模型</tag>
        <tag>层次分析法</tag>
      </tags>
  </entry>
  <entry>
    <title>各类损失函数</title>
    <url>/2026/03/11/%E5%90%84%E7%B1%BB%E6%8D%9F%E5%A4%B1%E5%87%BD%E6%95%B0/</url>
    <content><![CDATA[<p>为不同任务选择不同的损失函数σ&#96;∀´)σ</p>
<span id="more"></span>
<h1 id="何为损失函数"><a href="#何为损失函数" class="headerlink" title="何为损失函数"></a>何为损失函数</h1><p>损失函数（loss function，也称代价函数，成本函数等）相信大家都耳熟能详。损失函数是用来衡量模型参数的质量的函数, 衡量的方式是比较网络输出和真实输出的差异。<br>神经网络中的损失函数作用如下图：<br><img src="/../images/loss_function.jpg" alt="神经网络示意图"><br>损失函数根据用途一般分为分类损失函数以及回归损失函数，不同任务都有自己适合的损失函数。</p>
<h1 id="分类任务损失函数"><a href="#分类任务损失函数" class="headerlink" title="分类任务损失函数"></a>分类任务损失函数</h1><h2 id="多分类"><a href="#多分类" class="headerlink" title="多分类"></a>多分类</h2><p>在多分类任务通常使用softmax将logits转换为概率的形式，所以多分类的交叉熵损失也叫做softmax损失：<br>$$<br>L &#x3D; -\sum_{i&#x3D;1}^{n} y_i \log\left(S\left(f_\theta(x_i)\right)\right)<br>$$<br>其中:</p>
<ul>
<li>$ y_i $ 是样本x属于某一个类别的真实概率</li>
<li>而f(x)是样本属于某一类别的预测分数</li>
<li>S是softmax激活函数，将属于某一类别的预测分数转换成概率</li>
<li>L用来衡量真实值y和预测值f(x)之间差异性的损失结果</li>
</ul>
<p>api：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">nn.CrossEntropyLoss()</span><br><span class="line"><span class="comment"># nn.CrossEntropyLoss()=softmax + 损失计算</span></span><br></pre></td></tr></table></figure>
<p>注意，该损失函数<strong>自带softmax激活函数</strong>，也就是再前面处理x时不需要写激活函数。</p>
<h2 id="二分类"><a href="#二分类" class="headerlink" title="二分类"></a>二分类</h2><p>公式：<br>$$<br>L &#x3D; -y \log \hat{y} - (1 - y) \log(1 - \hat{y})<br>$$<br>api：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">nn.BCELoss()</span><br><span class="line"><span class="comment"># 没有自带的激活函数，一般用sigmoid，需要手动添加</span></span><br></pre></td></tr></table></figure>
<p>其实就是<a href="https://longnamehaha.github.io/2025/11/25/%E9%80%BB%E8%BE%91%E5%9B%9E%E5%BD%92/" title="看看逻辑回归">逻辑回归</a>的损失函数，此处不再赘述。</p>
<h1 id="回归任务损失函数"><a href="#回归任务损失函数" class="headerlink" title="回归任务损失函数"></a>回归任务损失函数</h1><h2 id="MAE损失函数"><a href="#MAE损失函数" class="headerlink" title="MAE损失函数"></a>MAE损失函数</h2><p>Mean absolute loss(MAE)也被称为L1 Loss，是以绝对误差作为距离:<br>$$<br>L &#x3D; \frac{1}{n} \sum_{i&#x3D;1}^{n} \left| y_i - f_\theta(x_i) \right|<br>$$<br>简单理解就是绝对值函数 y&#x3D;|x|  </p>
<p>特点是：</p>
<ul>
<li>由于L1 loss具有稀疏性，为了惩罚较大的值，因此常常将其作为<br>正则项添加到其他loss中作为约束。</li>
<li>L1 loss的最大问题是梯度在零点不平滑，导致会跳过极小值（这点我们再后面会处理）</li>
</ul>
<h2 id="MSE损失函数"><a href="#MSE损失函数" class="headerlink" title="MSE损失函数"></a>MSE损失函数</h2><p>Mean Squared Loss&#x2F; Quadratic Loss(MSE loss)也被称为L2 loss，或欧氏距离，它以误差的平方和的均值作为距离:</p>
<p>$$<br>L &#x3D; \frac{1}{n} \sum_{i&#x3D;1}^{n} \left( y_i - f_\theta(x_i) \right)^2<br>$$<br>简单理解就是平方函数 y&#x3D;x*x  </p>
<p>特点是：</p>
<ul>
<li>L2 loss也常常作为正则项。</li>
<li>当预测值与目标值相差很大时, 梯度容易爆炸（离群惩罚严重）</li>
</ul>
<h2 id="Smooth-L1损失函数"><a href="#Smooth-L1损失函数" class="headerlink" title="Smooth L1损失函数"></a>Smooth L1损失函数</h2><p>Smooth L1说的是光滑之后的L1:<br>$$<br>\text{smooth}_{L_1}(x) &#x3D;<br>\begin{cases}<br>0.5 x^2 &amp; \text{if } |x| &lt; 1 \<br>|x| - 0.5 &amp; \text{otherwise}<br>\end{cases}<br>$$<br>综合了L1和L2：</p>
<ul>
<li>在[-1,1]之间实际上就是L2损失，这样解决了L1的不光滑问题</li>
<li>在[-1,1]区间外，实际上就是L1损失，这样就解决了离群点梯度爆炸的问题</li>
</ul>
<h1 id="代码演示"><a href="#代码演示" class="headerlink" title="代码演示"></a>代码演示</h1><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"><span class="keyword">import</span> torch.nn <span class="keyword">as</span> nn</span><br><span class="line"></span><br><span class="line"><span class="comment"># 分类损失函数：交叉熵损失使用nn.CrossEntropyLoss()实现。nn.CrossEntropyLoss()=softmax + 损失计算</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">test01</span>():</span><br><span class="line">    <span class="comment"># 设置真实值: 可以是热编码后的结果也可以不进行热编码</span></span><br><span class="line">    <span class="comment"># y_true = torch.tensor([[0, 1, 0], [0, 0, 1]], dtype=torch.float32)</span></span><br><span class="line">    <span class="comment"># 注意的类型必须是64位整型数据</span></span><br><span class="line">    y_true = torch.tensor([<span class="number">1</span>, <span class="number">2</span>], dtype=torch.int64)</span><br><span class="line">    y_pred = torch.tensor([[<span class="number">0.2</span>, <span class="number">0.6</span>, <span class="number">0.2</span>], [<span class="number">0.1</span>, <span class="number">0.8</span>, <span class="number">0.1</span>]], requires_grad=<span class="literal">True</span>, dtype=torch.float32)</span><br><span class="line">    <span class="comment"># 实例化交叉熵损失</span></span><br><span class="line">    loss = nn.CrossEntropyLoss()</span><br><span class="line">    <span class="comment"># 计算损失结果</span></span><br><span class="line">    my_loss = loss(y_pred, y_true).detach().numpy()</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;loss:&#x27;</span>, my_loss)</span><br><span class="line"><span class="keyword">def</span> <span class="title function_">test02</span>():</span><br><span class="line">    <span class="comment"># 1 设置真实值和预测值</span></span><br><span class="line">    <span class="comment"># 预测值是sigmoid输出的结果</span></span><br><span class="line">    y_pred = torch.tensor([<span class="number">0.6901</span>, <span class="number">0.5459</span>, <span class="number">0.2469</span>], requires_grad=<span class="literal">True</span>)</span><br><span class="line">    y_true = torch.tensor([<span class="number">0</span>, <span class="number">1</span>, <span class="number">0</span>], dtype=torch.float32)</span><br><span class="line">    <span class="comment"># 2 实例化二分类交叉熵损失</span></span><br><span class="line">    loss = nn.BCELoss()</span><br><span class="line">    <span class="comment"># 3 计算损失</span></span><br><span class="line">    my_loss = loss(y_pred, y_true).detach().numpy()</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;loss：&#x27;</span>, my_loss)</span><br><span class="line"><span class="comment"># 计算inputs与target之差的绝对值</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">test03</span>():</span><br><span class="line">    <span class="comment"># 1 设置真实值和预测值</span></span><br><span class="line">    y_pred = torch.tensor([<span class="number">1.0</span>, <span class="number">1.0</span>, <span class="number">1.9</span>], requires_grad=<span class="literal">True</span>)</span><br><span class="line">    y_true = torch.tensor([<span class="number">2.0</span>, <span class="number">2.0</span>, <span class="number">2.0</span>], dtype=torch.float32)</span><br><span class="line">    <span class="comment"># 2 实例MAE损失对象</span></span><br><span class="line">    loss = nn.L1Loss()</span><br><span class="line">    <span class="comment"># 3 计算损失</span></span><br><span class="line">    my_loss = loss(y_pred, y_true).detach().numpy()</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;loss:&#x27;</span>, my_loss)</span><br><span class="line"><span class="keyword">def</span> <span class="title function_">test04</span>():</span><br><span class="line">    <span class="comment"># 1 设置真实值和预测值</span></span><br><span class="line">    y_pred = torch.tensor([<span class="number">1.0</span>, <span class="number">1.0</span>, <span class="number">1.9</span>], requires_grad=<span class="literal">True</span>)</span><br><span class="line">    y_true = torch.tensor([<span class="number">2.0</span>, <span class="number">2.0</span>, <span class="number">2.0</span>], dtype=torch.float32)</span><br><span class="line">    <span class="comment"># 2 实例MSE损失对象</span></span><br><span class="line">    loss = nn.MSELoss()</span><br><span class="line">    <span class="comment"># 3 计算损失</span></span><br><span class="line">    my_loss = loss(y_pred, y_true).detach().numpy()</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;myloss:&#x27;</span>, my_loss)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">test05</span>():</span><br><span class="line">    <span class="comment"># 1 设置真实值和预测值</span></span><br><span class="line">    y_true = torch.tensor([<span class="number">0</span>, <span class="number">3</span>])</span><br><span class="line">    y_pred = torch.tensor ([<span class="number">0.6</span>, <span class="number">0.4</span>], requires_grad=<span class="literal">True</span>)</span><br><span class="line">    <span class="comment"># 2 实例smmothL1损失对象</span></span><br><span class="line">    loss = nn.SmoothL1Loss()</span><br><span class="line">    <span class="comment"># 3 计算损失</span></span><br><span class="line">    my_loss = loss(y_pred, y_true).detach().numpy()</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;loss:&#x27;</span>, my_loss)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">&quot;__main__&quot;</span>:</span><br><span class="line">    test01()</span><br><span class="line">    <span class="comment"># test02()</span></span><br><span class="line">    <span class="comment"># test03()</span></span><br><span class="line">    <span class="comment"># test04()</span></span><br><span class="line">    <span class="comment"># test05()</span></span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>深度学习</category>
      </categories>
      <tags>
        <tag>深度学习</tag>
        <tag>张量</tag>
        <tag>神经网络</tag>
        <tag>机器学习</tag>
        <tag>损失函数</tag>
      </tags>
  </entry>
  <entry>
    <title>各类激活函数</title>
    <url>/2026/03/03/%E5%90%84%E7%B1%BB%E6%BF%80%E6%B4%BB%E5%87%BD%E6%95%B0/</url>
    <content><![CDATA[<p>为不同神经层挑选合适的激活函数</p>
<span id="more"></span>

<h1 id="神经层简介"><a href="#神经层简介" class="headerlink" title="神经层简介"></a>神经层简介</h1><p>神经网络中信息只向一个方向移动，即从输入节点向前移动，通过隐藏节点，再向输出节点移动。其中的基本部分是:</p>
<ol>
<li>输入层（Input Layer）: 即输入x的那一层（如图像、文本、声音等）。每个输入特征对应一个神经元。输入层将数据传递给下一层的神经元。</li>
<li>输出层（Output Layer）: 即输出y的那一层。输出层的神经元根据网络的任务（回归、分类等）生成最终的预测结果。</li>
<li>隐藏层（Hidden Layers）: 输入层和输出层之间都是隐藏层，神经网络的“深度”通常由隐藏层的数量决定。隐藏层的神经元通过加权和激活函数处理输入，并将结果传递到下一层。</li>
</ol>
<h2 id="神经层特点"><a href="#神经层特点" class="headerlink" title="神经层特点"></a>神经层特点</h2><ul>
<li>同一层的神经元之间没有连接</li>
<li>第 N 层的每个神经元和第 N-1层 的所有神经元相连（这就是full connected的含义），这就是全连接神经网络</li>
<li>全连接神经网络接收的样本数据是二维的，数据在每一层之间需要以二维的形式传递</li>
<li>第N-1层神经元的输出就是第N层神经元的输入</li>
<li>每个连接都有一个权重值（w系数和b系数）</li>
</ul>
<h2 id="激活"><a href="#激活" class="headerlink" title="激活"></a>激活</h2><p>每一个神经元工作时，前向传播会产生两个值，内部状态值（加权求和值）和激活值；反向传播时会产生激活值梯度和内部状态值梯度。  </p>
<h3 id="内部状态值"><a href="#内部状态值" class="headerlink" title="内部状态值"></a>内部状态值</h3><p>神经元或隐藏单元的内部存储值，它反映了当前神经元接收到的输入、历史信息以及网络内部的权重计算结果。<br>$$ z&#x3D;W ⋅ x+b $$</p>
<ul>
<li>W：权重矩阵</li>
<li>x：输入值</li>
<li>b：偏置</li>
</ul>
<h3 id="激活值"><a href="#激活值" class="headerlink" title="激活值"></a>激活值</h3><p>通过激活函数（如 ReLU、Sigmoid、Tanh）对内部状态值进行<strong>非线性</strong>变换后得到的结果。激活值决定了当前神经元的输出。<br>$$ a&#x3D;f(z) $$</p>
<ul>
<li>f：激活函数</li>
<li>z：内部状态值</li>
</ul>
<p>激活函数的作用就是为神经网络注入非线性因素，没有引入非线性因素的网络，不管中间有多少层，都等价于使用一个线性模型来拟合（由线性代数知识很容易证明）<br>因此，通过给网络输出增加激活函数, 实现引入非线性因素, 使得网络模型可以逼近任意函数, 提升网络对复杂问题的拟合能力.</p>
<h1 id="常见的激活函数"><a href="#常见的激活函数" class="headerlink" title="常见的激活函数"></a>常见的激活函数</h1><h2 id="sigmoid"><a href="#sigmoid" class="headerlink" title="sigmoid"></a>sigmoid</h2><p>sigmoid已经是老面孔了，在前面的<a href="https://longnamehaha.github.io/2025/11/25/%E9%80%BB%E8%BE%91%E5%9B%9E%E5%BD%92/" title="看看">逻辑回归</a>中我们也遇到过<br>$$ sigmoid(x) &#x3D; \frac{1}{1 + e^{-x}} $$<br>sigmoid 函数可以将任意的输入映射到 (0, 1) 之间，当输入的值大致在 &lt;-6 或者 &gt;6 时，意味着输入任何值得到的激活值都是差不多的，这样会丢失部分的信息。比如：输入 100 和输出 10000 经过 sigmoid 的激活值几乎都是等于 1 的，但是输入的数据之间相差 100 倍的信息就丢失了。</p>
<p>对于 sigmoid 函数而言，输入值在 [-6, 6] 之间输出值才会有明显差异，输入值在 [-3, 3] 之间才会有比较好的效果。</p>
<p>一般来说， sigmoid 网络在 5 层之内就会产生梯度消失现象。而且，该激活函数并不是以 0 为中心的，所以在实践中这种激活函数使用的很少。sigmoid函数一般只用于二分类的输出层，如前面提到的逻辑回归。</p>
<h2 id="Tanh"><a href="#Tanh" class="headerlink" title="Tanh"></a>Tanh</h2><p>仅从图像上看，tanh函数及导函数与sigmoid的类似<br>$$ tanh(x) &#x3D; \frac{1-e^{-2x}}{1 + e^{-2x}} $$</p>
<p>Tanh 函数将输入映射到 (-1, 1) 之间，图像以 0 为中心，在 0 点对称，当输入 大概&lt;-3 或者 &gt;3 时将被映射为 -1 或者 1。其导数值范围 (0, 1)，当输入的值大概 &lt;-3 或者 &gt; 3 时，其导数近似 0。</p>
<p>与 Sigmoid 相比，它是以 0 为中心的，且梯度相对于sigmoid大，使得其收敛速度要比 Sigmoid 快，减少迭代次数。但是，Tanh 两侧的导数也为 0，同样会造成梯度消失。</p>
<p>若使用时可在隐藏层使用tanh函数，在输出层使用sigmoid函数。</p>
<h2 id="ReLU"><a href="#ReLU" class="headerlink" title="ReLU"></a>ReLU</h2><p>$$ ReLU(x)&#x3D;max(x,0) $$</p>
<p>ReLU 激活函数将小于 0 的值映射为 0，而大于 0 的值则保持不变，它更加重视正信号，而忽略负信号，这种激活函数运算更为简单，能够提高模型的训练效率。</p>
<p>当x&lt;0时，ReLU导数为0，而当x&gt;0时，则不存在饱和问题。所以，ReLU 能够在x&gt;0时保持梯度不衰减，从而缓解<strong>梯度消失</strong>问题。然而，随着训练的推进，部分输入会落入小于0区域，导致对应权重无法更新。这种现象被称为<strong>神经元死亡</strong>。</p>
<p>ReLU是目前最常用的激活函数。<br>采用sigmoid函数，计算量大（指数运算），反向传播求误差梯度时，计算量相对大，而采用Relu激活函数，整个过程的计算量节省很多。<br>sigmoid函数反向传播时，很容易就会出现梯度消失的情况，从而无法完成深层网络的训练。<br>Relu会使一部分神经元的输出为0，这样就造成了网络的稀疏性，并且减少了参数的相互依存关系，缓解了<strong>过拟合</strong>问题的发生。</p>
<p>ReLU还有很多衍生形式，</p>
<h3 id="Leaky-ReLU"><a href="#Leaky-ReLU" class="headerlink" title="Leaky ReLU"></a>Leaky ReLU</h3><p>$$<br>f(x)&#x3D;<br>\begin{cases}<br>x \quad 0 \leq x \<br>0.01x \quad x &lt; 0<br>\end{cases}<br>$$</p>
<h3 id="PReLU"><a href="#PReLU" class="headerlink" title="PReLU"></a>PReLU</h3><p>$$<br>f(x)&#x3D;<br>\begin{cases}<br>x \quad 0 \leq x \<br>\alpha x \quad x &lt; 0<br>\end{cases}<br>$$</p>
<h2 id="softmax"><a href="#softmax" class="headerlink" title="softmax"></a>softmax</h2><p>softmax用于多分类过程中，它是二分类函数sigmoid在多分类上的推广，目的是将多分类的结果以概率的形式展现出来。<br>$$softmax(z_i) &#x3D; \frac{e^{z_i}}{\sum_j e^{z_j}}$$<br>Softmax 就是将网络输出的 logits 通过 softmax 函数，就映射成为(0,1)的值，而这些值的累和为1（满足概率的性质），那么我们将它理解成概率，选取概率最大（也就是值对应最大的）节点，作为我们的预测目标类别。</p>
<h1 id="图像演示"><a href="#图像演示" class="headerlink" title="图像演示"></a>图像演示</h1><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"></span><br><span class="line">plt.rcParams[<span class="string">&#x27;font.sans-serif&#x27;</span>] = [<span class="string">&#x27;SimHei&#x27;</span>]  <span class="comment"># 用来正常显示中文标签</span></span><br><span class="line">plt.rcParams[<span class="string">&#x27;axes.unicode_minus&#x27;</span>] = <span class="literal">False</span>  <span class="comment"># 用来正常显示负号</span></span><br><span class="line"></span><br><span class="line">fig,axes=plt.subplots(<span class="number">1</span>,<span class="number">2</span>)</span><br><span class="line">x=torch.linspace(-<span class="number">20</span>,<span class="number">20</span>,<span class="number">1000</span>)</span><br><span class="line">y=torch.sigmoid(x)</span><br><span class="line">axes[<span class="number">0</span>].plot(x,y)</span><br><span class="line">axes[<span class="number">0</span>].set_title(<span class="string">&#x27;sigmoid激活函数&#x27;</span>)</span><br><span class="line">axes[<span class="number">0</span>].grid()</span><br><span class="line">x=torch.linspace(-<span class="number">20</span>,<span class="number">20</span>,<span class="number">1000</span>,requires_grad=<span class="literal">True</span>)</span><br><span class="line">torch.sigmoid(x).<span class="built_in">sum</span>().backward()</span><br><span class="line">axes[<span class="number">1</span>].plot(x.detach(),x.grad)</span><br><span class="line">axes[<span class="number">1</span>].set_title(<span class="string">&#x27;sigmoid激活函数导数&#x27;</span>)</span><br><span class="line">axes[<span class="number">1</span>].grid()</span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure>
<h1 id="激活函数的选择"><a href="#激活函数的选择" class="headerlink" title="激活函数的选择"></a>激活函数的选择</h1><p>对于隐藏层:</p>
<ul>
<li>优先选择ReLU激活函数</li>
<li>如果ReLu效果不好，那么尝试其他激活，如Leaky ReLu等。</li>
<li>如果你使用了ReLU， 需要注意一下Dead ReLU问题， 避免出现0梯度从而- 导致过多的神经元死亡。</li>
<li>少用使用sigmoid激活函数，可以尝试使用tanh激活函数</li>
</ul>
<p>对于输出层:</p>
<ul>
<li>二分类问题选择sigmoid激活函数</li>
<li>多分类问题选择softmax激活函数</li>
<li>回归问题选择identity激活函数</li>
</ul>
]]></content>
      <categories>
        <category>深度学习</category>
      </categories>
      <tags>
        <tag>深度学习</tag>
        <tag>张量</tag>
        <tag>神经网络</tag>
        <tag>机器学习</tag>
        <tag>激活函数</tag>
      </tags>
  </entry>
  <entry>
    <title>微分方程模型应用</title>
    <url>/2025/12/08/%E5%BE%AE%E5%88%86%E6%96%B9%E7%A8%8B%E6%A8%A1%E5%9E%8B%E5%BA%94%E7%94%A8/</url>
    <content><![CDATA[<p>一些简单的微分方程机理模型 ( ´•̥×•̥&#96; )</p>
<span id="more"></span> 
<pre><code>前置知识：微分方程
</code></pre>
<h1 id="求解微分方程"><a href="#求解微分方程" class="headerlink" title="求解微分方程"></a>求解微分方程</h1><p>这里主要使用python求解  </p>
<h2 id="1-odeint函数"><a href="#1-odeint函数" class="headerlink" title="1.odeint函数"></a>1.odeint函数</h2><p>odeint函数是scipy中的一个简单函数，用于快速求解简单微分方程<br>函数接口：</p>
<ul>
<li>func：微分方程函数或方程组，它必须是一个函数，该函数有至少两个入参，第一个参数为一个数组，表示当前因变量值。第二个参数是一个标量，表示当前自变量值（通常是时间）</li>
<li>y0：一个数组，表示初始条件</li>
<li>t：一个数组，表示积分的时间点，第一个元素为积分起点</li>
</ul>
<h2 id="2-solve-ivp函数"><a href="#2-solve-ivp函数" class="headerlink" title="2.solve_ivp函数"></a>2.solve_ivp函数</h2><p>这是一个更强大的ODE<br>函数接口：</p>
<ul>
<li>func：同上</li>
<li>y0：同上</li>
<li>t_span：二元组，起始时间和结束时间</li>
<li>method（可选）：表示积分方法</li>
</ul>
<h1 id="马尔萨斯人口模型"><a href="#马尔萨斯人口模型" class="headerlink" title="马尔萨斯人口模型"></a>马尔萨斯人口模型</h1><h2 id="原始模型假设："><a href="#原始模型假设：" class="headerlink" title="原始模型假设："></a>原始模型假设：</h2><ul>
<li>人口数设为 $x(t)$ 对时间连续可微</li>
<li>人口变化仅由于出生和死亡，每个个体生育能力相等</li>
<li>人口增长率r为<strong>常数</strong>（r&#x3D;出生率-死亡率）</li>
</ul>
<p>可得：<br>$$<br>\begin{cases}<br>\frac{dx}{dt}&#x3D;rx  \<br>x(0)&#x3D;x_0<br>\end{cases}<br>$$<br>解得<br>$$ x(t)&#x3D;x_0e^{rt} $$</p>
<h2 id="求解过程："><a href="#求解过程：" class="headerlink" title="求解过程："></a>求解过程：</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> scipy.integrate <span class="keyword">import</span> odeint</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">model</span>(<span class="params">x,t,r</span>):</span><br><span class="line">    dxdt=r*x</span><br><span class="line">    <span class="keyword">return</span> dxdt</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    x0=<span class="number">100</span></span><br><span class="line">    t=np.linspace(<span class="number">0</span>,<span class="number">1000</span>,<span class="number">1000</span>)</span><br><span class="line">    r=<span class="number">0.001</span></span><br><span class="line">    X=odeint(model,x0,t,args=(r,))</span><br><span class="line">    plt.plot(t,X)</span><br><span class="line">    plt.show()</span><br></pre></td></tr></table></figure>
<h2 id="模型评价"><a href="#模型评价" class="headerlink" title="模型评价"></a>模型评价</h2><p>从求解结果可以看出来，这是指数增长。自然界很多像这样的例子，比如最后一天长满一半水池的荷花，生长的竹子等。18-20世纪的数据确实也符合这个规律，这就是最理想情况下生物的生长，高中生物学中称之为“J型曲线”<br>但是，20世纪以后的人口数据就和该预测结果相差愈来愈大。由指数增长特点，地球人口会爆炸的增长，这不符合常理，因为地球的资源是有限的，由高中生物学，环境有最大环境容纳量，因此需要对模型进行修正。  </p>
<h1 id="阻滞增长模型"><a href="#阻滞增长模型" class="headerlink" title="阻滞增长模型"></a>阻滞增长模型</h1><h2 id="模型优化"><a href="#模型优化" class="headerlink" title="模型优化"></a>模型优化</h2><p>需要对人口增长速度进行限制，由此我们可以得到一些很自然的假设：</p>
<ol>
<li>人口增长率不是一个常数，而是一个随 x 变化的减函数 $r(x)$ </li>
<li>$r(x)&#x3D;r-sx$ (优先采用线性假设) </li>
<li>环境存在最大容量 $x_m$ ,即当 $x&#x3D;x_m$ 时，x增长率为0<br>由2，3可得<br>$$  r(x)&#x3D;r(1-\frac{x}{x_m})<br>$$<br>则前面微分方程可优化为：<br>$$<br>\begin{cases}<br>\frac{dx}{dt}&#x3D;r(1-\frac{x}{x_m})x  \<br>x(0)&#x3D;x_0<br>\end{cases}<br>$$<br>可以解得：<br>$$ x(t)&#x3D;\frac{x_m}{1+(\frac{x_m}{x_0}-1)e^{-r(t-t_0)}}<br>$$</li>
</ol>
<h2 id="模型解释"><a href="#模型解释" class="headerlink" title="模型解释"></a>模型解释</h2><p>对上面的结果求二阶导，由凹凸函数的知识可以知道：  </p>
<ul>
<li>首先当 $0&lt;x&lt;x_m$ 时，函数单调递增。</li>
<li>当 $0&lt;x&lt; \frac{x_m}{2}$ 时，为凹函数</li>
<li>当 $\frac{x_m}{2}&lt;x&lt;x_m$ 时，为凸函数</li>
</ul>
<p>即在 $x&#x3D; \frac{x_m}{2}$ 时，函数增长最快。<br>从高中生物学也可以知道，这就是“S型增长”，即有环境容纳量上限的增长。  </p>
<h2 id="部分核心代码"><a href="#部分核心代码" class="headerlink" title="部分核心代码"></a>部分核心代码</h2><p>结合前面的原始代码，下面几个地方进行修正即可</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">model</span>(<span class="params">x,t,r,K</span>):</span><br><span class="line">    dxdt=x*r*(<span class="number">1</span>-x/K)</span><br><span class="line">    <span class="keyword">return</span> dxdt</span><br><span class="line"></span><br><span class="line">X=odeint(model,x0,t,args=(r,K))</span><br></pre></td></tr></table></figure>
<h1 id="沃尔泰拉模型"><a href="#沃尔泰拉模型" class="headerlink" title="沃尔泰拉模型"></a>沃尔泰拉模型</h1><h2 id="模型引入"><a href="#模型引入" class="headerlink" title="模型引入"></a>模型引入</h2><p>沃尔泰拉模型（Volterra模型），即<strong>捕食者-猎物模型</strong>，是数学家沃尔泰拉在研究一战时期地中海鲨鱼比例异常变化是提出的模型。于此相关的就是经典的地中海鲨鱼问题：<br>意大利生物学家Ancona曾致力于鱼类种群相互制约关系的研究，他从第一次世界大战期间，地中海各港口捕获的几种鱼类捕获量百分比的资料中，发现鲨鱼等的比例有明显增加，而供其捕食的食用鱼的百分比却明显下降。显然战争使捕鱼量下降，食用鱼增加，鲨鱼等也随之增加，但为何鲨鱼的比例大幅增加呢？<br>他无法解释这个现象，于是求助于著名的意大利数学家V.Volterra，希望建立一个<strong>食饵—捕食</strong>系统的数学模型，定量地回答这个问题。</p>
<h2 id="模型构建"><a href="#模型构建" class="headerlink" title="模型构建"></a>模型构建</h2><p>设t时刻猎物数量为 $x_1(t)$ ,捕食者数量为 $x_2(t)$  </p>
<p>借用前面的模型，仅考虑被捕食者自由生长，有：<br>$$ \frac{dx_1}{dt}&#x3D;r_1x_1<br>$$<br>由于捕食者出现，食饵数量下降，且捕食者数量越多，消耗食饵数量越大，设：<br>$$ \frac{dx_1}{dt}&#x3D;x_1(r_1- \lambda_ 1 x_2)<br>$$<br>其中 $\lambda_ 1&gt;0$ 表示捕食者捕食食饵的能力。  </p>
<p>对捕食者来说，若仅有捕食者存在，没有食饵，捕食者数量会不断减少至0，即：<br>$$ \frac{dx_2}{dt}&#x3D;-r_2x_2<br>$$<br>由于食饵出现，对捕食者有供养作用，且食饵数量越多，对捕食者的供养能力越大，设：<br>$$ \frac{dx_2}{dt}&#x3D;x_2(-r_2+ \lambda_ 2 x_1)<br>$$<br>其中 $\lambda_ 2&gt;0$ 表示食饵对捕食者的供养能力。  </p>
<p>由上面就可以得到最简单的方程组。回到问题，我们考虑的是一战期间鲨鱼（捕食者）比例异常升高。一战期间，人类捕杀作用减少，因此该期间二者比例会改变，所以我们需要在前面的方程加上人类因素：  </p>
<p>$$<br>\begin{cases}<br>\frac{dx_1}{dt}&#x3D;x_1((r_1-e)- \lambda_ 1 x_2)  \<br>\frac{dx_2}{dt}&#x3D;x_2(-(r_2+e)+ \lambda_ 2 x_1)<br>\end{cases}<br>$$<br>式中，e表示人类捕杀的影响。在人类影响下，食饵自然增长率降低e，捕食者死亡率增加e（这里也可以给二者赋权，表示人类捕杀的偏好）<br>用代码绘图可以清楚说明当e下降，$x_2$比例上升。<br>绘制 $x_1$ 与 $x_2$ 图像，发现是一个圈，也符合高中生物的捕食者——猎物模型。</p>
<h2 id="求解代码"><a href="#求解代码" class="headerlink" title="求解代码"></a>求解代码</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> scipy.integrate <span class="keyword">import</span> odeint</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">model</span>(<span class="params">P,t,r1,r2,la1,la2,e</span>):</span><br><span class="line">    x1,x2=P</span><br><span class="line">    dx1dt=x1*((r1-e)-la1*x2)</span><br><span class="line">    dx2dt=x2*(-(r2+e)+la2*x1)</span><br><span class="line">    <span class="keyword">return</span> [dx1dt,dx2dt]</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    x1=<span class="number">100</span></span><br><span class="line">    x2=<span class="number">20</span></span><br><span class="line">    P=[x1,x2]</span><br><span class="line">    t=np.linspace(<span class="number">0</span>,<span class="number">100</span>,<span class="number">1000</span>)</span><br><span class="line">    r1=<span class="number">1.0</span></span><br><span class="line">    r2=<span class="number">1.0</span></span><br><span class="line">    la1=<span class="number">0.1</span></span><br><span class="line">    la2=<span class="number">0.1</span></span><br><span class="line">    e=<span class="number">0.05</span></span><br><span class="line">    X=odeint(model,P,t,args=(r1,r2,la1,la2,e))</span><br><span class="line">    X1=X[:,<span class="number">0</span>]</span><br><span class="line">    X2=X[:,<span class="number">1</span>]</span><br><span class="line">    plt.plot(X1,X2)</span><br><span class="line">    plt.show()</span><br></pre></td></tr></table></figure>
<h1 id="种间竞争模型"><a href="#种间竞争模型" class="headerlink" title="种间竞争模型"></a>种间竞争模型</h1><p>考虑在有限资源中，单个物种有：<br>$$ \frac{dx_1}{dt}&#x3D;r_1x_1(1-\frac{x_1}{N_1})<br>$$<br>N表示环境最大容量。<br>加上种群2的影响，有：<br>$$ \frac{dx_1}{dt}&#x3D;r_1x_1(1-\frac{x_1}{N_1}-\sigma_ 1\frac{x_2}{N_2})<br>$$<br>种群2数量越多，即 $\frac{x_2}{N_2}$ 越大，种群1增长数量越慢。<br>式中加入影响因子 $\sigma_ 1$ 表示单位数量的2种群消耗1种群的食物的速度的单位数量1种群的倍数。<br>比如，$\sigma_ 1&lt;1$ 表示在供养1种群的食物的竞争中，种群2弱于1。<br>同理可得：<br>$$ \frac{dx_2}{dt}&#x3D;r_2x_2(1-\frac{x_2}{N_2}-\sigma_ 2\frac{x_1}{N_1})<br>$$<br>若种群1和种群2选择食物的偏好完全相同，则上面的 $\sigma_ 1$ 和 $\sigma_ 2$ 有： $\sigma_ 1 \sigma_ 2&#x3D;1$<br>通常情况下，$\sigma_ 1 \sigma_ 2 \neq 1$ 需要通过实际情况确定。<br>其中求解方程即绘图过程于上面类似，不在赘述。  </p>
<h1 id="传染病模型（SIR-SIS）"><a href="#传染病模型（SIR-SIS）" class="headerlink" title="传染病模型（SIR&#x2F;SIS）"></a>传染病模型（SIR&#x2F;SIS）</h1><p>传染病模型通过微分方程刻画人群中“易感-感染-康复”的动态传播过程，核心是量化不同人群的数量变化关系。</p>
<h2 id="1-SIR模型（易感-感染-康复）"><a href="#1-SIR模型（易感-感染-康复）" class="headerlink" title="1. SIR模型（易感-感染-康复）"></a>1. SIR模型（易感-感染-康复）</h2><h3 id="模型假设"><a href="#模型假设" class="headerlink" title="模型假设"></a>模型假设</h3><ul>
<li>总人口固定为 $N$，分为三类人群：<ul>
<li>$S(t)$ ：易感者（未感染但可被传染的人群）；</li>
<li>$I(t)$ ：感染者（已感染且具传染性的人群）；</li>
<li>$R(t)$ ：康复者（已康复且获持久免疫力的人群）；<br>满足 $N &#x3D; S(t) + I(t) + R(t)$ 。</li>
</ul>
</li>
<li>易感者与感染者接触后，以速率 $\beta$ （单位时间内单个感染者接触的易感者数）被感染；</li>
<li>感染者以速率 $\gamma$ （单位时间内康复的感染者比例）康复；</li>
<li>无出生、死亡、人口迁移，康复者不再参与传播。</li>
</ul>
<h3 id="模型构建-1"><a href="#模型构建-1" class="headerlink" title="模型构建"></a>模型构建</h3><p>微分方程组为：<br>$$<br>\begin{cases}<br>\frac{dS}{dt} &#x3D; -\frac{\beta S I}{N} \<br>\frac{dI}{dt} &#x3D; \frac{\beta S I}{N} - \gamma I \<br>\frac{dR}{dt} &#x3D; \gamma I<br>\end{cases}<br>$$</p>
<ul>
<li>方程解释：<ul>
<li>$\frac{dS}{dt}$ ：易感者数量因感染减少，速率与易感者、感染者的接触频率成正比；</li>
<li>$\frac{dI}{dt}$ ：感染者数量变化 &#x3D; 新感染人数 - 康复人数；</li>
<li>$\frac{dR}{dt}$ ：康复者数量随感染者康复增加。</li>
</ul>
</li>
</ul>
<h3 id="核心参数：基本再生数-R-0"><a href="#核心参数：基本再生数-R-0" class="headerlink" title="核心参数：基本再生数 $R_0$"></a>核心参数：基本再生数 $R_0$</h3><p>定义 $R_0 &#x3D; \frac{\beta}{\gamma}$ （单个感染者平均传染的人数）：</p>
<ul>
<li>$ R_0 &gt; 1$ ：传染病会扩散流行；</li>
<li>$ R_0 &lt; 1$ ：传染病会逐渐消亡。</li>
</ul>
<h2 id="2-SIS模型（易感-感染-易感）"><a href="#2-SIS模型（易感-感染-易感）" class="headerlink" title="2. SIS模型（易感-感染-易感）"></a>2. SIS模型（易感-感染-易感）</h2><h3 id="模型假设-1"><a href="#模型假设-1" class="headerlink" title="模型假设"></a>模型假设</h3><p>适用于康复后无免疫力、会再次成为易感者的传染病（如普通流感），人群仅分 $ S(t) $ （易感者）和 $ I(t) $ （感染者），满足 $ N &#x3D; S(t) + I(t) $ ，其余假设同SIR模型。</p>
<h3 id="模型构建-2"><a href="#模型构建-2" class="headerlink" title="模型构建"></a>模型构建</h3><p>微分方程组为：<br>$$<br>\begin{cases}<br>\frac{dS}{dt} &#x3D; -\frac{\beta S I}{N} + \gamma I \<br>\frac{dI}{dt} &#x3D; \frac{\beta S I}{N} - \gamma I<br>\end{cases}<br>$$</p>
<ul>
<li>方程解释：康复者重新回到易感者群体（ $ \frac{dS}{dt} $ 增加 $ \gamma I $ 项），传染病可能呈现持续循环状态。</li>
</ul>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>本文围绕<strong>微分方程的机理建模应用</strong>，通过“现实假设→变量关系提炼→方程构建”的思路，介绍了多个经典模型：</p>
<ol>
<li><strong>马尔萨斯人口模型</strong>：以“增长率恒定”为假设，刻画无约束的指数增长（J型曲线），是单种群动态的基础模型，但忽略了资源限制；</li>
<li><strong>阻滞增长（Logistic）模型</strong>：引入“环境最大容纳量”，修正为S型非线性增长，更贴合资源有限的种群变化规律；</li>
<li><strong>沃尔泰拉模型</strong>：耦合捕食者-猎物的互动关系，刻画了种群数量的周期性振荡，解释了生态系统的此消彼长；</li>
<li><strong>种间竞争模型</strong>：扩展Logistic模型，加入种群间资源竞争项，量化了不同种群的资源消耗效率差异；</li>
<li><strong>传染病模型（SIR&#x2F;SIS）</strong>：划分人群类型，刻画“感染-康复”传播过程，核心参数 $ R_0 $ 可指导疫情防控。</li>
</ol>
<p>这些模型的共性是从“机理假设”出发，用微分方程描述变量的瞬时变化率，通过求解方程得到动态趋势；模型的优化过程，本质是逐步加入现实约束（资源限制、种间互动、免疫特性等），让预测更贴合实际。</p>
<p>微分方程建模的价值在于：不仅能拟合现有数据，更能通过参数调整预测系统长期趋势，为生态调控、人口规划、疫情防控等问题提供量化依据。</p>
]]></content>
      <categories>
        <category>数学建模</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>微分方程</tag>
        <tag>机理分析</tag>
      </tags>
  </entry>
  <entry>
    <title>插值算法</title>
    <url>/2025/12/23/%E6%8F%92%E5%80%BC%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<p>就讲讲最简单的应用，不涉及过多证明</p>
<span id="more"></span>
<h1 id="算法引入"><a href="#算法引入" class="headerlink" title="算法引入"></a>算法引入</h1><p>插值算法，简单来讲，就是通过已经有的离散的样本点来得到该样本点符合的函数式。<br>数模中有时用于数据点较少的情况，通过插值来获得更多估算的样本点或者得出样本点之间关系等</p>
<ul>
<li>内插：估算 x 在已知区间 $[x_0,x_n]$ 内的值</li>
<li>外插：估算 x 超出已知区间的值（容易失真）</li>
</ul>
<p>一般多使用内插。</p>
<h1 id="各类插值算法"><a href="#各类插值算法" class="headerlink" title="各类插值算法"></a>各类插值算法</h1><h2 id="线性插值"><a href="#线性插值" class="headerlink" title="线性插值"></a>线性插值</h2><p>直接将各个样本点用线段相连。<br>最简单的插值方法，当然有很多缺点，插值误差大等。</p>
<h2 id="多项式插值"><a href="#多项式插值" class="headerlink" title="多项式插值"></a>多项式插值</h2><p>若有n+1个样本点，可以假设插值函数为n次多项式。<br>由此可以列出n+1个方程。<br>由线性代数知识，可得该方程组秩为n+1（范德蒙德行列式），求出多项式各系数。</p>
<h2 id="牛顿插值法"><a href="#牛顿插值法" class="headerlink" title="牛顿插值法"></a>牛顿插值法</h2><p>从两个点开始，得到第一次插值多项式，然后增加一个点，结合上一次结果得到第二次插值多项式（根据插值函数各样本点函数值已知得到）。以此迭代得到最终结果。</p>
<h2 id="三次埃尔米特插值多项式"><a href="#三次埃尔米特插值多项式" class="headerlink" title="三次埃尔米特插值多项式"></a>三次埃尔米特插值多项式</h2><h3 id="龙格现象"><a href="#龙格现象" class="headerlink" title="龙格现象"></a>龙格现象</h3><p>前面的多项式插值和牛顿插值不可避免的涉及到多项式，而高阶多项式在数据较大时是发散的，尤其在外插时，等距节点下高次多项式插值，在区间边界剧烈振荡, 误差较大。</p>
<h3 id="埃尔米特插值"><a href="#埃尔米特插值" class="headerlink" title="埃尔米特插值"></a>埃尔米特插值</h3><p>埃尔米特插值对每两个数据点之间都采用不同的多项式进行插值，有效避免了仅使用一个合成的多项式插值产生的龙格现象（当然在外插时也会有误差）。<br>次数较小时，会退化为多项式或者线段插值，次数较大时，计算复杂其可能产生过拟合，综合考虑，一般采用三次多项式进行两个数据点之间的插值，即三次埃尔米插值。</p>
<h2 id="三次样条插值"><a href="#三次样条插值" class="headerlink" title="三次样条插值"></a>三次样条插值</h2><h3 id="更平滑"><a href="#更平滑" class="headerlink" title="更平滑"></a>更平滑</h3><p>前面的各种插值方法，都只考虑了函数在样本点处的值，没有考虑样本点实际表现出来的变化趋势等。<br>要更充分的挖掘样本点内信息，加上变化趋势的考虑等，可以加上导数。<br>即样本点得出的插值函数，需要存在导数，m次样条插值意味着样本点存在m-1阶连续导数。限制了可导的因素，插值函数相较于前面的方法更加平滑。<br>更高阶的导数要求，对样本点和计算的规范也越多。综合考虑，一般取m&#x3D;3，即三次样条插值</p>
<h1 id="代码演示1"><a href="#代码演示1" class="headerlink" title="代码演示1"></a>代码演示1</h1><figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line">x=<span class="number">1</span>:<span class="number">10</span>;</span><br><span class="line">y=<span class="built_in">log</span>(x);</span><br><span class="line"><span class="built_in">plot</span>(x,y,<span class="string">&#x27;o&#x27;</span>)</span><br><span class="line"><span class="built_in">hold</span> on;</span><br><span class="line">nx=<span class="number">0.01</span>:<span class="number">0.1</span>:<span class="number">10</span>;</span><br><span class="line">p=pchip(x,y,nx);</span><br><span class="line"><span class="built_in">plot</span>(nx,p)</span><br><span class="line"><span class="built_in">hold</span> on;</span><br><span class="line">p=spline(x,y,nx);</span><br><span class="line"><span class="built_in">plot</span>(nx,p,<span class="string">&#x27;b-&#x27;</span>)</span><br><span class="line"><span class="built_in">legend</span>(<span class="string">&#x27;样本点&#x27;</span>,<span class="string">&#x27;三次埃尔米插值&#x27;</span>,<span class="string">&#x27;三次样条插值&#x27;</span>,<span class="string">&#x27;Location&#x27;</span>,<span class="string">&#x27;SouthEast&#x27;</span>);</span><br></pre></td></tr></table></figure>
<h1 id="更高维度"><a href="#更高维度" class="headerlink" title="更高维度"></a>更高维度</h1><h2 id="api"><a href="#api" class="headerlink" title="api"></a>api</h2><p>当然，样本点不一定只有x，y，可能涉及更高维度，不够原理和二维的近似。<br>使用函数 <code>interp2</code> 表示样本点为二维的，<code>interpn</code> 表示样本点为n维的。</p>
<h2 id="代码演示2"><a href="#代码演示2" class="headerlink" title="代码演示2"></a>代码演示2</h2><figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line">x=<span class="number">-1</span>:<span class="number">0.5</span>:<span class="number">1</span>;</span><br><span class="line">y=x;</span><br><span class="line">[x,y]=<span class="built_in">meshgrid</span>(x,y);</span><br><span class="line">z=<span class="number">1</span>-x.^<span class="number">2</span>-y.^<span class="number">2</span>;</span><br><span class="line">nx=<span class="number">-1</span>:<span class="number">0.1</span>:<span class="number">1</span>;</span><br><span class="line">ny=nx;</span><br><span class="line">[nx,ny]=<span class="built_in">meshgrid</span>(nx,ny);</span><br><span class="line">p=interp2(x,y,z,nx,ny,<span class="string">&#x27;spline&#x27;</span>);</span><br><span class="line">mesh(nx,ny,p)</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>数学建模</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>插值算法</tag>
        <tag>三次样条插值</tag>
      </tags>
  </entry>
  <entry>
    <title>手机价格预测案例</title>
    <url>/2026/03/15/%E6%89%8B%E6%9C%BA%E4%BB%B7%E6%A0%BC%E9%A2%84%E6%B5%8B%E6%A1%88%E4%BE%8B/</url>
    <content><![CDATA[<p>简单ANN案例 (:3[___]&#x3D;</p>
<span id="more"></span>
<h1 id="案例需求分析"><a href="#案例需求分析" class="headerlink" title="案例需求分析"></a>案例需求分析</h1><p>小明创办了一家手机公司，他不知道如何估算手机产品的价格。为了解决这个问题，他收集了多家公司的手机销售数据。该数据为二手手机的各个性能的数据，最后根据这些性能得到4个价格区间，作为这些二手手机售出的价格区间。<br>我们需要帮助小明找出手机的功能（例如：RAM等）与其售价之间的某种关系。我们可以使用机器学习的方法来解决这个问题，也可以构建一个全连接的网络。<br>需要注意的是: 在这个问题中，我们不需要预测实际价格，而是一个价格范围，它的范围使用 0、1、2、3 来表示，所以该问题也是一个分类问题。接下来我们还是按照四个步骤来完成这个任务：</p>
<ul>
<li>准备训练集数据</li>
<li>构建要使用的模型</li>
<li>模型训练</li>
<li>模型预测评估</li>
</ul>
<h1 id="数据集介绍"><a href="#数据集介绍" class="headerlink" title="数据集介绍"></a>数据集介绍</h1><p>相关的原始数据可以在阿里云天池找到，关键词：二手手机价格预测<br>包含信息预览：</p>
<ul>
<li>battery_power: 电池一次可储存的总能量，单位为毫安时</li>
<li>blue : 是否有蓝牙</li>
<li>clock_speed: 微处理器执行指令的速度</li>
<li>dual_sim: 是否支持双卡</li>
<li>fc: 前置摄像头百万像素</li>
<li>。。。</li>
<li>price_range: 价格区间 (0, 1, 2, 3)</li>
</ul>
<p>数据共有 2000 条, 其中 1600 条数据作为训练集, 400 条数据用作测试集。 我们使用 sklearn 的数据集划分工作来完成。并使用 PyTorch 的 TensorDataset 来将数据集构建为 Dataset 对象，方便构造数据集加载对象。</p>
<h1 id="完整代码"><a href="#完整代码" class="headerlink" title="完整代码"></a>完整代码</h1><p>代码流程各个模块我们前面都提到过，这里用该案例串联起完整流程</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"><span class="keyword">from</span> torch.utils.data <span class="keyword">import</span> TensorDataset</span><br><span class="line"><span class="keyword">from</span> torch.utils.data <span class="keyword">import</span> DataLoader</span><br><span class="line"><span class="keyword">import</span> torch.nn <span class="keyword">as</span> nn</span><br><span class="line"><span class="keyword">import</span> torch.optim <span class="keyword">as</span> optim</span><br><span class="line"><span class="keyword">from</span> sklearn.datasets <span class="keyword">import</span> make_regression</span><br><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> train_test_split</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">from</span> torchsummary <span class="keyword">import</span> summary</span><br><span class="line"><span class="keyword">import</span> time</span><br><span class="line"><span class="keyword">from</span> sklearn.preprocessing <span class="keyword">import</span> StandardScaler</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">create_dataset</span>():</span><br><span class="line">    data=pd.read_csv(<span class="string">&quot;data/手机价格预测.csv&quot;</span>)</span><br><span class="line">    x=data.iloc[:,:-<span class="number">1</span>]</span><br><span class="line">    y=data.iloc[:,-<span class="number">1</span>]</span><br><span class="line">    <span class="built_in">print</span>(x.shape)</span><br><span class="line">    <span class="built_in">print</span>(y.shape)</span><br><span class="line">    x=x.astype(np.float32)</span><br><span class="line">    x_train,x_test,y_train,y_test=train_test_split(x,y,train_size=<span class="number">0.8</span>,shuffle=<span class="literal">True</span>,stratify=y)</span><br><span class="line">    scaler = StandardScaler()</span><br><span class="line">    x_train = scaler.fit_transform(x_train)  <span class="comment"># 训练集拟合+转换</span></span><br><span class="line">    x_test = scaler.transform(x_test)</span><br><span class="line">    train_data = TensorDataset(torch.from_numpy(x_train), torch.tensor(y_train.values))</span><br><span class="line">    test_data = TensorDataset(torch.from_numpy(x_test), torch.tensor(y_test.values))</span><br><span class="line">    <span class="comment">#      训练集      测试集    输入特征维度  输出特征维度 </span></span><br><span class="line">    <span class="keyword">return</span> train_data,test_data,x.shape[<span class="number">1</span>],<span class="built_in">len</span>(np.unique(y))</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Model</span>(nn.Module):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, in_dim,out_dim</span>):</span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line">        <span class="variable language_">self</span>.linear1=nn.Linear(in_dim,<span class="number">128</span>)</span><br><span class="line">        <span class="variable language_">self</span>.linear2=nn.Linear(<span class="number">128</span>,<span class="number">256</span>)</span><br><span class="line">        <span class="variable language_">self</span>.linear3=nn.Linear(<span class="number">256</span>,<span class="number">512</span>)</span><br><span class="line">        <span class="variable language_">self</span>.linear4=nn.Linear(<span class="number">512</span>,<span class="number">128</span>)</span><br><span class="line">        <span class="variable language_">self</span>.output=nn.Linear(<span class="number">128</span>,out_dim)</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self,x</span>):</span><br><span class="line">        x=torch.relu(<span class="variable language_">self</span>.linear1(x))</span><br><span class="line">        x=torch.relu(<span class="variable language_">self</span>.linear2(x))</span><br><span class="line">        x=torch.relu(<span class="variable language_">self</span>.linear3(x))</span><br><span class="line">        x=torch.relu(<span class="variable language_">self</span>.linear4(x))</span><br><span class="line">        <span class="comment"># x=torch.softmax(self.output(x),dim=1)</span></span><br><span class="line">        <span class="comment"># 这步不需要，因为多分类交叉熵损失函数自带softmax</span></span><br><span class="line">        x=<span class="variable language_">self</span>.output(x)</span><br><span class="line">        <span class="keyword">return</span> x</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">train</span>(<span class="params">train_data,in_dim,out_dim</span>):</span><br><span class="line">    train_loader=DataLoader(train_data,<span class="number">16</span>,<span class="literal">True</span>)</span><br><span class="line">    model=Model(in_dim,out_dim)</span><br><span class="line">    criterion=nn.CrossEntropyLoss()</span><br><span class="line">    optimizer=optim.Adam(model.parameters(),lr=<span class="number">1e-4</span>)</span><br><span class="line">    epochs=<span class="number">50</span></span><br><span class="line">    <span class="keyword">for</span> epoch <span class="keyword">in</span> <span class="built_in">range</span>(epochs):</span><br><span class="line">        total_loss,batch_cnt=<span class="number">0.0</span>,<span class="number">0</span></span><br><span class="line">        start=time.time()</span><br><span class="line">        <span class="keyword">for</span> x,y <span class="keyword">in</span> train_loader:</span><br><span class="line">            model.train() <span class="comment"># 训练模式</span></span><br><span class="line">            y_pred=model(x)</span><br><span class="line">            loss=criterion(y_pred,y)</span><br><span class="line">            optimizer.zero_grad()</span><br><span class="line">            loss.backward()</span><br><span class="line">            optimizer.step()</span><br><span class="line">            total_loss+=loss.item()</span><br><span class="line">            batch_cnt+=<span class="number">1</span></span><br><span class="line">        <span class="built_in">print</span>(<span class="string">f&#x27;epoch: <span class="subst">&#123;epoch + <span class="number">1</span>&#125;</span>, loss: <span class="subst">&#123;total_loss / batch_cnt:<span class="number">.4</span>f&#125;</span>, time: <span class="subst">&#123;time.time() - start:<span class="number">.2</span>f&#125;</span>s&#x27;</span>)</span><br><span class="line">    torch.save(model.state_dict(), <span class="string">&#x27;phone.pth&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">evaluate</span>(<span class="params">test_data,in_dim,out_dim</span>):</span><br><span class="line">    model=Model(in_dim,out_dim)</span><br><span class="line">    model.load_state_dict(torch.load(<span class="string">&quot;phone.pth&quot;</span>))</span><br><span class="line">    test_loader=DataLoader(test_data,batch_size=<span class="number">8</span>,shuffle=<span class="literal">False</span>)</span><br><span class="line">    cnt=<span class="number">0</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span> x,y <span class="keyword">in</span> test_loader:</span><br><span class="line">        model.<span class="built_in">eval</span>()</span><br><span class="line">        y_pred=model(x)</span><br><span class="line">        y_pred=torch.argmax(y_pred,dim=-<span class="number">1</span>)</span><br><span class="line">        cnt+=(y_pred==y).<span class="built_in">sum</span>()</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;acc: <span class="subst">&#123;cnt/<span class="built_in">len</span>(test_data)&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">&quot;__main__&quot;</span>:</span><br><span class="line">    train_data,test_data,in_dim,out_dim=create_dataset()</span><br><span class="line">    <span class="comment"># model=Model(in_dim,out_dim)</span></span><br><span class="line">    <span class="comment"># summary(model,input_size=(16,in_dim))</span></span><br><span class="line">    train(train_data,in_dim,out_dim)</span><br><span class="line">    evaluate(test_data,in_dim,out_dim)</span><br></pre></td></tr></table></figure>

<p>我们通过以下方面进行调优:</p>
<ol>
<li>对输入数据进行标准化</li>
<li>调整优化方法</li>
<li>调整学习率</li>
<li>增加批量归一化层</li>
<li>增加网络层数、神经元个数</li>
<li>增加训练轮数</li>
</ol>
<p>…</p>
]]></content>
      <categories>
        <category>深度学习</category>
      </categories>
      <tags>
        <tag>深度学习</tag>
        <tag>张量</tag>
        <tag>神经网络</tag>
        <tag>机器学习</tag>
      </tags>
  </entry>
  <entry>
    <title>模糊综合评价</title>
    <url>/2025/12/29/%E6%A8%A1%E7%B3%8A%E7%BB%BC%E5%90%88%E8%AF%84%E4%BB%B7/</url>
    <content><![CDATA[<p>常用的主观评价类模型</p>
<span id="more"></span>

<h1 id="“模糊”概念"><a href="#“模糊”概念" class="headerlink" title="“模糊”概念"></a>“模糊”概念</h1><p>模糊综合评价的核心思想就是：用模糊数学的方法，把这些模糊的定性描述，转化为可计算的定量结果。关键是两个核心概念：</p>
<ol>
<li>模糊集：比如“满意”就是一个模糊集，不是非黑即白的“满意”或“不满意”，而是有程度差异。</li>
<li>隶属度：衡量某个元素属于某个模糊集的程度，用 $\mu_A(x)$ 表示（A是模糊集，x是评价对象），取值范围是 $0 \leq \mu_A(x) \leq 1$。比如用户对产品外观的“满意隶属度”是0.8，就说明接近“满意”但还没到满分；0.3则说明偏向“不满意”。</li>
</ol>
<h1 id="核心步骤"><a href="#核心步骤" class="headerlink" title="核心步骤"></a>核心步骤</h1><p>以“评价新产品用户满意度”为例：</p>
<h2 id="步骤1：确定评价对象、因素集和评语集"><a href="#步骤1：确定评价对象、因素集和评语集" class="headerlink" title="步骤1：确定评价对象、因素集和评语集"></a>步骤1：确定评价对象、因素集和评语集</h2><ul>
<li><p>评价对象：某新款蓝牙耳机</p>
</li>
<li><p>因素集U（要评价的指标）：比如 U &#x3D; {外观设计, 性价比, 使用手感, 续航能力}（用 $U &#x3D; {u_1, u_2, …, u_n}$ 表示，n是指标个数）</p>
</li>
<li><p>评语集V（评价结果等级）：比如 V &#x3D; {很满意, 满意, 一般, 不满意}（用 $V &#x3D; {v_1, v_2, …, v_m}$ 表示，m是等级个数）</p>
</li>
</ul>
<h2 id="步骤2：确定各评价因素的权重"><a href="#步骤2：确定各评价因素的权重" class="headerlink" title="步骤2：确定各评价因素的权重"></a>步骤2：确定各评价因素的权重</h2><p>层次分析法，熵权法等。</p>
<h2 id="步骤3：构建模糊评价矩阵R"><a href="#步骤3：构建模糊评价矩阵R" class="headerlink" title="步骤3：构建模糊评价矩阵R"></a>步骤3：构建模糊评价矩阵R</h2><p>这一步是把“每个指标对应不同评语的隶属度”整理成矩阵。比如邀请50个用户打分，对“外观设计”：25人很满意、20人满意、5人一般、0人不满意，那么外观的隶属度就是 [25&#x2F;50, 20&#x2F;50, 5&#x2F;50, 0&#x2F;50] &#x3D; [0.5, 0.4, 0.1, 0.0]。</p>
<p>把所有指标的隶属度整理成矩阵R（n行m列，n是指标数，m是评语数）：</p>
<p>$$R &#x3D; \begin{bmatrix} 0.5 &amp; 0.4 &amp; 0.1 &amp; 0.0 \ 0.3 &amp; 0.5 &amp; 0.2 &amp; 0.0 \ 0.4 &amp; 0.3 &amp; 0.2 &amp; 0.1 \ 0.2 &amp; 0.4 &amp; 0.3 &amp; 0.1 \end{bmatrix}$$</p>
<p>解释：第一行是外观的隶属度，第二行是性价比，第三行是手感，第四行是续航。</p>
<h2 id="步骤4：模糊合成运算（核心计算）"><a href="#步骤4：模糊合成运算（核心计算）" class="headerlink" title="步骤4：模糊合成运算（核心计算）"></a>步骤4：模糊合成运算（核心计算）</h2><p>用权重集W和评价矩阵R做合成运算，得到综合评价结果B，公式是 $B &#x3D; W \circ R$（“∘”是模糊合成算子，常用“加权平均法”，适合需要量化结果的场景，也有主因素决定型算子，如取大取小等，有些会运用于需要一票否决的地方）。</p>
<p>计算过程（以加权平均为例）：每个评语的得分 &#x3D; 权重1×对应隶属度1 + 权重2×对应隶属度2 + … + 权重n×对应隶属度n</p>
<p>代入数值计算：</p>
<ul>
<li><p>很满意得分：0.2×0.5 + 0.3×0.3 + 0.2×0.4 + 0.3×0.2 &#x3D; 0.1 + 0.09 + 0.08 + 0.06 &#x3D; 0.33</p>
</li>
<li><p>满意得分：0.2×0.4 + 0.3×0.5 + 0.2×0.3 + 0.3×0.4 &#x3D; 0.08 + 0.15 + 0.06 + 0.12 &#x3D; 0.41</p>
</li>
<li><p>一般得分：0.2×0.1 + 0.3×0.2 + 0.2×0.2 + 0.3×0.3 &#x3D; 0.02 + 0.06 + 0.04 + 0.09 &#x3D; 0.21</p>
</li>
<li><p>不满意得分：0.2×0.0 + 0.3×0.0 + 0.2×0.1 + 0.3×0.1 &#x3D; 0 + 0 + 0.02 + 0.03 &#x3D; 0.05</p>
</li>
</ul>
<p>所以综合评价结果B &#x3D; [0.33, 0.41, 0.21, 0.05]</p>
<h2 id="步骤5：得出评价结论"><a href="#步骤5：得出评价结论" class="headerlink" title="步骤5：得出评价结论"></a>步骤5：得出评价结论</h2><p>找B中最大值对应的评语！这里“满意”的得分0.41最高，所以结论是：这款蓝牙耳机的用户满意度为“满意”。</p>
<h1 id="矩阵运算"><a href="#矩阵运算" class="headerlink" title="矩阵运算"></a>矩阵运算</h1><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"></span><br><span class="line">W = np.array([<span class="number">0.5</span>, <span class="number">0.3</span>, <span class="number">0.2</span>])  <span class="comment"># 权重集（已归一化）</span></span><br><span class="line">R = np.array([</span><br><span class="line">    [<span class="number">0.6</span>, <span class="number">0.3</span>, <span class="number">0.1</span>, <span class="number">0</span>],</span><br><span class="line">    [<span class="number">0.4</span>, <span class="number">0.4</span>, <span class="number">0.2</span>, <span class="number">0</span>],</span><br><span class="line">    [<span class="number">0.3</span>, <span class="number">0.5</span>, <span class="number">0.2</span>, <span class="number">0</span>]</span><br><span class="line">])  <span class="comment"># 3指标×4评语的隶属度矩阵</span></span><br><span class="line">V = np.array([<span class="number">90</span>, <span class="number">80</span>, <span class="number">70</span>, <span class="number">60</span>])  <span class="comment"># 评语量化分值</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 模糊合成运算</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">fuzzy_synthesis</span>(<span class="params">W, R</span>):</span><br><span class="line">    B = np.dot(W, R) </span><br><span class="line">    B = B / np.<span class="built_in">sum</span>(B) <span class="keyword">if</span> np.<span class="built_in">sum</span>(B) != <span class="number">0</span> <span class="keyword">else</span> B</span><br><span class="line">    <span class="keyword">return</span> B</span><br><span class="line"></span><br><span class="line"><span class="comment"># 综合得分</span></span><br><span class="line">B = fuzzy_synthesis(W, R)</span><br><span class="line">score = np.dot(B, V.T)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;综合评价向量 B:&quot;</span>, B)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;综合得分:&quot;</span>, <span class="built_in">round</span>(score, <span class="number">2</span>))</span><br></pre></td></tr></table></figure>

<h1 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h1><ol>
<li><p>优势：比传统打分更贴合实际场景，结果也更客观，比如不会因为某个指标满分就掩盖其他指标的不足。</p>
</li>
<li><p>注意点：权重的确定很关键，如果权重设置不合理，结果会偏差很大。</p>
</li>
</ol>
]]></content>
      <categories>
        <category>数学建模</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>评价类模型</tag>
        <tag>模糊综合评价</tag>
      </tags>
  </entry>
  <entry>
    <title>神经网络优化方法</title>
    <url>/2026/03/12/%E7%A5%9E%E7%BB%8F%E7%BD%91%E7%BB%9C%E4%BC%98%E5%8C%96%E6%96%B9%E6%B3%95/</url>
    <content><![CDATA[<p>一个好的神经网络离不开优化 d(&#96;･∀･)b</p>
<span id="more"></span>
<h1 id="神经网络优化方法"><a href="#神经网络优化方法" class="headerlink" title="神经网络优化方法"></a>神经网络优化方法</h1><p>训练多层神经网络有很多种算法，误差反向传播算法（Back Propagation）是其中最杰出的代表，它是目前最成功的神经网络学习算法。现实任务使用神经网络时，大多是在使用 BP 算法进行训练。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">W新=W旧-梯度*学习率</span><br></pre></td></tr></table></figure>
<p>这是 BP 算法中最核心的公式，对神经网络的优化过程自然也从这个公式入手，主要从梯度下降和学习率两个地方着手优化。  </p>
<p>也因为这些优化很常用，pytorch也可以用optimizer优化器来进行训练过程，详细见下代码部分。</p>
<p><strong>在进行模型训练时，让我们先回顾三个基础的概念：</strong></p>
<ol>
<li><strong>Epoch</strong>: 使用全部数据对模型进行以此完整训练，训练次数</li>
<li><strong>Batch</strong>: 使用训练集中的小部分样本对模型权重进行以此反向传播的参数更新，每次训练每批次样本数量</li>
<li><strong>Iteration</strong>: 使用一个 Batch 数据对模型进行一次参数更新的过程，每次训练批次数</li>
</ol>
<p>这些参数在之前我们搭建神经网络时出现过，后面的一些优化算法也会涉及到他们。</p>
<h2 id="梯度下降过程"><a href="#梯度下降过程" class="headerlink" title="梯度下降过程"></a>梯度下降过程</h2><p>相信大家已经非常熟悉梯度下降了<br>在梯度下降优化算法中，可能会碰到以下情况：</p>
<ul>
<li>碰到平缓区域，梯度值较小，参数优化变慢</li>
<li>碰到 “鞍点” ，梯度为0，参数无法优化</li>
<li>碰到局部最小值，参数不是最优</li>
</ul>
<h3 id="动量算法Momentum"><a href="#动量算法Momentum" class="headerlink" title="动量算法Momentum"></a>动量算法Momentum</h3><h4 id="指数加权平均"><a href="#指数加权平均" class="headerlink" title="指数加权平均"></a>指数加权平均</h4><p>我们最常见的算数平均指的是将所有数加起来除以数的个数，每个数的权重是相同的。指数加权平均指的是给每个数赋予不同的权重求得平均数。移动平均数，指的是计算最近邻的 N 个数来获得平均数。</p>
<p><strong>指数移动加权平均</strong>则是参考各数值，并且各数值的权重都不同，距离越远的数字对平均数计算的贡献就越小（权重较小），距离越近则对平均数的计算贡献就越大（权重越大）。</p>
<p>比如：明天气温怎么样，和昨天气温有很大关系，而和一个月前的气温关系就小一些。</p>
<p>当梯度下降碰到 “峡谷” 、”平缓”、”鞍点” 区域时, 参数更新速度变慢。 Momentum 通过<strong>指数加权平均法</strong>，累计历史梯度值，进行参数更新，越近的梯度值对当前参数更新的重要性越大。</p>
<p>相关的算法在<a href="https://longnamehaha.github.io/2025/12/15/ARIMA/" title="看看ARIMA">ARIMA</a>中也有应用.</p>
<h4 id="详细公式"><a href="#详细公式" class="headerlink" title="详细公式"></a>详细公式</h4><p><strong>梯度计算公式</strong>：<br>​				$$s_t&#x3D;βs_{t−1}+(1−β)g_t$$<br><strong>参数更新公式</strong>：<br>​				$$w_t&#x3D;w_{t−1}−ηs_t$$</p>
<ul>
<li><p>$s_t$是当前时刻指数加权平均梯度值</p>
</li>
<li><p>$s_{t-1}$是历史指数加权平均梯度值</p>
</li>
<li><p>$g_t$是当前时刻的梯度值</p>
</li>
<li><p>β 是调节权重系数，通常取 0.9 或 0.99</p>
</li>
<li><p>η是学习率</p>
</li>
<li><p>$w_t$是当前时刻模型权重参数</p>
</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">举个例子，假设：权重 β 为 <span class="number">0.9</span>，例如：</span><br><span class="line">第一次梯度值：s1 = g1 = w1 </span><br><span class="line">第二次梯度值：s2 = <span class="number">0.9</span>*s1 + g2*<span class="number">0.1</span> </span><br><span class="line">第三次梯度值：s3 = <span class="number">0.9</span>*s2 + g3*<span class="number">0.1</span> </span><br><span class="line">第四次梯度值：s4 = <span class="number">0.9</span>*s3 + g4*<span class="number">0.1</span> </span><br><span class="line"><span class="number">1.</span> w 表示初始梯度</span><br><span class="line"><span class="number">2.</span> g 表示当前轮数计算出的梯度值</span><br><span class="line"><span class="number">3.</span> s 表示历史梯度移动加权平均值</span><br><span class="line"></span><br><span class="line">梯度下降公式中梯度的计算，就不再是当前时刻t的梯度值，而是历史梯度值的指数移动加权平均值。</span><br><span class="line">公式修改为：</span><br><span class="line">Wt = Wt-<span class="number">1</span> - η*St</span><br><span class="line">Wt：当前时刻模型权重参数</span><br><span class="line">St：当前时刻指数加权平均梯度值</span><br><span class="line">η：学习率</span><br></pre></td></tr></table></figure>
<p>简单来讲，就是为梯度下降增加一个<strong>惯性</strong>，或者叫动量，在遇到平缓地区时，由于惯性作用，更新速度不会影响太大。</p>
<h4 id="代码演示"><a href="#代码演示" class="headerlink" title="代码演示"></a>代码演示</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">test01</span>():</span><br><span class="line">    <span class="comment"># 1 初始化权重参数</span></span><br><span class="line">    w = torch.tensor([<span class="number">1.0</span>], requires_grad=<span class="literal">True</span>, dtype=torch.float32)</span><br><span class="line">    loss = ((w ** <span class="number">2</span>) / <span class="number">2.0</span>).<span class="built_in">sum</span>()</span><br><span class="line">    <span class="comment"># 2 实例化优化方法：SGD 指定参数beta=0.9</span></span><br><span class="line">    optimizer = torch.optim.SGD([w], lr=<span class="number">0.01</span>, momentum=<span class="number">0.9</span>)</span><br><span class="line">    <span class="comment"># 3 第1次更新 计算梯度，并对参数进行更新</span></span><br><span class="line">    optimizer.zero_grad()</span><br><span class="line">    loss.backward()</span><br><span class="line">    optimizer.step()</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;第1次: 梯度w.grad: %f, 更新后的权重:%f&#x27;</span> % (w.grad.numpy(), w.detach().numpy()))</span><br><span class="line">    <span class="comment"># 4 第2次更新 计算梯度，并对参数进行更新</span></span><br><span class="line">    <span class="comment"># 使用更新后的参数机选输出结果</span></span><br><span class="line">    loss = ((w ** <span class="number">2</span>) / <span class="number">2.0</span>).<span class="built_in">sum</span>()</span><br><span class="line">    optimizer.zero_grad()</span><br><span class="line">    loss.backward()</span><br><span class="line">    optimizer.step()</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;第2次: 梯度w.grad: %f, 更新后的权重:%f&#x27;</span> % (w.grad.numpy(), w.detach().numpy()))</span><br></pre></td></tr></table></figure>
<h3 id="AdaGrad"><a href="#AdaGrad" class="headerlink" title="AdaGrad"></a>AdaGrad</h3><p>AdaGrad 通过对不同的参数分量使用不同的学习率，<strong>AdaGrad 的学习率总体会逐渐减小</strong>，这是因为 AdaGrad 认为：在起初时，我们距离最优目标仍较远，可以使用较大的学习率，加快训练速度，随着迭代次数的增加，学习率逐渐下降。</p>
<h4 id="详细公式-1"><a href="#详细公式-1" class="headerlink" title="详细公式"></a>详细公式</h4><p>计算步骤：</p>
<ol>
<li><p>初始化学习率 η、初始化参数w、小常数 σ &#x3D; 1e-10, 防止除0</p>
</li>
<li><p>初始化梯度累计变量 s &#x3D; 0</p>
</li>
<li><p>从训练集中采样 m 个样本的小批量，计算梯度$g_t​$</p>
</li>
<li><p><strong>累积平方梯度</strong>，⊙ 表示各个分量相乘，:<br>$$s_t​ &#x3D; s_{t-1}​ + g_t ⊙ g_t​$$</p>
</li>
<li><p>更新后学习率 η 的计算公式如下：<br>$$η&#x3D;\frac{η}{\sqrt{s_t}+σ}$$</p>
</li>
<li><p>权重参数更新公式如下：</p>
</li>
</ol>
<p>$$w_t &#x3D; w_{t-1} - \frac{η}{\sqrt{s_t}+σ} · g_t$$</p>
<ol start="7">
<li>重复 3-7 步骤</li>
</ol>
<p><strong>AdaGrad 缺点是可能会使得学习率过早、过量的降低，导致模型训练后期学习率太小，较难找到最优解。</strong></p>
<h4 id="代码演示-1"><a href="#代码演示-1" class="headerlink" title="代码演示"></a>代码演示</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">test02</span>():</span><br><span class="line">    <span class="comment"># 1 初始化权重参数</span></span><br><span class="line">    w = torch.tensor([<span class="number">1.0</span>], requires_grad=<span class="literal">True</span>, dtype=torch.float32)</span><br><span class="line">    loss = ((w ** <span class="number">2</span>) / <span class="number">2.0</span>).<span class="built_in">sum</span>()</span><br><span class="line">    <span class="comment"># 2 实例化优化方法：adagrad优化方法</span></span><br><span class="line">    optimizer = torch.optim.Adagrad([w], lr=<span class="number">0.01</span>)</span><br><span class="line">    <span class="comment"># 3 第1次更新 计算梯度，并对参数进行更新</span></span><br><span class="line">    optimizer.zero_grad()</span><br><span class="line">    loss.backward()</span><br><span class="line">    optimizer.step()</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;第1次: 梯度w.grad: %f, 更新后的权重:%f&#x27;</span> % (w.grad.numpy(), w.detach().numpy()))</span><br><span class="line">    <span class="comment"># 4 第2次更新 计算梯度，并对参数进行更新</span></span><br><span class="line">    <span class="comment"># 使用更新后的参数机选输出结果</span></span><br><span class="line">    loss = ((w ** <span class="number">2</span>) / <span class="number">2.0</span>).<span class="built_in">sum</span>()</span><br><span class="line">    optimizer.zero_grad()</span><br><span class="line">    loss.backward()</span><br><span class="line">    optimizer.step()</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;第2次: 梯度w.grad: %f, 更新后的权重:%f&#x27;</span> % (w.grad.numpy(), w.detach().numpy()))</span><br></pre></td></tr></table></figure>

<h3 id="RMSProp"><a href="#RMSProp" class="headerlink" title="RMSProp"></a>RMSProp</h3><p><strong>RMSProp 优化算法是对 AdaGrad 的优化</strong>。最主要的不同是，其使用<strong>指数加权平均梯度</strong>替换历史梯度的平方和。</p>
<h4 id="详细公式-2"><a href="#详细公式-2" class="headerlink" title="详细公式"></a>详细公式</h4><p>计算过程：</p>
<ol>
<li><p>初始化学习率 η、初始化权重参数w、小常数 σ &#x3D; 1e-10</p>
</li>
<li><p>初始化梯度累计变量 s &#x3D; 0</p>
</li>
<li><p>从训练集中采样 m 个样本的小批量，计算梯度 $g_t$</p>
</li>
<li><p>使用指数加权平均累计历史梯度（对比AdaGrad，仅此步优化）：<br>$$s_t &#x3D; β·s_{t-1} + (1-β)g_t⊙g_t$$</p>
</li>
<li><p>更新后学习率 η 的计算公式如下：<br>$$η&#x3D;\frac{η}{\sqrt{s_t}+σ}$$</p>
</li>
<li><p>权重参数更新公式如下：</p>
</li>
</ol>
<p>$$w_t &#x3D; w_{t-1} - \frac{η}{\sqrt{s_t}+σ} · g_t$$</p>
<ol start="7">
<li>重复 3-7 步骤</li>
</ol>
<p>RMSProp 与 AdaGrad 最大的区别是对梯度的累积方式不同，对于每个梯度分量仍然使用不同的学习率。</p>
<p><strong>RMSProp 通过引入衰减系数β，控制历史梯度对历史梯度信息获取的多少</strong>. 被证明在神经网络非凸条件下的优化更好，学习率衰减更加合理一些。</p>
<p>需要注意的是：AdaGrad 和 RMSProp 都是对于不同的参数分量使用不同的学习率，如果某个参数分量的梯度值较大，则对应的学习率就会较小，如果某个参数分量的梯度较小，则对应的学习率就会较大一些。</p>
<h4 id="代码演示-2"><a href="#代码演示-2" class="headerlink" title="代码演示"></a>代码演示</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">test03</span>():</span><br><span class="line">    <span class="comment"># 1 初始化权重参数</span></span><br><span class="line">    w = torch.tensor([<span class="number">1.0</span>], requires_grad=<span class="literal">True</span>, dtype=torch.float32)</span><br><span class="line">    loss = ((w ** <span class="number">2</span>) / <span class="number">2.0</span>).<span class="built_in">sum</span>()</span><br><span class="line">    <span class="comment"># 2 实例化优化方法：RMSprop算法，其中alpha对应beta</span></span><br><span class="line">    optimizer = torch.optim.RMSprop([w], lr=<span class="number">0.01</span>, alpha=<span class="number">0.9</span>)</span><br><span class="line">    <span class="comment"># 3 第1次更新 计算梯度，并对参数进行更新</span></span><br><span class="line">    optimizer.zero_grad()</span><br><span class="line">    loss.backward()</span><br><span class="line">    optimizer.step()</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;第1次: 梯度w.grad: %f, 更新后的权重:%f&#x27;</span> % (w.grad.numpy(), w.detach().numpy()))</span><br><span class="line">    <span class="comment"># 4 第2次更新 计算梯度，并对参数进行更新</span></span><br><span class="line">    <span class="comment"># 使用更新后的参数机选输出结果</span></span><br><span class="line">    loss = ((w ** <span class="number">2</span>) / <span class="number">2.0</span>).<span class="built_in">sum</span>()</span><br><span class="line">    optimizer.zero_grad()</span><br><span class="line">    loss.backward()</span><br><span class="line">    optimizer.step()</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;第2次: 梯度w.grad: %f, 更新后的权重:%f&#x27;</span> % (w.grad.numpy(), w.detach().numpy()))</span><br></pre></td></tr></table></figure>

<h3 id="Adam"><a href="#Adam" class="headerlink" title="Adam"></a>Adam</h3><h4 id="公式原理"><a href="#公式原理" class="headerlink" title="公式原理"></a>公式原理</h4><ul>
<li><p>Momentum 使用指数加权平均计算当前的梯度值</p>
</li>
<li><p>AdaGrad、RMSProp 使用自适应的学习率</p>
</li>
<li><p>Adam优化算法（Adaptive Moment Estimation，自适应矩估计）将 Momentum 和 RMSProp 算法结合在一起</p>
<ul>
<li>修正梯度: 使⽤梯度的指数加权平均 </li>
<li>修正学习率: 使⽤梯度平⽅的指数加权平均</li>
</ul>
</li>
<li><p><strong>原理</strong>：Adam 是结合了 <strong>Momentum</strong> 和 <strong>RMSProp</strong> 优化算法的优点的自适应学习率算法。它计算了梯度的一阶矩（平均值）和二阶矩（梯度的方差）的自适应估计，从而动态调整学习率。</p>
</li>
</ul>
<p>简单来讲，<strong>Adam&#x3D;Momentum+RMSProp</strong></p>
<ul>
<li><p><strong>梯度计算公式</strong>：<br>$$m_t&#x3D;β_1m_{t−1}+(1−β_1)g_t$$<br>$$s_t&#x3D;β_2s_{t−1}+(1−β_2)gt^2​$$<br>$$\hat{m_t} &#x3D; \frac{m_t}{1−β_1^t}$$<br>$$\hat{s_t}​&#x3D;\frac{s_t}{1−β_2^t}​$$</p>
</li>
<li><p><strong>权重参数更新公式</strong>:<br>$$w_t​ &#x3D; w_{t−1}​ − \frac{η}{\sqrt{\hat{s_t}}+ϵ}\hat{m_t}​$$</p>
</li>
</ul>
<p>其中，$m_t$ 是梯度的一阶矩估计，$s_t$ 是梯度的二阶矩估计，$ \hat{m_t}$和 $ \hat{s_t} $ 是偏差校正后的估计。</p>
<h4 id="代码实现"><a href="#代码实现" class="headerlink" title="代码实现"></a>代码实现</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">test04</span>():</span><br><span class="line">    <span class="comment"># 1 初始化权重参数</span></span><br><span class="line">    w = torch.tensor([<span class="number">1.0</span>], requires_grad=<span class="literal">True</span>)</span><br><span class="line">    loss = ((w ** <span class="number">2</span>) / <span class="number">2.0</span>).<span class="built_in">sum</span>()</span><br><span class="line">    <span class="comment"># 2 实例化优化方法：Adam算法，其中betas是指数加权的系数</span></span><br><span class="line">    optimizer = torch.optim.Adam([w], lr=<span class="number">0.01</span>, betas=[<span class="number">0.9</span>, <span class="number">0.99</span>])</span><br><span class="line">    <span class="comment"># 3 第1次更新 计算梯度，并对参数进行更新</span></span><br><span class="line">    optimizer.zero_grad()</span><br><span class="line">    loss.backward()</span><br><span class="line">    optimizer.step()</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;第1次: 梯度w.grad: %f, 更新后的权重:%f&#x27;</span> % (w.grad.numpy(), w.detach().numpy()))</span><br><span class="line">    <span class="comment"># 4 第2次更新 计算梯度，并对参数进行更新</span></span><br><span class="line">    <span class="comment"># 使用更新后的参数机选输出结果</span></span><br><span class="line">    loss = ((w ** <span class="number">2</span>) / <span class="number">2.0</span>).<span class="built_in">sum</span>()</span><br><span class="line">    optimizer.zero_grad()</span><br><span class="line">    loss.backward()</span><br><span class="line">    optimizer.step()</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;第2次: 梯度w.grad: %f, 更新后的权重:%f&#x27;</span> % (w.grad.numpy(), w.detach().numpy()))</span><br></pre></td></tr></table></figure>


<h3 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h3><table>
<thead>
<tr>
<th><strong>优化算法</strong></th>
<th><strong>优点</strong></th>
<th><strong>缺点</strong></th>
<th><strong>适用场景</strong></th>
</tr>
</thead>
<tbody><tr>
<td><strong>SGD</strong></td>
<td>简单、容易实现。</td>
<td>收敛速度较慢，容易震荡，特别是在复杂问题中。</td>
<td>用于简单任务，或者当数据特征分布相对稳定时。</td>
</tr>
<tr>
<td><strong>Momentum</strong></td>
<td>可以加速收敛，减少震荡，特别是在高曲率区域。</td>
<td>需要手动调整动量超参数，可能会在小步长训练中过度更新。</td>
<td>用于非平稳优化问题，尤其是深度学习中的应用。</td>
</tr>
<tr>
<td><strong>AdaGrad</strong></td>
<td>自适应调整学习率，适用于稀疏数据。</td>
<td>学习率会在训练过程中逐渐衰减，可能导致早期停滞。</td>
<td>适合稀疏数据，如 NLP 或推荐系统中的特征。</td>
</tr>
<tr>
<td><strong>RMSProp</strong></td>
<td>解决了 AdaGrad 学习率过早衰减的问题，适应性强。</td>
<td>需要选择合适的超参数，更新可能会过于激进。</td>
<td>适用于动态问题、非平稳目标函数，如深度学习训练。</td>
</tr>
<tr>
<td><strong>Adam</strong></td>
<td>结合了 Momentum 和 RMSProp 的优点，适应性强且稳定。</td>
<td>需要调节更多的超参数，训练过程中可能会产生较大波动。</td>
<td>广泛适用于各种深度学习任务，特别是非平稳和复杂问题。</td>
</tr>
</tbody></table>
<ul>
<li><p><strong>简单任务和较小的模型</strong>：SGD 或 Momentum</p>
</li>
<li><p><strong>复杂任务或有大量数据</strong>：Adam 是最常用的选择，因其在大部分任务上都表现优秀</p>
</li>
<li><p><strong>需要处理稀疏数据或文本数据</strong>：Adagrad 或 RMSProp</p>
</li>
</ul>
<h2 id="学习率衰减方法"><a href="#学习率衰减方法" class="headerlink" title="学习率衰减方法"></a>学习率衰减方法</h2><p>在前面梯度下降的优化方法中，某些方法或多或少已经涉及到了学习率的优化。<br>一般来说，在训练的初始阶段，学习率要比较大些，在训练后期，学习率应该减少，下面介绍一些学习率衰减方法。<br>这里的学习衰减方法，都需要设定一个衰减系数gamma，<br>学习率更新：$$ lr&#x3D;lr·gamma$$<br>所以，广义来说，学习率都是指数衰减的。</p>
<h3 id="等间隔学习率衰减"><a href="#等间隔学习率衰减" class="headerlink" title="等间隔学习率衰减"></a>等间隔学习率衰减</h3><p>如下图所示，我们可以手动干预学习率，一般训练初期学习率大，后期学习率小，前面的算法中也提到过.<br><img src="/../images/learning_rate_decay.jpg" alt="学习率衰减示意图"><br>等间距衰减最为简单，只需要设定间隔以及下降比例就行。  </p>
<p>api：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 2.优化器</span></span><br><span class="line">optimizer = optim.SGD([w], lr=LR, momentum=<span class="number">0.9</span>)</span><br><span class="line"><span class="comment"># 3.设置学习率下降策略</span></span><br><span class="line">scheduler_lr = optim.lr_scheduler.StepLR(optimizer, step_size=<span class="number">50</span>, gamma=<span class="number">0.5</span>)</span><br></pre></td></tr></table></figure>
<p>既然做完优化方法的一种，一般跟着优化器后面.</p>
<h3 id="指定间隔学习率衰减"><a href="#指定间隔学习率衰减" class="headerlink" title="指定间隔学习率衰减"></a>指定间隔学习率衰减</h3><p>原理同上，不过这里需要指定在哪几轮训练时衰减  </p>
<p>api:</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># milestones：设定调整轮次:[50, 125, 160]</span></span><br><span class="line">optim.lr_scheduler.MultiStepLR(optimizer, milestones, gamma=<span class="number">0.1</span>)</span><br></pre></td></tr></table></figure>
<h3 id="指数衰减"><a href="#指数衰减" class="headerlink" title="指数衰减"></a>指数衰减</h3><p>在神经网络训练过程中，我们一般很少关注学习率具体在哪几轮需要变化，所以指数衰减成为了适配性最广的衰减方法，但是由于涉及指数运算，计算过程可能稍复杂，计算开销稍大。  </p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># gamma：指数的底</span></span><br><span class="line"><span class="comment"># 调整方式</span></span><br><span class="line"><span class="comment"># lr= lr∗ gamma^epoch</span></span><br><span class="line">optim.lr_scheduler.ExponentialLR(optimizer, gamma)</span><br></pre></td></tr></table></figure>
<h3 id="代码演示-3"><a href="#代码演示-3" class="headerlink" title="代码演示"></a>代码演示</h3><p>下面以指数衰减为例演示训练具体流程，其他方法同理，仅需改变函数名，注意各个方法需要输入的参数即可.</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">test</span>():</span><br><span class="line">    <span class="comment"># 1.参数初始化</span></span><br><span class="line">    LR = <span class="number">0.1</span>  <span class="comment"># 设置学习率初始化值为0.1</span></span><br><span class="line">    iteration = <span class="number">10</span></span><br><span class="line">    max_epoch = <span class="number">200</span></span><br><span class="line">    y_true = torch.tensor([<span class="number">0</span>])</span><br><span class="line">    x = torch.tensor([<span class="number">1.0</span>])</span><br><span class="line">    w = torch.tensor([<span class="number">1.0</span>], requires_grad=<span class="literal">True</span>)</span><br><span class="line">    <span class="comment"># 2.优化器</span></span><br><span class="line">    optimizer = optim.SGD([w], lr=LR, momentum=<span class="number">0.9</span>)</span><br><span class="line">    <span class="comment"># 3.设置学习率下降策略</span></span><br><span class="line">    gamma = <span class="number">0.95</span></span><br><span class="line">    scheduler_lr = optim.lr_scheduler.ExponentialLR(optimizer, gamma=gamma)</span><br><span class="line">    <span class="comment"># 4.获取学习率的值和当前的epoch</span></span><br><span class="line">    lr_list, epoch_list = <span class="built_in">list</span>(), <span class="built_in">list</span>()</span><br><span class="line">    <span class="keyword">for</span> epoch <span class="keyword">in</span> <span class="built_in">range</span>(max_epoch):</span><br><span class="line">        lr_list.append(scheduler_lr.get_last_lr())</span><br><span class="line">        epoch_list.append(epoch)</span><br><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(iteration):  <span class="comment"># 遍历每一个batch数据</span></span><br><span class="line">            loss = ((w*x-y_true)**<span class="number">2</span>)/<span class="number">2.0</span></span><br><span class="line">            optimizer.zero_grad()</span><br><span class="line">            <span class="comment"># 反向传播</span></span><br><span class="line">            loss.backward()</span><br><span class="line">            optimizer.step()</span><br><span class="line">        <span class="comment"># 更新下一个epoch的学习率</span></span><br><span class="line">        scheduler_lr.step()</span><br><span class="line">    <span class="comment"># 5.绘制学习率变化的曲线</span></span><br><span class="line">    plt.plot(epoch_list, lr_list, label=<span class="string">&quot;Multi Step LR Scheduler&quot;</span>)</span><br><span class="line">    plt.xlabel(<span class="string">&quot;Epoch&quot;</span>)</span><br><span class="line">    plt.ylabel(<span class="string">&quot;Learning rate&quot;</span>)</span><br><span class="line">    plt.legend()</span><br><span class="line">    plt.show()</span><br></pre></td></tr></table></figure>

<h1 id="小结-1"><a href="#小结-1" class="headerlink" title="小结"></a>小结</h1><table>
<thead>
<tr>
<th>方法</th>
<th>等间隔学习率衰减 (Step Decay)</th>
<th>指定间隔学习率衰减 (Exponential Decay)</th>
<th>指数学习率衰减 (Exponential Moving Average Decay)</th>
</tr>
</thead>
<tbody><tr>
<td><strong>衰减方式</strong></td>
<td>固定步长衰减</td>
<td>指定步长衰减</td>
<td>平滑指数衰减，历史平均考虑</td>
</tr>
<tr>
<td><strong>实现难度</strong></td>
<td>简单易实现</td>
<td>相对简单，容易调整</td>
<td>需要额外历史计算，较复杂</td>
</tr>
<tr>
<td><strong>适用场景</strong></td>
<td>大型数据集、较为简单的任务</td>
<td>对训练平稳性要求较高的任务</td>
<td>高精度训练，避免过快收敛</td>
</tr>
<tr>
<td><strong>优点</strong></td>
<td>直观，易于调试，适用于大批量数据</td>
<td>易于调试，稳定训练过程</td>
<td>平滑且考虑历史更新，收敛稳定性较强</td>
</tr>
<tr>
<td><strong>缺点</strong></td>
<td>学习率变化较大，可能跳过最优点</td>
<td>在某些情况下可能衰减过快，导致优化提前停滞</td>
<td>超参数调节较为复杂，可能需要更多的计算资源</td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>深度学习</category>
      </categories>
      <tags>
        <tag>深度学习</tag>
        <tag>张量</tag>
        <tag>神经网络</tag>
        <tag>机器学习</tag>
      </tags>
  </entry>
  <entry>
    <title>神经网络构建</title>
    <url>/2026/03/04/%E7%A5%9E%E7%BB%8F%E7%BD%91%E7%BB%9C%E6%9E%84%E5%BB%BA/</url>
    <content><![CDATA[<p>神经网络构建方法</p>
<span id="more"></span>
<h1 id="参数初始化方法"><a href="#参数初始化方法" class="headerlink" title="参数初始化方法"></a>参数初始化方法</h1><h2 id="常用初始化方法"><a href="#常用初始化方法" class="headerlink" title="常用初始化方法"></a>常用初始化方法</h2><ul>
<li><p>均匀分布初始化<br>权重参数初始化从区间均匀随机取值，默认区间为（0，1）。可以设置为在 $(-\frac{1}{\sqrt d} , \frac{1}{\sqrt d})$ 均匀分布中生成当前神经元的权重，其中d为神经元的输入数量</p>
</li>
<li><p>正态分布初始化<br>随机初始化从均值为0，标准差是1的高斯分布中取样，使用一些很小的值对参数W进行初始化</p>
</li>
<li><p>全0初始化<br>将神经网络中的所有权重参数初始化为 0</p>
</li>
<li><p>全1初始化<br>将神经网络中的所有权重参数初始化为 1</p>
</li>
<li><p>固定值初始化<br>将神经网络中的所有权重参数初始化为某个固定值</p>
</li>
<li><p>kaiming 初始化，也叫做 HE 初始化<br>HE 初始化分为正态分布的 HE 初始化、均匀分布的 HE 初始化.</p>
<ul>
<li>正态分布的he初始化<br>  它是从 [0, std] 中抽取样本的，$std &#x3D; \sqrt \frac {2}{fan_{in}}$</li>
<li>均匀分布的he初始化<br>  它从 [-limit，limit] 中的均匀分布中抽取样本, $limit &#x3D; \sqrt \frac {6}{fan_{in}}$<br>  fan_in 输入层神经元的个数</li>
</ul>
</li>
<li><p>xavier 初始化，也叫做 Glorot初始化<br>该方法也有两种，一种是正态分布的 xavier 初始化、一种是均匀分布的 xavier 初始化.</p>
<ul>
<li>正态化的Xavier初始化<br>  它是从 [0, std] 中抽取样本的，$std &#x3D; \sqrt \frac {2}{fan_{in}+fan_{out}}$</li>
<li>均匀分布的Xavier初始化<br>  [-limit，limit] 中的均匀分布中抽取样本, $limit &#x3D; \sqrt \frac {6}{fan_{in}+fan_{out}}$<br>  fan_in 是输入层神经元的个数， fan_out 是输出层神经元个数</li>
</ul>
</li>
</ul>
<h2 id="初始化方法的选择"><a href="#初始化方法的选择" class="headerlink" title="初始化方法的选择"></a>初始化方法的选择</h2><ol>
<li><p>根据激活函数的类型选择对应的初始化方法</p>
<ul>
<li>Sigmoid&#x2F;Tanh：Xavier 初始化</li>
<li>ReLU&#x2F;Leaky ReLU：kaiming 初始化</li>
</ul>
</li>
<li><p>网络的深度</p>
<ul>
<li>浅层网络：随机初始化即可</li>
<li>深层网络：需要考虑方差平衡，如 Xavier 或 kaiming 初始化</li>
</ul>
</li>
</ol>
<h1 id="神经网络的搭建"><a href="#神经网络的搭建" class="headerlink" title="神经网络的搭建"></a>神经网络的搭建</h1><p>在pytorch中定义深度神经网络其实就是层堆叠的过程，继承自nn.Module，需要实现两个方法：</p>
<ul>
<li>__init__方法中定义网络中的层结构，主要是全连接层，并进行初始化</li>
<li>forward方法，在实例化模型的时候，底层会自动调用该函数。该函数中为初始化定义的layer传入数据，进行前向传播等。</li>
</ul>
<h2 id="具体代码"><a href="#具体代码" class="headerlink" title="具体代码"></a>具体代码</h2><p>我们以下面这个图片的简单神经网络为例<br><img src="/../images/neural_network.jpg" alt="神经网络示意图"><br>先安装torchsummary, 可以计算模型参数, 查看模型结构</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">pip install torchsummary</span><br></pre></td></tr></table></figure>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"><span class="keyword">import</span> torch.nn <span class="keyword">as</span> nn</span><br><span class="line"><span class="keyword">from</span> torchsummary <span class="keyword">import</span> summary</span><br><span class="line"><span class="comment"># 创建神经网络模型类</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Model</span>(nn.Module):</span><br><span class="line">    <span class="comment"># 初始化属性值</span></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="built_in">super</span>(Model, <span class="variable language_">self</span>).__init__() <span class="comment"># 调用父类的初始化属性值</span></span><br><span class="line">        <span class="variable language_">self</span>.linear1 = nn.Linear(<span class="number">3</span>, <span class="number">3</span>) <span class="comment"># 创建第一个隐藏层模型, 3个输入特征,3个输出特征</span></span><br><span class="line">        nn.init.xavier_normal_(<span class="variable language_">self</span>.linear1.weight) <span class="comment"># 初始化权重</span></span><br><span class="line">        nn.init.zeros_(<span class="variable language_">self</span>.linear1.bias)</span><br><span class="line">        <span class="comment"># 创建第二个隐藏层模型, 3个输入特征(上一层的输出特征),2个输出特征</span></span><br><span class="line">        <span class="variable language_">self</span>.linear2 = nn.Linear(<span class="number">3</span>, <span class="number">2</span>)</span><br><span class="line">        <span class="comment"># 初始化权重</span></span><br><span class="line">        nn.init.kaiming_normal_(<span class="variable language_">self</span>.linear2.weight, nonlinearity=<span class="string">&#x27;relu&#x27;</span>)</span><br><span class="line">        nn.init.zeros_(<span class="variable language_">self</span>.linear2.bias)</span><br><span class="line">        <span class="comment"># 创建输出层模型</span></span><br><span class="line">        <span class="variable language_">self</span>.out = nn.Linear(<span class="number">2</span>, <span class="number">2</span>)</span><br><span class="line">    <span class="comment"># 创建前向传播方法,自动执行forward()方法，这里是使用类似__init__的魔法方法，函数名字不能修改</span></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x</span>):</span><br><span class="line">        <span class="comment"># 数据经过第一个线性层</span></span><br><span class="line">        x = <span class="variable language_">self</span>.linear1(x)</span><br><span class="line">        <span class="comment"># 使用sigmoid激活函数</span></span><br><span class="line">        x = torch.sigmoid(x)</span><br><span class="line">        <span class="comment"># 数据经过第二个线性层</span></span><br><span class="line">        x = <span class="variable language_">self</span>.linear2(x)</span><br><span class="line">        <span class="comment"># 使用relu激活函数</span></span><br><span class="line">        x = torch.relu(x)</span><br><span class="line">        <span class="comment"># 数据经过输出层</span></span><br><span class="line">        x = <span class="variable language_">self</span>.out(x)</span><br><span class="line">        <span class="comment"># 使用softmax激活函数</span></span><br><span class="line">        <span class="comment"># dim=-1:每一维度行数据相加为1</span></span><br><span class="line">        x = torch.softmax(x, dim=-<span class="number">1</span>)</span><br><span class="line">        <span class="keyword">return</span> x</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&quot;__main__&quot;</span>:</span><br><span class="line">    <span class="comment"># 实例化model对象</span></span><br><span class="line">    my_model = Model()</span><br><span class="line">    <span class="comment"># 随机产生数据</span></span><br><span class="line">    my_data = torch.randn(<span class="number">5</span>, <span class="number">3</span>)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;mydata shape&quot;</span>, my_data.shape)</span><br><span class="line">    <span class="comment"># 数据经过神经网络模型训练</span></span><br><span class="line">    output = my_model(my_data)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;output shape--&gt;&quot;</span>, output.shape)</span><br><span class="line">    <span class="comment"># 计算模型参数</span></span><br><span class="line">    <span class="comment"># 计算每层每个神经元的w和b个数总和</span></span><br><span class="line">    summary(my_model, input_size=(<span class="number">3</span>,), batch_size=<span class="number">5</span>) </span><br><span class="line">    <span class="comment"># 查看模型参数</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;======查看模型参数w和b======&quot;</span>)</span><br><span class="line">    <span class="keyword">for</span> name, parameter <span class="keyword">in</span> my_model.named_parameters():</span><br><span class="line">        <span class="built_in">print</span>(name, parameter)</span><br></pre></td></tr></table></figure>
<h2 id="结果说明"><a href="#结果说明" class="headerlink" title="结果说明"></a>结果说明</h2><p>以第一个隐层为例：该隐层有3个神经元，每个神经元的参数为：4个（w1,w2,w3,b1），所以一共用3x4&#x3D;12个参数。<br>同理可以得到一共有26个参数，即所有层中的权重w和偏置b的数量</p>
]]></content>
      <categories>
        <category>深度学习</category>
      </categories>
      <tags>
        <tag>深度学习</tag>
        <tag>张量</tag>
        <tag>神经网络</tag>
        <tag>机器学习</tag>
      </tags>
  </entry>
  <entry>
    <title>张量(麻辣烫)</title>
    <url>/2026/02/16/%E5%BC%A0%E9%87%8F-%E9%BA%BB%E8%BE%A3%E7%83%AB/</url>
    <content><![CDATA[<p>补补基础</p>
<span id="more"></span>
<pre><code>深度学习前置
</code></pre>
<h1 id="什么是张量"><a href="#什么是张量" class="headerlink" title="什么是张量"></a>什么是张量</h1><p>（这里当然是讲深度学习中的概念，不是物理的张量）<br>张量是将向量和矩阵推广到任意维度的泛化形式，张量有多种维度，从形状上简单来看，0维张量（标量）相当于一个数字，一维张量类似行向量，二维的就像是矩阵了。<br>递推的讲，二维张量中各个元素都一维张量，三维张量中各个元素都二维张量…<br>在常用的pytorch框架中，张量以类形式封装，类似numpy数组，但优点是可以具有GPU加速能力</p>
<h1 id="张量的创建"><a href="#张量的创建" class="headerlink" title="张量的创建"></a>张量的创建</h1><p>张量相关库：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch</span><br></pre></td></tr></table></figure>
<h2 id="tensor"><a href="#tensor" class="headerlink" title="tensor"></a>tensor</h2><p>根据指定数据创建张量</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1. 张量标量</span></span><br><span class="line">data = torch.tensor(<span class="number">10</span>)</span><br><span class="line"><span class="comment"># 2. numpy 数组, data 为 float64</span></span><br><span class="line">data = np.random.randn(<span class="number">2</span>, <span class="number">3</span>)</span><br><span class="line">data = torch.tensor(data)</span><br><span class="line"><span class="comment"># 3. 列表, 默认元素类型 float32</span></span><br><span class="line">data = [[<span class="number">10.</span>, <span class="number">20.</span>, <span class="number">30.</span>], [<span class="number">40.</span>, <span class="number">50.</span>, <span class="number">60.</span>]]</span><br><span class="line">data = torch.tensor(data)</span><br></pre></td></tr></table></figure>
<h2 id="Tensor"><a href="#Tensor" class="headerlink" title="Tensor"></a>Tensor</h2><p>在前面增加了创建指定数据的张量的功能，如</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">data = torch.Tensor(<span class="number">2</span>, <span class="number">3</span>)</span><br></pre></td></tr></table></figure>
<p>不过使用最多的还是 <code>torch.tensor</code></p>
<h2 id="类型-Tensor"><a href="#类型-Tensor" class="headerlink" title="类型+Tensor"></a>类型+Tensor</h2><p>相当于在Tensor基础上限定元素类型<br>如 <code>IntTensor</code>, <code>FloatTensor</code>(float32), <code>DoubleTensor</code>(float64) 等</p>
<h2 id="创建线性张量"><a href="#创建线性张量" class="headerlink" title="创建线性张量"></a>创建线性张量</h2><p>其实函数名等和普通列表一样</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 在指定区间(不含end)按照步长生成元素 [start, end, step)</span></span><br><span class="line">data = torch.arange(<span class="number">0</span>, <span class="number">10</span>, <span class="number">2</span>)</span><br><span class="line"><span class="comment"># 在指定区间(含end)按照元素个数生成 [start, end, steps]</span></span><br><span class="line">data = torch.linspace(<span class="number">0</span>, <span class="number">9</span>, <span class="number">10</span>)</span><br></pre></td></tr></table></figure>
<h2 id="随机张量"><a href="#随机张量" class="headerlink" title="随机张量"></a>随机张量</h2><p>用随机种子设置随机张量<br>设定随机种子后，用 <code>randn</code> 方法得到的张量就都一样了</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 创建随机张量</span></span><br><span class="line">data = torch.randn(<span class="number">2</span>, <span class="number">3</span>)  <span class="comment"># 创建2行3列张量</span></span><br><span class="line"><span class="comment"># 查看随机数种子</span></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&#x27;随机数种子:&#x27;</span>, torch.initial_seed())</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 随机数种子设置 </span></span><br><span class="line">torch.manual_seed(<span class="number">100</span>)</span><br><span class="line">data = torch.randn(<span class="number">2</span>, <span class="number">3</span>)</span><br><span class="line"><span class="built_in">print</span>(data)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&#x27;随机数种子:&#x27;</span>, torch.initial_seed()) <span class="comment"># 输出100</span></span><br></pre></td></tr></table></figure>
<h2 id="全0-全1张量"><a href="#全0-全1张量" class="headerlink" title="全0&#x2F;全1张量"></a>全0&#x2F;全1张量</h2><p>根据指定形状或者自定的形状来创建</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 指定形状全1张量</span></span><br><span class="line">data = torch.ones(<span class="number">2</span>, <span class="number">3</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 根据张量形状创建全1张量</span></span><br><span class="line">data = torch.ones_like(data)</span><br></pre></td></tr></table></figure>
<p>全0张量同理  </p>
<p>或者使用更加通用的方法: 直接全填充某指定值</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">data = torch.full([<span class="number">2</span>, <span class="number">3</span>], <span class="number">10</span>)</span><br><span class="line">data = torch.full_like(data, <span class="number">20</span>)</span><br></pre></td></tr></table></figure>
<h1 id="相关运算"><a href="#相关运算" class="headerlink" title="相关运算"></a>相关运算</h1><h2 id="类型转换"><a href="#类型转换" class="headerlink" title="类型转换"></a>类型转换</h2><p>转换用类型名使用类型+Tensor</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">data = torch.full([<span class="number">2</span>, <span class="number">3</span>], <span class="number">10</span>)</span><br><span class="line"><span class="built_in">print</span>(data.dtype) <span class="comment"># int64</span></span><br><span class="line"><span class="comment"># 转换为 float64 类型</span></span><br><span class="line">data = data.<span class="built_in">type</span>(torch.DoubleTensor)</span><br><span class="line"><span class="built_in">print</span>(data.dtype) <span class="comment"># float64</span></span><br></pre></td></tr></table></figure>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">data = data.<span class="built_in">type</span>(torch.LongTensor)</span><br><span class="line">data = data.<span class="built_in">type</span>(dtype=torch.float16)</span><br></pre></td></tr></table></figure>
<p>或者直接用具体的类型同名函数，如</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">data = data.short()</span><br><span class="line">data = data.<span class="built_in">int</span>()</span><br></pre></td></tr></table></figure>
<h2 id="和numpy相互转换"><a href="#和numpy相互转换" class="headerlink" title="和numpy相互转换"></a>和numpy相互转换</h2><p>直接的转换仅是浅拷贝，如</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">data_tensor = torch.tensor([<span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>])</span><br><span class="line"><span class="comment"># 使用张量对象中的 numpy 函数进行转换</span></span><br><span class="line">data_numpy = data_tensor.numpy()</span><br><span class="line"><span class="comment"># data_tensor 和 data_numpy 共享内存</span></span><br><span class="line"><span class="comment"># data_tensor[0] = 100</span></span><br><span class="line">data_numpy[<span class="number">0</span>] = <span class="number">100</span></span><br><span class="line"><span class="built_in">print</span>(data_tensor)</span><br><span class="line"><span class="built_in">print</span>(data_numpy)</span><br></pre></td></tr></table></figure>
<p>使用copy可以深拷贝</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">data_tensor = torch.tensor([<span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>])</span><br><span class="line">data_numpy = data_tensor.numpy().copy()</span><br><span class="line"><span class="comment"># 不共享内存</span></span><br><span class="line"><span class="comment"># data_tensor[0] = 2</span></span><br><span class="line">data_numpy[<span class="number">0</span>] = <span class="number">100</span></span><br><span class="line"><span class="built_in">print</span>(data_tensor)</span><br><span class="line"><span class="built_in">print</span>(data_numpy)</span><br></pre></td></tr></table></figure>
<p>此处使用tensor方法转换，也还有其他方法<br>当张量仅含一个元素，即标量张量，可以用item方法提取出该数字</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 当张量只包含一个元素时, 可以通过 item() 函数提取出该值</span></span><br><span class="line">data = torch.tensor([<span class="number">30</span>,])</span><br><span class="line"><span class="built_in">print</span>(data.item())</span><br></pre></td></tr></table></figure>
<h2 id="数组运算"><a href="#数组运算" class="headerlink" title="数组运算"></a>数组运算</h2><p>可以直接用+-*&#x2F; +&#x3D; -&#x3D;等，也可以使用对应的函数如add，sub<br>这里使用的函数进行计算，分为对原数据修改或者不修改<br>如果是张量对一个数进行运算，即对该张量所有元素都进行计算  </p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">data = torch.randint(<span class="number">0</span>, <span class="number">10</span>, [<span class="number">2</span>, <span class="number">3</span>])</span><br><span class="line"><span class="built_in">print</span>(data)</span><br><span class="line"><span class="comment"># 1. 不修改原数据</span></span><br><span class="line">new_data = data.add(<span class="number">10</span>)  <span class="comment"># 等价 new_data = data + 10</span></span><br><span class="line"><span class="built_in">print</span>(new_data)</span><br><span class="line"><span class="comment"># 2. 直接修改原数据 带下划线的函数为修改原数据本身</span></span><br><span class="line">data.add_(<span class="number">10</span>)  <span class="comment"># 等价 data += 10</span></span><br><span class="line"><span class="built_in">print</span>(data)</span><br><span class="line"><span class="comment"># 3. 其他函数</span></span><br><span class="line"><span class="built_in">print</span>(data.sub(<span class="number">100</span>))</span><br><span class="line"><span class="built_in">print</span>(data.mul(<span class="number">100</span>))</span><br><span class="line"><span class="built_in">print</span>(data.div(<span class="number">100</span>))</span><br><span class="line"><span class="built_in">print</span>(data.neg())</span><br></pre></td></tr></table></figure>
<h2 id="矩阵相关乘法"><a href="#矩阵相关乘法" class="headerlink" title="矩阵相关乘法"></a>矩阵相关乘法</h2><h3 id="点乘"><a href="#点乘" class="headerlink" title="点乘"></a>点乘</h3><p>点乘，即Hadamard积，逐元素相乘，要求维度相同</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">data1 = torch.tensor([[<span class="number">1</span>, <span class="number">2</span>], [<span class="number">3</span>, <span class="number">4</span>]])</span><br><span class="line">data2 = torch.tensor([[<span class="number">5</span>, <span class="number">6</span>], [<span class="number">7</span>, <span class="number">8</span>]])</span><br><span class="line"><span class="comment"># 第一种</span></span><br><span class="line">data = torch.mul(data1, data2)</span><br><span class="line"><span class="built_in">print</span>(data)</span><br><span class="line"><span class="comment"># 第二种</span></span><br><span class="line">data = data1 * data2</span><br><span class="line"><span class="built_in">print</span>(data)</span><br></pre></td></tr></table></figure>
<h3 id="矩阵乘法"><a href="#矩阵乘法" class="headerlink" title="矩阵乘法"></a>矩阵乘法</h3><p>要求第一个列数等于第二个行数,可以用@</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 点积运算</span></span><br><span class="line">data1 = torch.tensor([[<span class="number">1</span>, <span class="number">2</span>], [<span class="number">3</span>, <span class="number">4</span>], [<span class="number">5</span>, <span class="number">6</span>]])</span><br><span class="line">data2 = torch.tensor([[<span class="number">5</span>, <span class="number">6</span>], [<span class="number">7</span>, <span class="number">8</span>]])</span><br><span class="line"><span class="comment"># 方式一:</span></span><br><span class="line">data3 = data1 @ data2</span><br><span class="line"><span class="built_in">print</span>(data3)</span><br><span class="line"><span class="comment"># 方式二:</span></span><br><span class="line">data4 = torch.matmul(data1, data2)</span><br><span class="line"><span class="built_in">print</span>(data4)</span><br></pre></td></tr></table></figure>
<h2 id="其他操作"><a href="#其他操作" class="headerlink" title="其他操作"></a>其他操作</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">data = torch.randint(<span class="number">0</span>, <span class="number">10</span>, [<span class="number">2</span>, <span class="number">3</span>], dtype=torch.float64)</span><br><span class="line"><span class="built_in">print</span>(data)</span><br><span class="line"><span class="comment"># 均值</span></span><br><span class="line"><span class="comment"># tensor 必须为 Float 或者 Double 类型</span></span><br><span class="line"><span class="built_in">print</span>(data.mean())</span><br><span class="line"><span class="built_in">print</span>(data.mean(dim=<span class="number">0</span>))  <span class="comment"># 按列</span></span><br><span class="line"><span class="built_in">print</span>(data.mean(dim=<span class="number">1</span>))  <span class="comment"># 按行</span></span><br><span class="line"><span class="comment"># 求和</span></span><br><span class="line"><span class="built_in">print</span>(data.<span class="built_in">sum</span>())</span><br><span class="line"><span class="built_in">print</span>(data.<span class="built_in">sum</span>(dim=<span class="number">0</span>))</span><br><span class="line"><span class="built_in">print</span>(data.<span class="built_in">sum</span>(dim=<span class="number">1</span>))</span><br><span class="line"><span class="comment"># 求平方(逐元素)</span></span><br><span class="line"><span class="built_in">print</span>(torch.<span class="built_in">pow</span>(data,<span class="number">2</span>))</span><br><span class="line"><span class="comment"># 平方根</span></span><br><span class="line"><span class="built_in">print</span>(data.sqrt())</span><br><span class="line"><span class="comment"># e^n 次方</span></span><br><span class="line"><span class="built_in">print</span>(data.exp())</span><br><span class="line"><span class="comment"># 对数</span></span><br><span class="line"><span class="built_in">print</span>(data.log())  <span class="comment"># 以 e 为底</span></span><br><span class="line"><span class="built_in">print</span>(data.log2())</span><br><span class="line"><span class="built_in">print</span>(data.log10())</span><br></pre></td></tr></table></figure>
<h1 id="索引操作"><a href="#索引操作" class="headerlink" title="索引操作"></a>索引操作</h1><p>索引均为前行后列<br>直接下标索引同python列表  </p>
<h2 id="列表索引"><a href="#列表索引" class="headerlink" title="列表索引"></a>列表索引</h2><p>多种列表索引操作容易弄混</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="built_in">print</span>(data[[<span class="number">0</span>, <span class="number">1</span>], [<span class="number">2</span>, <span class="number">3</span>]]) <span class="comment"># 取出(0,2)和(1,3)两个元素</span></span><br><span class="line"><span class="built_in">print</span>(data[[[<span class="number">0</span>], [<span class="number">1</span>]], [<span class="number">2</span>, <span class="number">3</span>]]) <span class="comment"># 取出第0行和第1行的第2，3列，共4个元素</span></span><br><span class="line"><span class="built_in">print</span>(data[:<span class="number">3</span>, :<span class="number">2</span>]) <span class="comment"># 前3行，前2列的</span></span><br></pre></td></tr></table></figure>
<h2 id="布尔索引"><a href="#布尔索引" class="headerlink" title="布尔索引"></a>布尔索引</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 第三列大于5的行数据</span></span><br><span class="line"><span class="built_in">print</span>(data[data[:, <span class="number">2</span>] &gt; <span class="number">5</span>])</span><br><span class="line"><span class="comment"># 第二行大于5的列数据</span></span><br><span class="line"><span class="built_in">print</span>(data[:, data[<span class="number">1</span>] &gt; <span class="number">5</span>])</span><br></pre></td></tr></table></figure>
<h1 id="形状的操作"><a href="#形状的操作" class="headerlink" title="形状的操作"></a>形状的操作</h1><p>得到形状用size或者shape  </p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">data = torch.tensor([[<span class="number">10</span>, <span class="number">20</span>, <span class="number">30</span>], [<span class="number">40</span>, <span class="number">50</span>, <span class="number">60</span>]])</span><br><span class="line"><span class="comment"># 用 shape 属性或者 size 方法都可以获得张量的形状</span></span><br><span class="line"><span class="built_in">print</span>(data.shape, data.shape[<span class="number">0</span>], data.shape[<span class="number">1</span>])</span><br><span class="line"><span class="built_in">print</span>(data.size(), data.size(<span class="number">0</span>), data.size(<span class="number">1</span>))</span><br><span class="line"><span class="comment">#上面两行等价</span></span><br><span class="line"><span class="comment"># 使用 reshape 函数修改张量形状</span></span><br><span class="line">new_data = data.reshape(<span class="number">1</span>, <span class="number">6</span>)</span><br><span class="line"><span class="built_in">print</span>(new_data.shape)</span><br></pre></td></tr></table></figure>
<p>使用reshape改变，要求数据个数相同</p>
<h2 id="squeeze和unsqueeze"><a href="#squeeze和unsqueeze" class="headerlink" title="squeeze和unsqueeze"></a>squeeze和unsqueeze</h2><p>可以扩展或者去掉某1维度, unsqueeze：扩展，squeeze：压缩维度, 如  </p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">mydata1 = torch.tensor([<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>])             </span><br><span class="line"><span class="built_in">print</span>(mydata1.shape, mydata1) <span class="comment"># 一个普通的数组 1维数据</span></span><br><span class="line">mydata2 = mydata1.unsqueeze(dim=<span class="number">0</span>)</span><br><span class="line"><span class="built_in">print</span>(mydata2, mydata2.shape)  <span class="comment">#1*5</span></span><br><span class="line">mydata3 = mydata1.unsqueeze(dim=<span class="number">1</span>)</span><br><span class="line"><span class="built_in">print</span>(mydata3, mydata3.shape)  <span class="comment">#5*1</span></span><br><span class="line">mydata4 = mydata1.unsqueeze(dim=-<span class="number">1</span>)</span><br><span class="line"><span class="built_in">print</span>(mydata4, mydata4.shape) <span class="comment">#5*1 同上</span></span><br><span class="line">mydata5 = mydata4.squeeze()</span><br><span class="line"><span class="built_in">print</span>(mydata5, mydata5.shape)  <span class="comment">#5</span></span><br></pre></td></tr></table></figure>
<h2 id="交换维度"><a href="#交换维度" class="headerlink" title="交换维度"></a>交换维度</h2><p>把数据都看成连续，通过transpose或者permute可以交换维度</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">data = torch.tensor(np.random.randint(<span class="number">0</span>, <span class="number">10</span>, [<span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>]))</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&#x27;data shape:&#x27;</span>, data.size())</span><br><span class="line"><span class="comment"># 交换1和2维度</span></span><br><span class="line">mydata2 = torch.transpose(data, <span class="number">1</span>, <span class="number">2</span>)</span><br><span class="line"><span class="built_in">print</span>(mydata2.shape)</span><br><span class="line"><span class="comment"># 将data 的形状修改为 (4, 5, 3), 需要变换多次</span></span><br><span class="line">mydata3 =  torch.transpose(data, <span class="number">0</span>, <span class="number">1</span>)</span><br><span class="line">mydata4 = torch.transpose(mydata3, <span class="number">1</span>, <span class="number">2</span>)</span><br><span class="line"><span class="built_in">print</span>(mydata4.shape)</span><br><span class="line"><span class="comment"># 使用 permute 函数将形状修改为 (4, 5, 3)</span></span><br><span class="line"><span class="comment"># 方法1</span></span><br><span class="line">mydata5 = torch.permute(data, [<span class="number">1</span>, <span class="number">2</span>, <span class="number">0</span>])</span><br><span class="line"><span class="built_in">print</span>(mydata5.shape)</span><br><span class="line"><span class="comment"># 方法2</span></span><br><span class="line">mydata6 = data.permute([<span class="number">1</span>, <span class="number">2</span>, <span class="number">0</span>])</span><br><span class="line"><span class="built_in">print</span>(mydata6.shape)</span><br></pre></td></tr></table></figure>
<p>交换维度操作也可以用view+contiguous等其他函数</p>
<h1 id="拼接操作"><a href="#拼接操作" class="headerlink" title="拼接操作"></a>拼接操作</h1><h2 id="cat"><a href="#cat" class="headerlink" title="cat"></a>cat</h2><p>torch.cat()函数可以将多个张量根据指定的维度拼接起来，不改变维度数。<br>各张量除了拼接的那个维度，其余维度大小必须一致</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">data1 = torch.randint(<span class="number">0</span>, <span class="number">10</span>, [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>])</span><br><span class="line">data2 = torch.randint(<span class="number">0</span>, <span class="number">10</span>, [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>])</span><br><span class="line"><span class="comment"># 1. 按0维度拼接</span></span><br><span class="line">new_data = torch.cat([data1, data2], dim=<span class="number">0</span>)</span><br><span class="line"><span class="built_in">print</span>(new_data)</span><br><span class="line"><span class="built_in">print</span>(new_data.shape)</span><br><span class="line"><span class="comment"># 2. 按1维度拼接</span></span><br><span class="line">new_data = torch.cat([data1, data2], dim=<span class="number">1</span>)</span><br><span class="line"><span class="built_in">print</span>(new_data)</span><br><span class="line"><span class="built_in">print</span>(new_data.shape)</span><br><span class="line"><span class="comment"># 3. 按2维度拼接</span></span><br><span class="line">new_data = torch.cat([data1, data2], dim=<span class="number">2</span>)</span><br><span class="line"><span class="built_in">print</span>(new_data)</span><br><span class="line"><span class="built_in">print</span>(new_data.shape)</span><br></pre></td></tr></table></figure>
<h2 id="stack"><a href="#stack" class="headerlink" title="stack"></a>stack</h2><p>torch.stack()函数会在一个新的维度上连接一系列张量，会增加一个新维度，所以所有输入张量的形状必须完全相同。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">data1 = torch.randint(<span class="number">0</span>, <span class="number">10</span>, [<span class="number">2</span>, <span class="number">3</span>])</span><br><span class="line">data2 = torch.randint(<span class="number">0</span>, <span class="number">10</span>, [<span class="number">2</span>, <span class="number">3</span>])</span><br><span class="line"><span class="comment"># 1. 在0维度上拼接</span></span><br><span class="line">new_data = torch.stack([data1, data2], dim=<span class="number">0</span>)</span><br><span class="line"><span class="built_in">print</span>(new_data)</span><br><span class="line"><span class="built_in">print</span>(new_data.shape)</span><br><span class="line"><span class="comment"># 2. 在1维度上拼接</span></span><br><span class="line">new_data = torch.stack([data1, data2], dim=<span class="number">1</span>)</span><br><span class="line"><span class="built_in">print</span>(new_data)</span><br><span class="line"><span class="built_in">print</span>(new_data.shape)</span><br><span class="line"><span class="comment"># 3. 在2维度上拼接</span></span><br><span class="line">new_data = torch.stack([data1, data2], dim=<span class="number">2</span>)</span><br><span class="line"><span class="built_in">print</span>(new_data)</span><br><span class="line"><span class="built_in">print</span>(new_data.shape)</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>深度学习</category>
      </categories>
      <tags>
        <tag>深度学习</tag>
        <tag>张量</tag>
        <tag>机器学习</tag>
      </tags>
  </entry>
  <entry>
    <title>灰色预测</title>
    <url>/2025/12/14/%E7%81%B0%E8%89%B2%E9%A2%84%E6%B5%8B/</url>
    <content><![CDATA[<p>用于数据量较少的模型</p>
<span id="more"></span>
<h1 id="模型引入"><a href="#模型引入" class="headerlink" title="模型引入"></a>模型引入</h1><h2 id="什么是灰色"><a href="#什么是灰色" class="headerlink" title="什么是灰色"></a>什么是灰色</h2><p>一个系统通常分为黑，白，灰。白色系统，即数据完全清楚已知的系统。黑色系统完全未知，而灰色系统就是介于二者之间，仅有部分是已知的。</p>
<h2 id="时间序列"><a href="#时间序列" class="headerlink" title="时间序列"></a>时间序列</h2><p>灰色模型通常用于<a href="https://longnamehaha.github.io/2025/12/15/ARIMA/" title="看看">时间序列</a>的预测，但是灰色预测更加适合已知的历史数据较少的情况</p>
<h1 id="核心过程"><a href="#核心过程" class="headerlink" title="核心过程"></a>核心过程</h1><p>灰色预测模型中，<strong>GM(1,1)（Gray Model (1,1)）</strong> 是最基础、最常用的单变量一阶灰色模型，适用于“单指标、短期、小样本”预测。括号中“1”分别表示“1个变量”和“1阶微分方程”。</p>
<h2 id="模型的核心假设"><a href="#模型的核心假设" class="headerlink" title="模型的核心假设"></a>模型的核心假设</h2><p>原始数据序列需满足“光滑性”和“指数增长趋势”（可通过级比检验验证），否则需进行数据预处理（如平移变换、对数变换）。</p>
<h2 id="数学推导"><a href="#数学推导" class="headerlink" title="数学推导"></a>数学推导</h2><p>设原始非负数据序列为：<br>$$ X^{(0)} &#x3D; {x^{(0)}(1), x^{(0)}(2), …, x^{(0)}(n)}, \quad n \geq 4 \quad (n为样本数) $$</p>
<h3 id="累加生成（AGO）"><a href="#累加生成（AGO）" class="headerlink" title="累加生成（AGO）"></a>累加生成（AGO）</h3><p>对原始序列进行<strong>一次累加生成</strong>（1-AGO），得到生成序列 $ X^{(1)} $，目的是增强数据的规律性（让杂乱数据呈现指数增长趋势）：  </p>
<p>$$ X^{(1)} &#x3D; {x^{(1)}_1, x^{(1)}_2, …, x^{(1)}_n} $$</p>
<p>其中，$ x^{(1)}(k) &#x3D; \sum_{i&#x3D;1}^k x^{(0)}(i) \quad (k&#x3D;1,2,…,n) $ (前缀和)</p>
<h3 id="构建白化微分方程"><a href="#构建白化微分方程" class="headerlink" title="构建白化微分方程"></a>构建白化微分方程</h3><p>假设 $ X^{(1)} $ 满足一阶线性微分方程（白化方程）：<br>$$ \frac{dx^{(1)}(t)}{dt} + a x^{(1)}(t) &#x3D; b $$</p>
<ul>
<li>$ a $：发展系数（反映序列增长趋势的强弱，$ |a|$ 越小，趋势越平稳）；  </li>
<li>$ b $：灰作用量（反映系统的外部输入或固有特性）。</li>
</ul>
<h3 id="最小二乘估计参数"><a href="#最小二乘估计参数" class="headerlink" title="最小二乘估计参数"></a>最小二乘估计参数</h3><p>将微分方程离散化，得到矩阵形式 $ Y &#x3D; B\hat{\alpha} $，其中：  </p>
<ul>
<li>观测向量 $ Y &#x3D; \begin{bmatrix} x^{(0)}(2) \ x^{(0)}(3) \ … \ x^{(0)}(n) \end{bmatrix} $  </li>
<li>数据矩阵 $ B &#x3D; \begin{bmatrix} -\frac{1}{2}[x^{(1)}(1)+x^{(1)}(2)] &amp; 1 \ -\frac{1}{2}[x^{(1)}(2)+x^{(1)}(3)] &amp; 1 \ … &amp; … \ -\frac{1}{2}[x^{(1)}(n-1)+x^{(1)}(n)] &amp; 1 \end{bmatrix} $  </li>
<li>参数向量 $ \hat{\alpha} &#x3D; \begin{bmatrix} a \ b \end{bmatrix} $</li>
</ul>
<p>通过最小二乘法求解参数：<br>$$ \hat{\alpha} &#x3D; (B^T B)^{-1} B^T Y $$</p>
<h3 id="求解白化方程与还原预测"><a href="#求解白化方程与还原预测" class="headerlink" title="求解白化方程与还原预测"></a>求解白化方程与还原预测</h3><p>白化方程的解析解（时间响应函数）为：<br>$$ x^{(1)}(t) &#x3D; \left( x^{(0)}(1) - \frac{b}{a} \right) e^{-a(t-1)} + \frac{b}{a} $$</p>
<p>对 $ x^{(1)}(t) $ 进行<strong>累减生成</strong>（差分）,（IG），还原得到原始序列的预测值 $ \hat{x}^{(0)}(k) $：<br>$$ \hat{x}^{(0)}(1) &#x3D; x^{(0)}(1) \quad $$<br>$$ \hat{x}^{(0)}(k) &#x3D; \hat{x}^{(1)}(k) - \hat{x}^{(1)}(k-1) \quad (k \geq 2) $$</p>
<h3 id="精度检验"><a href="#精度检验" class="headerlink" title="精度检验"></a>精度检验</h3><p>通过以下指标验证模型可靠性：  </p>
<ol>
<li><strong>平均相对误差（MAPE）</strong>：$ \text{MAPE} &#x3D; \frac{1}{n} \sum_{k&#x3D;1}^n \left| \frac{x^{(0)}(k) - \hat{x}^{(0)}(k)}{x^{(0)}(k)} \right| \times 100% $  <ul>
<li>一级精度：$ \text{MAPE} &lt; 10% $；二级精度：$ 10% \leq \text{MAPE} &lt; 20% $；三级精度：$ 20% \leq \text{MAPE} &lt; 30% $</li>
</ul>
</li>
<li><strong>后验差检验</strong>：  <ul>
<li>原始序列标准差 $ S_1 &#x3D; \sqrt{\frac{1}{n} \sum_{k&#x3D;1}^n (x^{(0)}(k) - \bar{x}^{(0)})^2} $  </li>
<li>残差序列 $ \varepsilon(k) &#x3D; x^{(0)}(k) - \hat{x}^{(0)}(k) $，残差标准差 $ S_2 &#x3D; \sqrt{\frac{1}{n} \sum_{k&#x3D;1}^n (\varepsilon(k) - \bar{\varepsilon})^2} $  </li>
<li>后验差比 $ C &#x3D; \frac{S_2}{S_1} $，小误差概率 $ P &#x3D; P(|\varepsilon(k) - \bar{\varepsilon}| &lt; 0.6745 S_1) $  </li>
<li>一级精度：$ C &lt; 0.35, P &gt; 0.95 $；二级精度：$ C &lt; 0.5, P &gt; 0.8 $；三级精度：$ C &lt; 0.65, P &gt; 0.7 $</li>
</ul>
</li>
</ol>
<h1 id="完整步骤"><a href="#完整步骤" class="headerlink" title="完整步骤"></a>完整步骤</h1><h2 id="数据预处理与检验"><a href="#数据预处理与检验" class="headerlink" title="数据预处理与检验"></a>数据预处理与检验</h2><ul>
<li><strong>数据要求</strong>：原始序列 $ X^{(0)} $ 为非负序列（若有负数，进行平移变换 $ x’(0)(k) &#x3D; x^{(0)}(k) + c $，$ c $ 为常数使所有数据非负）。  </li>
<li><strong>级比检验</strong>：验证数据是否适合GM(1,1)模型，计算级比 $ \lambda(k) &#x3D; \frac{x^{(0)}(k-1)}{x^{(0)}(k)} \quad (k&#x3D;2,…,n) $，若所有 $ \lambda(k) \in (e^{-2&#x2F;(n+1)}, e^{2&#x2F;(n+1)}) $，则适合建模；否则需进行数据变换（如对数变换、平移变换）。</li>
</ul>
<h2 id="累加生成（1-AGO）"><a href="#累加生成（1-AGO）" class="headerlink" title="累加生成（1-AGO）"></a>累加生成（1-AGO）</h2><p>计算 $ X^{(1)} $，增强数据规律性。</p>
<h2 id="构建矩阵-B-和-Y"><a href="#构建矩阵-B-和-Y" class="headerlink" title="构建矩阵 $ B $ 和 $ Y $"></a>构建矩阵 $ B $ 和 $ Y $</h2><p>按公式构造矩阵，注意维度匹配（$ B $ 为 $ (n-1) \times 2 $ 矩阵，$ Y $ 为 $ (n-1) \times 1 $ 向量）。</p>
<h2 id="参数估计（求解-a-b-）"><a href="#参数估计（求解-a-b-）" class="headerlink" title="参数估计（求解 $ a, b $）"></a>参数估计（求解 $ a, b $）</h2><p>通过最小二乘法计算 $ \hat{\alpha} &#x3D; (B^T B)^{-1} B^T Y $。</p>
<h2 id="预测与还原"><a href="#预测与还原" class="headerlink" title="预测与还原"></a>预测与还原</h2><ul>
<li>短期预测：计算 $ \hat{x}^{(1)}(n+1), \hat{x}^{(1)}(n+2), … $（竞赛中预测步长建议≤3，步长越长误差越大）。  </li>
<li>累减还原：得到原始序列的预测值 $ \hat{x}^{(0)}(n+1), … $。</li>
</ul>
<h2 id="精度检验-1"><a href="#精度检验-1" class="headerlink" title="精度检验"></a>精度检验</h2><p>计算MAPE、后验差比 $ C $、小误差概率 $ P $，若精度不达标，需优化模型（如残差修正、组合模型）。</p>
<h1 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h1><h2 id="Python版本"><a href="#Python版本" class="headerlink" title="Python版本"></a>Python版本</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">gm11</span>(<span class="params">x0, predict_num=<span class="number">1</span></span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    GM(1,1)灰色预测模型</span></span><br><span class="line"><span class="string">    :param x0: 原始序列（list或array，非负）</span></span><br><span class="line"><span class="string">    :param predict_num: 预测步数（默认1步，竞赛建议≤3）</span></span><br><span class="line"><span class="string">    :return: 原始序列预测值、未来预测值、模型参数(a,b)、平均相对误差(mape)</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    x0 = np.array(x0).reshape(-<span class="number">1</span>, <span class="number">1</span>)</span><br><span class="line">    n = <span class="built_in">len</span>(x0)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 1. 累加生成（1-AGO）</span></span><br><span class="line">    x1 = np.cumsum(x0, axis=<span class="number">0</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 2. 构建矩阵B和Y</span></span><br><span class="line">    B = np.zeros((n-<span class="number">1</span>, <span class="number">2</span>))</span><br><span class="line">    Y = np.zeros((n-<span class="number">1</span>, <span class="number">1</span>))</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(n-<span class="number">1</span>):</span><br><span class="line">        B[i, <span class="number">0</span>] = -<span class="number">0.5</span> * (x1[i] + x1[i+<span class="number">1</span>])</span><br><span class="line">        B[i, <span class="number">1</span>] = <span class="number">1</span></span><br><span class="line">        Y[i] = x0[i+<span class="number">1</span>]</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 3. 最小二乘法估计参数a, b</span></span><br><span class="line">    alpha = np.dot(np.dot(np.linalg.inv(np.dot(B.T, B)), B.T), Y)</span><br><span class="line">    a, b = alpha[<span class="number">0</span>, <span class="number">0</span>], alpha[<span class="number">1</span>, <span class="number">0</span>]</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 4. 时间响应函数与预测（x1_hat）</span></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">x1_hat</span>(<span class="params">k</span>):</span><br><span class="line">        <span class="keyword">return</span> (x0[<span class="number">0</span>, <span class="number">0</span>] - b/a) * np.exp(-a*(k-<span class="number">1</span>)) + b/a</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 5. 还原预测值（x0_hat）</span></span><br><span class="line">    x0_hat = np.zeros((n, <span class="number">1</span>))</span><br><span class="line">    x0_hat[<span class="number">0</span>] = x0[<span class="number">0</span>, <span class="number">0</span>]  <span class="comment"># 初始值不变</span></span><br><span class="line">    <span class="keyword">for</span> k <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">1</span>, n):</span><br><span class="line">        x0_hat[k] = x1_hat(k) - x1_hat(k-<span class="number">1</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 6. 未来预测</span></span><br><span class="line">    future_x0 = []</span><br><span class="line">    <span class="keyword">for</span> k <span class="keyword">in</span> <span class="built_in">range</span>(n+<span class="number">1</span>, n+<span class="number">1</span>+predict_num):</span><br><span class="line">        future_x0.append(x1_hat(k) - x1_hat(k-<span class="number">1</span>))</span><br><span class="line">    future_x0 = np.array(future_x0).reshape(-<span class="number">1</span>, <span class="number">1</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 7. 精度检验（平均相对误差MAPE）</span></span><br><span class="line">    mape = np.mean(np.<span class="built_in">abs</span>((x0 - x0_hat)/x0)) * <span class="number">100</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> x0_hat, future_x0, (a, b), mape</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&quot;__main__&quot;</span>:</span><br><span class="line">    <span class="comment"># 原始数据：某地区2018-2022年GDP（单位：亿元）</span></span><br><span class="line">    x0 = [<span class="number">500</span>, <span class="number">550</span>, <span class="number">610</span>, <span class="number">680</span>, <span class="number">760</span>]</span><br><span class="line">    predict_num = <span class="number">2</span>  <span class="comment"># 预测2023、2024年GDP</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 建模预测</span></span><br><span class="line">    x0_hat, future_x0, (a, b), mape = gm11(x0, predict_num)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 结果输出</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;模型参数：a=<span class="subst">&#123;a:<span class="number">.4</span>f&#125;</span>, b=<span class="subst">&#123;b:<span class="number">.4</span>f&#125;</span>&quot;</span>)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;平均相对误差MAPE：<span class="subst">&#123;mape:<span class="number">.2</span>f&#125;</span>%&quot;</span>)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;原始序列预测值：<span class="subst">&#123;x0_hat.flatten().<span class="built_in">round</span>(<span class="number">2</span>)&#125;</span>&quot;</span>)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;2023年预测GDP：<span class="subst">&#123;future_x0[<span class="number">0</span>,<span class="number">0</span>]:<span class="number">.2</span>f&#125;</span>亿元&quot;</span>)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;2024年预测GDP：<span class="subst">&#123;future_x0[<span class="number">1</span>,<span class="number">0</span>]:<span class="number">.2</span>f&#125;</span>亿元&quot;</span>)</span><br><span class="line">    </span><br><span class="line">    plt.rcParams[<span class="string">&#x27;font.sans-serif&#x27;</span>] = [<span class="string">&#x27;SimHei&#x27;</span>]  <span class="comment"># 支持中文</span></span><br><span class="line">    plt.figure(figsize=(<span class="number">10</span>, <span class="number">6</span>))</span><br><span class="line">    years = np.arange(<span class="number">2018</span>, <span class="number">2018</span>+<span class="built_in">len</span>(x0))</span><br><span class="line">    predict_years = np.arange(<span class="number">2018</span>+<span class="built_in">len</span>(x0), <span class="number">2018</span>+<span class="built_in">len</span>(x0)+predict_num)</span><br><span class="line">    </span><br><span class="line">    plt.plot(years, x0, <span class="string">&#x27;o-&#x27;</span>, label=<span class="string">&#x27;原始GDP&#x27;</span>, linewidth=<span class="number">2</span>, markersize=<span class="number">8</span>)</span><br><span class="line">    plt.plot(years, x0_hat.flatten(), <span class="string">&#x27;s-&#x27;</span>, label=<span class="string">&#x27;拟合GDP&#x27;</span>, linewidth=<span class="number">2</span>, markersize=<span class="number">8</span>)</span><br><span class="line">    plt.plot(predict_years, future_x0.flatten(), <span class="string">&#x27;^-&#x27;</span>, label=<span class="string">&#x27;预测GDP&#x27;</span>, linewidth=<span class="number">2</span>, markersize=<span class="number">8</span>, color=<span class="string">&#x27;red&#x27;</span>)</span><br><span class="line">    </span><br><span class="line">    plt.xlabel(<span class="string">&#x27;年份&#x27;</span>, fontsize=<span class="number">12</span>)</span><br><span class="line">    plt.ylabel(<span class="string">&#x27;GDP（亿元）&#x27;</span>, fontsize=<span class="number">12</span>)</span><br><span class="line">    plt.title(<span class="string">f&#x27;GM(1,1)灰色预测模型：某地区GDP预测（MAPE=<span class="subst">&#123;mape:<span class="number">.2</span>f&#125;</span>%）&#x27;</span>, fontsize=<span class="number">14</span>)</span><br><span class="line">    plt.legend(fontsize=<span class="number">10</span>)</span><br><span class="line">    plt.grid(<span class="literal">True</span>, alpha=<span class="number">0.3</span>)</span><br><span class="line">    plt.show()</span><br></pre></td></tr></table></figure>

<h2 id="Matlab版本"><a href="#Matlab版本" class="headerlink" title="Matlab版本"></a>Matlab版本</h2><figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="params">[x0_hat, future_x0, a, b, mape]</span> = <span class="title">gm11</span><span class="params">(x0, predict_num)</span></span></span><br><span class="line">    <span class="comment">% GM(1,1)灰色预测模型</span></span><br><span class="line">    <span class="comment">% 输入：x0-原始序列（行向量），predict_num-预测步数</span></span><br><span class="line">    <span class="comment">% 输出：x0_hat-原始序列预测值，future_x0-未来预测值，a-发展系数，b-灰作用量，mape-平均相对误差</span></span><br><span class="line">    </span><br><span class="line">    x0 = x0(:);  <span class="comment">% 转为列向量</span></span><br><span class="line">    n = <span class="built_in">length</span>(x0);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">% 1. 累加生成（1-AGO）</span></span><br><span class="line">    x1 = cumsum(x0);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">% 2. 构建矩阵B和Y</span></span><br><span class="line">    B = <span class="built_in">zeros</span>(n<span class="number">-1</span>, <span class="number">2</span>);</span><br><span class="line">    Y = <span class="built_in">zeros</span>(n<span class="number">-1</span>, <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">for</span> <span class="built_in">i</span> = <span class="number">1</span>:n<span class="number">-1</span></span><br><span class="line">        B(<span class="built_in">i</span>, <span class="number">1</span>) = <span class="number">-0.5</span> * (x1(<span class="built_in">i</span>) + x1(<span class="built_in">i</span>+<span class="number">1</span>));</span><br><span class="line">        B(<span class="built_in">i</span>, <span class="number">2</span>) = <span class="number">1</span>;</span><br><span class="line">        Y(<span class="built_in">i</span>) = x0(<span class="built_in">i</span>+<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">% 3. 最小二乘法估计参数</span></span><br><span class="line">    alpha = (B&#x27; * B) \ B&#x27; * Y;</span><br><span class="line">    a = alpha(<span class="number">1</span>);</span><br><span class="line">    b = alpha(<span class="number">2</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">% 4. 时间响应函数与还原预测</span></span><br><span class="line">    x0_hat = <span class="built_in">zeros</span>(n, <span class="number">1</span>);</span><br><span class="line">    x0_hat(<span class="number">1</span>) = x0(<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">for</span> k = <span class="number">2</span>:n</span><br><span class="line">        x1_hat_k = (x0(<span class="number">1</span>) - b/a) * <span class="built_in">exp</span>(-a*(k<span class="number">-1</span>)) + b/a;</span><br><span class="line">        x1_hat_k_1 = (x0(<span class="number">1</span>) - b/a) * <span class="built_in">exp</span>(-a*(k<span class="number">-2</span>)) + b/a;</span><br><span class="line">        x0_hat(k) = x1_hat_k - x1_hat_k_1;</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">% 5. 未来预测</span></span><br><span class="line">    future_x0 = <span class="built_in">zeros</span>(predict_num, <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">for</span> k = <span class="number">1</span>:predict_num</span><br><span class="line">        t = n + k;</span><br><span class="line">        x1_hat_t = (x0(<span class="number">1</span>) - b/a) * <span class="built_in">exp</span>(-a*(t<span class="number">-1</span>)) + b/a;</span><br><span class="line">        x1_hat_t_1 = (x0(<span class="number">1</span>) - b/a) * <span class="built_in">exp</span>(-a*(t<span class="number">-2</span>)) + b/a;</span><br><span class="line">        future_x0(k) = x1_hat_t - x1_hat_t_1;</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">% 6. 计算MAPE</span></span><br><span class="line">    mape = <span class="built_in">mean</span>(<span class="built_in">abs</span>((x0 - x0_hat) ./ x0)) * <span class="number">100</span>;</span><br><span class="line"><span class="keyword">end</span></span><br><span class="line"></span><br><span class="line"><span class="comment">% ---------------------- 测试代码 ----------------------</span></span><br><span class="line">x0 = [<span class="number">500</span>, <span class="number">550</span>, <span class="number">610</span>, <span class="number">680</span>, <span class="number">760</span>];</span><br><span class="line">predict_num = <span class="number">2</span>;</span><br><span class="line">[x0_hat, future_x0, a, b, mape] = gm11(x0, predict_num);</span><br><span class="line"></span><br><span class="line">fprintf(<span class="string">&#x27;模型参数：a=%.4f, b=%.4f\n&#x27;</span>, a, b);</span><br><span class="line">fprintf(<span class="string">&#x27;平均相对误差MAPE：%.2f%%\n&#x27;</span>, mape);</span><br><span class="line">fprintf(<span class="string">&#x27;原始序列预测值：&#x27;</span>);</span><br><span class="line"><span class="built_in">disp</span>(x0_hat&#x27;);</span><br><span class="line">fprintf(<span class="string">&#x27;2023年预测GDP：%.2f亿元\n&#x27;</span>, future_x0(<span class="number">1</span>));</span><br><span class="line">fprintf(<span class="string">&#x27;2024年预测GDP：%.2f亿元\n&#x27;</span>, future_x0(<span class="number">2</span>));</span><br><span class="line"></span><br><span class="line"><span class="comment">% 可视化</span></span><br><span class="line">years = <span class="number">2018</span>:<span class="number">2022</span>;</span><br><span class="line">predict_years = <span class="number">2023</span>:<span class="number">2024</span>;</span><br><span class="line"><span class="built_in">plot</span>(years, x0, <span class="string">&#x27;o-&#x27;</span>, <span class="string">&#x27;LineWidth&#x27;</span>, <span class="number">2</span>, <span class="string">&#x27;MarkerSize&#x27;</span>, <span class="number">8</span>, <span class="string">&#x27;DisplayName&#x27;</span>, <span class="string">&#x27;原始GDP&#x27;</span>);</span><br><span class="line"><span class="built_in">hold</span> on;</span><br><span class="line"><span class="built_in">plot</span>(years, x0_hat&#x27;, <span class="string">&#x27;s-&#x27;</span>, <span class="string">&#x27;LineWidth&#x27;</span>, <span class="number">2</span>, <span class="string">&#x27;MarkerSize&#x27;</span>, <span class="number">8</span>, <span class="string">&#x27;DisplayName&#x27;</span>, <span class="string">&#x27;拟合GDP&#x27;</span>);</span><br><span class="line"><span class="built_in">plot</span>(predict_years, future_x0&#x27;, <span class="string">&#x27;^-&#x27;</span>, <span class="string">&#x27;LineWidth&#x27;</span>, <span class="number">2</span>, <span class="string">&#x27;MarkerSize&#x27;</span>, <span class="number">8</span>, <span class="string">&#x27;Color&#x27;</span>, <span class="string">&#x27;red&#x27;</span>, <span class="string">&#x27;DisplayName&#x27;</span>, <span class="string">&#x27;预测GDP&#x27;</span>);</span><br><span class="line">xlabel(<span class="string">&#x27;年份&#x27;</span>, <span class="string">&#x27;FontSize&#x27;</span>, <span class="number">12</span>);</span><br><span class="line">ylabel(<span class="string">&#x27;GDP（亿元）&#x27;</span>, <span class="string">&#x27;FontSize&#x27;</span>, <span class="number">12</span>);</span><br><span class="line">title(sprintf(<span class="string">&#x27;GM(1,1)灰色预测模型：某地区GDP预测（MAPE=%.2f%%）&#x27;</span>, mape), <span class="string">&#x27;FontSize&#x27;</span>, <span class="number">14</span>);</span><br><span class="line"><span class="built_in">legend</span>(<span class="string">&#x27;FontSize&#x27;</span>, <span class="number">10</span>);</span><br><span class="line">grid on;</span><br><span class="line"><span class="built_in">hold</span> off;</span><br></pre></td></tr></table></figure>

<h1 id="总结与应用场景"><a href="#总结与应用场景" class="headerlink" title="总结与应用场景"></a>总结与应用场景</h1><p>灰色预测的核心优势是“小样本、贫信息”，在数学建模中常用于以下场景：  </p>
<ol>
<li>短期经济预测（如GDP、居民收入、物价指数）；  </li>
<li>公共卫生预测（如疫情初期确诊人数、疫苗接种需求）；  </li>
<li>工程预测（如设备故障时间、原材料消耗、能耗预测）；  </li>
<li>环境预测（如空气质量指数、污染物浓度短期预测）。</li>
</ol>
<p>在竞赛中，若遇到“数据少、无明确分布、短期预测”需求，可优先考虑GM(1,1)模型，并通过“残差修正+组合模型”提升精度。</p>
]]></content>
      <categories>
        <category>数学建模</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>灰色预测</tag>
      </tags>
  </entry>
  <entry>
    <title>自动微分</title>
    <url>/2026/02/27/%E8%87%AA%E5%8A%A8%E5%BE%AE%E5%88%86/</url>
    <content><![CDATA[<p>神经网络重要模块</p>
<span id="more"></span>
<h1 id="前置"><a href="#前置" class="headerlink" title="前置"></a>前置</h1><h2 id="神经网络流程"><a href="#神经网络流程" class="headerlink" title="神经网络流程"></a>神经网络流程</h2><p>训练神经网络时，最常用的算法就是反向传播。在该算法中，参数（主要是模型权重）会根据损失函数关于对应参数的梯度进行调整。  </p>
<p>大概来说</p>
<ul>
<li>通过公式 $y&#x3D;w*x+b$ 得到预测值</li>
<li>使用 $loss(y,z)$ 计算得到损失值</li>
<li>对其求导，得到梯度，以此反向更新原参数w，b</li>
<li>更新：W新&#x3D;Wjiu<br>为了计算这些梯度，PyTorch内置了名为 torch.autograd 的微分模块。它支持任意计算图的自动梯度计算</li>
</ul>
<h2 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h2><ul>
<li>PyTorch不支持向量张量对向量张量的求导,只支持标量张量对向量张量的求导</li>
<li>大多底层操作为浮点型，记得转为float32</li>
<li>x如果是张量,y必须是标量(一个值)才可以进行求导</li>
<li>计算梯度: y.backward(), y是一个标量</li>
<li>获取x点的梯度值: x.grad, 会累加上一次的梯度值</li>
</ul>
<h2 id="演示"><a href="#演示" class="headerlink" title="演示"></a>演示</h2><p>先简单演示一下更新过程</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"><span class="comment"># requires_grad=:默认为False,不会自动计算梯度;为True的话是将自动计算的梯度值保存到grad中</span></span><br><span class="line">w=torch.tensor(<span class="number">10</span>,requires_grad=<span class="literal">True</span>,dtype=torch.<span class="built_in">float</span>)</span><br><span class="line">loss=<span class="number">2</span>*w**<span class="number">2</span></span><br><span class="line"><span class="comment"># print(type(loss.grad_fn))</span></span><br><span class="line"><span class="comment"># print(loss.sum())</span></span><br><span class="line">loss.<span class="built_in">sum</span>().backward() <span class="comment"># 标量张量才能求导，常用的操作是sum变为标量</span></span><br><span class="line">w.data=w.data - <span class="number">0.01</span> * w.grad</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;new_w: <span class="subst">&#123;w&#125;</span>&quot;</span>)</span><br></pre></td></tr></table></figure>

<h1 id="具体过程"><a href="#具体过程" class="headerlink" title="具体过程"></a>具体过程</h1><h2 id="加入循环"><a href="#加入循环" class="headerlink" title="加入循环"></a>加入循环</h2><p>此处流程和前面类似，加入了多次迭代和上面一些注意事项的考虑</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">w=torch.tensor(<span class="number">10</span>,requires_grad=<span class="literal">True</span>,dtype=torch.<span class="built_in">float</span>)</span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">101</span>):</span><br><span class="line">    loss=<span class="number">2</span>*w**<span class="number">2</span>+<span class="number">20</span></span><br><span class="line">    <span class="comment"># 默认梯度会累加，所以除第一次，都要清零</span></span><br><span class="line">    <span class="keyword">if</span> w.grad <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>:</span><br><span class="line">        w.grad.zero_()</span><br><span class="line">    loss.<span class="built_in">sum</span>().backward()</span><br><span class="line">    w.data=w.data - <span class="number">0.01</span> * w.grad</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&#x27;第 <span class="subst">&#123;i&#125;</span> 次，w：<span class="subst">&#123;w:<span class="number">.5</span>f&#125;</span>，梯度值：<span class="subst">&#123;w.grad:<span class="number">.5</span>f&#125;</span>&#x27;</span>)</span><br></pre></td></tr></table></figure>
<p>从结果可以看出梯度逐渐降低</p>
<h2 id="小问题"><a href="#小问题" class="headerlink" title="小问题"></a>小问题</h2><p>设置requires_grad&#x3D;True后，此张量数组就无法转为numpy了，这里系统会提示你使用detach函数</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">t1=torch.tensor([<span class="number">10</span>,<span class="number">20</span>],requires_grad=<span class="literal">True</span>,dtype=torch.<span class="built_in">float</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&#x27;t1: <span class="subst">&#123;t1&#125;</span>,type: <span class="subst">&#123;<span class="built_in">type</span>(t1)&#125;</span>&#x27;</span>)</span><br><span class="line"><span class="comment"># n1=t1.numpy()</span></span><br><span class="line">t2=t1.detach().numpy()</span><br><span class="line">t3=t1.detach().numpy().copy()</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&#x27;t2: <span class="subst">&#123;t2&#125;</span>,type: <span class="subst">&#123;<span class="built_in">type</span>(t2)&#125;</span>&#x27;</span>)</span><br><span class="line">t1.data[<span class="number">0</span>]=<span class="number">100</span></span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&#x27;t1: <span class="subst">&#123;t1&#125;</span>&#x27;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&#x27;t2: <span class="subst">&#123;t2&#125;</span>&#x27;</span>) <span class="comment"># 与t1共享内存 </span></span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&#x27;t3: <span class="subst">&#123;t3&#125;</span>&#x27;</span>) <span class="comment"># 使用copy后不共享</span></span><br></pre></td></tr></table></figure>
<h2 id="更加细节"><a href="#更加细节" class="headerlink" title="更加细节"></a>更加细节</h2><p>现在设置损失函数并进行反向传播</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 输入张量 2*5</span></span><br><span class="line">x = torch.ones(<span class="number">2</span>, <span class="number">5</span>)</span><br><span class="line"><span class="comment"># 目标值是 2*3    </span></span><br><span class="line">y = torch.zeros(<span class="number">2</span>, <span class="number">3</span>)</span><br><span class="line"><span class="comment"># 设置要更新的权重和偏置的初始值</span></span><br><span class="line">w = torch.randn(<span class="number">5</span>, <span class="number">3</span>, requires_grad=<span class="literal">True</span>)</span><br><span class="line">b = torch.randn(<span class="number">3</span>, requires_grad=<span class="literal">True</span>) <span class="comment"># 广播机制</span></span><br><span class="line"><span class="comment"># 设置网络的输出值</span></span><br><span class="line">z = torch.matmul(x, w) + b  <span class="comment"># 矩阵乘法 等价于：x@w+b</span></span><br><span class="line"><span class="comment"># 设置损失函数</span></span><br><span class="line">loss = torch.nn.MSELoss()</span><br><span class="line">loss = loss(z, y)</span><br><span class="line"><span class="comment"># 自动微分</span></span><br><span class="line">loss.backward()</span><br><span class="line"><span class="comment"># 打印 w,b 变量的梯度</span></span><br><span class="line"><span class="comment"># backward 函数计算的梯度值会存储在张量的 grad 变量中</span></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;W的梯度:&quot;</span>, w.grad)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;b的梯度&quot;</span>, b.grad)</span><br></pre></td></tr></table></figure>
<h1 id="案例"><a href="#案例" class="headerlink" title="案例"></a>案例</h1><p>一个使用pytorch框架实现线性回归的简单案例</p>
<h2 id="涉及的API"><a href="#涉及的API" class="headerlink" title="涉及的API"></a>涉及的API</h2><ul>
<li>nn.MSELoss() 代替平方损失函数</li>
<li>data.DataLoader 代替数据加载器，其相关参数：<ul>
<li>dataset&#x3D;:数据集对象</li>
<li>batch_size&#x3D;:批量训练样本数据</li>
<li>shuffle&#x3D;:样本数据是否进行乱序（训练集选择True）</li>
</ul>
</li>
<li>optim.SGD 代替优化器</li>
<li>nn.Linear 代替假设函数，参数：  <ul>
<li>in_features指的是输入张量的大小size</li>
<li>out_features指的是输出张量的大小size</li>
</ul>
</li>
</ul>
<h2 id="具体代码"><a href="#具体代码" class="headerlink" title="具体代码"></a>具体代码</h2><p>数据处理过程：<br>numpy对象张量 -&gt; tensor数据集对象 -&gt; dataset -&gt; 数据加载器dataloader</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"><span class="keyword">from</span> torch.utils.data <span class="keyword">import</span> TensorDataset  <span class="comment"># 构造数据集对象</span></span><br><span class="line"><span class="keyword">from</span> torch.utils.data <span class="keyword">import</span> DataLoader  <span class="comment"># 数据加载器</span></span><br><span class="line"><span class="keyword">from</span> torch <span class="keyword">import</span> nn  <span class="comment"># nn模块中有平方损失函数和假设函数</span></span><br><span class="line"><span class="keyword">from</span> torch <span class="keyword">import</span> optim  <span class="comment"># optim模块中有优化器函数</span></span><br><span class="line"><span class="keyword">from</span> sklearn.datasets <span class="keyword">import</span> make_regression  <span class="comment"># 创建线性回归模型数据集</span></span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"></span><br><span class="line">plt.rcParams[<span class="string">&#x27;font.sans-serif&#x27;</span>] = [<span class="string">&#x27;SimHei&#x27;</span>]  <span class="comment"># 用来正常显示中文标签</span></span><br><span class="line">plt.rcParams[<span class="string">&#x27;axes.unicode_minus&#x27;</span>] = <span class="literal">False</span>  <span class="comment"># 用来正常显示负号</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">create_dataset</span>():</span><br><span class="line">    x, y, coef = make_regression(n_samples=<span class="number">100</span>,</span><br><span class="line">                                 n_features=<span class="number">1</span>,</span><br><span class="line">                                 noise=<span class="number">10</span>,</span><br><span class="line">                                 coef=<span class="literal">True</span>,</span><br><span class="line">                                 bias=<span class="number">14.5</span>,</span><br><span class="line">                                 random_state=<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 将构建数据转换为张量类型</span></span><br><span class="line">    x = torch.tensor(x)</span><br><span class="line">    y = torch.tensor(y)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> x, y, coef</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&quot;__main__&quot;</span>: </span><br><span class="line">    <span class="comment"># 1. 数据处理</span></span><br><span class="line">    <span class="comment"># 构造数据集</span></span><br><span class="line">    x, y, coef = create_dataset()</span><br><span class="line">    <span class="comment"># 构造数据集对象</span></span><br><span class="line">    dataset = TensorDataset(x, y)</span><br><span class="line">    dataloader = DataLoader(dataset=dataset, batch_size=<span class="number">16</span>, shuffle=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 2. 构造模型</span></span><br><span class="line">    model = nn.Linear(in_features=<span class="number">1</span>, out_features=<span class="number">1</span>)</span><br><span class="line">    <span class="comment"># 构造平方损失函数</span></span><br><span class="line">    criterion = nn.MSELoss()</span><br><span class="line">    <span class="comment"># 构造优化函数</span></span><br><span class="line">    optimizer = optim.SGD(params=model.parameters(), lr=<span class="number">1e-2</span>)</span><br><span class="line">    epochs = <span class="number">100</span></span><br><span class="line">    <span class="comment"># 损失的变化</span></span><br><span class="line">    epoch_loss = [] </span><br><span class="line">    total_loss=<span class="number">0.0</span> </span><br><span class="line">    train_sample=<span class="number">0.0</span></span><br><span class="line">    <span class="comment"># 3. 训练过程</span></span><br><span class="line">    <span class="keyword">for</span> _ <span class="keyword">in</span> <span class="built_in">range</span>(epochs):</span><br><span class="line">        <span class="keyword">for</span> train_x, train_y <span class="keyword">in</span> dataloader:</span><br><span class="line">            <span class="comment"># 分批次训练模型，每次循环将一个batch的训练数据送入模型</span></span><br><span class="line">            y_pred = model(train_x.<span class="built_in">type</span>(torch.float32))</span><br><span class="line">            <span class="comment"># 计算损失值,均方误差,当前批次所有样本的平均误差</span></span><br><span class="line">            loss = criterion(y_pred, train_y.reshape(-<span class="number">1</span>, <span class="number">1</span>).<span class="built_in">type</span>(torch.float32))</span><br><span class="line">            total_loss += loss.item() </span><br><span class="line">            <span class="comment"># loss是平均误差,所以样本数+1</span></span><br><span class="line">            train_sample += <span class="number">1</span></span><br><span class="line">            <span class="comment"># 梯度清零</span></span><br><span class="line">            optimizer.zero_grad()</span><br><span class="line">            <span class="comment"># 自动微分(反向传播)</span></span><br><span class="line">            loss.backward()</span><br><span class="line">            <span class="comment"># 更新参数</span></span><br><span class="line">            optimizer.step()</span><br><span class="line">       <span class="comment"># 计算所有batch的平均误差作为当前epoch的误差 </span></span><br><span class="line">        epoch_loss.append(total_loss/train_sample)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 4. 可视化 </span></span><br><span class="line">    plt.plot(<span class="built_in">range</span>(epochs), epoch_loss) </span><br><span class="line">    plt.title(<span class="string">&#x27;损失变化曲线&#x27;</span>) </span><br><span class="line">    plt.grid() </span><br><span class="line">    plt.show()</span><br><span class="line">    <span class="comment"># 绘制拟合直线</span></span><br><span class="line">    plt.scatter(x, y)</span><br><span class="line">    x = torch.linspace(x.<span class="built_in">min</span>(), x.<span class="built_in">max</span>(), <span class="number">1000</span>)</span><br><span class="line">    y1 = torch.tensor([v * model.weight + model.bias <span class="keyword">for</span> v <span class="keyword">in</span> x])</span><br><span class="line">    y2 = torch.tensor([v * coef + <span class="number">14.5</span> <span class="keyword">for</span> v <span class="keyword">in</span> x])</span><br><span class="line">    plt.plot(x, y1, label=<span class="string">&#x27;训练&#x27;</span>)</span><br><span class="line">    plt.plot(x, y2, label=<span class="string">&#x27;真实&#x27;</span>)</span><br><span class="line">    plt.grid()</span><br><span class="line">    plt.legend()</span><br><span class="line">    plt.show()</span><br></pre></td></tr></table></figure>
<h1 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h1><p>PyTorch模型构建的流程为:</p>
<ol>
<li>准备训练集数据</li>
<li>构建要使用的模型</li>
<li>设置损失函数和优化器</li>
<li>模型训练</li>
</ol>
<p>自动微分是其中重要的部分，在训练部分用于更新参数</p>
]]></content>
      <categories>
        <category>深度学习</category>
      </categories>
      <tags>
        <tag>深度学习</tag>
        <tag>张量</tag>
        <tag>机器学习</tag>
        <tag>自动微分</tag>
      </tags>
  </entry>
  <entry>
    <title>熵权法</title>
    <url>/2025/12/04/%E7%86%B5%E6%9D%83%E6%B3%95/</url>
    <content><![CDATA[<p>水一篇</p>
<span id="more"></span>  
<pre><code>前置知识：最好会层次分析法
</code></pre>
<h1 id="模型引出"><a href="#模型引出" class="headerlink" title="模型引出"></a>模型引出</h1><p>熵权法和前面的层次分析法一样，是一种赋权方法。但不一样的是，熵权法是完全客观的方法，仅依赖数据本身的分布。<br>先来看个例子：<br>两个人比较身材，他们的身高只差一厘米，但体重差10千克，那么我们重点需要比较两人的体重而不是身高。在这个例子中，身高的权重就比较低，体重的权重比较高，并且根据数据是可以具体算出二者权重的。</p>
<h1 id="模型原理"><a href="#模型原理" class="headerlink" title="模型原理"></a>模型原理</h1><p>由信息论基本原理：</p>
<ul>
<li>信息是系统有序程度的度量</li>
<li>熵是系统无序程度的度量</li>
</ul>
<p>信息熵是衡量随机变量不确定性的量化指标—-不确定性越高，熵值越大；确定性越高，熵值越小。  </p>
<p>简单的例子：</p>
<ul>
<li>太阳从东边升起。这是必然时间，没有不确定性，信息熵为0</li>
<li>掷一枚均匀硬币，结果是正面。有不确定性，熵不为0</li>
<li>掷一次六面骰子，结果是 3 点。不确定性比掷硬币更高，熵更大</li>
</ul>
<p>在熵权法中，使用信息熵值来判断某个指标的离散程度。离散程度越大，权重越高，反之权重小。  </p>
<h1 id="主要过程"><a href="#主要过程" class="headerlink" title="主要过程"></a>主要过程</h1><h2 id="1-数据标准化"><a href="#1-数据标准化" class="headerlink" title="1.数据标准化"></a>1.数据标准化</h2><p>$$<br> \widetilde{x_{ij}}&#x3D;\frac{x_{ij}}{\sqrt{\sum_{i&#x3D;1}^{n}{x_{ij}^2}}}<br> $$<br>如果 $x_{ij}$ 中有负数，也可以用：<br>$$<br> \widetilde{x_{ij}}&#x3D;\frac{x_{ij}-min(x_{1j},x_{2j},…,x_{nj})}{max(x_{1j},x_{2j},…,x_{nj})-min(x_{1j},x_{2j},…,x_{nj})}<br> $$</p>
<h2 id="2-归一化"><a href="#2-归一化" class="headerlink" title="2.归一化"></a>2.归一化</h2><p>$$<br> p_{ij}&#x3D;\frac{\widetilde{x_{ij}}}{\sum_{i&#x3D;1}^{n}{\widetilde {x_{ij}}}}<br> $$</p>
<h2 id="3-计算熵权"><a href="#3-计算熵权" class="headerlink" title="3.计算熵权"></a>3.计算熵权</h2><p>$$ e_j&#x3D;-\frac{1}{ln(n)}\sum_{i&#x3D;1}^{n}{p_{ij}}ln(p_{ij})$$<br>注意，此处定义 $ln(0)&#x3D;0$<br>$$ d_j&#x3D;1-e_j $$<br>然后再归一化：<br>$$ W_j&#x3D;\frac{d_j}{\sum_{j&#x3D;1}^{m}{d_j}}$$</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>熵权法是一种基于数据客观分布的评价类模型赋权方法。但是，绝对的客观有时也是不行的，比如上面评价身材的例子，如果身高都一样，那么权重为0，说明比较中身材完全与身高无关，但实际上身材和身高是密切相关的。我们在评价类问题中，往往需要综合多种方法，比如使用层次分析法的主观方法和客观的熵权法的组合来更精确的评价目标。</p>
]]></content>
      <categories>
        <category>数学建模</category>
      </categories>
      <tags>
        <tag>数学建模</tag>
        <tag>评价类模型</tag>
        <tag>熵权法</tag>
      </tags>
  </entry>
  <entry>
    <title>逻辑回归</title>
    <url>/2025/11/25/%E9%80%BB%E8%BE%91%E5%9B%9E%E5%BD%92/</url>
    <content><![CDATA[<p>简单的逻辑回归和样例ヾ(<em>´∀ ˋ</em>)ﾉ</p>
<span id="more"></span>  <!-- 就加这行！前面的内容（标题+前置知识）首页不显示，只显文章标题 -->
<p><del>这篇文章是本蒟蒻第一个博客，各处表述难免欠妥，希望各位佬指出</del></p>
<pre><code>前置知识：线性回归，简单概率论，一点点机器学习基础
</code></pre>
<h1 id="什么是逻辑回归"><a href="#什么是逻辑回归" class="headerlink" title="什么是逻辑回归"></a>什么是逻辑回归</h1><p>逻辑回归 <code>logistic regression</code> 是一种经典分类算法，通常用于二分类。逻辑回归是一种对数线性模型(从下面的公式也可以看出来)  </p>
<h1 id="逻辑回归基础"><a href="#逻辑回归基础" class="headerlink" title="逻辑回归基础"></a>逻辑回归基础</h1><p>先来了解一下逻辑斯谛分布, 设 <code>X</code> 为连续随机变量, 逻辑斯谛分布的累积分布函数为：</p>
<p>$$ F(x) &#x3D; P(X \leq x) &#x3D; \frac{1}{1 + e^{-\frac{x - \mu}{\gamma}}} $$</p>
<p>对应的概率密度函数（CDF 的导数）为：</p>
<p>$$ f(x) &#x3D; F’(x) &#x3D; \frac{e^{-\frac{x - \mu}{\gamma}}}{\gamma \cdot \left(1 + e^{-\frac{x - \mu}{\gamma}}\right)^2} $$</p>
<p>式中 $\mu$ 为位置参数,  $\gamma$ 为形状参数  </p>
<p><del>好复杂看不懂</del><br>先看看简单的。<br>当逻辑斯谛分布的位置参数 $\mu$、形状参数 $\gamma$ 时，其累积分布函数（CDF）就是 sigmoid 函数（激活函数）：<br>$$ sigmoid(x) &#x3D; \frac{1}{1 + e^{-x}} $$</p>
<p>作用：将($-\infty, +\infty$) 映射到 (0, 1)</p>
<p>再结合线性回归，就可以做到逻辑回归最简单的应用: 二分类  </p>
<p>就是<strong>将线性回归的输出作为逻辑回归的输入</strong><br>具体一点，就是将线性回归的值，带入上面的激活函数，映射到 (0-1) 后，再根据阈值分类<br>选定阈值（0-1）后大于阈值为正样本， 小于为负样本</p>
<p>$$h(x)&#x3D;sigmoid(w^T·x+b)$$</p>
<h2 id="下面介绍逻辑回归损失函数"><a href="#下面介绍逻辑回归损失函数" class="headerlink" title="下面介绍逻辑回归损失函数"></a>下面介绍逻辑回归损失函数</h2><p>$$ Loss(L) &#x3D; -y_i \log p - (1 - y_i) \log(1 - p) $$</p>
<p>式中，$y_i$ 为逻辑回归的预估的类别，A类为1，B类为0，即正样本只看前半部分，负样本只看后半部分。<br> $p$ 为上面 h(x) 的返回值，即概率。<br>损失函数的工作原理：每个样本有A，B两个类别，<strong>真实类别对应的位置，概率值越大越好</strong>  </p>
<p>下面具体讲讲概率分布  </p>
<h3 id="对于一个样本"><a href="#对于一个样本" class="headerlink" title="对于一个样本"></a>对于一个样本</h3><p>假设有0，1两个类别，显然有：<br>$$<br>L&#x3D;<br>\begin{cases}<br>p &amp; y &#x3D; 1 \quad (\text{样本为正类}) \<br>1-p &amp; y &#x3D; 0 \quad (\text{样本为负类})<br>\end{cases}<br>$$<br>等价于<br>$$ L&#x3D;p^y(1-p)^{1-y} $$</p>
<h3 id="推广到n个样本"><a href="#推广到n个样本" class="headerlink" title="推广到n个样本"></a>推广到n个样本</h3><p>$$P&#x3D;\displaystyle \prod_{i&#x3D;1}^{n}{p^{y_i}(1-p)^{1-{y_i}}}$$</p>
<p>这个就是所有样本都预测正确的概率。</p>
<p>问题转换为：当上述联合概率取最大时，估计 $w$ 和 $b$ ，就变成了极大似然估计。</p>
<h3 id="极大似然函数转对数似然函数"><a href="#极大似然函数转对数似然函数" class="headerlink" title="极大似然函数转对数似然函数"></a>极大似然函数转对数似然函数</h3><p>对上述式子取对数，有:<br>$$H(L)&#x3D;\sum_{i&#x3D;1}^n y_i \log p + (1 - y_i) \log(1 - p) $$</p>
<p>因为是损失函数，所以应该取最小值，只需加个负号。</p>
<p>$$Loss(L)&#x3D;-\sum_{i&#x3D;1}^n y_i \log p + (1 - y_i) \log(1 - p) $$<br>也就得到了之前是式子。<br>通过这个转换，将最小化损失函数的任务交给了极大似然函数，所以逻辑回归本质上没有新的算法。  </p>
<p>总结即是：逻辑回归损失函数&#x3D; - 极大似然估计函数  </p>
<h1 id="实战训练——癌症预测"><a href="#实战训练——癌症预测" class="headerlink" title="实战训练——癌症预测"></a>实战训练——癌症预测</h1><p>逻辑回归的包  </p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.linear_model <span class="keyword">import</span> LogisticRegression</span><br></pre></td></tr></table></figure>

<p>简单介绍一下api</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">LogisticRegression(solver=<span class="string">&#x27;liblinear&#x27;</span>,penalty=<span class="string">&#x27;l2&#x27;</span>,C=<span class="number">1.0</span>)</span><br></pre></td></tr></table></figure>
<p>其中 <code>liblinear</code> 在数据集较小是比较快，对大数据集也可以换成 <code>sag</code> or <code>saga</code><br> <code>penalty</code> 表示正则化种类，l1或者l2<br>C 表示正则化力度<br>默认将数量少的类别作为正例  </p>
<p>下面是完整代码，只是换成了 <code>LogisticRegression</code> ,其他和别的方法一样</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> train_test_split</span><br><span class="line"><span class="keyword">from</span> sklearn.linear_model <span class="keyword">import</span> LogisticRegression</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> accuracy_score</span><br><span class="line"><span class="keyword">from</span> sklearn.preprocessing <span class="keyword">import</span> MinMaxScaler</span><br><span class="line"><span class="keyword">from</span> sklearn.preprocessing <span class="keyword">import</span> StandardScaler</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">d1</span>():</span><br><span class="line">    df = pd.read_csv(<span class="string">&quot;breast-cancer-wisconsin.data&quot;</span>, sep=<span class="string">&quot;,&quot;</span>, header=<span class="literal">None</span>)</span><br><span class="line">    <span class="comment"># df.info()</span></span><br><span class="line">    df=df.replace(to_replace=<span class="string">&#x27;?&#x27;</span>,value=np.NaN)</span><br><span class="line">    df=df.dropna()</span><br><span class="line">    df.info()</span><br><span class="line">    x=df.iloc[:,<span class="number">1</span>:-<span class="number">1</span>]</span><br><span class="line">    y=df.iloc[:,-<span class="number">1</span>]</span><br><span class="line">    x_train,x_test,y_train,y_test = train_test_split(x,y,test_size=<span class="number">0.2</span>,random_state=<span class="number">22</span>)</span><br><span class="line">    transfer=StandardScaler()</span><br><span class="line">    x_train=transfer.fit_transform(x_train)</span><br><span class="line">    x_test=transfer.transform(x_test)</span><br><span class="line">    model=LogisticRegression()</span><br><span class="line">    model.fit(x_train,y_train)</span><br><span class="line">    y_pred=model.predict(x_test)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&#x27;score: <span class="subst">&#123;model.score(x_test,y_test)&#125;</span>&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    d1()</span><br></pre></td></tr></table></figure>


<h1 id="模型的评估"><a href="#模型的评估" class="headerlink" title="模型的评估"></a>模型的评估</h1><p>像上面那样，仅使用 ‘准确率’ 进行估计，事实上是不精确，甚至是不可取的  </p>
<p>比如：1000 人中仅 10 人患癌（正例占 1%，健康人占 99%，极度不平衡）若模型 “躺平” 全预测 “健康”，准确率 &#x3D; 990&#x2F;1000&#x3D;99%（看似优秀），但漏诊了所有 10 名患者（相当于最坏情况下根本没测出来）。<br>而如果说继续提升这个准确率，确实可以提高测出来的比例，但是这在精度上的要求将是巨大的，现实情况样本容量巨大，实现是不可能的。  </p>
<p>在正负样例容量差距较大时，需要进行进一步的评估，下面介绍一些方法  </p>
<h2 id="混淆矩阵"><a href="#混淆矩阵" class="headerlink" title="混淆矩阵"></a>混淆矩阵</h2><table>
<thead>
<tr>
<th align="center"></th>
<th align="center">正例（预测）</th>
<th align="center">反例（预测）</th>
</tr>
</thead>
<tbody><tr>
<td align="center">正例（真实）</td>
<td align="center">真正例（TP）</td>
<td align="center">伪反例（FN）</td>
</tr>
<tr>
<td align="center">反例（真实）</td>
<td align="center">伪正例（FP）</td>
<td align="center">真反例（TN）</td>
</tr>
</tbody></table>
<p>其中，T&#x2F;F表示真&#x2F;假，P&#x2F;N表示正&#x2F;负（样例）<br>比如伪反例，就是本来应该是正例，被错误预测为反例，即假的反例 <code>False Negative</code>  </p>
<p>下面的评估需要用到混淆矩阵  </p>
<ul>
<li>精确率 <code>precision</code><br>$$ P&#x3D; \frac{TP}{TP + FP} $$</li>
<li>召回率（查全率） <code>recall</code><br>$$ P&#x3D; \frac{TP}{TP + FN} $$</li>
<li><code>F1 score</code> (用于综合上面两个)<br>$$ P&#x3D; \frac{2·precision·recall}{precision + recall} $$</li>
</ul>
<p>从各公式就可以明显看出来这三个值的含义了，再简单解释一下，精确率就是预测出来的正例确实是正例的概率，查全率就是有多少正例被预测出来了。这两个指标当然越大越好，F1值即综合上面两个指标，也就是可以用F1值作为模型好坏的评价，<strong>F1值越大模型越好</strong>。  </p>
<p>代码也很简单，先加个包</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> accuracy_score, f1_score,confusion_matrix</span><br></pre></td></tr></table></figure>
<p>其他的和上面的代码一样，得分改成F1值</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="built_in">print</span>(<span class="string">f&#x27;F1 Score (pos_label=4): <span class="subst">&#123;f1_score(y_test, y_pred, pos_label=<span class="number">4</span>):<span class="number">.4</span>f&#125;</span>&#x27;</span>)</span><br></pre></td></tr></table></figure>
<p>当然这里也可以不加 <code>pos_label</code> ，默认样本少的为正例<br>也可以打印出混淆矩阵来看看  </p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">cm = confusion_matrix(y_test, y_pred)</span><br><span class="line">cm_df = pd.DataFrame(</span><br><span class="line">    cm,</span><br><span class="line">    index=[<span class="string">&quot;真实_良性(2)&quot;</span>, <span class="string">&quot;真实_恶性(4)&quot;</span>],</span><br><span class="line">    columns=[<span class="string">&quot;预测_良性(2)&quot;</span>, <span class="string">&quot;预测_恶性(4)&quot;</span>]</span><br><span class="line">)</span><br><span class="line"><span class="built_in">print</span>(cm_df)</span><br></pre></td></tr></table></figure>
<p>至此，就可以得到更加精准的模型评估  </p>
<h2 id="ROC曲线和AUC指标"><a href="#ROC曲线和AUC指标" class="headerlink" title="ROC曲线和AUC指标"></a>ROC曲线和AUC指标</h2><p>这两个就是进一步拓展了，这里简单讲一点  </p>
<p>ROC <code>Receiver Operating Characteristic Curve</code> 曲线可以就是上面混淆矩阵的可视化  </p>
<ul>
<li><p>横轴（FPR）：假阳性率 &#x3D; 假阳性样本数&#x2F;(假阳性+真阴性样本数) → 模型误将负样本判为正样本的比例（反映“误诊”风险）  </p>
</li>
<li><p>纵轴（TPR）：真阳性率 &#x3D; 真阳性样本数&#x2F;(真阳性+假阴性样本数) → 模型正确识别正样本的比例（即召回率，反映“漏诊”风险）</p>
</li>
</ul>
<p>同一数据集下，性能更优的模型ROC曲线会更靠近左上角（TPR高且FPR低） </p>
<p>AUC <code>Area Under the ROC Curve</code> 是ROC曲线下方的面积，取值范围为0~1：</p>
<p>核心意义：代表模型在随机抽取一个正样本和一个负样本时，正样本被预测为正类的概率大于负样本的概率（即模型区分正&#x2F;负样本的能力, 也可以作为模型评分）<br>使用AUC可以直接比较多个模型的整体性能  </p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>逻辑回归看着带“回归”二字，其实是做分类的，尤其适合分二类（比如判断是不是癌症）。</p>
<p>核心思路很简单：先拿线性回归算出一个结果，再用sigmoid函数把这个结果变成0到1之间的概率，定个阈值（比如0.5），概率超阈值就是正例，反之就是负例。</p>
<p>实战里要注意：</p>
<ul>
<li>先给特征做标准化（不然模型不准）；</li>
<li>遇到像癌症预测这种“少数人患病、多数人健康”的情况，别只看准确率（容易骗人），用混淆矩阵、F1分数或者AUC这些指标才靠谱，能避免漏诊、误诊。</li>
</ul>
<p>它的好处就是模型简单、训练快，还能看懂每个特征的影响（比如哪个指标对判断癌症更重要），是新手入门机器学习的好选择</p>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>机器学习</tag>
        <tag>逻辑回归</tag>
      </tags>
  </entry>
  <entry>
    <title>电力负荷预测项目</title>
    <url>/2026/02/27/%E7%94%B5%E5%8A%9B%E8%B4%9F%E8%8D%B7%E9%A2%84%E6%B5%8B%E9%A1%B9%E7%9B%AE/</url>
    <content><![CDATA[<p>机器学习案例</p>
<span id="more"></span>
<p>如果你看到这句话，说明我正在写</p>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>深度学习</tag>
        <tag>张量</tag>
        <tag>机器学习</tag>
        <tag>自动微分</tag>
      </tags>
  </entry>
  <entry>
    <title>随机森林</title>
    <url>/2025/11/27/%E9%9A%8F%E6%9C%BA%E6%A3%AE%E6%9E%97/</url>
    <content><![CDATA[<p>简单的随机森林(๑´ڡ&#96;๑)</p>
<span id="more"></span>
<pre><code>前置知识：决策树
</code></pre>
<h1 id="一点背景"><a href="#一点背景" class="headerlink" title="一点背景"></a>一点背景</h1><h2 id="集成学习"><a href="#集成学习" class="headerlink" title="集成学习"></a>集成学习</h2><h3 id="什么是集成学习"><a href="#什么是集成学习" class="headerlink" title="什么是集成学习"></a>什么是集成学习</h3><p>普通模型将数据训练，得到结果。集成学习是一种机器学习的思想，将多个模型组合成一个精度更高的模型（强学习器）。被组合的小模型成为弱学习器（基学习器）<br>通过多个模型集成来提升整体精度，而不是专门培养一个模型  </p>
<ul>
<li>一个单一强大模型：能力很强，但有过拟合风险，而且抗风险能力弱</li>
<li>多个小模型组合：也可以有很强的整体能力，但是减少了过拟合风险，抗风险能力也提高了</li>
</ul>
<h3 id="集成学习的分类"><a href="#集成学习的分类" class="headerlink" title="集成学习的分类"></a>集成学习的分类</h3><p>Bagging（并行）和Boosting（串行）<br>Bagging是将原始数据进行多个流程训练综合得到结果，多个过程可以并行（比如在学校一学期同时学数学和英语）<br>Boosting是将原始数据经过第一个过程处理后，输入第二个过程继续…（比如数学先学习1+1，再学微积分）  </p>
<p>本文探讨的随机森林就是Bagging中的一个<br>Boosting主要有：Adaboost，GBDT，XGBoost等  </p>
<h2 id="决策树回顾"><a href="#决策树回顾" class="headerlink" title="决策树回顾"></a>决策树回顾</h2><p>决策树是一颗二叉树，是监督学习中经典的分类&#x2F;回归模型，它通过模拟人类“分而治之”的决策过程，将复杂问题拆解为一系列简单判断。简单来说，各结点就是各判断条件，根据判断结果的真伪细分到下面的子树继续判断。  </p>
<p>决策树有多种，ID3，C4.5，CART等，随机森林主要用到CART决策树  </p>
<p>决策树有剪枝操作，比如限制最大深度，结点继续划分的条件等，这些在随机森林api中会再看到  </p>
<p>从这两个的名字也可以猜出来，其实随机森林的 ‘森林’ 就是由一个个决策树构成的。  </p>
<h1 id="Bagging思想"><a href="#Bagging思想" class="headerlink" title="Bagging思想"></a>Bagging思想</h1><p>随机森林是一种基于Bagging思想的集成学习方法，以决策树作为弱学习器。<br>具体过程：</p>
<ul>
<li>将训练集通过随机抽样（有放回）分为几个小训练集</li>
<li>各个小训练集再各自随机挑选一些特征进行训练  </li>
<li>构建出n个决策树，然后这些决策树进行平权投票，选出最终结果</li>
</ul>
<p>这里注意是要有放回的随机抽样，而且抽样比例最好不要太少。如果不抽样，那同样的训练集训练出来的决策树都是一样的。有放回保证了各决策树有交叉相连的关系，因此每棵树不会有较大偏差，既有交集又有差异，更好发挥投票的效果。  </p>
<h1 id="实战训练——泰坦尼克号生存预测"><a href="#实战训练——泰坦尼克号生存预测" class="headerlink" title="实战训练——泰坦尼克号生存预测"></a>实战训练——泰坦尼克号生存预测</h1><h2 id="api介绍"><a href="#api介绍" class="headerlink" title="api介绍"></a>api介绍</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.ensemble <span class="keyword">import</span> RandomForestClassifier</span><br><span class="line">model=RandomForestClassifier()</span><br></pre></td></tr></table></figure>
<p>主要用到的参数：  </p>
<ul>
<li>n_estimators: 决策树数量</li>
<li>max_features&#x3D;”auto”: 决策树构建时使用的最大特征数量（列数），默认 <code>max_features</code> &#x3D;sqrt（n_features），也可以自己调整，有线性，对数等</li>
<li>bootstrap: 是否放回。一般都是使用默认True</li>
<li>max_depth: 最大深度（同决策树）</li>
<li>…其他参数（沿用决策树的参数，这里就不写完了）</li>
</ul>
<h2 id="完整代码"><a href="#完整代码" class="headerlink" title="完整代码"></a>完整代码</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> train_test_split</span><br><span class="line"><span class="keyword">from</span> sklearn.ensemble <span class="keyword">import</span> RandomForestClassifier</span><br><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> GridSearchCV</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">d1</span>():</span><br><span class="line">    df=pd.read_csv(<span class="string">&quot;titanic_train.csv&quot;</span>)</span><br><span class="line">    df.info()</span><br><span class="line">    x=df[[<span class="string">&quot;Pclass&quot;</span>,<span class="string">&quot;Age&quot;</span>,<span class="string">&quot;Sex&quot;</span>]].copy()</span><br><span class="line">    y=df[<span class="string">&quot;Survived&quot;</span>]</span><br><span class="line">    x[<span class="string">&#x27;Age&#x27;</span>].fillna(value=df[<span class="string">&quot;Age&quot;</span>].mean(),inplace=<span class="literal">True</span>)</span><br><span class="line">    x=pd.get_dummies(x) <span class="comment">#处理非数值类型</span></span><br><span class="line">    x_train,x_test,y_train,y_test = train_test_split(x,y,test_size=<span class="number">0.2</span>,random_state=<span class="number">23</span>)</span><br><span class="line">    rfc=RandomForestClassifier(max_depth=<span class="number">6</span>,random_state=<span class="number">10</span>)</span><br><span class="line">    rfc.fit(x_train,y_train)</span><br><span class="line">    y_pred=rfc.predict(x_test)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&#x27;score: <span class="subst">&#123;rfc.score(x_test,y_test)&#125;</span>&#x27;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment">#交叉验证网格搜索</span></span><br><span class="line">    model=RandomForestClassifier()</span><br><span class="line">    param=&#123;<span class="string">&quot;n_estimators&quot;</span>:[<span class="number">40</span>,<span class="number">50</span>,<span class="number">60</span>,<span class="number">70</span>],<span class="string">&quot;max_depth&quot;</span>:[<span class="number">2</span>,<span class="number">4</span>,<span class="number">6</span>,<span class="number">8</span>,<span class="number">10</span>],<span class="string">&quot;random_state&quot;</span>:[<span class="number">10</span>]&#125;</span><br><span class="line">    grid_search=GridSearchCV(model,param_grid=param,cv=<span class="number">2</span>)</span><br><span class="line">    grid_search.fit(x_train,y_train)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&#x27;score_2: <span class="subst">&#123;grid_search.score(x_test,y_test)&#125;</span>&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    d1()</span><br></pre></td></tr></table></figure>
<p>流程都是一样的，只是换个模型  </p>
<p>这里仅举分类的例子，同样的，随机森林也可以做回归，具体差异和决策树的分类与回归一样，这里就不赘述。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>随机森林是基于 Bagging 思想的集成学习方法，核心是通过 “样本随机 + 特征随机” 构建多棵独立的 CART 决策树，最终通过投票（分类）或平均（回归）得到结果，是机器学习中 “开箱即食” 的强学习器。  </p>
<p>关键要理解其具体过程，即先对样本随机，然后再对特征随机。</p>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>机器学习</tag>
        <tag>集成学习</tag>
        <tag>随机森林</tag>
      </tags>
  </entry>
</search>
